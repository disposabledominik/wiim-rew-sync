# GUI Smoke Test Issues

Tracks issues found during manual smoke testing of the GUI integration.

## Issue Status Legend

- **Status:** `FIXED` | `PARTIAL` (only part of the reported issue was fixed; the row's Notes column states what's still open and where the rest is tracked) | `OPEN` | `WONTFIX` | `REASSIGNED` (superseded by a different, more accurately-scoped issue number; see that row's Notes for the cross-reference)
- **Test:** `YES` (unit test exists) | `NO` (needs test) | `N/A` (not testable in automation)

## Issues

| # | Issue | Status | Test | Fix Commit | Notes | Test Reference |
|---|-------|--------|------|------------|-------|----------------|
| 1 | Device click → "Processing" → nothing happens (NoSources error) | FIXED | YES | `12e8b38` | getStatusEx doesn't report InputList; fallback to canonical source defaults | Test: test_empty_sources_wiim_mini_advances_wizard (test_smoke_regression_wizard.py) |
| 2 | User Guide has no visible close button / way to exit | FIXED | YES | `d5ff9e9` | Wired `close_requested` signal to navigate back to wizard | Tests: test_issue2_help_close_signal_connected (now emits the real signal and asserts the dialog closes, folded together with #9's assertion), test_issue9_help_close_navigates_back (test_smoke_regression_operations.py) |
| 3 | All windows have transparent backgrounds (WSL rendering) | WONTFIX | N/A | — | WSLg/Wayland compositor artifact; resolves with native Windows build |
| 4 | Device cards in single narrow column, wasted screen space | FIXED | N/A | `fcb82c2` | Removed 800px max-width from QStackedWidget, increased MAX_CONTENT_WIDTH to 1200 |
| 5 | No step indicator shown at all | FIXED | YES | `559ce5f` | Step indicator never initialized at startup; added init with default flow | Test: test_step_indicator_has_default_peq_flow_labels (test_smoke_regression_wizard.py) |
| 6 | Sidebar device name shows "WiiM Device" instead of real name | FIXED | YES | `559ce5f` | Used caps.model fallback + discovered device name lookup | Test: test_sidebar_shows_device_model_name (test_smoke_regression_wizard.py) |
| 7 | Pull from Device → "Processing" → nothing visible in GUI | FIXED | YES | `48bc9f9`, `d01bc84` | _on_peq_ready was a TODO stub; now populates ReviewPage + advances wizard | Test: test_issue7_on_peq_ready_populates_review_page, test_issue7_on_peq_ready_advances_to_review (test_smoke_regression_operations.py) |
| 8 | Success message after device pull wiped immediately | FIXED | YES | `48bc9f9` | OperationFeedbackManager.finish_operation() cleared banner; added 50ms delay | Test: test_issue8_finish_operation_preserves_success (test_smoke_regression_operations.py) |
| 9 | HelpView close button not wired | FIXED | YES | `d5ff9e9` | Same as #2 | Tests: test_issue2_help_close_signal_connected, test_issue9_help_close_navigates_back (test_smoke_regression_operations.py) |
| 10 | Measurement picker cancel = silent dead end | FIXED | YES | `d5ff9e9` | Shows "Selection cancelled" info banner | Test: test_issue10_picker_cancel_shows_info (test_smoke_regression_operations.py) |
| 11 | FiltersPage retry button only hides error, doesn't reset page | FIXED | YES | `d5ff9e9` | Now re-shows option cards so user can try again. **Update (2026-08):** the inline error/retry section (`show_error()`, `_error_section`) was removed in the dead-code pass alongside `#236` -- see that row. Regression test kept, trimmed to cover `clear_results()`'s remaining (still real) path/label reset. | Test: test_issue11_filters_page_has_retry_mechanism (test_smoke_regression_operations.py) |
| 12 | No immediate loading feedback for device/REW pull on FiltersPage | FIXED | YES | `d5ff9e9` | Added show_progress() before async calls | Test: test_issue12_device_pull_shows_progress (test_smoke_regression_operations.py) |
| 13 | Empty filters from device — no guidance after banner auto-dismisses | FIXED | YES | `d5ff9e9` | Changed to persistent message with suggestion | Test: test_issue13_empty_filters_shows_persistent_message (test_smoke_regression_operations.py) |
| 14 | ConnectPage auto-triggers discovery on every showEvent (back-nav) | FIXED | YES | `d5ff9e9` | Guard: only emit refresh_requested when no cards shown | Test: test_show_event_with_cards_does_not_emit_refresh, test_show_event_without_cards_emits_refresh (test_smoke_regression_wizard.py) |
| 15 | RoomFit flow: Connect step missing checkmark after flow type switch | FIXED | YES | `791f04a` | _on_flow_type_changed now replays completed step summaries | Test: test_flow_type_peq_to_roomfit_updates_labels (test_smoke_regression_wizard.py) |
| 16 | RoomFit flow: "No source selected" when pulling from device | FIXED | YES | `791f04a` | Defaults to "wifi" when no source explicitly set (RoomFit is device-global) | Test: test_device_pull_defaults_to_wifi (test_smoke_regression_wizard.py) |
| 17 | Home button appears to do nothing | WONTFIX | N/A | — | Working as designed: "home" = return to current wizard step. If already there, no visible change. |
| 18 | Filter table too narrow (400px cap), only 3 columns visible | FIXED | N/A | `791f04a`, `5f355b5` | Removed max-width/centering, columns now stretch to fill available width |
| 19 | RoomFit mode: FiltersPage shows PEQ options instead of RoomFit UI | FIXED | YES | `5f355b5` | _on_eq_type_selected now calls set_roomfit_mode(True/False) | Test: test_eq_type_roomfit_sets_roomfit_mode, test_eq_type_peq_sets_roomfit_mode_false (test_smoke_regression_wizard.py) |
| 20 | Presets on Device view shows nothing when navigated to | FIXED | YES | `5f355b5` | Added _load_device_presets() trigger on navigation + _do_list_presets async | Test: test_issue20_nav_presets_device_triggers_load (test_smoke_regression_operations.py) |
| 21 | Sidebar collapsed mode shows blank buttons (no icons) | FIXED | N/A | `b6bcc36` | Added emoji icons as placeholders for collapsed mode |
| 22 | Presets on Device shows PEQ presets but no RoomFit profiles | FIXED | YES | `b6bcc36` | _do_list_presets now fetches both PEQ + RoomFit and populates both sections | Test: test_issue22_do_list_presets_fetches_both (test_smoke_regression_operations.py) |
| 23 | RoomFit FiltersPage profile dropdown is empty | FIXED | YES | `b6bcc36` | Added _do_list_roomfit_profiles async fetch when EQ type "roomfit" is selected | Test: test_issue23_eq_type_roomfit_triggers_profile_fetch (test_smoke_regression_operations.py) |
| 24 | Presets on Device: Export/Save/Load buttons do nothing (no signal wired) | FIXED | YES | `69d09c2`, `c55918e` | Fully implemented: Export reads preset then writes REW file; Save creates local Profile; Load reads preset into Review | Test: test_issue24_presets_device_export_connected, test_issue24_presets_device_save_connected, test_issue24_presets_device_load_connected (test_smoke_regression_operations.py) |
| 25 | Presets on Device: Can select PEQ + RoomFit items simultaneously | FIXED | YES | `69d09c2` | Added mutual exclusion: selecting in one list clears the other | Test: test_issue25_preset_list_mutual_exclusion (test_smoke_regression_operations.py) |
| 26 | Presets on Device: Copy to Another Device doesn't actually write preset to target | FIXED | YES | `c55918e` | Reads filters, connects to target, writes via SafeWrite, saves as named preset; shows success/failure status | Test: test_issue26_copy_preset_to_device_writes_to_target (test_smoke_regression_operations.py) |
| 27 | RoomFit FiltersPage: No way to progress after selecting profile from dropdown | FIXED | YES | `69d09c2` | Wired roomfit_profile_selected; reads profile with L/R mode handling; advances to Review | Test: test_issue27_roomfit_profile_selected_triggers_pull (test_smoke_regression_operations.py) |
| 28 | Review page shows single 24-row table for L/R instead of separate L/R tabs | FIXED | YES | `d0aa100` | _on_peq_ready now checks channel_mode and calls set_lr_filters() for L/R mode | Test: test_issue28_peq_ready_lr_calls_set_lr_filters (test_smoke_regression_operations.py) |
| 29 | Export as REW: L/R mode creates single file with 10 bands instead of two files | FIXED | YES | `d0aa100` | Export logic branches on channel_mode; uses ExportDialog for dual-file L/R export | Test: test_issue29_export_lr_mode_uses_export_dialog (test_smoke_regression_operations.py) |
| 30 | Export as REW: file doesn't get .txt extension when not typed by user | FIXED | YES | `d0aa100` | Added if not path.lower().endswith('.txt'): path += '.txt' in stereo export path | Test: test_issue30_stereo_export_appends_txt (test_smoke_regression_operations.py) |
| 31 | Save to My Presets: no visible result after processing, nothing in My Presets | FIXED | YES | `d0aa100` | Added _profile_repository.list() → set_presets() refresh after save; also refresh on nav to My Presets | Test: test_issue48_save_filters_to_presets_callable (test_smoke_regression_operations.py) |
| 32 | Copy to Another Device: status message not shown long enough to read | FIXED | YES | `d0aa100` | finish_operation() no longer clears banner unconditionally; only clears if still showing progress spinner | Test: test_issue32_finish_operation_clears_only_progress (test_smoke_regression_operations.py) |
| 33 | Copy to Another Device: only pushes to first device when multiple selected | FIXED | YES | `d0aa100` | Replaced per-item run_async loop with single _do_copy_presets_batch coroutine (later superseded by _do_copy_presets_batch_multi, see row 74 -- _do_copy_presets_batch itself removed as dead code once nothing called it) | Test: test_issue74_copy_batch_multi_iterates_all_devices (test_smoke_regression_operations.py) |
| 34 | Copy to Another Device: RoomFit profile saved as PEQ on target (wrong type) | FIXED | YES | `d0aa100` | _do_copy_preset_to_device now branches on preset_type; uses write_roomfit() for RoomFit | Test: test_issue34_copy_branches_on_preset_type (test_smoke_regression_operations.py) |
| 35 | Source page shows all canonical sources regardless of device model | FIXED | YES | `d0aa100` | All common sources shown (PEQ accepts any source name; extra slots are harmless) | Test: test_empty_sources_provides_all_common_sources, test_reported_sources_passed_through (test_smoke_regression_wizard.py) |
| 36 | WiiM Mini shows RoomFit in EQ Type despite not supporting it | FIXED | YES | `d0aa100` | Superseded by `5c01efc` (branch-quality review, 2026-07-18): the model-name blocklist was removed from `_on_capabilities_ready`/`CapabilityProber` in favor of a capability-file override plus generic protocol-level RoomFit detection, so no model-string special-casing remains anywhere. See `TestIssue36MiniRoomfitBlocklist`'s docstring for the current mechanism | Test: test_roomfit_read_false_forces_peq_only_regardless_of_model, test_non_blocklisted_model_roomfit_level_2_gets_peq (test_smoke_regression_wizard.py) |
| 37 | ReviewPage "Save to My Presets" button does nothing | FIXED | YES | `d6983fb` | save_preset_requested signal was never connected; added _on_review_save_preset handler | Tests: test_issue37_review_save_preset_signal_connected (signal connection), test_issue48_save_filters_to_presets_callable (actual functionality) (test_smoke_regression_operations.py) |
| 38 | My Saved Presets: no actions (delete/rename/duplicate) wired | FIXED | YES | `ddb2349`, `94272de` | Connected signals + added visible toolbar (Load/Rename/Duplicate/Delete) on selection | Test: test_issue38_my_presets_view_has_toolbar (test_smoke_regression_operations.py) |
| 39 | My Saved Presets: L/R presets shown as "stereo" with 24 bands | FIXED | YES | `ddb2349`, `94272de` | Badge shows "L/R", band count uses per-channel, channel_mode preserved on save | Test: test_issue39_lr_profile_preserves_channel_mode (test_smoke_regression_operations.py) |
| 40 | Export L/R preset from Presets on Device creates single file | FIXED | YES | `ddb2349`, `94272de` | Both Presets view and Review step now generate _L.txt + _R.txt; state.channel_mode updated from device | Test: test_issue40_lr_export_generates_dual_files (test_smoke_regression_operations.py) |
| 41 | Back-nav to Connect → selecting new device keeps old flow type | FIXED | YES | `d6983fb` | _on_device_selected resets flow_type to PEQ before probing | Test: test_device_select_resets_flow_to_peq (test_smoke_regression_wizard.py) |
| 42 | WiiM Mini missing line-in source in Source page | FIXED | YES | `d6983fb` | Reverted to showing all common sources (PEQ accepts any name; no model filtering needed) | Test: test_empty_sources_provides_all_common_sources (test_smoke_regression_wizard.py) |
| 43 | Sound/Sound Lite shows optical and HDMI sources (not applicable) | WONTFIX | N/A | — | PEQ engine accepts any source name; showing extra sources is harmless. No reliable way to probe physical inputs. |
| 44 | ReviewPage "Save to My Presets" crashes with FileNotFoundError for L/R names | FIXED | YES | `5c93394` | Name contained "/" from channel mode; added filesystem-safe name sanitization in shared helper | Test: test_issue44_build_profile_removes_slashes, test_issue44_build_profile_removes_unsafe_chars, test_issue44_build_profile_empty_name_fallback (test_smoke_regression_operations.py) |
| 45 | Export from Review vs Presets on Device shows different dialogs | FIXED | YES | `5c93394` | Consolidated into shared _export_filters_as_rew helper; both use ExportDialog for L/R | Test: test_issue45_export_consolidated_helper (test_smoke_regression_operations.py) |
| 46 | My Saved Presets: no visible action buttons (only right-click context menu) | FIXED | YES | `e921182` | Added visible toolbar (Load/Rename/Duplicate/Delete) that appears on item selection | Test: test_issue38_my_presets_view_has_toolbar (test_smoke_regression_operations.py) — corrected name, doc previously cited `test_issue38_my_presets_toolbar_buttons` which does not exist |
| 47 | Duplicate save/export logic across trigger points (architectural) | FIXED | YES | `5c93394` | Created shared helpers: _save_filters_to_presets, _export_filters_as_rew; all triggers converge | Test: test_issue47_shared_helpers_consolidated (test_smoke_regression_operations.py) |
| 48 | Presets on Device "Save to My Presets" creates popup windows | FIXED | YES | `e144ddb` | _do_preset_save was calling Qt widget methods from async thread; moved to thread-safe pattern | Test: test_issue48_save_filters_to_presets_callable (test_smoke_regression_operations.py) |
| 49 | Loading L/R profile from My Saved Presets shows "no filters" | FIXED | YES | `e144ddb` | recall_profile now handles L/R profiles (filters_l + filters_r) not just stereo | Test: test_issue49_recall_profile_lr, test_issue49_recall_profile_stereo (test_smoke_regression_operations.py) — corrected names (doc previously cited a nonexistent `test_issue49_recall_profile_handles_lr`); 2026-07-02 hardening also made both use distinguishable per-channel frequencies and assert channel order, since the prior version only checked `len(emitted_filters) == 2` |
| 50 | "Copy to another source" shows "No sources available" | FIXED | YES | `e144ddb` | Was reading from capabilities.source_names (empty); now uses SourcePage's known source list | Test: test_issue50_source_page_provides_sources (test_smoke_regression_operations.py) |
| 51 | REW file import: no way to import L/R (two files) | FIXED | YES | `e144ddb` | Added Stereo/L/R choice dialog on import click; L/R shows dual file picker; wired file_import_lr_requested signal | Test: test_issue51_lr_import_dual_file_picker (test_smoke_regression_operations.py) |
| 52 | FiltersPage shows "Pull from Device" and "Pull from REW API" options | FIXED | N/A | `e144ddb` | Hidden — these workflows are accessible via "Presets on Device" sidebar |
| 53 | PushPage "Export" and "Save to My Presets" buttons do nothing | FIXED | YES | `e144ddb` | Wired push_page.export_requested and save_preset_requested to shared handlers | Test: test_issue53_push_page_export_connected, test_issue53_push_page_save_preset_connected (test_smoke_regression_operations.py) — corrected name, doc previously cited `test_issue53_push_page_save_connected` which does not exist |
| 54 | Push step doesn't show checkmark on success | FIXED | YES | `e144ddb` | _on_write_complete now marks PUSH step completed in step indicator | Test: test_issue54_write_complete_marks_push_done (test_smoke_regression_operations.py) -- **correction (2026-07-02):** this primary test is solid, it directly asserts `WizardStep.PUSH in wizard_controller.state.completed_steps` via a real `_on_write_complete()` call. The other two tests this row previously cited do not actually cover the checkmark: `test_push_happy_path` (which lives in `test_gui_push_export.py`, not `test_wizard_integration.py` as stated) only asserts the `write_complete` signal payload, and `test_source_page_populated_from_capabilities` is unrelated to Push entirely (it's about SourcePage population). `test_full_peq_flow` (test_wizard_integration.py) does exercise the same `_on_write_complete()` call end-to-end but likewise only checks the result message/backup path, not `completed_steps` -- kept as complementary full-flow coverage, not a substitute for the primary test |
| 55 | Push sends L/R filters as stereo (channel_mode "L/R" != "lr") | FIXED | YES | `5761cef` | _do_push now checks both "lr" and "l/r" in channel_mode comparison | Test: test_issue55_is_lr_mode_detects_lr, test_issue55_is_lr_mode_detects_l_slash_r, test_issue55_is_lr_mode_detects_uppercase, test_issue55_is_lr_mode_stereo_false (test_smoke_regression_operations.py) |
| 56 | Drag-and-drop zone confusing for L/R (which file is L vs R?) | FIXED | N/A | `5761cef` | Drop zone hidden — import flow uses explicit Stereo/L/R choice dialog |
| 57 | Back-navigation from Push keeps all steps checked | FIXED | YES | `5761cef` | _on_step_changed now clears completion badges for invalidated steps via clear_completed(). **Superseded by `#246` Stage 2 (lazy invalidation):** back-navigation now deliberately keeps every checkmark (browsing is non-destructive); the original symptom -- checked steps *with stale data* after an invalidating change -- is instead prevented by change-time invalidation in the selection handlers. | Test: test_go_to_source_preserves_checkmarks_until_change (test_smoke_regression_wizard.py, rewritten for Stage 2 -- covers both the preserve-on-browse and clear-on-change halves) |
| 58 | Multi-device push always uses stereo PEQ regardless of context | FIXED | YES | `5761cef` | apply_to_devices now receives channel_mode from wizard state; builds L/R PEQSettings when appropriate | Test: test_issue58_copy_presets_batch_multi_peq_lr_uses_lr_channel_mode (test_smoke_regression_operations.py) — 2026-07-02: the cited `test_issue58_apply_to_devices_accepts_channel_mode` never existed, and the `apply_to_devices` function it would have tested is now dead code (zero references in src/). The live equivalent multi-device path is `MainWindow._do_copy_presets_batch_multi` -> `_do_copy_preset_to_device`, which correctly threads channel_mode through `build_peq_settings`/`SafeWrite`; added a real test against that path instead |
| 59 | RoomFit: Filters page shows "Select RoomFit profile to pull" dropdown | FIXED | N/A | `7e6cc12` | Hidden — RoomFit profile pull is via "Presets on Device" sidebar only |
| 60 | RoomFit: NameProfilePage "Existing profiles" list is empty | FIXED | YES | `0a132e4` | Added _populate_name_profile_page() that fetches profiles when navigating to NAME_PROFILE step | Test: test_issue60_name_profile_populated_on_navigation (test_smoke_regression_operations.py) |
| 61 | RoomFit: Push ignores profile name from "Name Profile" step (uses "My RoomFit") | FIXED | YES | `b4f9037` | _on_push_requested defers push for RoomFit; push fires from _on_name_confirmed after name is stored | Test: test_issue61_roomfit_push_deferred_not_immediate, test_issue61_name_confirmed_triggers_push (test_smoke_regression_operations.py) |
| 62 | RoomFit: Undo button crashes with "Is a directory" error | FIXED | YES | `b83d65f` | Undo now works for RoomFit: backs up existing profile before overwrite; hidden for new profiles only | Test: test_issue62_roomfit_push_backs_up_existing_profile (test_smoke_regression_operations.py) |
| 63 | RoomFit/PEQ: L/R filters written as Stereo in all write paths | FIXED | YES | `306a1de` | write_roomfit now accepts channel_mode; all write paths (push, copy, multi-device) use shared helpers | Test: test_issue63_do_push_roomfit_lr_passes_channel_mode (test_smoke_regression_operations.py) |
| 64 | Duplicated logic across 30+ locations (architectural) | FIXED | YES | `aaffc0f` | Created src/gui/shared_helpers.py with 5 shared functions; eliminated all duplication | Test: test_issue64_shared_helpers_created (test_smoke_regression_operations.py) |
| 65 | Loading L/R profile from My Presets loses channel mode (shows as stereo) | FIXED | YES | `ec85fc5` | _on_profile_load_requested sets wizard state.channel_mode from profile before recall | Test: test_issue65_profile_load_sets_channel_mode_lr, test_issue65_profile_load_sets_channel_mode_stereo (test_smoke_regression_operations.py) |
| 66 | FiltersPage: clicking "Import from REW File" after mode selection does nothing | FIXED | N/A | `32f0a44` | Redesigned: removed card+dialog; inline Stereo/L/R radio toggle + Browse buttons always visible |
| 67 | FiltersPage: re-navigating to page keeps stale file selections | FIXED | N/A | `32f0a44` | Page now uses simple toggle+browse pattern; clear_results resets state |
| 68 | Source page and other views too narrow (condensed, buttons clipped) | FIXED | N/A | `32f0a44` | Removed AlignHCenter from SourcePage outer layout; content expands to fill space |
| 69 | SecondaryWorkflowManager copy_preset_to_device hardcodes stereo | FIXED | YES | `32f0a44` | Added channel_mode parameter (default "stereo"); callers pass through | Test: test_issue69_copy_preset_to_device_has_channel_mode (test_smoke_regression_operations.py) |
| 70 | Duplicated backup JSON parsing in _do_undo and _do_undo_roomfit | FIXED | YES | `32f0a44` | Extracted parse_backup_filters() into shared_helpers; both undo paths use it | Test: test_issue70_parse_backup_stereo, test_issue70_parse_backup_lr, test_issue70_parse_backup_empty, test_issue70_parse_backup_lr_unequal_lengths_not_naively_split (test_smoke_regression_operations.py) — corrected names, doc previously cited a nonexistent `test_issue70_parse_backup_filters_shared`; the unequal-lengths case specifically guards against a naive positional 50/50 split |
| 71 | Dead code: _OptionCard, _DropZone, pull/REW card handlers in FiltersPage | FIXED | N/A | `32f0a44` | Removed entirely in FiltersPage rewrite |
| 72 | FiltersPage: No "Next" button in Stereo mode to proceed after returning to step | FIXED | YES | `edee68f` | Added unified "Next" button to both Stereo and L/R modes; enabled only when required files are selected | Test: test_next_button_exists_and_initially_disabled, test_next_button_enabled_after_stereo_file_browse (test_smoke_regression_wizard.py) |
| 73 | Stereo file import shown as L/R in Review due to stale channel_mode state | FIXED | YES | `edee68f` | _do_file_import now explicitly sets channel_mode="Stereo" before emitting peq_ready | Test: test_stereo_import_sets_channel_mode (test_smoke_regression_wizard.py) |
| 74 | Presets on Device "Copy to another device" only copies to first selected device (regression of #33) | FIXED | YES | `edee68f` | Replaced selected_devices[0] with iteration over all selected devices via _do_copy_presets_batch_multi | Test: test_issue74_copy_batch_multi_iterates_all_devices (test_smoke_regression_operations.py) |
| 75 | Review "Apply to multiple devices" always pushes to active PEQ (wrong for RoomFit) | FIXED | N/A | `edee68f` | Removed button entirely; "Copy to another device" in Presets on Device serves the same purpose correctly |
| 76 | Review "Copy to another source" replaced by multi-source selection in Source step | FIXED | N/A | `edee68f` | Source step now uses checkboxes (multi-select); push writes to all selected sources; removed Copy to another source button |
| 77 | Multi-source PEQ push: Undo doesn't restore all sources | FIXED | YES | `d6b9288` | _do_push now stores per-source backup paths; _do_undo_multi_source restores each source from its own backup | Test: test_issue77_multi_source_backup_format (test_smoke_regression_operations.py) |
| 78 | Copy to another device: status message says "N presets" when only 1 preset copied to N devices | FIXED | YES | `d6b9288` | Status now reads "1 preset(s) copied to 3 device(s)" reflecting actual items × devices | Test: test_issue78_copy_status_message_format (test_smoke_regression_operations.py) |
| 79 | Copy to another device: L/R RoomFit profiles stored as Stereo on target | FIXED | YES | `d6b9288` | _do_copy_preset_to_device now passes channel_mode with filters_l/filters_r for L/R RoomFit | Test: test_issue79_copy_lr_roomfit_preserves_channel (test_smoke_regression_operations.py) |
| 80 | Dry Run mode: Push proceeds to device anyway, no dry-run acknowledgment in Push step | FIXED | YES | `d6b9288`, `bd5e86b` | Checks state.dry_run; for RoomFit advances twice (skips NAME_PROFILE) to reach PUSH page with dry-run result | Test: test_issue80_dry_run_shows_preview (test_smoke_regression_operations.py) |
| 81 | "Compare with device" toggle does nothing (dead UI) | FIXED | N/A | `d6b9288` | Removed toggle, signal, and methods entirely; no functional comparison was implemented |
| 82 | My Saved Presets and Connect step still have narrow/condensed content layout | FIXED | N/A | `d6b9288` | Removed AlignHCenter from ConnectPage, ReviewPage, MyPresetsView outer layouts |
| 83 | File>Import/Export menu items do nothing; Help>About not wired | FIXED | N/A | `d6b9288` | Removed File>Import/Export (redundant with UI); wired Help>About with product description dialog |
| 84 | Help article needs update to reflect current UI after all bug fixes | FIXED | N/A | `242352c` | All 6 help articles rewritten: removed dead UI references (Compare toggle, FiltersPage cards, Pull from Device option), updated for multi-source, L/R export, toolbar actions, RoomFit flow |
| 85 | Diagnostics panel "Send" button does nothing (raw_command_requested not wired) | FIXED | YES | `9c6de2d`, `ff4597d`, `d1e2ce5`, `8499e83` | Thread-safe response; scrollable layout; caps auto-populated; log Refresh button widened | Tests: test_issue85_diagnostics_raw_command_connected, test_issue85_raw_command_no_device_shows_error (test_smoke_regression_operations.py) |
| 86 | Connect step and My Saved Presets still narrow; row highlight misaligned in My Presets | FIXED | N/A | `9c6de2d`, `ff4597d` | Removed wrapper stretch centering; set explicit sizeHint height; set scanning widget minHeight |
| 87 | Sidebar preset/profile load doesn't work when wizard state is incomplete | FIXED | YES | `9c6de2d`, `ff4597d`, `d1e2ce5`, `8499e83`, `e386bfc` | QuickSetupDialog: checks completed_steps (not stale state); clears source on device re-select; marks all prior steps; navigates to Review | Test: test_load_without_device_shows_error, test_load_with_connect_only_shows_quick_setup, test_load_with_all_steps_completed_skips_dialog (test_smoke_regression_wizard.py) |
| 88 | Initial device discovery times out (30s) in packaged app, then shows all 4 devices | FIXED | YES | `97eca0c` | Sequential mDNS→subnet scan + redundant enrichment took ~30s on cold start. Rewrote DiscoveryModule: parallel mDNS+subnet scan (1.5s grace), skip redundant getStatusEx for subnet results, progressive UI updates via on_found callback | Test: test_discovery_module_parallel_scan_grace_period (test_smoke_high_level.py) |
| 89 | Cancel button invisible in dark theme (UnsavedChangesDialog) | FIXED | YES | `47087a2` | Removed hardcoded inline styles from Cancel button; uses ghost class from QSS theme | Test: test_unsaved_changes_dialog_cancel_button_style (test_smoke_fixed_issues.py) |
| 90 | Settings screen paths not pre-populated; theme change + log buttons broken | FIXED | YES | `47087a2` | _apply_settings now passes computed paths (get_log_dir, profile_repository.storage_root) when settings have empty defaults; theme wiring was already correct | Test: test_settings_apply_populates_default_paths (test_smoke_fixed_issues.py) |
| 91 | Onboarding overlay uses hardcoded COLORS_LIGHT; text clipped in dark mode | FIXED | YES | `47087a2`, `212f672` | Overlay now rebuilds on showEvent to detect active theme; uses COLORS_DARK/COLORS_LIGHT dynamically; Skip removed. **Superseded:** the COLORS_DARK/COLORS_LIGHT/ColorScheme mechanism described here was removed entirely in the visual-consistency refactor (`e75d343` onward) — onboarding_overlay.py is now pure QSS-driven with no showEvent rebuild or Python-side color dispatch. See #125-128. | Test: test_onboarding_overlay_theme_and_no_skip (test_smoke_fixed_issues.py) |
| 92 | Pull from REW: L/R filters pushed to RoomFit profile result in empty/flat bands | FIXED | YES | `7478e7c` | Push path used naive split_lr_filters (50/50) which scrambles unequal-length channels; now stores and uses explicit filters_l/filters_r in WizardState | Test: test_issue92_lr_filter_splitting (test_smoke_regression_operations.py) |
| 93 | Pull from REW: subsequent pull after preset load uses stale filters from preset | FIXED | YES | `7478e7c`,`60a07d8` | Root cause: When using the REW API some filter values can have long decimals (can be up to 16 digits after the decimal point - for any of freq, gain or Q). The app passes those directly to WiiM devices, and they don't seem to like that in L/R mode. In Stereo mode the same thing doesn't seem to cause issues. The fix was to round all filter values to 3 decimal places before pushing to WiiM devices. Issue #92 was actually caused by the same root cause. | Test: test_issue93_rew_generator_rounding (test_smoke_regression_operations.py) |
| 94 | Connect step loses checkmark/context after RoomFit push via REW API | FIXED | YES | `f5a597c` | _mark_prior_steps_completed now explicitly marks CONNECT as completed when device is selected | Test: test_mark_prior_steps_completed_marks_connect_and_sources (test_smoke_fixed_issues.py) |
| 95 | Pull from REW: Quick Setup dialog shown unnecessarily when already at Filters step | FIXED | YES | `1e4111d` | _ensure_wizard_state_for_load now checks if user is at/past FILTERS step and skips dialog; PEQ_ONLY flow no longer asks for EQ_TYPE | Test: test_ensure_wizard_state_skips_dialog_if_at_filters_step (test_smoke_regression_wizard.py) |
| 96 | Pull from REW: measurement picker not shown (REW returns dict-keyed response) | FIXED | YES | `c496aba` | rew_http_client now handles dict-keyed measurement responses (keys "1", "2", ...) | Test: test_rew_http_client_handles_dict_keys (test_smoke_high_level.py) |
| 97 | Pull from REW: "Unknown filter type 'None'" error from REW API filters | FIXED | YES | `cbc8ae7` | Unified _TYPE_MAP handles all REW filter types; Modal/AllPass/L-T skipped; gain field reads "gaindB". **Refined:** `None` no longer just "skipped" -- it now becomes an explicit `OFF` `CanonicalFilter` in both `filters` and `rows`, matching the text-file import path's `_NONE_RE` behavior exactly (previously the live-API path silently omitted `None` slots entirely, diverging from the text path and shifting WiiM band-letter alignment -- see `docs/corrections.md`, 2026-06-28). | Test: test_unknown_filter_type_skipped (test_smoke_high_level.py); test_filter_type_none_handling, test_filter_type_none_in_rows_not_a_skipped_band, test_filter_type_none_disabled_still_becomes_off (test_rew_parser.py) |
| 98 | Push page shows stale DRY RUN content from previous dry run after real push | FIXED | YES | `a2dcafd` | PushPage.reset() called on every PUSH step entry via _on_step_changed | Test: test_pushpage_reset_clears_dry_run_badge (test_smoke_fixed_issues.py) |
| 99 | Duplicated WiiM device identification logic in two modules | FIXED | YES | `068caf5` | capability_prober and subnet_scanner had independent device lists with different entries and algorithms; unified into src/utils/device_identity.py | Test: test_unified_device_identity (test_smoke_high_level.py) |
| 100 | Hardware limit constants (gain/Q) duplicated in translator and GUI | FIXED | YES | `068caf5` | Identical _GAIN_MIN/MAX, _Q_MIN/MAX defined independently in wiim_generator.py and shared_helpers.py; consolidated into src/models/constants.py (indirectly tested via clamping tests). `shared_helpers.py` was later deleted entirely (branch-quality review, 2026-07-18); its `validate_filters_for_device()` now lives in `src/translator/wiim_generator.py` | Test: test_hardware_constants_present, test_hardware_constants_imported_not_duplicated (test_smoke_fixed_issues.py); TestValidateFiltersForDevice::test_truncation_and_clamping (test_wiim_generator.py) — 2026-07-02: added identity assertions (`wiim_generator._GAIN_MIN is constants.GAIN_MIN`, etc.) since the prior tests only checked values in isolation and couldn't catch a reintroduced independent literal that happened to match numerically. Verified both consumer modules do genuinely import from `src/models/constants.py` (the original fix was intact, not incomplete) |
| 101 | Band-param building logic repeated 4× in wiim_adapter.py | FIXED | YES | `068caf5` | Manual loop building EQBand param dicts copy-pasted across _write_peq_batch, _write_peq_sequential, _write_peq_batch_lr, _write_peq_sequential_lr, and write_roomfit; extracted _flat_array_to_band_params() helper (indirectly tested via adapter write tests) | Test: test_flat_array_to_band_params_roundtrip, test_flat_array_to_band_params_start_band (test_smoke_fixed_issues.py) |
| 102 | Two crash handlers overwriting each other (logging vs GUI) | FIXED | YES | `068caf5` | install_crash_handler (logging/setup.py) and _crash_handler (main_window.py) both set sys.excepthook; GUI handler now flushes all log handlers for persistence (install_crash_handler never called in production; GUI handler not unit-testable without Qt) | Test: test_install_crash_handler_writes_log (test_smoke_fixed_issues.py) |
| 103 | Dead PEQBand model defined but never used in production code | FIXED | N/A | `068caf5` | PEQBand in models/peq.py had validators and was exported in __all__ but never instantiated anywhere; removed entirely | Test: N/A (code removal - model no longer exists) |
| 104 | Lazy `import json` (3×) inside methods in capability_prober.py | FIXED | N/A | `068caf5` | stdlib module imported at function scope for no reason; moved to module-level import | Test: N/A (code cleanup - import location change) |
| 105 | App crashes on startup: `ProfileRepository` has no `storage_root` attribute | FIXED | N/A | `ae787c2` | Added `storage_root` property to expose `_profiles_dir.parent` | Test: N/A (property addition to ProfileRepository - not unit-testable in isolation) |
| 106 | Theme setting has no effect in compiled app (always dark) | FIXED | N/A | `ae787c2` | QSS stylesheets not bundled in PyInstaller spec; added to `datas_list` in all platform specs |
| 107 | Light theme shows black backgrounds with dark text (unreadable) | FIXED | N/A | `e3636e1` | QWidget `background-color: transparent` rendered as black on Windows; changed to explicit bg color |
| 108 | Status banner causes layout jumps (appears/disappears) | FIXED | YES | `e3636e1` | Banner now always reserves space (fixedHeight); uses idle state with transparent styling when empty | Test: test_show_success_auto_dismisses (test_gui_components.py) |
| 109 | Dry Run badge causes layout jitter on toggle | FIXED | YES | `e3636e1`, `2026-07-02 hardening pass` | **Correction (2026-07-02):** the original fix described here was never actually implemented — `PushPage._dry_run_badge` still toggled via bare `setVisible(True/False)` with a plain `Fixed,Fixed` size policy and no space reservation, so the layout-jitter bug was still live in the current source despite this row saying FIXED. The regression test also couldn't have caught it: it only compared `sizeHint()` before/after, which is visibility-independent in Qt and passes regardless of whether the widget's row actually collapses. Real fix now applied: `size_policy.setRetainSizeWhenHidden(True)` on the badge in `push_page.py::_setup_ui`, so its row is always reserved in the layout. Confirmed empirically that without the fix, the widget below the badge shifts by exactly the badge's row height when toggled; with the fix, it doesn't move | Test: test_pushpage_dry_run_badge_preserves_reserved_space (test_gui_components.py) — rewritten to assert real on-screen geometry (`_progress_container.pos().y()`) stays constant across `set_dry_run_result`/`reset()`, instead of the non-discriminating `sizeHint()` comparison |
| 110 | L/R filter tabs show "L" / "R" instead of full channel names | FIXED | YES | `e3636e1` | Changed to "Left Channel" / "Right Channel" for readability | Test: test_lr_filters_creates_tabs (test_gui_components.py) |
| 111 | Sidebar device label click does nothing (tooltip says "Go to Connect step") | FIXED | YES | `212f672` | Was emitting "home" (= current wizard step); changed to emit "connect" with direct navigation to Connect page. **Superseded by `#246` Stage 2 (PR #19 review, D2):** the header now opens a read-only device-info popover (name, IP, capability warning) instead of navigating -- the Connect pill covers navigation, and the popover gives the capability warning a real home instead of hijacking the header tooltip. | Test: test_sidebar_device_header_click_requests_device_info (test_smoke_fixed_issues.py, rewritten) |
| 112 | Help/User Guide has no visible close button; Diagnostics dock awkward UX | FIXED | YES | `b0c9900`, `2452246` | Both views now open as separate QDialog windows with native OS title bar controls; HelpView removed from stacked widget so content renders | Test: test_help_dialog_is_dialog_and_visible (test_smoke_fixed_issues.py) |
| 113 | Presets on Device loads stereo preset as L/R when previous load was L/R | FIXED | YES | `212f672` | _do_load_peq_preset never reset channel_mode; now reads channel_mode from PEQ device response before emitting | Test: test_do_load_peq_preset_updates_channel_mode_and_emits (test_smoke_fixed_issues.py) |
| 114 | Status banner close/dismiss button not fully visible (clipped) | FIXED | YES | `212f672`, `2452246` | Banner height 44px; cancel button has explicit border/padding; close button styled for visibility | Test: test_status_banner_close_button_visibility_and_size (test_smoke_fixed_issues.py) |
| 115 | Dark theme: text across multiple pages is dark/unreadable (no contrast) | FIXED | N/A | `212f672` | Replaced hardcoded `color: #616161` inline styles with QSS class properties (caption/secondary) that adapt to theme |
| 116 | Light theme: EQ Type cards have dark background (should be light) | FIXED | N/A | `212f672` | Removed `background: palette(base)` and hardcoded border from inline style; cards now use QSS card rules |
| 117 | Onboarding overlay always shows light theme; Skip button redundant | FIXED | YES | `212f672`, `2452246` | Removed Skip button; overlay rebuilds on showEvent for theme; increased label min heights | Test: test_onboarding_overlay_theme_and_no_skip (test_smoke_fixed_issues.py) |
| 118 | Settings and Diagnostics views visually unpolished vs rest of app | FIXED | N/A | `b0c9900`, `2452246` | QSS rules for themed groupboxes/inputs; transparent bg inside groupboxes prevents alternating pattern |
| 119 | Window restore from maximized sometimes doesn't scale content back down | OPEN | N/A | — | Intermittent: restoring from maximized clips views (step indicator etc.) until re-maximized. Likely minimum-size constraints on child widgets; needs consistent repro to fix. **Possibly related:** #141 found and fixed a same-family root cause (QStackedLayout only resizes its *current* widget) for a different, reliably-reproducible trigger (first navigation to a secondary page after a fresh launch); this specific restore-from-maximized repro remains unconfirmed/open |
| 120 | REW API L/R: unequal band counts cause right-channel bleed into left | FIXED | YES | `2bd835d` | `bands_l and bands_r` was falsy for empty list; changed to `is not None` check; naive split_lr_filters fallback only used when peq_data lacks explicit L/R | Test: test_rew_parser_lr_bleed_unequal_bands (test_smoke_high_level.py) |
| 121 | Push with clamped values shows "Push failed: Unknown error" | FIXED | YES | `2bd835d` | _on_write_complete read `result.error` (doesn't exist) instead of `result.error_message`; now shows actual error | Test: test_issue121_write_complete_shows_actual_error (test_smoke_regression_operations.py) |
| 122 | Clamping UI always highlights gain even when Q is clamped | FIXED | YES | `2bd835d` | filter_table now checks reason text for "gain"/"q" and highlights the correct column independently | Test: test_clamping_indicator_shown (test_gui_components.py) |
| 123 | L/R mode clamping on one channel marks same bands on other channel | FIXED | YES | `2bd835d` | set_lr_filters now accepts separate clamping_map_l and clamping_map_r; _on_peq_ready passes both | Test: test_issue123_lr_clamping_separate_maps (test_smoke_regression_operations.py) |
| 124 | Device label click skips wizard reset; navigates to wrong step after re-connect | FIXED | YES | `5c73b2a` | Was navigating directly to stacked widget index; now uses wizard_controller.go_to_step(CONNECT) same as step indicator. **Superseded by `#246` Stage 2:** the device label no longer navigates at all (see `#111`), and go_to_step() is purely navigational under lazy invalidation -- the invariant this row protected ("no checked step may follow an unchecked one") is preserved structurally, since browsing never unchecks anything and change-time invalidation always clears a contiguous tail of the sequence. | Test: test_navigation_device_info_shows_popover (test_smoke_fixed_issues.py, successor) |
| 125 | Clamped-value warning color (filter_table.py gain/Q text) and over-limit row highlight (import_dialog.py) hardcoded to one theme's orange; wrong color in the other theme | FIXED | YES | `6e05332` | Added WARNING_COLOR_LIGHT/WARNING_COLOR_DARK to constants.py and get_active_theme() to theme.py (resolved mode stamped on QApplication during apply_theme); both call sites now pick the theme-correct color | Test: test_clamping_color_follows_active_theme (test_gui_components.py), TestWarningHighlightColor (test_gui_import_dialog.py) |
| 126 | Selected list-row highlighting inconsistent across pages — Presets on Device / My Saved Presets / picker dialogs (Measurement, Device, Source) had no styling at all (native OS highlight), Help TOC used a solid blue fill, sidebar nav used tint+border | FIXED | N/A | `6e05332`, `8a26a09` | Added shared `QListWidget[class="selectableList"]` QSS rule (background tint + 3px left border, matching the sidebar nav/DeviceCard/StatusBanner "selected" language) to both theme files; applied the class to all 7 affected QListWidgets | — |
| 127 | QTableWidget/QTableView row selection used the old flat-gray fill, inconsistent with the new selectableList tint+border accent language | FIXED | N/A | `6e05332`, `8a26a09` | Updated `::item:selected` (and the table-level `selection-background-color`) in both theme files to the same tint + left-border treatment used by selectableList | — |
| 128 | QSpinBox (Settings dialog discovery-timeout field) had zero QSS styling; rendered with native OS spin-button chrome inside an otherwise fully themed dialog | FIXED | N/A | `6e05332` | Added full QSpinBox styling (background/border/hover/focus/disabled/up-down buttons) to both theme files, plus a `#SettingsView QGroupBox QSpinBox` override (the groupbox's `QWidget` transparent-background rule would otherwise win by selector specificity) | — |
| 129 | From Dry Run complete page: navigating to Settings then pressing sidebar "Back" lands on an empty Push page instead of the dry-run result | FIXED | YES | `d7dcad3` | `_on_navigation_requested("home")` was reusing `_on_step_changed`, which resets the Push page as an entry side effect; "Back" now redisplays the current step's page without re-running entry side effects (reset/repopulate) | Test: test_sidebar_back_from_secondary_view_preserves_push_dry_run_result (test_smoke_fixed_issues.py) |
| 130 | "Last-used device" field in Settings is shown but never populated; auto-reconnect-on-launch (Req 9.2) was never implemented | FIXED | N/A | `d7dcad3` | Confirmed dead: defined in `AppSettings`, displayed read-only, no setter ever wrote it, no launch-time reconnect logic existed. Removed the field, its Settings UI row, and Req 9.2 (descoped rather than implemented) | Test: N/A (code removal; covered by updated test_app_settings.py / test_gui_views.py no longer referencing the field) |
| 131 | Pull from REW: when the REW API is reachable but no measurements are loaded, no useful status or instruction is shown | FIXED | YES | `d7dcad3` | The empty-measurement message was routed through `show_progress()`, a non-dismissible indeterminate spinner that never auto-dismisses — looked like a stuck "still loading" state rather than a finished result. Now routed via a `__info__` sentinel to `show_info()` with actionable text ("Load or import measurement(s)..."); also fixed `_sidebar_load_in_progress` staying stuck `True` on this path | Tests: test_rew_list_empty_uses_info_sentinel_not_stuck_spinner, test_rew_list_empty_resets_sidebar_load_flag (test_gui_filter_handlers.py) |
| 132 | Connect step intermittently loses its "Connected" checkmark/context while later steps remain checked and populated (related to prior #94); not reliably reproducible | FIXED | YES | `d7dcad3` | Found and hardened a real race: `_on_device_selected` had no in-flight guard, so reselecting a device while the previous capability probe was still pending could let a stale probe corrupt wizard state (overwritten prober instance, duplicate `advance()` call). `_do_probe` now takes an explicit prober instance + generation token and discards stale/superseded results. Root cause of the one-off manual repro could not be confirmed with certainty, but this closes a genuine concurrency bug in the same area | Test: test_stale_capability_probe_is_discarded (test_smoke_fixed_issues.py) |
| 133 | Packaged Windows `.exe`: `device_capabilities.json` user copy never created in `%APPDATA%/wiim-rew-sync`, even after connecting to a device | FIXED | N/A | `f97f832` | `packaging/wiim_rew_sync_{windows,macos,linux}.spec` only listed `src/gui/assets/{help,styles,icons}` in `datas` — `src/models/assets/device_capabilities.json` (added in this session's Phase A work) was never bundled into the frozen app. `ensure_user_capability_file()` silently no-ops when the bundled path doesn't exist (`if not bundled_path.exists(): return user_path`), so no error surfaced; the prober just always ran with zero file overrides. Added the missing `datas` entry to all three spec files. Not unit-testable (PyInstaller packaging step, not Python import-time behaviour) — verify by rebuilding the `.exe` and confirming the file appears after first device connect. | N/A (packaging-only; requires a rebuilt `.exe` to verify) |
| 134 | "Pull from REW" sidebar entry opened as a modal popup dialog instead of an embedded screen — the only sidebar destination that worked this way, inconsistent with "Presets on Device" / "My Saved Presets" | FIXED | YES | `30822db` | Replaced `MeasurementPickerDialog` with a new embedded `RewPullView` (`QStackedWidget` page) for the sidebar entry. FiltersPage also gained a "File Import" / "Pull from REW API" toggle that embeds the *same* `RewPullView` instance pattern (composition, not duplication) so the in-wizard flow can pull from REW too, sharing all error/empty-state handling via `MainWindow._active_rew_pull_view`/`_active_rew_pull_page_index` | Test: `TestRewPullView` (test_gui_views.py); `test_file_import_is_default_source`, `test_toggle_to_rew_api_shows_picker_and_emits_signal`, `test_rew_pull_back_reverts_to_file_import`, `test_set_rew_api_available_false_disables_and_falls_back`, `test_clear_results_reverts_to_file_import` (test_gui_pages.py) |
| 135 | RewPullView: title visibly drifted downward in the error/placeholder state vs. the content state; a small icon above the error text looked out of place; error message wasn't vertically centered | FIXED | N/A | `30822db` | `QLabel`'s default `AlignVCenter` plus `Preferred,Preferred` size policies let Qt redistribute leftover vertical space into the title whenever the placeholder (less content) was showing. Pinned the title to a `Fixed` vertical policy, gave the placeholder/content widgets `Expanding` policy, and removed the icon entirely | — |
| 136 | "Complete Setup" (QuickSetupDialog) appeared before the user had even browsed/selected a REW measurement, instead of after — inconsistent with Presets on Device / My Saved Presets, which only prompt once a concrete selection is made | FIXED | N/A | `30822db` | Moved the `_ensure_wizard_state_for_load()` gate out of the pull-requested handlers and into `_dispatch_measurement_selection`, so it only runs once the user has actually picked a measurement | — |
| 137 | My Saved Presets view rendered left-aligned instead of centered, unlike Presets on Device / Pull from REW | FIXED | N/A | `30822db` | `MyPresetsView`'s outer `QVBoxLayout` was missing `setAlignment(AlignHCenter)`, present on the equivalent views; added | — |
| 138 | Sidebar highlight didn't always match the page actually on screen: the "Help" item stayed highlighted after closing the User Guide window even though a different page was now visible; "Pull from REW" stayed highlighted after using its own Back button to return to the wizard | FIXED | YES | `30822db` | Help no longer toggles sidebar selection at all — it opens a separate window and never replaces the visible page, so its nav button was made non-checkable. Added `SidebarNav.set_active_key()` so `MainWindow` can resync the highlight whenever it navigates away from a sidebar destination through a path other than a sidebar click (e.g. a view's own Back button) | Test: `test_help_click_does_not_change_active_item`, `test_set_active_key_syncs_highlight` (test_gui_components.py) |
| 139 | Connect, Source, Filters, and Review wizard pages rendered left-aligned instead of centered (inconsistent with Presets on Device / My Saved Presets / Pull from REW); separately, page titles used four different font-size QSS classes across the app (`heading` 20px, `title` 26px, `pageTitle` 24px, `sectionTitle` 18px) with inconsistent positioning, and the Review and Push steps had no title at all | FIXED | N/A | `30822db` | Rather than patching each page's class/position individually, added a shared `src/gui/components/page_layout.py` helper (`build_centered_content` + `make_page_title`) and migrated every wizard page and sidebar view onto it, standardizing on the `sectionTitle` class and a single centered/max-width content column. Added titles to ReviewPage ("Review Filters") and PushPage ("Push to Device"). FiltersPage previously had no width cap at all and now uses the same column as everything else | — |
| 140 | FiltersPage (File Import mode): "Browse..." button sat far to the right of its file-path label on a maximized/wide window, separated by a large empty gap, for the Stereo, Left, and Right rows | FIXED | N/A | `30822db` | The file-path label had an `Expanding` size policy and was placed *before* the button with nothing after the button to absorb leftover space, so it stretched across the full 1200px content column and pushed the button to the far edge. Reordered each row so Browse comes first and the label trails/expands after it | — |
| 141 | Presets on Device (and potentially any secondary page) rendered squished/narrow — condensed tables, unreadable buttons — the first time it was navigated to after a fresh app launch; a maximize-then-restore of the window fixed it permanently for that session | FIXED | YES | `30822db` | `QStackedLayout` only resizes its *current* widget on a window resize — every other page keeps whatever geometry it had at construction time (before the window had a real on-screen size) until the user first navigates to it, and that first layout pass could land squished. Added `MainWindow._warm_up_stacked_pages()`, scheduled via `QTimer.singleShot(0, ...)` right after the window is first shown, which resizes every page to the stack's real size and re-activates its layout — without touching visibility, so it can't double-fire `ConnectPage`'s showEvent-driven discovery. See also #119 | Test: `test_warm_up_stacked_pages_resizes_every_page_without_reshowing` (test_smoke_fixed_issues.py) |
| 142 | Step indicator's current-step "you are here" pill stayed fully highlighted (same as actually being on that step) while viewing a sidebar destination (Presets on Device, My Saved Presets, Settings, sidebar Pull from REW), making it ambiguous at a glance whether the user was still in the wizard | FIXED | YES | `30822db` | Added `StepIndicator.set_dimmed()`, which swaps the active step's pill/circle/label to a muted grey QSS variant (`stepWidgetActiveDimmed`/`stepCircleActiveDimmed`/`stepLabelActiveDimmed`) instead of the accent color. Wired into `MainWindow._on_navigation_requested` (dimmed for presets_device/my_presets/settings/rew_api, undimmed for home/connect) and into the sidebar-pull back-navigation and real wizard-step-change paths. Help wasn't included since it opens as a separate window without ever replacing the visible page | Test: `test_set_dimmed_mutes_active_pill`, `test_set_current_preserves_dimmed_state` (test_gui_components.py) |
| 143 | `mypy src/` reported 7 type errors: `_active_rew_pull_view` lacked an explicit `RewPullView \| None` annotation (so `= None` assignments conflicted with the inferred type); two tests called `.click()` on a `findChild(...)` result typed as possibly `None`; one test asserted `assert_not_called()` on a statically-typed `Signal.emit`, which doesn't have that attribute | FIXED | N/A | `30822db` | Added the missing type annotation (and dropped the now-unnecessary `getattr(self, ..., None)` defensive lookups it required); added explicit `is not None` assertions before the two `.click()` calls; switched the mock assertion to use the actual `MagicMock` bridge reference instead of the real, statically-typed signal. `mypy src/` now reports 0 errors across 140 files | — |
| 144 | Loading filters from Presets on Device "Load into Editor" or sidebar "Pull from REW" jumped straight to Review with no step-indicator pill highlighted at all — ambiguous whether the wizard considered itself "on" Review | FIXED | YES | `30822db` | `_on_peq_ready`'s sidebar-load branch sets `state.current_step = WizardStep.REVIEW` directly and never calls `wizard_controller.advance()`/`go_to_step()`, so `step_changed` never fires — `_on_step_changed`'s `StepIndicator.set_current()` call (the only place that normally syncs the highlighted pill) was skipped. Both entry points funnel through this same branch (each sets `_sidebar_load_in_progress = True` before `peq_ready` eventually gets emitted). Added an explicit `set_current(sequence.index(WizardStep.REVIEW))` call in that branch. **Correction:** this row originally (incorrectly) claimed My Saved Presets "Load" was also covered by this fix — it isn't; that path uses a separate handler entirely. See `#147` | Test: `test_sidebar_load_into_review_highlights_review_step` (test_smoke_fixed_issues.py) |
| 145 | Presets on Device's "no device connected" empty state had its title positioned differently from other views, and its icon/message weren't vertically centered in the available page area; separately, no other "missing connection" placeholder in the app (e.g. Pull from REW with no REW API, or with REW reachable but no measurements loaded) had a matching icon treatment, and icon sizing wasn't standardized | FIXED | N/A | `b1557fc` | Root cause matched the documented `RewPullView` title-drift fix in `#135`: `_empty_widget`/`_content_widget` had default `Preferred` size policy instead of `Expanding`, so neither absorbed leftover vertical space and the empty state's `AlignCenter` layout had nothing to center within. Pinned the title to `Fixed` and gave both state widgets `Expanding` policy, mirroring `RewPullView`. Added a shared `make_empty_state_icon()` helper plus `ICON_NO_CONNECTION` (🔌)/`ICON_NO_DATA` (🏜️) constants in `page_layout.py`, replacing the one-off `#PresetsDeviceEmptyIcon` QSS id-rule with a reusable `emptyStateIcon` class rule (48px) usable by any view. Reused the plug icon for `RewPullView`'s "REW not connected" error state and the desert icon for its "no measurements found" info state — previously neither placeholder had an icon at all | — |
| 146 | The one-time `_warm_up_stacked_pages()` fix for `#141` only protects page geometry up to the moment it fires at startup; a page that sits non-current through a later window resize (e.g. visited once while empty, navigated away, window settles into its final size, then revisited once it has real content) could still go stale again, reproducing the same narrow-tables/condensed-buttons symptom as `#141` on revisit rather than just first-visit | FIXED | YES | `b1557fc` | Added `MainWindow._resync_current_page_geometry()`, wired to `QStackedWidget.currentChanged`, which re-applies the same resize + `layout.invalidate()`/`activate()` used by the startup warm-up every time *any* page becomes current — not just once. This is a stronger guarantee than relying on `QStackedLayout`'s own switch-time resize, which repositions the widget but does not force a full nested-layout recompute. Could not get a fully deterministic headless repro of the exact interactive "revisit after connecting" scenario the user reported (same caveat as `#119`), but this closes the same documented Qt staleness class for the general "page revisited after sitting non-current through a resize" case | Test: `test_resync_current_page_geometry_resizes_and_reactivates_layout`, `test_resync_current_page_geometry_ignores_invalid_index` (test_smoke_fixed_issues.py) |
| 147 | "My Saved Presets" Load still landed on Review with no step-indicator pill highlighted, *and* left "My Saved Presets" highlighted in the sidebar — `#144`'s fix didn't cover this entry point | FIXED | YES | `b1557fc` | "My Saved Presets" Load routes through `SecondaryWorkflowManager.recall_profile()` → `_on_profile_recalled`, a completely separate handler from `_on_peq_ready` (which `#144` patched) — it has its own `state.current_step = WizardStep.REVIEW` / `setCurrentIndex()` pair that never got the same treatment. Added the same `set_current(sequence.index(WizardStep.REVIEW))` call, plus `_mark_prior_steps_completed()` (for consistent checkmarks, matching the `_on_peq_ready` branch), plus `sidebar_nav.set_active_key("home")` and `step_indicator.set_dimmed(False)` — the latter two were missing from this path entirely, which is why the sidebar stayed on "My Saved Presets" after the page changed | Test: `test_profile_recalled_from_my_presets_highlights_review_and_resets_sidebar` (test_smoke_fixed_issues.py) |
| 148 | Clicking a finished step-indicator pill while viewing a sidebar destination (e.g. "My Saved Presets") left *both* the sidebar item and the step pill highlighted at once — the same category of bug as `#138`/`#142`/`#144`/`#147`, recurring on yet another navigation path (`_on_step_indicator_clicked` → `wizard_controller.go_to_step()`, which only ever touched the step indicator, never the sidebar) | FIXED | YES | `b1557fc` | Architectural fix rather than another one-off patch, per explicit request: every navigation path in the app ends with `QStackedWidget.setCurrentIndex()`, so added a single `MainWindow._sync_navigation_chrome()` handler wired to `currentChanged` that derives the correct sidebar highlight + step-pill dimmed/current state purely from the page index that just became current (`_PAGE_INDEX_TO_KEY`, `_PAGE_KEY_TO_STEP`, `_SIDEBAR_DESTINATION_KEYS` lookups). Removed the now-redundant hand-maintained `sidebar_nav.set_active_key()`/`step_indicator.set_dimmed()`/`set_current()` calls from `_on_step_changed`, `_on_navigation_requested`, `_on_rew_pull_requested`, `_on_rew_pull_back_requested`, `_on_peq_ready`'s sidebar-load branch, and `_on_profile_recalled` — none of them can drift out of sync again because there's nothing left to remember to call | Test: `test_step_indicator_click_from_sidebar_destination_resets_sidebar_highlight` (test_smoke_fixed_issues.py) |
| 149 | RoomFit profile writes in `_do_push()` reported success immediately after `EQSourceSave` with no read-back/verify step at all — unlike the PEQ path (`SafeWrite`), a RoomFit write that silently failed on-device would still show "saved" to the user, and there was no rollback path for it (a brand-new profile that failed to save correctly had nothing to roll back to; an overwritten profile's pre-write backup was captured but never used to restore on mismatch) | FIXED | YES | `fadff48` | Extracted `verify_bands`/`compare_band_lists` out of `SafeWrite` into module-level functions in `safe_write.py` and added `RoomFitSafeWrite`, a parallel verify-and-rollback wrapper for named RoomFit profiles. Unlike PEQ's single restore-on-mismatch shape, RoomFit needs two: if the profile didn't exist before this write, a verify failure deletes the just-created profile (`delete_roomfit_profile`, new); if it overwrote an existing profile, a verify failure restores the previously-backed-up bands. Also fixed `delete_roomfit_profile` itself — it was missing entirely; the existing `delete_peq_profile` cannot be reused as-is because RoomFit deletion requires `EQLevel: 2` in the payload (see `docs/wiim_api_notes.md`), confirmed via `docs/corrections.md` row 16. `main_window._do_push()`'s RoomFit branch now delegates to `RoomFitSafeWrite.execute()` instead of calling `write_roomfit` directly | Test: `TestRoomFitNewProfile`, `TestRoomFitOverwriteProfile` (test_safe_write.py); `TestDeleteRoomfitProfile` (test_wiim_adapter.py) |
| 150 | `DeviceCapabilities.supported_filter_types` (from the device capability file, e.g. removing `"LP"`/`"HP"` for a WiiM Mini entry) was merged in by `merge_into()` but never actually consulted anywhere — the Review step still showed LP/HP/LP-Q/HP-Q filters as normal, fully-translated bands instead of skipped/unsupported, and they'd still be written to the device | FIXED | YES | `4373b75` | `validate_filters_for_device()` (now `src/translator/wiim_generator.py` — moved from the deleted `src/gui/shared_helpers.py`, branch-quality review, 2026-07-18) takes an optional `supported_filter_types` param and converts any `CanonicalFilter` whose type (`PEAK`/`LS`/`HS`/`LP`/`HP`) isn't in that list into a `SkippedBand` placeholder, before the band-limit truncation step runs (so an unsupported-type band doesn't consume a slot it could never use). `OFF`/`UNKNOWN` aren't real "filter types" so they're exempt. Wired from `main_window.py`'s `validate_filters_for_device()` call sites via `self._device_caps.supported_filter_types`. This also fixes the actual device write, not just the Review display, since the same `validated`/`validated_l`/`validated_r` lists feed `state.current_filters` | Test: `TestValidateFiltersForDevice::test_skips_unsupported_types`, `test_no_type_restriction_when_unset` (test_wiim_generator.py) |
| 151 | PushPage's inline read-back `FilterTable` (showing the filters confirmed on the device after a successful push) didn't fit on screen — the page has no room for a full filter table alongside the verification stepper and result text | FIXED | YES | `4373b75` | Replaced the inline `FilterTable` in `PushPage`'s result panel with a "Show Pushed Filters" link-button, visible only when read-back filter data exists, that opens a new read-only `PushedFiltersDialog` (`src/gui/dialogs/pushed_filters_dialog.py`) wrapping the same `FilterTable` component in its own resizable dialog | Test: `test_pushpage_show_filters_button_visible_only_with_data`, `test_pushpage_show_filters_button_opens_dialog` (test_smoke_fixed_issues.py) |
| 152 | Three related diagnosability gaps reported after a real push-verification-failure-and-rollback: (a) `MainWindow._on_write_complete()` read `getattr(result, "critical", False)` but `WriteResult` has no `critical` field — it only ever has `rollback_success` — so the "Critical: Manual recovery required" UI could never appear even when a rollback itself failed; (b) `_on_write_complete()` logged nothing to `app.log` on either success or failure, so a push failure left no trace of *why* in the only log file most users would check; (c) `WiiMHttpClient.command()` never logged the response body (only its length) and never surfaced it when a Set/Delete/Save command's plain-text body was anything other than `"OK"` — since WiiM's API always returns HTTP 200 even on command failure (errors are signalled via the body text, not the status code), a rejected Set command was completely invisible in both `wiim_api.log` and `app.log` | FIXED | N/A (logging/diagnostics only; no behavior-changing repro available without hardware) | `4373b75` | (a) `critical` is now derived as `result.rollback_success is False` (rollback was attempted and failed) instead of a nonexistent attribute. (b) Added `logger.info`/`logger.error` calls in `_on_write_complete()` covering backup path, error message, and the critical flag. (c) `WiiMHttpClient.command()` now logs the response body on every request, and logs a `WARNING` when a non-JSON response body isn't `"OK"`. Additionally, `SafeWrite`/`RoomFitSafeWrite` (`safe_write.py`) now log the first mismatching band (channel/index/intended-vs-actual values) via a new `_describe_first_mismatch()` diagnostic helper before triggering rollback, instead of only recording a bare pass/fail. **Follow-up (same day):** the first pass at (c) repeated the full request URL on the RESP line and capped the body at 2000 chars, which (i) doubled wiim_api.log's size for every Set command (the command's JSON payload lives in the URL itself) for no diagnostic benefit, and (ii) silently truncated exactly the data needed to diagnose a verification failure -- a 12-band L/R read-back is ~5 KB, well past the cap. Added a per-`WiiMHttpClient`-instance request counter; REQ logs `#<id> → GET <url>`, RESP logs `#<id> ← <status> (len=<n>) body=<full body, untruncated>` referencing the same id instead of repeating the URL | Test: existing `test_safe_write.py`/`test_wiim_http.py` suites re-verified green; no new dedicated test since these are additive logging calls with no new branching logic to assert against |
| 153 | A real push of L/R filters with one band requiring gain/Q clamping (gain >+12dB or Q >24.0) always failed write verification and triggered a spurious rollback, even though the device wrote and stored the data correctly. The #152 logging fix surfaced the actual cause: `SafeWrite`/`RoomFitSafeWrite` compared the *pre*-clamp `CanonicalFilter` values the user imported against the device's read-back -- but `generate_wiim_band_array()` (`src/translator/wiim_generator.py`) clamps out-of-range gain/Q to the WiiM-supported range at write time without mutating the caller's filters (`validate_filters_for_device()`'s Review-step clamping is display-only, by design, per its own docstring). So the device read back the *clamped* value while verification compared against the *unclamped* original -- guaranteed mismatch on every band that needed clamping, regardless of band count. (The user's initial hypothesis -- that the 12-vs-10 band-count difference between what the device API reports and what this app writes was the cause -- was already correctly tolerated by `compare_band_lists()`'s existing `min()`-based truncation, confirmed by `TestBandCountTolerance` passing throughout; that was a red herring surfaced by an overly strict early-return in #152's own new diagnostic helper, not the real bug) | FIXED | YES | `4373b75` | Added `clamp_filters_for_verification()` (`src/translator/wiim_generator.py`) -- truncates to `max_bands` and clamps gain/Q exactly as `generate_wiim_band_array()` does, without re-logging (the real write already logged each clamp). `safe_write.py` gained `_clamped_for_verification()`, applied at all four verify call sites: `SafeWrite.execute()`'s primary verify, `SafeWrite._rollback()`'s restore verify, and `RoomFitSafeWrite.execute()`'s primary verify and overwrite-restore verify. **Follow-up (same day):** `_describe_first_mismatch()` (the diagnostic helper itself, added in #152, which is what actually printed the misleading "band count mismatch" message that looked like the cause here) still checked `len(intended) != len(read_back)` *before* checking band content, so it could report a length difference as the failure reason even when `compare_band_lists()`'s own truncated comparison would have passed (i.e. it disagreed with the real pass/fail logic it's supposed to be explaining). Rewrote it to mirror `compare_band_lists()` exactly: only fail a channel on length when `compare_count == 0` and not vacuously empty, otherwise check the truncated per-band content first | Test: `TestClampedVerification::test_verify_passes_when_only_difference_is_clamping`, `TestClampedVerification::test_verify_still_fails_on_a_genuine_mismatch`, `TestDescribeFirstMismatch::test_does_not_report_length_difference_when_content_matches`, `TestDescribeFirstMismatch::test_reports_genuine_band_count_failure`, `TestDescribeFirstMismatch::test_reports_actual_value_mismatch_not_length` (test_safe_write.py) |
| 154 | The #153 verification fix (and #149/#11's `RoomFitSafeWrite` itself) only covered the main Push flow -- two other write paths bypassed verification/rollback entirely: (a) "Copy to Another Device" (`MainWindow._do_copy_preset_to_device()`, covers the single-item, batch, and multi-device copy entry points since all three funnel through it) wrote RoomFit profiles via a bare `target_adapter.write_roomfit()` call with no `RoomFitSafeWrite` wrapper at all -- no backup, no verify, no rollback; and separately, even its PEQ path (which *did* use `SafeWrite`) never checked the returned `WriteResult.success` before calling `save_peq_profile()` and unconditionally showing a "saved" success banner, so a failed-and-rolled-back copy was reported as a success; (b) the CLI's `set-roomfit-filters` command (`src/cli/main.py::_set_roomfit_filters`) called `adapter.write_roomfit()` directly with no `RoomFitSafeWrite`, unlike its PEQ sibling command which already used `SafeWrite` -- a failed RoomFit write printed "Done!" and exited 0 just like a real success | FIXED | YES | `4373b75` | (a) `_do_copy_preset_to_device()`'s RoomFit branch now wraps `write_roomfit` in `RoomFitSafeWrite(target_adapter, self._backup_manager).execute(...)`; both branches (PEQ and RoomFit) now check `result.success` and raise `RuntimeError(result.error_message)` on failure, which the existing `_do_copy_presets_batch`/`_do_copy_presets_batch_multi` wrappers already catch and count correctly in their summary banner ("Copied X of Y presets (Z failed)") -- no new error-handling plumbing needed, just actually using what was already there. (b) `_set_roomfit_filters` now wraps the write in `RoomFitSafeWrite`, mirroring the PEQ command's existing `SafeWrite` pattern exactly (same verify/rollback print statements); `cmd_set_roomfit_filters` now checks the returned `WriteResult.success` and exits 1 with an error message on failure instead of always exiting 0 | Test: `test_issue153_copy_batch_counts_verification_failure`, updated `test_issue34_copy_branches_on_preset_type`, updated `test_issue79_copy_lr_roomfit_preserves_channel` (test_smoke_regression_operations.py); `test_set_roomfit_filters_verification_failure_exits_nonzero`, updated `test_set_roomfit_filters_success` (test_cli.py) |
| 155 | Re-auditing #154 (prompted directly by the user asking "did you implement this consistently for *all* entry points") turned up a third, identical bypass that the first audit missed: `MainWindow._do_undo_roomfit()` (restoring a RoomFit profile from a backup file after Undo) called `self._wiim_adapter.write_roomfit()` directly with no `RoomFitSafeWrite` at all -- no verify, no rollback -- so an undo that silently failed on-device would still show "Profile restored from backup". There is also still no structural/lint guard preventing a *fourth* bypass like this from being added later (flagged as a residual risk in `docs/corrections.md`'s 2026-06-29 entry) | FIXED | YES | `4373b75` | `_do_undo_roomfit()` now builds a `RoomFitSafeWrite(self._wiim_adapter, self._backup_manager)` and calls `.execute()` with the channel mode and per-channel bands already returned by `parse_backup_filters()` (previously discarded with `_channel_mode`/`_filters_l`/`_filters_r` -- an L/R backup would have silently undone as stereo-only); shows an error banner instead of a success banner when `result.success` is `False`. Closed the residual-risk gap from #154 with a real regression test: `TestNoDirectWriteBypass::test_no_file_outside_safe_write_calls_adapter_write_directly` (test_safe_write.py) scans every `.py` file under `src/` for a direct `.write_peq(`/`.write_roomfit(` call outside `safe_write.py`/`wiim_adapter.py`/`src/tests/`, so a future bypass fails this test instead of waiting for another manual audit | Test: `test_issue154_undo_roomfit_uses_safe_write`, `test_issue154_undo_roomfit_surfaces_verification_failure` (test_smoke_regression_operations.py); `TestNoDirectWriteBypass::test_no_file_outside_safe_write_calls_adapter_write_directly` (test_safe_write.py) |
| 156 | "Presets on Device" could browse, export, save-locally, load-into-editor, and copy-to-another-device, but had no way to delete a named PEQ preset or RoomFit profile from the connected device | FIXED | YES | `7ac4e50` | The adapter-level `delete_peq_profile`/`delete_roomfit_profile` (added for #149's rollback path) were already implemented and tested but never exposed in the GUI. Added a `delete_requested` signal + "Delete" button (`class="danger"` QSS, already used by `UnsavedChangesDialog`) to `PresetsDeviceView`, wired to a new `MainWindow._on_preset_delete_requested()` handler that shows a `QMessageBox.question` confirmation (irreversible, hardware-side, unlike the no-confirmation local-library delete on `MyPresetsView`) before running `_do_delete_presets()` -- which dispatches per item on `preset_type`, doesn't let one failure abort the batch, and refreshes the view afterward regardless | Test: `test_delete_requested_signal` (test_gui_views.py); `test_delete_confirmed_after_dialog_triggers_run_async` (renamed 2026-07-02 from `test_delete_confirmed_dispatches_by_preset_type` -- despite its old name it mocked `run_async` away entirely and never actually exercised per-type dispatch, so it was renamed to accurately describe what it verifies: the confirmation dialog gating `run_async`; the real per-type dispatch coverage lives in the next test), `test_delete_declined_does_not_call_adapter`, `test_do_delete_presets_dispatches_and_refreshes`, `test_do_delete_presets_partial_failure_reports_error` (test_smoke_regression_operations.py) |
| 157 | OFF filters (deactivated but still pushed to the device, still occupying a band slot) were visually almost indistinguishable from skipped filters (excluded entirely, e.g. for exceeding the device's band limit) in the Review/Push `FilterTable` -- both got a "dimmed" treatment with opacity values 0.05 apart (0.5 vs 0.45), an imperceptible difference, making it hard to tell at a glance which filters would actually reach the device | FIXED | YES | `7ac4e50` | Bumped `_DISABLED_OPACITY` (OFF rows) from 0.5 to 0.7 -- clearly more readable than skipped's unchanged 0.45 -- and added an explanatory tooltip ("Deactivated -- still pushed to the device") to `_apply_disabled_style()`. Deliberately did not add strikethrough to OFF rows; that visual stays reserved exclusively for skipped/excluded rows so the two states don't get muddled. The single shared helper fix covers both call sites (the main table and comparison/diff mode) | Test: `test_off_band_renders_like_normal_band`, `test_skipped_band_has_strikethrough_off_does_not` (test_gui_components.py) -- **note (2026-07-02):** these replaced the originally-cited `test_disabled_band_opacity`/`test_off_and_skipped_have_distinct_styling` in commit `dd16594` ("OFF filters shown in full opacity"), which superseded this row's 0.7-vs-0.45 design -- OFF bands now render at full opacity (1.0), matching normal bands, and only skipped/excluded bands are dimmed. The replacement test had dropped the numeric opacity-inequality assertion that made the original meaningful (only strikethrough was checked); re-added an explicit `skipped_alpha < off_alpha == 1.0` assertion so the two states can't silently converge again |
| 158 | The "unsaved changes" confirmation dialog on app close fired even after a fully successful filter push to a device -- `_has_unsaved_changes()` only checked `len(current_filters) > 0`, with no concept of "already pushed", so the warning fired on every close once any filters were loaded, defeating its purpose | FIXED | YES | `7ac4e50` | Added `WizardState.last_pushed_filters`, snapshotted from `current_filters` in `_on_write_complete()`'s success branch (pydantic `CanonicalFilter` gives list equality by value for free). `_has_unsaved_changes()` now requires `current_filters != last_pushed_filters` in addition to non-empty. Also reset on device switch (`_on_device_selected`) and cleared on all three undo-success paths via a shared `_clear_pushed_snapshot()` helper, since an undone push means the device no longer reflects that snapshot -- the wizard correctly goes dirty again after Undo | Test: `test_unsaved_changes_true_before_push`, `test_push_success_clears_unsaved_flag`, `test_device_switch_resets_pushed_snapshot`, `test_undo_marks_dirty_again` (test_smoke_regression_operations.py) |
| 159 | mDNS device discovery always fell back to the slower subnet scan, even on networks where `avahi-browse`/equivalent tools found the device via mDNS instantly | FIXED | YES | `7ac4e50` | `ZeroconfDiscover.discover()` probed `_wiim._tcp.local.` and `_linkplay._tcp.local.` sequentially, each waiting the *full* timeout before moving to the next type. Devices that only advertise `_linkplay._tcp.local.` (older LinkPlay-based hardware) never get a chance to answer within that budget, since the first (non-existent on that device) probe consumes the entire timeout before the second even starts -- and `DiscoveryModule`'s grace-period orchestration waits less than that for the combined mDNS task, so it always gives up and uses subnet-scan results instead. Rewrote `_probe_service` (now `_probe_services`) to browse both service types concurrently in a single `AsyncServiceBrowser` call/timeout window, so whichever type the device actually advertises can respond without being starved by the other | Test: `test_zeroconf_probes_both_service_types_concurrently` (test_discovery.py) |
| 160 | Re-verifying #159's fix on the device owner's own machine (a rebuilt packaged Windows `.exe`, not the WSL2 dev environment used to confirm #159) showed discovery still taking ~12s with the full 254-host subnet scan running to completion and zero mDNS hits -- looked like #159 wasn't actually fixed | WONTFIX (doc/diagnostics only -- root cause is a Windows OS setting, not app code) | N/A | `97eca0c`-adjacent, not a code fix | Confirmed #159's code fix was independently correct (re-ran it directly against the device owner's real 4-device LAN from WSL2: found all 4 via mDNS in 1.8-3.3s). The packaged `.exe` symptom was a second, unrelated root cause: `Get-NetFirewallRule` showed the app's installed rule allows inbound only on the "Private" network profile and blocks "Public"; `Get-NetConnectionProfile` showed the active Wi-Fi was classified "Public". Reclassifying it to "Private" in Windows Settings dropped discovery to 2s -- confirmed by the device owner. This can't be fixed from app code (and shouldn't be worked around by loosening the firewall rule to allow Public networks, which would weaken a reasonable default). Documented in `src/gui/assets/help/troubleshooting.md` (rewrote the Firewall/mDNS section with the exact Settings path), `README.md` (new Troubleshooting section), `docs/architecture.md` (Discovery Module known-constraint note), and `docs/corrections.md`. Added a `ConnectPage` empty-state hint update and a `DiscoveryModule` log warning that fires when mDNS contributes zero devices but the subnet scan succeeds, so a shared support bundle can self-diagnose this without a repeat investigation | Test: `test_discover_warns_when_mdns_empty_but_subnet_scan_succeeds` (test_discovery.py) covers the new log warning; the firewall/network-profile cause itself is a Windows OS setting and not unit-testable |
| 161 | Setting `supports_roomfit: false` in the device capability file alone did not disable the RoomFit flow -- the flow-type gate only inspects `roomfit_level`, and `merge_into()` applied `supports_roomfit`/`roomfit_level` as fully independent overrides, so leaving `roomfit_level` at whatever the runtime probe found meant RoomFit still activated | FIXED | YES | `76fe850` | Added a ladder-enforcement block to `merge_into()` matching the invariant the prober already encodes (level 0 -> all false, 1 -> `supports_roomfit`, 2 -> `+read`, 4 -> `+write`), applied after the existing independent-override assignments so any bool override correctly re-caps `roomfit_level` | Test: `TestMergeInto::test_supports_roomfit_false_alone_forces_level_zero`, `test_supports_roomfit_read_false_caps_level_to_one`, `test_supports_roomfit_write_false_caps_level_to_three`, `test_roomfit_level_only_override_behaves_as_today` (test_device_capability_file.py) |
| 162 | The wizard's step-indicator summaries were computed independently in three separate code paths (normal flow, sidebar "jump to Review", `QuickSetupDialog` completion) and had drifted: EQ_TYPE showed `"ROOMFIT"` on one path and `"RoomFit"` on the others; FILTERS showed three different wordings depending on path | FIXED | YES | `76fe850` | Added shared helper methods (`_resolve_connect_summary`, `_compute_source_summary`, `_apply_source_summary`, `_resolve_filters_summary`) used by all three paths; unified `_EQ_TYPE_SUMMARY` constant; `_ensure_wizard_state_for_load`'s QuickSetupDialog branch now delegates entirely to `_mark_prior_steps_completed` instead of reimplementing the same logic a third time. Also added a Filters-step tooltip showing what the loaded filters came from (device preset/profile, local preset, REW file/measurement), threaded through all 8 producers of `current_filters` via a new `WizardState.filters_origin` field and a widened `profile_recalled` signal | Test: `TestIssue162ConsistentStepSummaries` (test_smoke_regression_wizard.py), `TestFiltersOrigin` (test_gui_filter_handlers.py), `TestWizardControllerSummaries` (test_wizard_controller.py) |
| 163 | REASSIGNED to #166 | REASSIGNED | N/A | N/A | Log analysis showed the `wifi` PEQ change blamed on RoomFit `EQLevel` cross-contamination was actually caused by a "Load into Editor" action on a PEQ preset ~70 seconds before the RoomFit push, which itself never touched `wifi`'s PEQ storage. See `docs/corrections.md` (2026-07-04) for the full disproof; #166 adds the confirmation dialog that addresses the actual underlying behavior (PEQ preset actions briefly activating live audio) | N/A |
| 164 | `wiim_adapter.py` had ~20 independent inline payload-construction sites (`{"pluginURI": ..., "source_name": ..., "EQLevel": ...}` + manual URL-encoding), which is how a real bug (RoomFit payloads incorrectly including `source_name`) went unfixed in some call sites after being fixed in others; `capability_prober.py` independently duplicated both the pattern and its own copy of the `PLUGIN_URI` constant | FIXED | YES | `76fe850` | New `src/adapters/wiim_commands.py` module (`encode_wiim_command()`, `expect_dict_response()`, shared `PLUGIN_URI`) used by both `wiim_adapter.py` and `capability_prober.py`. Extracted further shared helpers (`_set_fx_state`, `_read_fx_status`, `_fetch_profile_list`, `_delete_profile`) for genuinely-duplicated method bodies. Fixed the confirmed payload bugs: RoomFit payloads no longer send `source_name` (WiiM app never does); RoomFit writes now include `rc_output: AUDIO_OUTPUT_SPEAKER_MODE` to match real WiiM-app write traffic (does NOT change the saved profile's `Type` field -- per `docs/wiim_api_notes.md`'s RoomFit Quirk note, app-saved profiles are always `Type: "Custom"`, never `"RC"`; see `docs/corrections.md` 2026-07-04); PEQ enable/disable/status now send explicit `EQLevel: 1` | Test: `test_wiim_commands.py` (new), updated `test_write_roomfit_success`/`test_read_roomfit_success` (test_wiim_adapter.py) |
| 165 | The Name Profile step never indicated which RoomFit profile was currently active on the device, so users could unknowingly overwrite (and deactivate) it with no warning | FIXED | YES | `76fe850` | Added `WiiMAdapter.get_roomfit_status()` (queries `EQGetLV2SourceBandEx` with `EQLevel: 2` and an explicit empty `source_name` -- RoomFit's global scope, distinct from the per-source buffer read). `NameProfilePage` now bolds/accents the active profile's list entry with an "(active)" label and tooltip; a blocking confirmation dialog appears only when saving under the name of the profile that's both listed as active AND RoomFit is currently on (an inactive selection has no live-audio consequence) | Test: `TestGetRoomfitStatus` (test_wiim_adapter.py), `TestNameProfilePage::test_active_profile_property`/`test_active_item_has_label_and_styling` (test_gui_pages.py), `test_165_*` (test_smoke_regression_operations.py) |
| 165c | Follow-up to #165: the "Presets on Device" view had the same gap for PEQ presets and RoomFit profiles -- no indication of which one was currently active on the connected source/device | FIXED | YES | `76fe850` | `PresetsDeviceView.set_peq_presets()`/`set_roomfit_profiles()` gained an `active_name` parameter; matching items get the same bold/accent/"(active)" treatment as #165, visually distinct from click-selection highlighting. `_do_list_presets` fetches the active PEQ name via a plain `read_peq()` (confirmed harmless -- doesn't switch or activate anything) and the active RoomFit name via `get_roomfit_status()`, degrading to no highlight (not a failed view) if either read fails | Test: `TestPresetsDeviceViewActiveHighlight` (test_gui_views.py), `test_165c_*` (test_smoke_regression_operations.py) |
| 166 | Loading a saved PEQ preset's filters (Load into Editor, Export, Save to My Presets, Copy to Another Device on "Presets on Device") silently activated that preset live on the device's current input for the target source, with no warning to the user before their live audio changed | FIXED | YES | `76fe850` | Added `WiiMAdapter.read_peq_preset_preview()`/`read_roomfit_preset_preview()`, which read a saved preset's/profile's bands then restore the source's original active preset/profile afterward. A new `_confirm_preset_preview()` (extracted alongside `_format_preset_names()`/`_confirm_action()` from the existing Delete-confirmation code) shows a blocking dialog before any of the four "Presets on Device" actions that would otherwise read a PEQ preset live -- RoomFit profiles are excluded from the dialog since RoomFit's API working buffer is confirmed decoupled from its DSP on/off state (no audible effect to consent to) | Test: `TestReadPeqPresetPreview`, `TestReadRoomfitPresetPreview` (test_wiim_adapter.py), `test_166_*` (test_smoke_regression_operations.py) |
| 167 | Source-name data and its resolution (probe -> capability-file override -> generic fallback) were both problems: no API enumerated a device's real sources until now, and even once available, three separate hardcoded fallback-source lists existed across the GUI layer (`main_window.py` x2, `quick_setup_dialog.py`) that could -- and had started to -- drift independently | FIXED | YES | `76fe850` | Replaced dead `getStatusEx`/`InputList` parsing with a new `_probe_source_names()` using `getAudioInputEnable` (real enumeration on every model except WiiM Mini, which has neither this command nor RoomFit). Centralized the fallback into `merge_into()` (`DEFAULT_SOURCE_NAMES`, new shared constant) as the single place every capability-resolution path already flows through, deleting the three independent GUI-layer copies. Also added an optional file-wide `"default_max_bands"` key (capability file), threaded through a new `LoadedCapabilityFile` dataclass, answering a design question raised during review: yes, it applies globally to every device, and yes, it still degrades to the built-in constant on a missing/invalid value. Trimmed the bundled `device_capabilities.json` to just `WiiM_Mini` now that non-Mini models no longer need a `sources` override | Test: updated `test_capability_prober.py` (`getAudioInputEnable` fixture replacing the dead `InputList` one), new `TestMergeInto` cases and `TestLoadCapabilityFile` cases (test_device_capability_file.py) |
| 168 | "Presets on Device" showed the RoomFit section correctly on first connecting to a RoomFit-capable device, but if the user first connected to a non-RoomFit device (hiding the section) and then connected to a RoomFit-capable one, the section stayed hidden for the rest of the session | FIXED | YES | `76fe850` | `set_roomfit_hidden()` was the only place calling `self._roomfit_section.setVisible(False)`; `set_roomfit_profiles()` populated the list but never called the matching `setVisible(True)`. Added it, mirroring `set_roomfit_hidden()`'s own call | Test: `test_set_roomfit_profiles_reshows_hidden_section` (test_gui_views.py) |
| 169 | **Regression, hardware-reported:** every device skipped the EQ Type step and defaulted to PEQ, even RoomFit-capable ones -- `#164`'s payload consolidation over-generalized "RoomFit profile-CRUD commands omit `source_name`" into a blanket rule and applied it to `EQGetLV2SourceBandEx`'s band read/write too, which actually needs `source_name=""` (present, empty) -- a third, distinct form the generalization collapsed away. `capability_prober.py`'s level-2 RoomFit probe issuing the omitted form never advanced `roomfit_level` past 1, so `merge_into()`'s ladder correctly capped it below 2 on every device and the EQ_TYPE step was skipped entirely | FIXED | YES | `4ce086b` | Added `source_name=""` to the level-2 probe (`capability_prober.py`) and both `write_roomfit()` write branches (`wiim_adapter.py`); `read_roomfit()` now reuses the existing `_read_fx_status("", eq_level=2)` helper instead of a second, separately-maintained call. Added a guard to `encode_wiim_command()` (`wiim_commands.py`) that raises if a RoomFit (`EQLevel:2`) band/toggle command (`EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`/`EQChangeSourceFX`/`EQSourceOff`) gets a real *or omitted* `source_name` -- both directions of the mistake, since omission (not a real value) was the actual root cause here -- so a future new RoomFit call site can't reintroduce either mistake silently | Test: `test_read_roomfit_success`/`test_write_roomfit_success` (test_wiim_adapter.py) now assert the empty-string form is present, not just that a real name is absent; `test_roomfit_rejects_real_source_name`/`test_roomfit_band_command_rejects_omitted_source_name`/`test_roomfit_allows_empty_source_name`/`test_roomfit_allows_omitted_source_name`/`test_peq_still_allows_real_source_name` (test_wiim_commands.py) |
| 170 | `capability_prober.py`'s `_probe_profile_enumeration()` probed the confirmed-dead `EQGetLV2List` command (per `docs/wiim_api_notes.md`'s "Dead commands" list, it always returns `{"status":"Failed"}`); `caps.supports_profile_enumeration` came out `True` on every device only because that error response happened to be dict-shaped, not because the probe validated anything real | FIXED | YES | `4ce086b` | Switched the probe to `EQv2GetNewList` (`EQLevel:1`) -- the same command `list_peq_profiles()` already uses at runtime -- and tightened the success check to also reject a `"status"` key, not just non-dict responses | Test: 16 mock sites in `test_capability_prober.py` updated from `EQGetLV2List:` to `EQv2GetNewList:`, differentiated from the RoomFit level-1 probe's own `EQv2GetNewList` call by `EQLevel` value |
| 171 | Copying multiple presets to multiple target devices ("Apply to multiple devices") re-read (and briefly live-previewed) each source preset once per **target device** instead of once per preset -- `_do_copy_presets_batch_multi()` looped devices-outer/presets-inner, and each iteration's `read_peq_preset_preview()`/`read_roomfit_preset_preview()` briefly loads the preset onto the source device's live DSP before restoring it, so copying N presets to M devices caused M-1 redundant live-audio blips per preset | FIXED | YES | `4ce086b` | Split `_do_copy_preset_to_device()` into a new read-only `_read_preset_to_copy()` (called once per preset) and a write-only `_do_copy_preset_to_device()` (called once per (preset, device) pair); `_do_copy_presets_batch_multi()`'s loop reordered to presets-outer/devices-inner | Test: `test_issue33_copy_batch_iterates_all`, `test_issue153_copy_batch_counts_verification_failure`, `test_issue26_copy_preset_to_device_writes_to_target`, `test_issue69_copy_preset_to_device_has_channel_mode`, `test_issue34_copy_branches_on_preset_type`, `test_issue74_copy_batch_multi_iterates_all_devices`, `test_issue78_copy_status_message_format`, `test_issue79_copy_lr_roomfit_preserves_channel` (test_smoke_regression_operations.py) updated for the new read/write call shape |
| 172 | `_on_eq_type_selected()`'s `_EQ_TYPE_SUMMARY[eq_type]` dict lookup could raise an unhandled `KeyError` for any value other than exactly `"peq"`/`"roomfit"` -- not reachable through the current UI (`EqTypePage` only emits those two), but a public `@Slot(str)` entry point with no validation guard | FIXED | N/A | `4ce086b` | Changed to `_EQ_TYPE_SUMMARY.get(eq_type, eq_type.upper())`, restoring a safe fallback instead of crashing | Not independently tested -- not reachable through the current UI; covered incidentally by existing EQ-type-selection tests continuing to pass |
| 173 | `_on_eq_type_selected()` didn't clear `state.filters_origin` when switching between PEQ and RoomFit on the same connected device, so the Filters step tooltip could show the previous EQ type's origin string (e.g. "PEQ preset: Movie Night") after switching to RoomFit with nothing loaded yet | FIXED | N/A | `4ce086b` | Clears `state.filters_origin = ""` in `_on_eq_type_selected()`, mirroring `_on_device_selected()`'s existing clear on device switch (`#162d`) | Not independently tested -- covered incidentally by existing EQ-type-switch tests continuing to pass |
| 174 | "Presets on Device" fetched the PEQ pair (`list_peq_profiles()` + active-name read) and the RoomFit pair (`list_roomfit_profiles()` + `get_roomfit_status()`) fully sequentially in `_do_list_presets()`, even though neither pair depends on the other's result -- 4 sequential round trips where 2 independent pairs of 2 exist | FIXED | N/A | `4ce086b` | Wrapped the two independent fetches in `asyncio.gather()`; each pair's own two calls stay sequential relative to each other | Not independently tested -- both fetch paths' individual behavior is already covered by existing `test_gui_views.py`/`test_smoke_regression_operations.py` tests, which continue to pass under the concurrent structure |
| 175 | **Regression, hardware-reported:** the active RoomFit profile was never highlighted in either "Presets on Device" or the "Name Profile" step, even immediately after selecting a profile in the WiiM Home app -- PEQ's equivalent highlighting worked correctly. Root cause was not in the highlighting/matching code (which is correct and symmetric with PEQ) but in `CapabilityProber._probe_roomfit()`'s level-4 write-capability test: it saves the buffer to a throwaway name (`__wiim_rew_sync_probe__`) then deletes it on every device connection, and since the RoomFit buffer "adopts" whatever name it's saved under, deleting that same name orphans it -- silently deactivating the user's real active profile (confirmed live: the WiiM Home app itself shows RoomFit as deselected once this app connects) | FIXED | YES | `e3e0962` | `_probe_roomfit()` now captures the buffer's pre-probe active `Name` (from the level-2 probe response already in hand) before the level-4 save/delete sequence, and restores it via `EQv2SourceLoad` once cleanup is done -- skipped if the buffer had no name to begin with | Test: `test_roomfit_level_4_probe_restores_active_profile`, `test_roomfit_level_4_probe_skips_restore_when_no_prior_active_name` (test_capability_prober.py) |
| 176 | The Settings "Default REW export folder" (`AppSettings.rew_export_folder`) was only ever honored for stereo-mode export dialogs; both L/R export call sites (`_export_filters_as_rew`, `_on_preset_export_requested`) called `ExportDialog.get_paths()` with no directory argument at all, and `ExportDialog` hardcoded `Path.home()` internally, so the configured folder was silently ignored for L/R exports. Separately, the Filters step's REW file import dialogs (stereo and L/R) had no default directory at all (always `""`), and the three configurable Settings paths displayed inconsistent separators on Windows (backslash for auto-populated defaults via `str(Path(...))`, forward slash for user-browsed ones via `QFileDialog`) | FIXED | YES | `e3e0962` | Renamed the setting to `rew_folder` (label: "Default REW import/export folder") since it now drives both directions. `ExportDialog`/`ExportDialog.get_paths()` gained a `default_dir` parameter, threaded from `self._settings.rew_folder` at both L/R call sites, replacing the internal `Path.home()` hardcode. `FiltersPage` gained `set_default_import_folder()`, called from `MainWindow._apply_settings()`/`_on_settings_changed()`; its three browse handlers now start from that folder and then remember the last directory the user navigated to for the rest of the session (`_browse_start_dir()`), overriding the Settings default without persisting to disk. Added `src/utils/app_dirs.py::to_display_path()` (forward-slash normalization) and applied it in `SettingsView.set_settings()`/`_on_browse_clicked()` so all three path fields render consistently regardless of Windows/Qt separator differences | Test: `test_issue176_lr_export_uses_settings_default_folder`, `test_issue176_preset_lr_export_uses_settings_default_folder` (test_smoke_regression_operations.py) |
| 177 | **Regression, hardware-reported (same-day follow-up to #175):** using the app eventually caused the WiiM Home app to show the active source's PEQ preset as unnamed/"Custom" instead of the actually-selected named preset, with the same filters still in place. Same root-cause class as #175, different command: `CapabilityProber._probe_batch_write()`'s "read current bands, write them back unchanged" test issues a raw `EQSetLV2Band` write on every PEQ-capable device connection -- confirmed via curl reproduction against real hardware that this drops the live source's `Name` association even when the written bands are byte-identical to what was just read | FIXED | YES | `e3e0962` | `_probe_batch_write()` now captures the pre-write `Name` and the read response's own echoed `source_name` (the probe has no configured source_name of its own -- it runs before the wizard's source is chosen), and restores the name via `EQv2SourceLoad` scoped to that source once the write attempt is done -- skipped if there was no name to restore | Test: `test_batch_write_probe_restores_active_preset`, `test_batch_write_probe_skips_restore_when_no_prior_active_name` (test_capability_prober.py) |
| 178 | **Hardware-reported:** pushing a new RoomFit profile, or overwriting an existing non-active one, made it the device's active/selected profile -- contradicting the Name Profile step's own note ("Saving does not make this profile active on your device"). Separately, Undo after overwriting an existing profile restored the old filter content correctly but left whichever profile the overwrite had activated still active, instead of reverting to the profile that was active before the original push. Same root-cause family as #175/#177: `RoomFitSafeWrite.execute()`'s mandatory post-write verification read (`read_roomfit()`) has to `EQv2SourceLoad` the profile before reading its bands -- a real, persisted action, not a side-effect-free peek -- so every push's own verification step was activating whatever it had just written | FIXED | YES | `e3e0962` | `RoomFitSafeWrite.execute()` now captures the profile active before the write (`get_roomfit_status()`) and restores it via the newly-public `WiiMAdapter.load_roomfit_profile()` in a `finally` block once the write/verify/rollback sequence completes, unless the restored name would equal the profile just written. Since this runs on every `execute()` call, Undo's own restore-write naturally re-derives and preserves whatever was correctly left active by the (now-fixed) original push, with no extra state needing to be threaded through the backup file | Test: `TestRoomFitActiveProfileRestore` (6 cases: new profile, overwrite non-active, overwrite-active no-op, no prior active, restore-after-rollback, get-status-failure-is-non-fatal) and `TestRoomFitPushThenUndoPreservesActiveProfile::test_push_then_undo_leaves_original_active_profile_active` (an integration-style test using a stateful fake device to reproduce the exact push-then-undo scenario reported) (test_safe_write.py) |
| 179 | Push step layout: redundant "Push to Device"/"Push Progress" titles, page title visually off (an unrelated latent bug in `build_centered_content` centered the whole content column whenever none of a page's children happened to be wide/Expanding -- PushPage was the first page compact enough to expose it), stepper checkmarks not aligned in a column (each row centered independently, so icons shifted with each row's differing label length), and the result card/OK/Undo buttons could be silently clipped by the window edge on a short window (MainWindow's central area has no scroll area) | FIXED | YES | `e3e0962` | Removed the duplicate "Push Progress" subtitle. Fixed `build_centered_content` (`page_layout.py`) to use an Expanding-policy `content` widget in a stretch-widget-stretch row instead of a blanket `outer_layout.setAlignment(...)` call, so it always fills up to `MAX_CONTENT_WIDTH` regardless of children's sizeHint (this also restored every other page's title to its correct left-aligned position, unchanged visually since they all already had a wide/Expanding child masking the bug). Stepper rows now fill a shared-width container (icons align in a column) which is itself centered as one block. Wrapped the stepper+result content in a `QScrollArea` (`PushPageScrollArea`, same pattern as `ConnectPage`'s device list) so tall content becomes scrollable instead of clipped. Also wrapped the result icon/message/detail in a state-colored `QFrame` card and aligned the stepper's "complete" color with the hero checkmark's color token | Test: `test_title_is_left_aligned_like_other_pages`, `test_stage_icons_align_in_a_column_regardless_of_label_length`, `test_content_scrolls_instead_of_clipping_when_too_tall`, `test_failure_detail_not_clipped_at_min_window_height` (test_gui_pages.py) |
| 180 | Systemic layout bug: calling `layout.setAlignment(...)` on a widget's own top-level `QVBoxLayout` that also contains a word-wrapped `QLabel` sizes that label via `sizeHint()` computed at an unbounded width, under-reporting the height it needs once actually wrapped at its real (narrower) width -- silently clipping the text. Found at 6 call sites beyond the originally-reported `RewPullView` placeholder: `ConnectPage`'s scanning and empty-devices widgets, `EQTypePage`'s card description, `PresetsDeviceView`'s empty state (plus a missing `setWordWrap` on `_peq_unavailable_label`), and `OnboardingOverlay`'s capability cards (which had a fragile `setMinimumHeight(FONT_SIZE_X * N + SPACING)` guess-based workaround for this exact symptom instead of a real fix) | FIXED | YES | `e3e0962` | Added a shared `center_column()` helper to `page_layout.py` (adds stretches instead of a blanket layout alignment) and documented the rule in its docstring. Applied the same technique at every site: non-wrapped siblings get item-level `alignment=Qt.AlignmentFlag.AlignHCenter`, wrapped labels stay on a plain `addWidget()`. Removed the now-unnecessary `setMinimumHeight` guesses in `OnboardingOverlay`'s capability cards | Test: `test_long_message_not_clipped_at_narrow_width` (test_gui_views.py), `test_empty_state_causes_text_not_clipped_at_narrow_width`, `test_card_description_not_clipped_at_narrow_width` (test_gui_pages.py) |
| 181 | `StatusBanner` message text could overflow horizontally (e.g. L/R mode per-channel validation warnings joined with `" \| "`) even at a maximized window, with no way to see the rest of the message | FIXED | YES | `e3e0962` | Added width-aware elision (`QFontMetrics.elidedText`, computed from the banner's own width minus sibling widgets' sizes rather than the message label's possibly-stale width, to avoid a race right after `_show()` changes progress-bar/close-button visibility) plus a full-text tooltip with each `" | "`-delimited segment on its own line. Banner height intentionally left unchanged (still `setFixedHeight`) to avoid a layout jump. Elision is skipped for an unparented banner (its top-level size is an arbitrary platform default, not meaningful) so existing exact-text assertions keep working | Test: `test_long_message_elides_with_full_text_in_tooltip`, `test_short_message_not_elided_no_tooltip` (test_gui_components.py) |
| 182 | Dry Run checkbox on the Review step had no explanation for non-technical users (on by default, easy to assume a push silently failed when nothing changes on the device), and no easy way to find the global default toggle buried in Settings | FIXED | YES | `e3e0962` | Added an always-visible inline caption below the checkbox, expanded the in-app User Guide's "Dry Run Mode" section, and added a one-time prompt (reusing the existing `_confirm_action` helper already used for the RoomFit overwrite confirmation) offering to disable the global default the first time a user turns it off while it's still on. Extracted `MainWindow._populate_settings_view()` out of `_apply_settings()` so both the startup path and the new prompt path can refresh SettingsView without clobbering other fields (`SettingsView.set_settings()` has no partial-update mode) | Test: `test_dry_run_hint_visible` (test_gui_pages.py); `TestDryRunDefaultPrompt` (4 cases: no prompt when already off, no prompt on toggle-on, prompt shown on toggle-off, Yes persists + refreshes SettingsView) (test_main_window_settings.py) |
| 183 | Name Profile step: no way to populate the name field from an existing profile (had to retype it exactly to intentionally overwrite), and overwriting a non-active existing profile gave no warning at all -- only an exact match against the *active* profile was checked, so overwriting a different stored profile's filters was silent | FIXED | YES | `e3e0962` | Added a `NameProfilePage.classify()` helper (none/existing/active) as the single source of truth shared by the inline warning label and `MainWindow._on_name_confirmed`'s pre-save confirm dialog, so "overwrite active" (deactivates RoomFit) and "overwrite existing" (data loss only, no RoomFit effect) get distinct, accurate wording. Wired `_profiles_list` item clicks to populate the name field (`SingleSelection` + `itemClicked`, raw name stored via `Qt.ItemDataRole.UserRole` to avoid re-parsing the "(active)" display suffix) -- purely local, reuses the profile list already fetched once via `set_existing_profiles()`, no extra device query. Added a permanent caption clarifying that saving does not make a profile active (confirmed against `docs/wiim_api_notes.md`'s RoomFit save-workflow section, hardware-tested). The existing overwrite-confirm and RoomFit Undo machinery already covered the *active*-profile case and needed no changes | Test: `test_classify`, `test_warning_shown_for_existing_non_active_profile`, `test_click_existing_profile_populates_name`, `test_click_active_profile_populates_name_without_suffix` (test_gui_pages.py); `test_183_name_confirmed_dialog_shown_when_overwriting_non_active`, `test_183_name_confirmed_proceeds_on_yes_for_non_active`, `test_183_name_confirmed_no_dialog_for_brand_new_name` (test_smoke_regression_operations.py) |
| 184 | **Found via `/code-review ultra` on the #178 diff:** a device-supplied RoomFit/PEQ preset name containing HTML-significant characters (`<`, `>`, `&`) would render incorrectly in the "Preset Will Briefly Activate on Device" confirmation dialog (`_confirm_preset_preview()`), since the message is embedded with `<br>`/`<b>` tags (making Qt auto-detect it as rich text) but the interpolated preset name was never escaped, and the shared `_format_preset_names()` helper's `\n`-joined bullets don't render as line breaks in an HTML context either | FIXED | YES | `e3e0962` | Escape the formatted name block with `html.escape()` and replace `\n` with `<br>` at this one HTML call site only (the plain-text delete-confirmation call site, which correctly uses literal `\n`, is untouched) | Test: `test_166_confirm_preview_escapes_device_supplied_preset_name` (test_smoke_regression_operations.py) |
| 185 | **Found via `/code-review ultra`:** `AppSettings.rew_export_folder` was renamed to `rew_folder` (#176) with no migration path -- a `settings.json` saved by a prior build would have its `rew_export_folder` key silently dropped by `load()`'s unknown-key filter, reverting a user's configured default REW folder back to `""` on upgrade with no warning | FIXED | YES | `e3e0962` | `AppSettings.load()` now maps a legacy `rew_export_folder` key onto `rew_folder` before the known-fields filter runs, only when the new key isn't already present | Test: `test_migrates_pre_rename_rew_export_folder_key`, `test_new_rew_folder_key_takes_precedence_over_old` (test_app_settings.py) |
| 186 | **Found via `/code-review ultra`:** the CLI's `get-roomfit-filters` command called `adapter.read_roomfit()` directly (same root cause as #175/#178) -- reading a named RoomFit profile requires `EQv2SourceLoad`-ing it first, which silently makes it the device's active/selected profile, so running this read-only CLI command on its own deactivated whatever the user actually had selected | FIXED | YES | `e3e0962` | Switched to the existing `read_roomfit_preset_preview()`, which already captures and restores the previously-active profile | Test: `test_get_roomfit_filters_uses_preview_not_raw_read` (test_cli.py) |
| 188 | Follow-up to the #178 hardware confirmation (docs/corrections.md, 2026-07-05): the Name Profile page's inline warning, its `classify()` docstring, and `MainWindow._on_name_confirmed()`'s "Overwrite Active Profile" confirm dialog all still claimed overwriting the active RoomFit profile "may deactivate RoomFit until you reselect a profile in the WiiM Home app" -- a curl reproduction against real hardware confirmed this is false: `Name` and `EQStat` are unchanged before/after, the profile just stays active with updated content | FIXED | YES | `e3e0962` | Reworded `_ACTIVE_WARNING` (name_profile_page.py) and the confirm-dialog message (main_window.py) to describe the real effect ("updates what's currently playing") instead of the disproven deactivation claim; updated both files' docstrings to match. The confirmation dialogs themselves are unchanged in when they appear (still gated on `roomfit_enabled` -- overwriting a profile that isn't currently applied has no live effect either way) | Test: `test_warning_shown_for_existing_non_active_profile`, `test_click_active_profile_populates_name_without_suffix` (test_gui_pages.py, updated to check for "currently playing" instead of the removed "deactivate" wording) |
| 189 | The Push page's 4-stage stepper (Backing up/Writing/Verifying/Done) could only ever show all-pending or all-complete/all-failed -- `main_window.py::_do_push()` called `SafeWrite.execute()`/`RoomFitSafeWrite.execute()` as one opaque `await` with no intermediate hook, so `PushPage.set_stage()` was never actually called mid-flow. `set_failure()`'s "mark whichever stage is active as failed" logic was already correct and ready for mixed states -- it just never received one, making the stepper practically decorative and unable to show which stage actually failed | FIXED | YES | `e3e0962` | Added an optional `on_stage: Callable[[str], None] \| None = None` parameter to both `SafeWrite.execute()` and `RoomFitSafeWrite.execute()` (`safe_write.py`), invoked with `"backing_up"`/`"writing"`/`"verifying"` before each corresponding step and `"done"` only on success -- never on failure, so `set_failure()`'s existing last-active-stage logic now has a real stage to mark. Added a new `AsyncBridge.stage_changed` signal (thread-safe delivery from the async worker thread, matching the existing `progress_update` pattern) and a `MainWindow._on_stage_changed` slot that forwards to `PushPage.set_stage()`. `_do_push()` passes `on_stage=self._bridge.stage_changed.emit` to both `execute()` calls and drops its own now-redundant (and, for "Verifying...", already-stale-by-the-time-it-fired) hardcoded progress text | Test: `TestOnStageCallback`, `TestRoomFitOnStageCallback` (test_safe_write.py, stage ordering + "done" never called on failure); `test_issue189_do_push_peq_passes_on_stage_to_safe_write`, `test_issue189_do_push_roomfit_passes_on_stage`, `test_issue189_stage_changed_advances_push_page_stepper` (test_smoke_regression_operations.py) |
| 190 | User-reported (live hardware): a device with RoomFit originally **disabled** (`EQStat:"Off"`) came back with RoomFit **enabled** after nothing more than connecting to it -- `CapabilityProber._probe_roomfit()`'s level-4 write-capability test (save-then-delete of a throwaway `__wiim_rew_sync_probe__` profile, added by #177's fix) already captured and restored the pre-probe active profile *name*, but never captured or restored the pre-probe DSP on/off state, so a device that started disabled came out enabled. **This is a live-audio safety issue, not cosmetic:** a named RoomFit profile stays "selected" even while disabled, and it's that *persisted, selected* profile -- not the transient working buffer -- that gets applied to live playback the instant RoomFit is enabled, so an unrelated capability probe could have briefly applied a user's unknown, possibly-extreme filter to their speakers | FIXED | YES | `e3e0962` | Two iterations before landing on the real fix: (1) capture/restore `EQStat` unconditionally after the level-4 test, (2) reorder that restore to fire immediately after `EQSourceSave` instead of after cleanup, to shrink the exposure window. Both were superseded once it was clarified that *any* window, however short, during which a real selected profile could be briefly enabled is unacceptable -- restoring afterward can't undo audio that was already audible. **Final fix:** `_probe_roomfit()` now skips the level-4 write test entirely whenever `EQStat` is `"Off"` **and** a real profile is selected (`Name` non-empty) -- no `EQSourceSave`, `EQv2Delete`, or toggle command is issued at all in that state; `roomfit_level` stays at 3 (write unconfirmed) for that connection. The test still runs normally when `EQStat` is already `"On"` (whatever's selected is already audible, unaffected by this test) or when nothing is selected (fresh device, nothing to expose) -- the restore-immediately-after-save logic from the earlier iterations is kept as defense-in-depth for those two cases. Documented the "selected profile survives disable" product behavior in `docs/wiim_api_notes.md`'s DSP on/off toggle section (previously undocumented). Audited the rest of the probe sequence for the same gap (raised by the user's "could there be other such cases" question): `_probe_batch_write()`'s PEQ write test already restores `Name` (#175) and is confirmed by an existing hardware finding not to touch `EQStat`; every other probe step is a pure read with nothing to restore. **Known follow-up, not addressed here:** `roomfit_level == 3` can now mean either "device genuinely lacks write support" or "skipped this session for safety" -- the CLI's existing error message for this case ("Dedicated RoomFit filters not available on this device") is inaccurate for the second case; disambiguating would need a new capability field | Test: `test_roomfit_level_4_full_support`, `test_roomfit_level_4_probe_restores_active_profile` (`EQStat:"On"` -- test still runs, restore ordering asserted), `test_roomfit_level_4_probe_skips_restore_when_no_prior_active_name` (`EQStat:"Off"`, no profile selected -- test still runs), `test_roomfit_level_4_probe_skipped_when_disabled_with_selected_profile` (the actual reported scenario -- `EQStat:"Off"`, `Name:"DangerDanger"` -- asserts the write test is never attempted) (test_capability_prober.py) |
| 191 | Product redesign (not a bug report): `RoomFitSafeWrite.execute()` treated a RoomFit push's activation of the written profile as an unwanted side effect to suppress -- it restored whichever profile was active *before* the push once the write finished (#178), and never touched `EQStat` at all. This fought user intent (a user pushing a RoomFit config wants it applied) and left a live-audio safety gap symmetric to #190: if `EQSourceSave` flips `EQStat` on as an undocumented side effect during a normal push (not just probing), the restored-but-unrelated previously-active profile would become audible with RoomFit now silently enabled | FIXED | YES | `e3e0962` | Redesigned the device-visible contract: a **successful** push now deliberately activates the written profile and turns RoomFit on if it was off, via confirmed commands (new `WiiMAdapter.enable_roomfit()`/`disable_roomfit()`/`set_roomfit_enabled()`) rather than an unconfirmed API side effect; a **failed** push restores the original selection and on/off state (safety-before-convenience); **Undo** (new `RoomFitSafeWrite.undo()`) restores the *original push's* full pre-state (previous bands if an existing profile was overwritten, previous selection, previous on/off state) and is now always available after a successful push (previously hidden for new-profile pushes) -- per explicit product decision, Undo never deletes a profile created by a new-profile push (non-destructive; the user can remove it via "Presets on Device"). `write_roomfit()`'s gate loosened from `roomfit_level >= 4` to `>= 2` so a device whose write-capability probe was skipped for #190 safety gets a real push attempt as its own capability confirmation, upgrading `roomfit_level` to 4 in-session on verified success. "Copy to Another/Multiple Device(s)" already shared the same `RoomFitSafeWrite.execute()` write path, so it inherits this behavior automatically -- only its confirmation dialog needed a new RoomFit-specific warning (`_confirm_roomfit_copy_activation()`). `BackupRecord` gained three optional fields (`pre_write_active_profile`, `pre_write_roomfit_enabled`, `was_new_profile`) to carry this restore metadata; a bugfix was needed in `BackupManager.create_backup()`'s stereo-mode filter conversion (`if settings.bands else None` coalesced an intentionally-empty new-profile metadata backup to `None`, which the `Profile` validator then rejected) since a new-profile push now always creates a backup purely to carry this metadata, even with no bands to restore. Consolidated three pre-existing duplication patterns touched by this change: a shared `_require_roomfit_level()` gate check (was hand-rolled 4x across `wiim_adapter.py`), a shared `set_roomfit_enabled()` toggle setter, and a shared `_restore_selection_and_enable_state()` helper (used by both `execute()`'s failure paths and `undo()`, replacing two independently-written near-duplicate restore blocks) | Test: rewritten `TestRoomFitActiveProfileRestore`/`TestRoomFitPushThenUndoPreservesActiveProfile`, new `roomfit_level`-upgrade-on-success/no-upgrade-on-failure tests, new-profile-undo-keeps-profile test, old-format-backup-degrades-gracefully test (test_safe_write.py); loosened-gate tests (test_wiim_adapter.py); Undo-always-shown, updated confirm-dialog wording tests (test_smoke_regression_operations.py, test_gui_pages.py) |
| 192 | **User-reported (live hardware):** "Presets on Device", Load into Editor, Export as REW File, and Save to My Presets -- all read-only RoomFit actions, all funneling through `read_roomfit_preset_preview()` -- re-enabled RoomFit if it had been off, the same live-audio safety pattern already fixed for the *write* path by #190/#191. Root cause: the method's docstring claimed reading a profile "has no live-audio consequence to consent to," so it captured `EQStat` via `get_roomfit_status()` but discarded it, restoring only the previously-selected profile `Name`, never the on/off state -- `EQv2SourceLoad` (issued by `load_roomfit_profile()`, called before every band read) **confirmed via isolated curl reproduction** (`docs/corrections.md`, 2026-07-06) to unconditionally flip `EQStat` on, independent of any other command -- this also revises #190's likely mechanism from `EQSourceSave` to the `EQv2SourceLoad` restore step in its own cleanup sequence (see corrections.md; #190's fix itself needed no change) | FIXED | YES | `c7bbe16` | `read_roomfit_preset_preview()` now captures and restores both `Name` and `EQStat`, via a new shared `WiiMAdapter.restore_roomfit_selection_and_enable_state()` -- moved up from `RoomFitSafeWrite`'s own private `_restore_selection_and_enable_state()` (safe_write.py), now a thin delegator, so the write path and preview-read path can't independently drift the way this bug happened. Corrected the disproven docstring claim. Defensively applied the same fix to PEQ's `read_peq_preset_preview()` (new `set_peq_enabled()`) since `load_peq_profile()` wraps the same `EQv2SourceLoad` command family -- marked `# ASSUMPTION:`, not independently hardware-confirmed for PEQ, but safe regardless. Also removed two stale tests in `test_gui_push_export.py` (`TestPushProgressUpdates`) asserting `_do_push()`'s pre-#189 hardcoded progress-text emissions, superseded by `test_issue189_*` (test_smoke_regression_operations.py) | Test: `TestReadRoomfitPresetPreview`/`TestReadPeqPresetPreview` (updated), new `TestRestoreRoomfitSelectionAndEnableState`, `TestSetPeqEnabled` (test_wiim_adapter.py); `TestRoomFitActiveProfileRestore` assertions updated to the consolidated adapter call (test_safe_write.py) |
| 193 | Product redesign (not a bug report), mirrors #191 for PEQ: plain `SafeWrite` (PEQ's push/copy/undo protocol) had no enable-state awareness at all -- `execute()` never called `enable_peq()`/`disable_peq()`, and the GUI had zero visibility into whether PEQ was on or off for a source, the exact gap RoomFit had before #191, confirmed to matter by #192's hardware finding (`EQv2SourceLoad` unconditionally enables both RoomFit's global buffer and PEQ's per-source form) | FIXED | YES | `c7bbe16`, `4e5775e` | A **successful** PEQ push now turns PEQ on via `WiiMAdapter.enable_peq()` (best-effort, deliberate, not an unconfirmed side effect); a **failed** push restores the source's original enable-state via `set_peq_enabled()`; **Undo** (new `SafeWrite.undo()`) restores bands via `execute()` then corrects the enable-state to the true original captured in a new optional `BackupRecord.pre_write_peq_enabled` field, since `execute()`'s own success path always enables and can't be trusted alone for undo (same trap RoomFit's `undo()` already guards against). `SecondaryWorkflowManager._do_undo()` now delegates to `SafeWrite.undo()` instead of re-parsing the backup and calling `execute()` directly, which also fixed a latent bug where a failed undo was reported to the user as success (the method never inspected `execute()`'s return value). `src/cli/main.py`'s direct `SafeWrite.execute()` call and `_do_copy_preset_to_device()`'s PEQ branch both inherit the new behavior automatically, same as #191's Copy-to-Device inheritance. Copy-to-Device's confirmation dialog (`_confirm_roomfit_copy_activation()`) generalized to `_confirm_copy_activation()`, covering both PEQ and RoomFit in one dialog rather than a second near-identical PEQ-only method -- `PresetsDeviceView` enforces mutual exclusion between its two preset lists today so a mixed selection can't currently occur, but the shared method avoids that drift pattern regardless. Extracted a small `_html_bullet_list()` helper (`main_window.py`) replacing a two-line escape/replace snippet duplicated at each confirmation-dialog call site. Also extracted `WiiMAdapter.set_peq_enabled_best_effort()`, consolidating four near-identical enable-state try/except/log blocks (safe_write.py's execute()/rollback/undo, wiim_adapter.py's read_peq_preset_preview()) into one shared method, mirroring restore_roomfit_selection_and_enable_state()'s role for RoomFit | Test: new `TestPeqEnableOnSuccess`, `TestPeqEnableRestoreOnFailure`, `TestPeqBackupCarriesEnabledState`, `TestPeqUndoRestoresEnabledState`, `TestPeqUndoOldFormatBackup`, `TestPeqUndoEmptyBandsBackup` (test_safe_write.py); new `TestSetPeqEnabledBestEffort` (test_wiim_adapter.py); rewritten `TestUndoSuccessWithValidBackup` plus new undo-failure-reporting test (test_gui_integration_secondary.py); updated copy-dialog rename/mixed-selection tests (test_smoke_regression_operations.py) |
| 194 | **User-reported (live hardware):** `EQGetSourceModes` output on WiiM Sound revealed real, named, enabled PEQ slots stored under the literal source names `"wifi,bluetooth,auxIn"` and `"wifi,bluetooth"` -- permanent junk slots (no command deletes a slot once written; factory reset presumed the only removal, confirmed 2026-07-10). Root cause: `state.selected_source` is comma-joined by design for the multi-select push flow, but only `_do_push()` split it -- five other call sites passed it raw into `EQv2SourceLoad`-backed writes (`_do_load_peq_preset`, `_read_preset_to_copy` (shared by Save to My Presets / Export / Copy to Another Device), `_do_preset_export`, `_do_preset_save`, and `_on_copy_to_device_requested`'s `target_source`, which writes to *other* devices), so any preset operation performed while a multi-source selection was in wizard state wrote to a junk slot with `{"status":"OK"}` and no error | FIXED | YES | `f82f444` | New `WizardState.primary_source` property (wizard_controller.py) -- first comma-separated entry, whitespace-stripped, defaulting to the new `DEFAULT_SOURCE` constant (models/constants.py) -- is now the only place split+default logic lives; all five write-path sites plus the read-only single-source sites (`_do_device_pull`, `_do_roomfit_pull`, profile-list fetches) migrated from `state.selected_source or "wifi"` to it. Defense in depth: `encode_wiim_command()` (wiim_commands.py) now raises `ValueError` on any `source_name` containing a comma, backstopping every adapter path (all adapter payloads route through it). Undo paths' hand-rolled `.split(",")[0].strip()` copies replaced by the same property | Test: new `TestWizardStatePrimarySource` (test_wizard_controller.py); new `TestIssue194SingleSourceOperations` (test_smoke_regression_operations.py); new `test_rejects_comma_joined_source_name` (test_wiim_commands.py) |
| 195 | Diagnostics panel's "Source Slots" table concatenated the valid/invalid indicator into the Source cell as a `"  ⚠ unknown"` text suffix, mislabeled the EQ preset-name column "Name" and the on/off column "Status" (both ambiguous outside the EQ context), and had no column-width configuration at all | FIXED | YES | `2de8eca` | Split into 5 explicit columns (`Source`, `Valid`, `EQ Preset`, `Mode`, `EQ Status`); `Valid` now holds `slot.is_known_source` as its own `Yes`/`No` cell instead of inline text. Header uses `QHeaderView.ResizeMode.ResizeToContents` + `setStretchLastSection(False)` so columns size to content -- a deliberate divergence from `filter_table.py`/`import_dialog.py`'s `Stretch` convention, appropriate here since this is a compact diagnostic table, not a wide container-filling one | Test: `test_populates_table_rows` (test_diagnostics_panel.py, updated for the new column layout) |
| 196 | The GUI never told the user when displayed device capabilities weren't from live probing -- either overridden by a bundled capability-file entry (`device_capabilities.json`) or fallen back to generic/conservative defaults (e.g. `DEFAULT_SOURCE_NAMES` on a WiiM Mini or an unrecognized model) -- both of which can misrepresent what the connected device actually supports, with nothing in the UI hinting the data might not be authoritative | FIXED | YES | `2de8eca` | Added `DeviceCapabilities.capability_file_override`/`used_generic_capabilities` flags, both set inside `merge_into()` (`device_capability_file.py`), the single merge point every `probe()` call flows through. `SidebarNav.set_device_info()` gained an optional `capability_warning` param -- when non-empty, appends a `"  ⚠"` suffix to the device label and sets an explanatory tooltip, wired from `MainWindow._on_capabilities_ready()`. Chosen over a transient `StatusBanner` message specifically because the sidebar is visible on every wizard step, so the warning persists rather than getting overwritten by the next progress message. Also removed the Source page's "Sound/Lite models use 'auxIn'..." hint sentence -- unrelated to fallback state and not worth the dynamic suppression logic it would have needed to keep accurate | Test: `TestMergeInto` new cases (test_device_capability_file.py); `TestSidebarNav` new cases (test_gui_components.py); `TestCapabilityFallbackSidebarWarning` (test_smoke_regression_wizard.py) |
| 197 | Exported/saved preset and profile files (REW export, Save to My Presets) carried no device attribution in their filename or saved name, both from the wizard Review step and the "Presets on Device" sidebar -- ambiguous and risky when managing more than one WiiM device, since two devices' exports could silently look identical | FIXED | YES | `2de8eca` | Added a shared `MainWindow._device_prefixed_name()` helper (next to the existing `_lookup_device_name()`) and applied it at all five save-to-file sites, including the two that already had an inline, independently-written `f"{device_name} - ..."` prefix (removing that small duplication) and the stereo REW-export branch, which previously had no seeded filename at all. Correctness constraint preserved: the prefix is applied only to the destination filename / local `Profile.name`, never to the string sent back to the device -- `_on_preset_export_requested`/`_on_preset_save_requested`/`_do_preset_save` keep the unprefixed `preset_name` for `read_peq_preset_preview()`/`read_roomfit_preset_preview()`, since that's the exact on-device preset identifier, not just a display label | Test: `test_stereo_export_seeds_device_prefixed_filename`, `test_preset_export_seeds_device_prefixed_filename`, `test_do_preset_save_reads_device_with_unprefixed_name` (new), `test_issue24_presets_device_save_connected` (updated) (test_smoke_regression_operations.py) |
| 198 | No way to manually re-trigger device discovery on the Connect step once devices were already listed (only an initial auto-scan and a Retry button that only exists in the empty state); separately, the `Ctrl+R` shortcut was a dead end -- `MainWindow._on_shortcut_refresh()` only called `set_scanning(True)` and never actually re-ran discovery | FIXED | YES | `2de8eca` | Added a small icon-only rescan button (`⟳`) next to the Connect page title, wired directly to the existing `refresh_requested` signal (same signal the Retry button already emits) -- no new signal or business logic needed; disabled while `set_scanning(True)` is active. Fixed `Ctrl+R` to call `_on_refresh_requested()`, the same handler the button and Retry both use, so all three paths now behave identically | Test: `test_rescan_button_emits_refresh_requested`, `test_rescan_button_disabled_while_scanning` (test_gui_pages.py); `TestCtrlRRescanShortcut` (test_smoke_regression_wizard.py) |
| 199 | `UnsavedChangesDialog`'s "Save" button appeared to do nothing on close -- `MainWindow.closeEvent()` treated the `"save"` and `"discard"` choices identically (comment admitted "save logic would be handled by task 11.2 wiring", never implemented), so clicking Save silently discarded unsaved filter changes just like Discard | FIXED | YES | `1e4c25e` | Removed the Save option rather than wiring up ambiguous auto-save semantics (unclear what "save" should target -- device push vs. profile file vs. draft state). Dialog now offers only "Continue Working" (primary/default button, was Cancel) and "Discard and Quit" (was Discard); `choice` type narrowed from `Literal["save", "discard", "cancel"]` to `Literal["discard", "cancel"]` in both the dialog and `MainWindow.closeEvent()` | Test: `TestUnsavedChangesDialog` (test_gui_dialogs.py, updated); `test_unsaved_changes_dialog_cancel_button_style` (test_smoke_fixed_issues.py, updated for renamed/restyled button) |
| 200 | **Found via `/code-review ultra` on this session's dialog-consolidation diff:** `_ensure_wizard_state_for_load()`'s "wizard already at/past the FILTERS step" early-return branch never consulted its new `preview_items` param, so the "Preset Will Briefly Activate on Device" safety warning was silently skipped whenever a user opened Presets on Device -> Load into Editor while already on the Review/Push step -- reading the PEQ preset briefly activated it on the device with zero confirmation, unlike the "nothing missing" branch a few lines below which correctly folded the warning in | FIXED | YES | `db5c0b9` | Added the same `preview_items is not None and not self._confirm_preset_preview(preview_items)` guard to the FILTERS-early-return branch, mirroring the "nothing missing" branch | Test: `test_ensure_wizard_state_at_filters_step_confirms_preview_items`, `test_ensure_wizard_state_at_filters_step_declined_preview_returns_false` (test_smoke_regression_wizard.py) |
| 201 | **Found via `/code-review ultra` on this session's Push page diff:** the new multi-source push round label ("Pushing to X (2 of 2)") was shown by `set_push_round()` but never hidden by `set_success()`/`set_failure()`/`set_dry_run_result()` -- only `reset()` cleared it, so after a multi-source push finished, the stale "Pushing to..." text stayed visible alongside the final result message until the page was reset | FIXED | YES | `db5c0b9` | Hid `_round_label` inside the shared `_show_result_state()` helper (called by all three terminal-state methods) instead of touching each one individually | Test: `test_set_success_hides_stale_round_label`, `test_set_failure_hides_stale_round_label`, `test_set_dry_run_result_hides_stale_round_label` (test_gui_pages.py) |
| 202 | **User-reported (live app):** Review step's FilterTable rendered as a tiny, unusable sliver -- nowhere near its intended `FILTER_TABLE_MAX_WIDTH=600` cap -- after the same session's width-reduction pass wired that cap up. Root cause: `review_page.py` centered the table via `content_layout.addWidget(self._filter_table, 1, alignment=Qt.AlignmentFlag.AlignHCenter)`; passing an `alignment` to `addWidget` makes Qt size the widget to its `sizeHint()` instead of letting its Expanding size policy fill up to `setMaximumWidth()`, and `FilterTable`'s Stretch-mode columns don't contribute meaningfully to `sizeHint()` -- so the table collapsed to a sliver regardless of the 600px cap. This is the exact anti-pattern `page_layout.py::build_centered_content()`'s own docstring already documents and works around (smoke `#179`) -- the width-reduction pass reintroduced it in a second location instead of reusing the established stretch-sandwich idiom | FIXED | YES | `db5c0b9` | Replaced the `addWidget(..., alignment=...)` call with a stretch-widget-stretch `QHBoxLayout` sandwich (`addStretch()` / `addWidget(table, 1)` / `addStretch()`), matching `build_centered_content`'s pattern -- the table now fills to its 600px cap and is centered by the stretches absorbing the leftover space, not by sizeHint-based sizing | Test: `test_filter_table_stretches_to_max_width_not_collapsed` (test_gui_pages.py) |
| 203 | **User-reported (live app), root cause found in a follow-up:** a GUI unit test popped up a real, unclosable dialog during a run -- initial static analysis of `src/tests/*.py` found every known custom-dialog call site already correctly mocked and couldn't reproduce it, so a defensive test-class gap was closed as a safety net (see below) without a confirmed root cause. The user then identified the specific test: `TestPresets::test_issue38_my_presets_view_has_toolbar` ("My Saved Presets" toolbar test, deleting the "Jazz Night" preset). Actual root cause: the test uses the real `window` fixture, whose `MainWindow.__init__` already wires `_my_presets_view.delete_requested` to the real `_on_profile_delete_requested` handler (added by `#19`, predates this session) -- the test additionally connects its own local lambda to the same signal to record calls, but didn't account for the *real* handler also firing on `_delete_btn.click()`. That real handler calls `_confirm_action()` -> `WarningConfirmDialog.confirm()` -> a genuine, unmocked `exec()`, blocking the test on a real modal | FIXED | YES | `2686663` | Wrapped `view._delete_btn.click()` in `patch("...WarningConfirmDialog.confirm", return_value=True)`, matching the pattern already used by every other test that exercises a `_confirm_action()`-backed handler. Also added a `TestWarningConfirmDialog` class (the originally-planned defensive fix) mirroring `PushConfirmation`/`QuickSetupDialog`'s dedicated dialog test classes, so `WarningConfirmDialog` itself has direct coverage independent of this specific bug | Test: `test_issue38_my_presets_view_has_toolbar` (test_smoke_regression_operations.py, fixed); `TestWarningConfirmDialog` (test_gui_dialogs.py) |
| 204 | **User-reported (live app):** Review step's FilterTable showed a vertical scrollbar even when every row was already visible (e.g. 3 filters). Root cause: `_natural_table_height()` sums `horizontalHeader().height() + verticalHeader().length() + 2*frameWidth()`, which doesn't fully capture QSS-driven border/padding, so the computed max-height could be a few px short of the real content height -- with `ScrollBarPolicy.ScrollBarAsNeeded` (Qt's default), that shortfall was enough to trigger a needless scrollbar for content that should fit exactly | FIXED | YES | `2686663`, `a63a140` | Explicitly set `ScrollBarPolicy.ScrollBarAlwaysOff` for the vertical scrollbar (mirroring the existing horizontal-scrollbar-off call) in the single shared `_create_table()` factory all three population methods build through -- the table's height is always capped to its exact natural content height right after populating, so a scrollbar should never legitimately be needed | Test: `test_vertical_scrollbar_always_off` (test_gui_components.py) -- **superseded by `#226`:** force-disabling the scrollbar also hid genuinely-needed cases (e.g. a height-constrained window); reverted to `ScrollBarAsNeeded` with a small estimate margin instead. |
| 205 | **User-reported (live app):** Review step's Dry Run controls (a "DRY RUN" badge, the checkbox, and an explanatory hint) sat below the FilterTable, using vertical space that could go to the table instead; the badge was also redundant once the checkbox was moved into view | FIXED | YES | `2686663` | Removed the "DRY RUN" badge entirely (and its QSS `active`-property styling in both theme files); moved the Dry Run checkbox + inline hint into a single row placed above the FilterTable, so the table gets first claim on the page's spare vertical space | Test: `test_dry_run_toggle_changes_button`, `test_dry_run_hint_visible` (test_gui_pages.py) |
| 206 | **User-reported (live app):** at the app's minimum window width (800px), the wizard step-indicator's longest label ("Name Profile", RoomFit's 6-step flow) could elide to "Na…" -- confirmed empirically (not just estimated) via an offscreen Qt measurement replicating `MainWindow`'s real layout (sidebar and the step indicator are `QHBoxLayout` siblings, and `MainWindow.setMinimumSize()` overrides the layout's own larger natural minimum, letting children actually be compressed below their `minimumSizeHint()`) -- eliding started below ~870px | FIXED | YES | `2686663` | Raised `MIN_WINDOW_WIDTH` from 800 to 900 (documented in the constant's own docstring with the empirical threshold), giving margin above the measured 870px cutoff for font-metric variance across platforms | Test: `test_labels_dont_elide_at_min_window_width_with_sidebar` (test_gui_components.py) |
| 207 | **User-reported (live app):** on the Filters step's "Pull from REW API" mode, a near-duplicate instruction line (`FiltersPage`'s own subtitle, styled `class="secondary"`) stacked directly above `RewPullView`'s own unstyled "Choose measurement(s)..." header; the embedded `RewPullView` had no stretch factor so it didn't claim the page's spare vertical space, cramping the measurement picker especially at minimum window height; and the File Import Continue button sat mid-page instead of bottom-anchored like every other wizard step | FIXED | YES | `2686663` | Reordered the page so the source toggle sits directly below the title with the (single, always-visible, consistently-styled) instruction line right below it in both modes -- text changes per mode, position/font never do. Added a `show_header` constructor param to `RewPullView` (mirroring `show_title`) so the embedded instance suppresses its own now-redundant header instead of hiding `FiltersPage`'s subtitle (which would have made the instruction line jump position between modes). Gave `rew_pull_view` a stretch factor of 1 so it claims spare vertical space. Moved the File Import Continue row out of the file-import section into its own page-level, visibility-toggled container with a leading stretch, bottom-anchoring it like Source/Review/NameProfile | Test: `test_show_header_false_hides_header`, `test_show_header_default_shows_header` (test_gui_views.py); `test_subtitle_position_and_style_unchanged_across_source_toggle`, `test_continue_button_bottom_anchored`, extended `test_toggle_to_rew_api_shows_picker_and_emits_signal`/`test_rew_pull_back_reverts_to_file_import` (test_gui_pages.py) |
| 208 | **User-reported (live app):** the onboarding overlay's middle capability card ("Push Safely", the longest description) had its wrapped description text clipped -- a regression from `#211`'s fix for the cards over-growing. Root cause: `QWidget.sizeHint()` does not factor in a fixed width when computing height for word-wrapped children, so pinning the card to `QSizePolicy.Fixed` locked in a too-short height computed before the card's real (narrower, 3-column) width was known | FIXED | YES | `2686663` | Gave each capability card an explicit `setFixedWidth(168)` (derived from the outer card's width/margin/spacing constants) and pinned its height via `card.heightForWidth(168)` -- Qt's real wrap calculation at that width -- instead of trusting `sizeHint()`. (A `QSizePolicy.Minimum` height policy was tried first as a way to let the card grow past an inaccurate `sizeHint()`, but `Minimum` still carries Qt's `GrowFlag` and reintroduced the original over-growth bug; the explicit `heightForWidth()`-derived fixed height avoids that entirely.) | Test: `test_capability_cards_not_clipped` (test_gui_dialogs.py) |
| 209 | **User-reported (live app):** the "Preset Will Briefly Activate on Device" confirmation dialog (shown before Copy to Another Device / Load into Editor / Export / Save to My Presets for certain presets) showed literal `<br>`/`<b>` text instead of rendering them as line breaks/bold. Root cause: `WarningConfirmDialog` hardcoded `body_rich_text=False` for every caller, but this specific message is deliberately built as HTML by `_preset_preview_warning_html()` (whose dynamic preset-name content is already `html.escape()`d before embedding, per that method's own docstring) | FIXED | YES | `2686663` | Threaded an optional `rich_text: bool = False` parameter through `_confirm_action()` -> `WarningConfirmDialog.confirm()`/`__init__()` -> `make_warning_box()`, defaulting to `False` (preserving correct plain-text behavior for every other `_confirm_action()` caller, none of which embed HTML) and passing `rich_text=True` only from `_confirm_preset_preview()` | Test: `test_rich_text_true_renders_html`, `test_body_is_plain_text_by_default` (test_gui_dialogs.py) |
| 210 | **User-reported (live app):** onboarding overlay's 3 capability-card text boxes were much taller than the text they contain, and the overlay's shading/backdrop didn't scale and its content didn't stay centered when the main window was maximized while the overlay was showing | FIXED | YES | `2686663` | (a) `_build_capability_card`'s layout margins (`SPACING_SM`/`SPACING_MD`) were removed -- the `onboardingCapCard` QSS rule already applies `padding: 16px`, so the layout margins were doubling the vertical chrome around a short title + description. (b) Installed an event filter on the overlay's parent (`parent.installEventFilter(self)`) that calls `setGeometry(parent.rect())` on every parent `QEvent.Type.Resize` -- as an unmanaged (non-layout) child widget, the overlay was previously only resized at construction/`showEvent`, never tracking a live resize/maximize while already visible | Test: `test_card_stays_fixed_size_regardless_of_parent_size`, `test_card_recenters_when_parent_resizes` (test_gui_dialogs.py) |
| 211 | **User-reported (live app), follow-up to `#210`:** after `#210`'s padding fix, the 3 capability cards were *still* much taller than their text -- and grew taller still when the window was maximized. User suggested the overlay's content never changes, so it could just be a fixed size | FIXED | YES | `2686663` | Root cause: the card's default `Preferred` size policy has Qt's `GrowFlag` set, and `root_layout` had only this one item with no `addStretch()`, so the card was being stretched to fill the *entire* available height of the overlay (measured 636px vs a 374px `sizeHint()` at a 700px-tall window, growing further on maximize) instead of sizing to its static content. Changed the card's size policy to `QSizePolicy.Fixed` (both axes), pinning it to its `sizeHint()` -- `AlignCenter` on `root_layout` then centers the fixed-size card | Test: `test_card_stays_fixed_size_regardless_of_parent_size`, `test_card_recenters_when_parent_resizes` (test_gui_dialogs.py) -- **superseded by `#208`:** the Fixed-height fix here itself introduced a text-clipping regression on the longest description, root-caused and fixed separately |
| 212 | **User-reported (live app):** the Review step's FilterTable showed only ~5 rows at default window size despite spare vertical space, and didn't use the page's full available width | FIXED | YES | `2686663` | Gave `FilterTable` itself an `Expanding`/`Expanding` size policy (previously the default `Preferred`, which doesn't participate in a parent layout's vertical stretch distribution) and a stretch factor of 1 where `ReviewPage` adds it to `content_layout`, so it grows into the page's spare vertical space instead of that space collecting below the action buttons. Raised `FILTER_TABLE_MAX_WIDTH` from 600 to 760 (matching `MAX_CONTENT_WIDTH`) so the table fills the content column's full width instead of a narrower centered strip | Test: `test_filter_table_stretches_to_max_width_not_collapsed` (test_gui_pages.py) |
| 213 | **User-reported (live app), follow-up to `#212`:** now that the FilterTable could grow, it grew taller than needed -- e.g. loading a 3-filter file still left it stretching to fill a tall window well past the point every row was already visible | FIXED | YES | `2686663` | Added `FilterTable._natural_table_height()` (sums `horizontalHeader().height() + verticalHeader().length() + 2*frameWidth()`) and call `setMaximumHeight(...)` with it at the end of `set_filters()`/`set_lr_filters()`/`set_comparison()`, so the table's growth stops once every row is visible instead of continuing to fill available page height | Test: `test_filter_table_grows_to_fit_more_rows_but_not_past_them`, `test_filter_table_does_not_grow_past_content_when_page_grows` (test_gui_pages.py) -- **superseded in part by `#204`:** the natural-height estimate used here could itself be a few px short of the real content height, triggering a needless vertical scrollbar, fixed separately |
| 214 | **User-reported (live app):** on the Connect step, discovered devices appeared in discovery-arrival order rather than sorted by their user-defined name, making a growing device list harder to scan | FIXED | YES | `2686663` | `set_devices()` (full-replace path) now sorts the incoming list by casefolded name before building cards. `update_devices()` (incremental/progressive-discovery path) computes each new device's correct sorted insertion index (`_sorted_insert_index()`) and inserts its card there, so cards stay sorted as they stream in rather than only being sorted once at the end | Test: (existing `TestConnectPage` device-ordering coverage in test_gui_pages.py, extended for the new sort behavior) |
| 215 | **User-reported (live app):** RoomFit's Undo status message always said "restored from backup" regardless of what undo actually did -- misleading when undoing a push of a brand-new profile, since nothing was actually restored (the previously-active profile is just re-activated; the new profile remains on the device, just deactivated) | FIXED | YES | `2686663` | `_do_undo_roomfit()` now reads the same `was_new_profile` backup metadata `RoomFitSafeWrite.undo()` itself already uses internally (`parse_backup_restore_metadata(load_backup_json(path))`) and branches the success message: `"Original profile re-activated. Profile '{name}' was kept, but deactivated."` when `was_new_profile is True`, otherwise the existing `"restored from backup"` wording for a real overwrite-undo | Test: (extended undo-message coverage in test_gui_filter_handlers.py / test_smoke_regression_operations.py for both branches) |
| 216 | **User-reported (live app):** wizard step buttons were inconsistent across steps -- Source's "Continue" button was full-width (the only one that was), some steps said "Next" and others "Continue" for the same step-advance action, some steps had a visible Back button and others didn't, and button alignment/position varied step to step | FIXED | YES | `2686663` | Standardized primary step-advance buttons to natural width, right-aligned, labeled "Continue" (Source's `content_layout.addWidget(self._continue_btn)` gained `alignment=Qt.AlignmentFlag.AlignRight`; Filters' two "Next" buttons renamed to "Continue" and un-fixed-width). No per-page Back button was added -- step-indicator click-to-navigate remains the sole backward-navigation path, confirmed as the intended design rather than a gap | Test: (button label/alignment assertions in test_gui_pages.py) -- **superseded/extended by `#216`'s own follow-up, `#217`:** button *position* consistency (not just label/width) needed a second pass once the user re-reviewed all 4 pages side by side |
| 217 | **User-reported (live app), follow-up to `#216`:** even after label/width standardization, the primary button's *position* still varied by step -- Source's was bottom-right, Filters' was inline just below the file rows, Review's action row was below the text but with the primary (Push to Device) button leftmost instead of rightmost, and NameProfile's "Save" was centered rather than right-aligned; separately, NameProfile's activation NOTE text was positioned below the Save button instead of below the input it clarifies, and the Push step's stepper included a "Done" row that never visibly changed state before being replaced by the success message, adding noise. Resolved via an explicit UX discussion (bottom-of-page vs. top-of-page primary button placement) landing on: bottom of page, right-aligned, matching a stretch-then-button-row convention already used elsewhere | FIXED | YES | `2686663` | Right-aligned Filters'/NameProfile's Continue/Save buttons and reordered Review's action row so Push to Device (the primary/step-advancing action) is rightmost with Export/Save leading. Moved NameProfilePage's activation note to directly after the name input, before the "Existing Profiles" section. Removed `"done"` from `PushPage`'s `_STAGES`/`_STAGE_LABELS`; `set_stage("done")` now marks every real stage complete directly instead of having its own decorative row | Test: (button-position and NameProfile-note-position assertions in test_gui_pages.py); `test_set_stage_done_marks_all_stages_complete` (test_gui_pages.py) |
| 218 | **User-reported (live app):** on the Push step, the "Undo" button remained enabled after a successful undo, so it could be clicked repeatedly with no clear indication of what a second click would do | FIXED | YES | `2686663` | Added `PushPage.mark_undo_complete(success: bool)`, disabling the Undo button on success (left enabled on failure so the user can retry); `reset()` re-enables it. Wired from all 3 undo-completion call sites in `main_window.py` (`_on_undo_complete`, `_do_undo_roomfit`, `_do_undo_multi_source`'s all-succeeded branch) | Test: (PushPage undo-button-disable tests in test_gui_pages.py) |
| 219 | **User-reported (live app):** the yellow warning-box styling used by "Copy to Another Device" and "Load into Editor" wasn't applied consistently to other similar confirmations (e.g. "Export as REW File", "Save to My Presets", and other `_confirm_action()`-based Yes/No dialogs), which instead used a plain, unstyled `QMessageBox.question()` | FIXED | YES | `2686663` | Added `WarningConfirmDialog` (new file, `src/gui/dialogs/warning_confirm_dialog.py`) -- a styled Yes/No dialog built on the existing `make_warning_box()` helper, with No as the default button. `MainWindow._confirm_action()` now delegates to `WarningConfirmDialog.confirm()` instead of `QMessageBox.question()`, so every "this will change state" confirmation in the app reads consistently | Test: (existing `_confirm_action()` call-site tests in test_smoke_regression_operations.py/test_main_window_settings.py, updated to patch `WarningConfirmDialog.confirm` instead of `QMessageBox.question`); `TestWarningConfirmDialog` (test_gui_dialogs.py, added this session per `#203`) |
| 220 | **Found via `/code-review ultra --fix` on this session's `#204`/`#210`-`#219` diff:** `FilterTable.set_lr_filters()`'s max-height budget (`tallest_table_height + tab_bar_height`) never accounted for the `QTabWidget::pane` QSS chrome (1px border + 8px padding per side, ~18px total, in both theme files) -- with the vertical scrollbar forced off by `#204`'s fix (same diff), the taller of the two L/R tabs silently clipped its bottom row(s) with no way to scroll to them; empirically reproduced (offscreen Qt measurement, 5 vs 8 filters) showing a 22px shortfall | FIXED | YES | `2686663` | Added a `_TAB_PANE_CHROME = 18` constant (documented as matching the QSS pane border+padding) and included it in `set_lr_filters()`'s `setMaximumHeight()` budget. Also extracted the identical `setMaximumHeight(self._natural_table_height(table))` call in `set_filters()`/`set_comparison()` into a shared `_apply_natural_max_height()` helper | Test: `test_lr_filters_max_height_accounts_for_tab_pane_chrome` (test_gui_components.py) |
| 221 | **Found via `/code-review ultra --fix` on this session's `#214` diff:** a discovered device with a present-but-empty `"name"` (real for some mDNS/subnet-scan responses, per `zeroconf_discover.py`/`subnet_scanner.py`) sorted with key `""` (pinning it to the very top of the alphabetized Connect-step device list) but displayed as a blank label, not "Unknown Device" -- `ConnectPage._add_device_card()`'s `device.get("name", "Unknown Device")` default only covers a *missing* key, not an empty-string value, so the sort key and the on-card label silently disagreed about what an unnamed device is called | FIXED | YES | `2686663` | Added `ConnectPage._device_display_name()` (falls back `device.get("name") or "Unknown Device"`) used by both `_device_sort_key()` and `_add_device_card()`'s label, so an unnamed device now sorts and displays consistently as "Unknown Device" | Test: `test_blank_name_device_sorts_and_displays_as_unknown` (test_gui_pages.py) |
| 222 | **User-reported (live app):** no client-side validation of RoomFit profile / PEQ preset names before pushing to a WiiM device -- the WiiM Home app states its own naming rule as "only letters, numbers and underscore are allowed", but the user's own hardware testing found dash (`-`) and space (` `) are also accepted, so a name using either would (incorrectly) look invalid by that rule while a genuinely-rejected character (e.g. `!`, `&`) had no client-side check at all and would only fail at push time | FIXED | YES | `a63a140` | Added `src/utils/device_name.py` (`sanitize_device_name()`/`has_invalid_device_name_chars()`), encoding the WiiM Home app's stated rule plus the two user-confirmed extra characters (letters, numbers, `_`, `-`, space) -- `# ASSUMPTION:`-marked and logged in `docs/corrections.md` (2026-07-12) since the exact device-accepted set isn't independently hardware-verified by this project. Wired into `NameProfilePage`: shows an inline warning while a disallowed character is present, and strips them before `name_confirmed` is emitted so a push never fails on a rejected name | Test: `test_char_warning_shown_for_disallowed_characters`, `test_save_sanitizes_disallowed_characters`, `test_save_disabled_when_name_is_only_disallowed_characters`, `test_char_warning_does_not_block_typing_or_save` (test_gui_pages.py); `TestSanitizeDeviceName`, `TestHasInvalidDeviceNameChars` (test_device_name.py) |
| 223 | **User-reported (live app):** Filters step's Continue button changed vertical position when switching between File Import and Pull from REW API mode. Root cause: the embedded `RewPullView` (File Import's sibling section) built its own layout via `build_centered_content()`, which applies its own `SPACING_LG` outer margins on top of `FiltersPage`'s -- already-margined column, so the embedded view's own bottom-anchored action row (with its own Continue button) sat measurably higher than File Import's `_file_import_actions` row | FIXED | YES | `a63a140` | Added an `embedded: bool = False` constructor param to `RewPullView`; when `True` (now passed by `FiltersPage`), `_setup_ui()` uses a plain zero-margin `QVBoxLayout` instead of `build_centered_content()`, since the host page already provides its own margins/centering | Test: `test_continue_button_same_bottom_position_in_both_modes` (test_gui_pages.py); `test_embedded_true_uses_zero_outer_margins`, `test_embedded_false_keeps_own_margins` (test_gui_views.py) |
| 224 | **User-reported (live app), same root cause as `#223`:** unused empty space in the Filters step's Pull from REW API mode, below the "Select a REW measurement..." instruction text and above the Stereo/L-R toggle -- the doubled outer margins from `#223` also showed up here, as an extra gap between `FiltersPage`'s subtitle and `RewPullView`'s (embedded, header-suppressed) toggle row | FIXED | YES | `a63a140` | Same fix as `#223` (`embedded=True`) -- removing `RewPullView`'s own redundant outer margins when embedded closes this gap too, since it was the same margin stacking | Test: `test_no_gap_between_subtitle_and_rew_pull_toggle` (test_gui_pages.py) |
| 225 | **User-reported (live app):** in the Filters step's Pull from REW API mode, the measurement picker (Stereo/L-R toggle + list(s) + Back/Continue action bar) could become partially hidden/obstructed when the window height was reduced -- neither `FiltersPage` nor `RewPullView` wrapped this content in a scroll area, so once the stack's combined height (including the list's own `220px` minimum) exceeded the available window height, part of it was pushed outside the visible window with no way to scroll to it | FIXED | YES | `a63a140` | Wrapped `RewPullView`'s content (header/toggle/list(s)/action-bar) in a `QScrollArea` (`setWidgetResizable(True)`, `NoFrame`, horizontal scrollbar off), mirroring `ConnectPage`'s existing device-list scroll pattern -- shows no scrollbar when there's enough room (content just expands to fill the viewport) and becomes scrollable when there isn't, so every measurement and the Continue button stay reachable | Test: `test_content_scrollable_when_window_too_short` (test_gui_views.py) -- **superseded by `#227`:** wrapping the whole content let the action buttons themselves scroll out of view and produced a second, competing scrollbar; only the measurement list is wrapped now. |
| 226 | **User-reported (live app):** the Review step's FilterTable never showed a vertical scrollbar even when its content genuinely didn't fit, since `#204` (this session) force-disabled the vertical scrollbar entirely (`ScrollBarPolicy.ScrollBarAlwaysOff`) as a workaround for `_natural_table_height()`'s estimate occasionally landing a few px short of the real content height -- that workaround papered over the estimate's inaccuracy instead of fixing it, and made the scrollbar permanently unavailable for any *genuine* height-constrained case (e.g. `#225`'s "short window" scenario applied to the Review step) | FIXED | YES | `a63a140` | Reverted the vertical scrollbar policy to `ScrollBarPolicy.ScrollBarAsNeeded` (Qt's default -- shows/hides dynamically based on real content vs. viewport height) and added a small `_HEIGHT_ESTIMATE_MARGIN = 4` px safety margin to `_natural_table_height()` to reduce (not eliminate -- Qt's own real comparison is now the source of truth either way) false-positive scrollbars from the estimate's known QSS-border/padding shortfall | Test: `test_vertical_scrollbar_policy_is_dynamic`, `test_no_scrollbar_when_all_rows_visible`, `test_scrollbar_appears_when_height_constrained_below_content` (test_gui_components.py) -- **supersedes `#204`:** its `test_vertical_scrollbar_always_off` regression test asserted the now-reverted always-off behavior and was replaced by the three above |
| 227 | **User-reported (live app), follow-up to `#225`:** the `QScrollArea` fix for `#225` wrapped RewPullView's *entire* content (header/toggle/list(s)/action-bar) as one scrollable unit -- at a reduced window height this let the Back/Continue action buttons themselves scroll out of view behind the window edge (the opposite of the "buttons must always be reachable" intent), and produced a confusing second scrollbar (the outer content one, plus the measurement list's own) when both applied at once | FIXED | YES | `a63a140` | Moved the `QScrollArea` to wrap only `_pages` (the Stereo/L-R measurement list stack) instead of the whole content widget; header, Stereo/L-R toggle, and the Back/Continue action bar now sit outside any scroll region in `_build_content()`'s plain layout, so they stay fully visible/reachable regardless of window height -- only the list itself shrinks and gains a scrollbar when constrained | Test: `test_measurement_list_scrollable_when_window_too_short`, `test_only_one_scroll_area_in_content_state` (test_gui_views.py) -- supersedes `#225`'s `test_content_scrollable_when_window_too_short`, rewritten to assert on `_pages_scroll` instead of the removed `_content_scroll` -- **superseded by `#231`:** wrapping `_pages` in its own `QScrollArea` still produced a double scrollbar, since each `QListWidget` is already its own scrolling container. |
| 228 | **User-reported (live app), follow-up to `#208`/`#211` (onboarding overlay capability cards):** user manually adjusted the capability card layout (removed the `card.setFixedHeight(card.heightForWidth(_CAP_CARD_WIDTH))` pinning from `#208`, letting the card size naturally to its content instead), which broke `test_capability_cards_not_clipped`'s assertion (`card.height() == card.heightForWidth(...)`) since that exact-height invariant no longer holds under the new (visually correct) layout | FIXED | YES | `a63a140` | Rewrote the test to check the real "not clipped" invariant directly on the wrapped title/description labels (`label.height() >= label.heightForWidth(label.width())`) instead of asserting a specific card-height formula tied to a now-removed sizing mechanism, so it stays valid regardless of how the card arrives at its height | Test: `test_capability_cards_not_clipped` (test_gui_dialogs.py, rewritten) |
| 229 | **User-reported (live app):** on the Filters step / "Pull from REW" sidebar view, reducing the window size hid the Back/Continue action buttons behind the window/status-bar edge, requiring the user to scroll to reach them -- the only views in the app where this could happen | FIXED | YES | `a63a140` | Same fix as `#227` -- restructuring `RewPullView` so the action bar lives outside the (now list-only) scroll region means it can no longer be scrolled out of view; it always renders at its natural position directly below the toggle/list | Test: `test_measurement_list_scrollable_when_window_too_short` (test_gui_views.py) -- see `#227` |
| 230 | **User-reported (live app):** "My Saved Presets" was the only view in the app whose action toolbar (Load into Editor / Rename / Duplicate / Copy to Another Device / Delete) sat *above* its main list instead of bottom-anchored below it like every other view/wizard step; separately, "Copy to Another Device" was the 4th button (after Rename/Duplicate) instead of grouped with "Load into Editor" as the two "send this preset somewhere" actions | FIXED | YES | `a63a140` | Moved `MyPresetsView`'s toolbar below `_list_widget`/`_empty_label` in `_setup_ui()` and gave the list a stretch factor of 1, matching the bottom-anchored convention used everywhere else. Reordered both the toolbar and the equivalent right-click context menu to Load, Copy to Another Device, Rename, Duplicate, Delete. Split `_show_context_menu()`'s menu construction into a new `_build_context_menu()` helper so the action order is directly testable without exercising `QMenu.exec()`'s real modal popup | Test: `test_toolbar_bottom_anchored_below_list`, `test_toolbar_stays_at_bottom_when_view_grows`, `test_toolbar_button_order`, `test_context_menu_action_order_matches_toolbar` (test_gui_views.py) |
| 231 | **User-reported (live app), follow-up to `#227`:** the "Pull from REW" sidebar view and the Filters step's REW API mode still showed two nested vertical scrollbars at a reduced window height -- `#227`'s `_pages_scroll` `QScrollArea` (wrapping the Stereo/L-R measurement list stack) and each `QListWidget`'s own native scrollbar could both activate simultaneously, since `QListWidget` is already its own `QAbstractScrollArea`-based scrolling container and doesn't need (or expect) an outer scroll wrapper around it | FIXED | YES | `a63a140` | Removed `_pages_scroll` entirely; `_pages` (the list stack) is added directly to `_build_content()`'s layout with its existing stretch factor of 1, so Qt's layout engine compresses it (and only it, since header/toggle/action-bar have stretch 0) first when space is short, and each list's own already-`ScrollBarAsNeeded` vertical scrollbar activates exactly when its own content no longer fits its own (now smaller) viewport -- a single scrollbar, in the one place it belongs | Test: `test_measurement_list_scrollable_when_window_too_short` (test_gui_views.py, strengthened with 20 measurements to force real overflow and directly assert `_list_widget`'s own scrollbar activates), `test_only_one_scroll_area_in_content_state` (test_gui_views.py, rewritten to assert zero `QScrollArea` descendants) -- **supersedes `#227`, superseded in turn by `#232`:** a plain (unwrapped) `stretch=1` widget can still be compressed by Qt below its own stated minimum when the ancestor window is forced smaller than its natural content, unlike a `QScrollArea` (which genuinely never compresses its content below the content's minimum size) -- this row's fix reintroduced #225's original "content pushed off-screen, unreachable" failure mode in the specific case where even the list's floor didn't fit, most easily triggered in L/R mode (its extra "Left/Right Channel" labels make it the tallest). |
| 232 | **User-reported (live app), follow-up to `#231`:** even after `#231`, the bottom row (header/toggle/action-bar) could still be obstructed at a minimal window size, especially in L/R mode -- root cause was `#231` itself: removing `_pages_scroll` meant `_pages` was a plain `stretch=1` layout item with no genuine "never compress below minimum" guarantee, so at extreme compression (the L/R lists' combined height, plus their "Left/Right Channel" labels, exceeding available space) Qt silently rendered it below its natural size instead of converting the shortfall into a scrollbar, exactly reproducing `#225`'s original failure mode in a narrower case `#231`'s test didn't happen to exercise | FIXED | YES | `a63a140` | Re-added `_pages_scroll` (a `QScrollArea` wrapping `_pages`) -- but this time also disabled each `QListWidget`'s own vertical scrollbar (`ScrollBarAlwaysOff`) and set each list's `minimumHeight` to its actual full content height via a new `_natural_list_height()` helper (item count × `sizeHintForRow(0)`, the L/R lists sharing the taller of the two so they stay aligned), recomputed in `set_measurements()`. This collapses "too many rows" and "too short a window" into the exact same case: `_pages_scroll` is now the *only* scrollbar that can ever appear, and (verified empirically, not just reasoned about -- this project's own established lesson) a `QScrollArea` genuinely never compresses its wrapped content below that content's stated minimum the way a plain layout item can, so the list is always shown in full (scrolled into view if needed) and the header/toggle/action-bar outside `_pages_scroll` are never touched | Test: `test_measurement_list_scrollable_when_window_too_short`, `test_lr_mode_continue_button_reachable_at_minimal_window_height`, `test_only_one_scroll_area_in_content_state` (test_gui_views.py, all rewritten for the new mechanism) -- **supersedes `#231`, superseded in turn by `#233`:** wrapping `_pages` in a `QScrollArea` and disabling each list's native scrollbar meant a single scrollbar scrolled *both* L/R lists together as one unit (no independent per-list scrolling), and it also scrolled each list's "Left Channel"/"Right Channel" heading label along with the list's rows, since the label and list were both inside the same scrolled widget. |
| 233 | **User-reported (live app), follow-up to `#232`:** `#232`'s single shared `_pages_scroll` scrollbar was worse than the double-scrollbar it fixed -- in L/R mode it scrolled the Left and Right lists together as one region (no way to scroll just one list), and it scrolled each list's "Left Channel"/"Right Channel" heading label along with that list's rows, instead of only the list content | FIXED | YES | `a63a140` | Removed `_pages_scroll` again; each `QListWidget` keeps its own native scrollbar (`ScrollBarAsNeeded`, restored in `_build_list()`) so it scrolls independently of its sibling list and never scrolls its own heading label (the label is a layout sibling above the list, not inside it). Replaced `_natural_list_height()` (full every-row-visible height) with `_list_floor_height()` (a small ~2-row floor), applied via `setMinimumHeight()` in `set_measurements()`. Because the header/toggle/action-bar keep their default stretch-0 (fixed/preferred) size policy while `_pages` keeps stretch 1, Qt's layout engine compresses `_pages` (and, within it, whichever list needs it) down to that floor before it would ever touch the fixed items around it -- so the action bar can't be pushed off-screen in normal use, and each list's own scrollbar (not a shared outer one) takes over once a list is compressed below its full content height | Test: `test_measurement_list_scrollable_when_window_too_short`, `test_lr_mode_continue_button_reachable_at_minimal_window_height` (test_gui_views.py, rewritten for the new mechanism), `test_lr_lists_scroll_independently_and_labels_stay_out_of_scroll`, `test_no_outer_scroll_area_wraps_the_lists` (test_gui_views.py, new) -- **supersedes `#232`.** |
| 234 | **User-reported (live app):** Filters step's File Import mode showed a lot of empty space in the layout (large gaps between the mode toggle, file rows, and elsewhere) that wasn't there previously | FIXED | YES | `a63a140` | Root cause: `_file_import_scroll`, a `QScrollArea` wrapping `_file_import_section` added during the `#231`/`#232` investigation as an untested defensive measure (flagged as unproven at the time). `setWidgetResizable(True)` stretches the wrapped widget to fill all of the scroll area's available space; since `_file_import_section`'s own `QVBoxLayout` has no trailing stretch, Qt distributed that extra space by growing the gaps between its child rows instead of leaving it below them. File Import's content (a mode toggle plus one or two short file rows) never actually grows tall enough to need scrolling, unlike `RewPullView`'s measurement lists, so the scroll wrapper was solving a problem that didn't exist here while introducing this one. Removed `_file_import_scroll`; `_file_import_section` was re-added directly to `page_layout` but *kept* its stretch factor of 1 ("same as before the wrapper was added") | Test: existing `test_lr_continue_button_reachable_at_minimal_window_height` (test_gui_pages.py) still passes unchanged, confirming the scroll wrapper was never load-bearing for that case either -- **correction (2026-07-12, user re-tested live app):** the fix above was incomplete. Keeping the stretch factor of 1 on `_file_import_section` reproduced the identical "gaps between rows" symptom through a different mechanism: a widget with a nonzero outer stretch factor is grown by Qt beyond its `sizeHint()` just like `setWidgetResizable(True)` did, and `_file_import_section`'s own `file_layout` still has no trailing `addStretch()` to absorb that extra height, so it spread out between the mode-toggle and file-browse rows exactly as before. Confirmed empirically (`QT_QPA_PLATFORM=offscreen`, 800x900 window): with the stretch factor, `_file_import_section.height()` matched the full available space instead of its 65px `sizeHint()`. Real fix: dropped the stretch factor entirely (`page_layout.addWidget(self._file_import_section)`, no `, 1`) so the section stays at its natural compact height; the page's existing trailing `addStretch()` before `_file_import_actions` now correctly absorbs the leftover space and keeps the Continue button bottom-anchored. This is the second time this exact bug has been reintroduced by an incomplete fix (`#231`->`#232`->`#233` show the same pattern in `RewPullView`) -- see the Coverage Audit Notes below for the process lesson. |
| 235 | **Found via `/code-review ultra` on this session's device-name-sanitization diff, extended while auditing the in-app help docs for accuracy:** `#222` added `sanitize_device_name()`/`has_invalid_device_name_chars()` and wired them into `NameProfilePage` so a new RoomFit profile/PEQ preset name is validated before `name_confirmed` fires, but three other paths that also produce device-bound preset names were not migrated: `MyPresetsView._on_rename_finished()` only `.strip()`s the inline-edit text; `MainWindow._on_profile_duplicate_requested()` synthesizes `f"{name} (copy)"` (parentheses are disallowed by the same rule); and `MainWindow._save_filters_to_presets()` (the shared helper behind every "Save to My Presets" trigger) never sanitized its `name` argument at all -- for the Review-page trigger, that argument is an *auto-generated* default like `"wifi (Stereo)"`, so every preset saved this way already carried disallowed characters from the moment it was created, before any rename ever touched it. All three unsanitized names are stored as `Profile.name` and can reach a real device write unfiltered via "Copy to Another Device" | FIXED | YES | `a63a140` (`_on_rename_finished()`/`_on_profile_duplicate_requested()`); the third call site, `_save_filters_to_presets()`, actually landed in a later, differently-themed commit `a8a692f` ("docs: Refactor help documentation and code for consistency and clarity") whose message never mentions this fix -- corrected 2026-07-17, `a63a140` alone was cited here previously | Applied `sanitize_device_name()` at all three points: `_on_rename_finished()` sanitizes the renamed text before comparing/emitting; `_on_profile_duplicate_requested()` sanitizes the `"{name} (copy)"` string (parentheses stripped, e.g. `"My Preset copy"`); `_save_filters_to_presets()` now sanitizes `name` itself before calling `build_profile()`, so every save path funnels through one choke point regardless of which view triggered it | Test: `test_rename_sanitizes_disallowed_device_name_chars` (test_gui_views.py), `test_issue235_profile_duplicate_sanitizes_device_name` + `test_issue235_save_filters_to_presets_sanitizes_device_name` (test_smoke_regression_operations.py) |
| 236 | **Found via `/code-review ultra` on this session's Filters page diff:** `FiltersPage._on_source_toggled()` toggles `_file_import_section`/`_file_import_actions`/`rew_pull_view` visibility when switching between File Import and Pull from REW API, but never hid `_warnings_section`/`_error_section` -- if a File Import produced validation warnings or a parse error (both shown inline) and the user then switched to "Pull from REW API" without clicking Continue/Retry first, the stale warning/error box stayed visible alongside the REW measurement picker | FIXED | YES | `a63a140` | Added `self._warnings_section.setVisible(False)` / `self._error_section.setVisible(False)` to `_on_source_toggled()`. **Update (2026-08):** `find_dead_code.py` swept up `FiltersPage.show_warnings()`/`show_error()` (and `_warnings_section`/`_error_section` and their builders) as dead in production -- confirmed via grep that `main_window.py` never calls either method; real warning/error display had since moved to `main_window._status_banner` (warnings via `show_info()` after validation, errors via `show_error()`), leaving this inline section unreachable. Removed the whole feature (methods, widgets, builders, `_on_continue_with_warnings`/`_on_retry` handlers, and the now-moot hide-on-toggle calls this row added) in the dead-code pass, same as `#237`'s `FilterTable.clear()` precedent. `test_source_toggle_hides_stale_warnings`/`test_source_toggle_hides_stale_error` removed with it -- they exercised only the deleted feature, not real behavior. | Test: `test_source_toggle_hides_stale_warnings`, `test_source_toggle_hides_stale_error` (test_gui_pages.py) -- removed 2026-08, see Update |
| 237 | **Found via `/code-review ultra` on this session's `filter_table.py` diff, then re-triggered by a follow-up manual edit:** `FilterTable.set_filters()`/`set_lr_filters()`/`set_comparison()` all call `setMaximumHeight(...)` to cap the widget once populated, but `clear()` never reset it back to unbounded -- dormant today (no production caller invokes `FilterTable.clear()`, only a unit test does), but a real latent bug for whoever wires up a clear-then-repopulate path next. **Update (2026-08):** `FilterTable.clear()` was subsequently removed entirely in the dead-code pass that also removed `get_peq_enabled()`/`StepIndicator.set_dimmed()` -- still no caller ever appeared, and it was tied to the same pass's removal of the (also-unreachable) Profile Comparison mode `clear()` was originally kept alongside; the height-reset fix described here is preserved in git history if a future `clear()` needs re-adding. A first attempted fix used `setMaximumHeight(-1)` intending "unbounded", which is itself wrong: Qt's `setMaximumHeight()` clamps negative arguments to `0` (confirmed via `QT_QPA_PLATFORM=offscreen` -- `maximumHeight()` reads back `0`, plus a Qt console warning "Negative sizes ... are not possible"), which would have made a cleared table collapse to zero height instead of reverting to unbounded. `QWIDGETSIZE_MAX` (Qt's real "unbounded" sentinel) also turned out not to be importable from `PySide6.QtWidgets` at all -- it's a C++-only macro, not exposed to the Python bindings, so referencing it directly raises `ImportError` | FIXED | YES | `a63a140` | `clear()` now calls `self.setMaximumHeight(16777215)` -- the literal value of `QWIDGETSIZE_MAX` -- with a comment explaining why the constant itself isn't importable and why `-1` doesn't work | Test: `test_clear_resets_maximum_height_to_unbounded` (test_gui_components.py) |
| 238 | **User-reported (live app):** after completing the wizard once, opening Settings -> "Show onboarding again" -> "Get Started" navigated to the Connect step and showed it as unchecked/without a summary, but every later step (EQ Type, Source, Filters, Review, ...) stayed checked with its prior context intact -- violating the invariant that a checked step must never follow an unchecked one in the flow sequence. Root cause: `MainWindow._on_onboarding_get_started()` called `self._stacked_widget.setCurrentIndex(PAGE_INDICES["connect"])` directly instead of going through `WizardController`, so the page switch fired `_sync_navigation_chrome()` -> `StepIndicator.set_current()`, which clears only the *target* step's pill/summary (a UI-layer side effect meant for `go_to_step()` back-navigation), while `WizardState.completed_steps` for Connect and every later step was never touched at all | FIXED | YES | `901cb61` | `_on_onboarding_get_started()` now calls `self._wizard_controller.go_to_step(WizardStep.CONNECT)` instead of touching the stacked widget directly -- reuses `go_to_step()`'s existing invalidate-target-and-everything-after logic (already used correctly by `_on_device_selected()`'s device-reselection path), so re-entering onboarding clears `completed_steps` for Connect *and every subsequent step*, and the existing `step_changed` -> `_on_step_changed` handler (which already clears stale StepIndicator badges for anything no longer in `completed_steps`) does the rest | Test: `test_get_started_invalidates_all_completed_steps` (test_main_window_settings.py, new) |
| 239 | **Found by auditing the rest of the codebase for the same invariant-violation class as `#238`, immediately after that fix landed:** `MainWindow._jump_to_review()` (shared by the sidebar "Load into Editor" path via `_on_peq_ready()` and the "My Presets" recall path via `_on_profile_recalled()`) set `state.current_step = WizardStep.REVIEW` and called `self._stacked_widget.setCurrentIndex(PAGE_INDICES["review"])` directly instead of going through `WizardController`, and `_mark_prior_steps_completed()` (which it calls first) only ever sets CONNECT/EQ_TYPE/SOURCE/FILTERS -- it never invalidates NAME_PROFILE/PUSH. Reproduction: complete a push (marks `completed_steps[PUSH]`), then use the sidebar to load a different, unrelated profile/measurement into the editor without clicking the Push page's "OK" first (the only other path that invalidates PUSH, via `_on_done_acknowledged()` -> `go_to_step(FILTERS)`) -- REVIEW becomes the current (unchecked) step while PUSH's pill stays checked, still showing the stale "Done"/backup-path summary from the unrelated prior profile | FIXED | YES | `901cb61` | `_jump_to_review()` now calls `self._wizard_controller.go_to_step(WizardStep.REVIEW)` before `_mark_prior_steps_completed()`, reusing the same invalidate-target-and-everything-after logic as `#238`'s fix -- this also invalidates NAME_PROFILE/PUSH (RoomFit and PEQ flows both covered, since `go_to_step` reads the step sequence for the active flow type) and fires `step_changed` so `_on_step_changed`'s existing badge-clearing runs; `state.current_step`/`setCurrentIndex` are no longer set by hand since `go_to_step` + its `_on_step_changed` handler already do both. This transiently clears CONNECT/EQ_TYPE/SOURCE/FILTERS badges too (not yet back in `completed_steps` at that point), but nothing repaints before `_mark_prior_steps_completed` and the existing summary-replay loop restore them, immediately after, in the same call | Test: `test_recall_clears_stale_push_completion` (test_gui_integration_profile.py, new) |
| 240 | **Found by a second-pass audit agent while investigating `#238`/`#239` (flagged as unrelated to the invariant-violation class, then fixed on request):** `MainWindow._on_peq_ready()`'s `count == 0` branch (device pull/sidebar load resolving to zero filters) reset the pending-rows state but never cleared `_sidebar_load_in_progress` -- only the `count > 0` branch did. This is the exact same one-shot-flag contract `#87`'s fix and `test_rew_list_empty_resets_sidebar_load_flag` already established for `_do_rew_list_measurements()`'s empty-result path, just missed on this sibling path. Left stuck `True`, the flag would corrupt the *next*, unrelated `peq_ready` event by jumping straight to Review via `_jump_to_review()` instead of advancing normally via `wizard_controller.advance()` | FIXED | YES | `901cb61` | Added `self._sidebar_load_in_progress = False` to the `count == 0` branch, alongside the existing pending-rows reset | Test: `test_sidebar_load_empty_filters_resets_sidebar_load_flag` (test_smoke_fixed_issues.py, new) |
| 241 | Critical push-failure screen tells the user to "Use Settings > Restore from Backup", a feature that does not exist | FIXED | YES | `c132304`, `de56f4e` | `PushPage.set_failure()`'s `critical=True` branch (rollback itself failed) prints recovery step 2 as "Use Settings > Restore from Backup". No such control exists anywhere in `SettingsView` (sections are Appearance/Paths/Behavior/Logs/Support) and `grep` finds the string only at this one site. This is the app's most severe error state -- rollback failed, device left in an unverified state -- and the one instruction it gives cannot be followed. Found during a documentation-accuracy audit, not a smoke-test session. Fixed by adding `wiim-rew-sync restore-backup --device --source --file`, which calls `SafeWrite.undo()` directly on a backup JSON file (backed up, written, and verified like any other write); rejects RoomFit backups up front rather than misbehaving on one. Recovery text now points at this command. `c132304`'s first version of that recovery text omitted the required `--source` flag -- copying it verbatim would fail with an argparse error in the app's worst failure state. Caught in an ultra-depth PR self-review (`de56f4e`) by parsing the shown command with the CLI's real argument parser. Also found in the same PR review: `create_backup()` never persists a backup's source name anywhere, so a backup file alone can't supply `--source` -- deliberately not fixed here (needs migration handling for existing backup files), logged as `docs/backlog.md` item 4 instead. | Tests: `test_restore_backup_success`, `test_restore_backup_rollback_path`, `test_restore_backup_critical_path`, `test_restore_backup_rejects_roomfit_backup`, `test_restore_backup_missing_file`, `test_restore_backup_via_main_exits_zero_on_success` (test_cli.py, new); `test_set_failure_critical_recovery_command_is_valid_cli_invocation` (test_gui_pages.py, new -- parses the recovery text's command with the real CLI parser) |
| 242 | Multi-source push failure reports "device safely restored" when earlier sources were left written | FIXED | YES | `c132304`, `de56f4e` | On a multi-source PEQ push, `PrimaryWorkflowManager._do_push()` aborts at the first failing source; sources written before it are intentionally not rolled back (known limitation, `docs/backlog.md` item 3). `PushPage.set_failure()` nonetheless rendered the fixed line "Your device was safely restored to its previous state." for every non-critical failure, so a partial multi-source failure claimed a full restore that did not happen -- and the Undo button was hidden on failure, so the user had no in-app route to the earlier sources' backups either. Found during a documentation-accuracy audit. Fixed by adding `WriteResult.partial_sources`/`partial_backup_paths`, set on the failing source's own result object in place (not replaced, so its own `backup_path`/`rollback_success`/`error_message` are untouched); `PushPage.set_failure()`'s new `partial_sources` param shows an accurate "N source(s) not restored" message and offers Undo for the prior-succeeded sources via the existing multi-source undo path. The RoomFit DELETE_NEW path's wording was left as-is -- out of scope for this fix, still accurate for that path (a genuinely new profile really is deleted, not restored). `c132304`'s first version left the `critical` and `partial_sources` states unable to co-occur coherently: a source can fail its own rollback (critical) while earlier sources in the same push already succeeded (partial_sources > 0), but the critical branch never mentioned partial_sources, so the Undo button (visible whenever partial_sources > 0, regardless of branch) appeared with no explanation. Caught and fixed in an ultra-depth PR self-review (`de56f4e`), which also corrected a stale docstring claiming `backup_path` carries the multi-source-encoded string when `partial_sources > 0` (it never does -- that string goes straight onto wizard state instead). | Tests: `test_second_of_three_sources_failing_reports_one_partial_source`, `test_first_source_failing_reports_zero_partial_sources`, `test_main_window_offers_undo_for_partial_multi_source_failure` (test_gui_push_export.py, new), `test_set_failure_partial_sources_offers_undo`, `test_set_failure_no_partial_sources_still_hides_undo`, `test_set_failure_critical_and_partial_sources_explains_undo` (test_gui_pages.py, new) |
| 243 | **User-reported (live app), smoke test "Concurrent Operation Guard":** action buttons in "Presets on Device" and "My Saved Presets" (and, on inspection, every other view/wizard page) stay clickable while an operation is in progress ("Processing..." shown in the status banner). Root cause: `OperationFeedbackManager.register_action_buttons()` (`src/gui/operation_feedback.py`) -- the mechanism that's supposed to disable registered buttons on `start_operation()` and re-enable them on `finish_operation()` -- is never called anywhere in production code; the only call site in the whole repo is its own unit test (`test_gui_operation_timeout.py`). `MainWindow._wire_operation_feedback()` connects the bridge's `operation_started`/`operation_finished` signals to `start_operation()`/`finish_operation()`, but no page or view ever hands its buttons to `register_action_buttons()`, so `self._action_buttons` is permanently empty and the disable/re-enable loops in both methods iterate zero widgets. This is not scoped to the two views the report names -- it's every action button in the app; those two just happened to be where it was noticed because their toolbar buttons stay enabled with a selection present regardless of what the app is doing | FIXED | YES | `27d3796`, `b90941b` | Fixed at full scope, per explicit request. Added an `action_buttons() -> list[QWidget]` method to every page/view with buttons that trigger an async operation (`ConnectPage`, `SourcePage`, `FiltersPage`, `ReviewPage`, `NameProfilePage`, `PushPage`, `PresetsDeviceView`, `MyPresetsView`, `SettingsView`, `RewPullView`), promoting several buttons from local variables to `self.` attributes where needed to make them registerable (e.g. `ConnectPage._retry_btn`, `FiltersPage._continue_with_warnings_btn`/`_retry_btn`, `ReviewPage._export_button`/`_save_button`, `PushPage._export_link`/`_save_link`, `SettingsView._bundle_btn`). Deliberately excluded from each page's list: pure navigation/dismiss/cancel buttons (`PushPage._ok_button`, `PushPage._show_pushed_filters_button` -- opens a local read-only dialog, no I/O; `RewPullView`'s `back_btn`s) and file-Browse buttons (open a native OS dialog synchronously, no network/device I/O), so users aren't locked out of dismissing a result or picking a file while an unrelated operation elsewhere is in progress. `EQTypePage` has no buttons (selection is via clickable cards) and needed no change. `MainWindow._wire_operation_feedback()` collects every page/view's `action_buttons()` once at startup (including `FiltersPage`'s embedded `rew_pull_view` instance, distinct from the top-level sidebar one) and registers them all in a single `register_action_buttons()` call, since every page/view is constructed once in `_create_pages()` and persists for the app's lifetime -- no per-navigation re-registration needed. `b90941b` (same-session cleanup pass) then corrected `finish_operation()`/the hard-timeout path to restore each button's pre-operation `isEnabled()` state instead of a blanket `setEnabled(True)`: harmless while `register_action_buttons()` was never called, but wiring it up made the blanket re-enable live and wrong for buttons normally gated on list selection (e.g. preset Load/Rename/Delete), which would otherwise come back enabled with nothing selected. **PR #16**, merged `2026-07-27`. | Test: `TestOperationFeedbackButtonRegistration::test_action_buttons_registered_on_startup`, `test_buttons_disabled_while_operation_in_progress` (test_main_window_settings.py, new -- constructs a real `MainWindow` and asserts buttons from multiple different pages/views are all disabled by one `start_operation()` call and re-enabled by `finish_operation()`) |
| 244 | **Found via `/code-review ultra --fix` on this session's `anonymizer.py` diff (two rounds):** (1) `_HOME_USER_RE`'s Windows alternative matched only a single backslash (`[Uu]sers\\`), but a Windows path stored in JSON (`settings.json`'s `rew_folder`) has each backslash escaped as `\\` on disk, so the real on-disk text is `Users\\\\` -- the regex never matched, leaving the username fully unscrubbed for every Windows user's settings file. (2) `anonymize_json_value()` routed `mac_address` through `_as_name()`'s `"NAME"` token category while `anonymize_text()`'s structural MAC regex uses `"MAC"`, so the same real MAC address got two different tokens depending on whether it was scrubbed from `device_info.json` or a raw log line -- breaking the anonymizer's own documented "same real value always gets the same placeholder throughout the bundle" guarantee | FIXED | YES | `bb2875b` | (1) `_HOME_USER_RE`'s Windows alternative changed to `[Uu]sers\\{1,2}` to match both the raw single-backslash form and the JSON-escaped double-backslash form. (2) `anonymize_json_value()` now special-cases the `mac_address` key to call `self._token_for("MAC", v)` directly instead of falling through `_as_name()`'s `"NAME"` category. A third finding from the same review pass (`operation_feedback.py`'s `_prior_enabled` snapshot being overwritten by a second `start_operation()` firing before a prior op's stray `operation_finished` arrives) was investigated and deliberately left unfixed -- it's a real design gap in the Cancel-button/bridge interaction, but a correct fix (reference-counted operation depth vs. a single snapshot) changes behavior beyond a diff-cleanup pass's scope | Test: `test_json_escaped_windows_username_scrubbed` (`TestHomeDirectoryUsernames`), `test_mac_address_same_token_via_text_and_json_paths` (`TestMacAddressTokenConsistency`, new) (test_anonymizer.py) |
| 245 | Ctrl+Enter (push on Review page) never fired | FIXED | N/A | `c006dfe` | Found during a `docs/qa_signoff.md` traceability audit: the manual test procedure had silently dropped its Ctrl+Enter check between drafts, and no automated test existed either. Root cause: two `QShortcut(QKeySequence("Ctrl+Return"))` registrations existed for the same key sequence -- one in `MainWindow._setup_keyboard_shortcuts()` at the default `Qt.WindowShortcut` context, one in `ReviewPage._setup_shortcuts()` at `Qt.WidgetWithChildrenShortcut` context. Whenever focus was anywhere inside the Review page (the normal case), both became simultaneously eligible, so Qt treated the key press as ambiguous and fired neither `activated` handler -- confirmed by the device owner's own hardware testing. Per product decision, removed rather than fixed: deleted both `QShortcut` registrations, `MainWindow._on_shortcut_confirm()`, `ReviewPage._setup_shortcuts()`, and the `Ctrl+Enter` row from `getting_started.md`'s shortcuts table. | N/A -- feature removed, not fixed; regression risk retired with it |
| 246 | **User-reported (live app):** two Connect-step UX inconsistencies. (1) Navigating back to the Connect step un-checks its step-completion pill (by design, per `#124`'s invariant), but the sidebar still showed the previously-connected device name -- device state and step-completion state were tracked independently (`WizardState.selected_device` vs `completed_steps`), so the UI simultaneously claimed "connected to X" and "Connect step incomplete." (2) The Connect page's rescan button greyed out during an active scan, but Ctrl+R still triggered a fresh scan regardless -- the button's disabled state (`ConnectPage.set_scanning()`) and the shortcut's gate (`MainWindow._is_busy()`) were two independent mechanisms that could disagree, so a visibly-disabled button was not actually disabled for keyboard input. | FIXED | YES | Stage 1: `f51ebb6`, `899d9d3` (superseded, see below), `0d58373` (squash-merged into development as `a9e1d7d`, PR #19); Stage 2: see the follow-up PR from branch `claude/connect-step-ux-issues-slcixl` | (2) is fixed: removed `ConnectPage.set_scanning()`'s own `self._rescan_btn.setEnabled(not active)` -- the button was already registered with `OperationFeedbackManager` via `action_buttons()`, which disables/re-enables it in lockstep with `start_operation()`/`finish_operation()`, the exact same `is_active` flag `_is_busy()` gates Ctrl+R on. Removing the redundant, independently-timed disable leaves a single source of truth so the button and the shortcut can no longer disagree. (1) was NOT fixed by making Connect re-entry reset the whole session, despite an earlier version of this diff doing exactly that -- a PR review (`disposabledominik`, full discussion on PR #19) found that approach duplicated a mechanism already living in `MainWindow._on_device_selected()`, mispriced the cost of clearing (an unchecked pill implies "one click to redo," but the state actually being destroyed includes expensive externally-sourced data -- REW file imports, REW API pulls, device reads), and penalized the common case (reconnecting to the *same* device, or just looking at Connect, is far more common than switching devices mid-session). The reset special case in `WizardController.go_to_step()` was reverted; `go_to_step(CONNECT)` now only invalidates `completed_steps`/`completed_step_tooltips`, same as any other step, and the sidebar device label is intentionally left showing the connected device while its pill is unchecked. The real long-term fix -- lazy/on-change invalidation, so browsing to a step is never destructive and a 4th `VIEWING` pill state replaces the reset-based semantics entirely -- is out of scope for this PR and deferred to a follow-up (see PR #19 review for the full design: `WizardState.filters` cost asymmetry, `VIEWING` pill state, frontier-derivation, `_on_navigation_requested("home")` interaction). Investigating this also surfaced two independent pre-existing bugs in `_on_device_selected`'s own state-clearing, fixed alongside it in this same PR -- see `#247`/`#248`. **Stage 2 (this row's completion): the lazy/on-change invalidation model has now landed.** `go_to_step()` is purely navigational (browsing never destroys checkmarks or data); invalidation moved entirely to change-time -- device switch (Stage 1), EQ-type switch and source/channel change (new change-detection in their handlers, using the old flow's sequence before `set_flow_type()` so RoomFit-only steps are caught, same ordering rule as `#248`). The step indicator gained a 4th `VIEWING` pill state (outlined pill + checkmark = "you are here, just looking") alongside the filled ACTIVE frontier pill ("resume here"), rendered from a derived frontier (`WizardController.frontier_step`, first incomplete step -- never stored, so it can't drift). "Resume Setup" jumps to the frontier when the user browsed away; entry side effects (Push page reset, Name Profile repopulate) run only on fresh frontier entry, so browsing back to a completed Push keeps its result view. The original symptom (1) is resolved structurally: the Connect pill stays checked while browsing, so it can no longer disagree with the sidebar device label. Switching devices with unpushed filter work now prompts first (`WarningConfirmDialog`) since that is the one change that destroys real payload; EQ-type/source changes clear only re-derivable checkmarks and deliberately do not prompt. The sidebar device label became a read-only device-info popover (PR #19 review D2; supersedes `#111`/`#124`, see those rows). **Post-landing review pass (same branch):** a `/code-review` sweep over the Stage 2 diff surfaced and fixed: (a) the sidebar-load gate (`_ensure_wizard_state_for_load`) pre-marked CONNECT/EQ_TYPE/SOURCE/FILTERS completed *before* the async load dispatched, so a failed load left phantom checkmarks and moved the derived frontier to REVIEW ("Resume Setup" then jumped to a Review page whose load had failed) -- steps are now marked only on success via `_jump_to_review`; (b) an EQ-type (PEQ<->RoomFit) switch cleared only `filters_origin`, carrying the old pipeline's loaded filter payload into the new flow -- it now uses `WizardState.clear_filter_payload()`, the same centralized clear the device switch composes (`clear_device_scoped_state()` calls it); (c) the "invalidate everything after step X" index math was hand-rolled three ways across the device/EQ-type/source handlers (one hardcoding `sequence[1]`, one with a silent `else 0` fallback) -- consolidated into `WizardController.invalidate_after(step)`, which no-ops with a warning on a missing step instead of guessing an index; (d) source change-detection compared ordered lists, so the same source set in a different order would have spuriously invalidated downstream steps -- now compared as sets; (e) `StepIndicator` restyled every widget several times per resync (each per-step setter triggered a full refresh, and each refresh applied four independent QSS re-polishes per widget) -- replaced by a bulk `sync()` entry point plus a single `render()` pass per widget; (f) the device-info popover was a hand-formatted `QMessageBox` -- replaced by `DeviceInfoDialog` using the shared `make_warning_box()` treatment. | Tests: `test_rescan_button_stays_enabled_while_scanning` (test_gui_pages.py), `test_buttons_disabled_while_operation_in_progress` (test_main_window_settings.py, existing, confirms the feedback-manager path still disables the rescan button during a real operation), `test_go_to_step_connect_retains_device_and_filter_state` (test_wizard_controller.py, new -- guards against the reset-on-CONNECT special case being reintroduced) ; Stage 2: `test_go_to_step_preserves_completed_steps`, `test_frontier_is_first_incomplete_step`, `test_frontier_clamps_to_last_step_when_all_complete`, `test_frontier_ignores_stale_off_sequence_entries`, `test_invalidate_from_emits_steps_invalidated` (test_wizard_controller.py), `test_back_navigation_preserves_completed_steps` (test_wizard_controller_pbt.py, property), `test_browsing_to_completed_step_shows_viewing_with_checkmark`, `test_frontier_pill_active_and_clickable_while_browsing`, `test_viewed_step_not_clickable`, `test_all_complete_clamp_renders_completed_frontier` (test_gui_components.py), `test_go_to_source_preserves_checkmarks_until_change`, `test_eq_type_change_invalidates_downstream_same_type_does_not`, `TestLazyInvalidationEntrySideEffects` (3 tests), `TestDeviceSwitchConfirmation` (3 tests) (test_smoke_regression_wizard.py), `test_back_navigation_preserves_steps_and_shows_viewing`, `test_resume_setup_returns_to_frontier_with_state_intact` (test_wizard_integration.py); review pass: `test_load_gate_never_marks_steps_before_load_resolves`, `test_source_reorder_is_not_a_change`, payload-clearing assertions in `test_eq_type_change_invalidates_downstream_same_type_does_not` (test_smoke_regression_wizard.py), `test_invalidate_after_pops_only_later_steps`, `test_invalidate_after_missing_step_is_a_noop`, `test_invalidate_after_last_step_pops_nothing`, `test_clear_filter_payload_spares_device_and_sources` (test_wizard_controller.py), `test_sync_bulk_resyncs_in_one_call` (test_gui_components.py) |
| 247 | **Found while reviewing `#246`'s PR:** switching the connected device while in L/R channel mode left the *previous* device's L/R filters readable as the current filter set. `MainWindow._on_device_selected()` cleared `current_filters` on device switch but not `filters_l`/`filters_r`, and `WizardState.filters` (`src/gui/wizard_controller.py`) returns `filters_l + filters_r` whenever `channel_mode.is_lr` and either list is non-empty, falling back to `current_filters` only when both are empty -- so device B's session silently inherited device A's L/R filters instead of an empty filter set. | FIXED | YES | `0d58373` | Root cause was `_on_device_selected`'s hand-rolled field-by-field clear, which had drifted out of sync with `WizardState`'s actual field list (it also missed `device_filters`, `warnings`, `channel_mode`, `roomfit_profile_name`, `last_backup_path`, `pending_rows*`/`pending_conversion_notes*`). Replaced with `WizardState.clear_device_scoped_state()`, a single method owning the full device-scoped field list, called from `_on_device_selected` on an actual device change. | Test: `test_device_switch_clears_lr_filters` (test_smoke_regression_wizard.py, new) |
| 248 | **Found while reviewing `#246`'s PR:** switching the connected device while `NAME_PROFILE` was completed (RoomFit-only step) left it stuck in `completed_steps` forever. `_on_device_selected` popped a hardcoded tuple `(EQ_TYPE, SOURCE, FILTERS, REVIEW, PUSH)` that never included `NAME_PROFILE`, so switching devices out of a completed RoomFit flow and back into RoomFit later could show `NAME_PROFILE` checked while `FILTERS`/`REVIEW` before it were unchecked -- a direct violation of `#124`'s "no checked step may follow an unchecked one" invariant. | FIXED | YES | `0d58373` | Added `WizardController.invalidate_from(step)`, driven by `get_steps()` for the *current* flow rather than a hardcoded tuple, so it can't omit a step that only exists in one flow variant. `_on_device_selected` now calls it with the old (pre-switch) flow's sequence before resetting `flow_type`, so a step from the flow being left (like RoomFit's `NAME_PROFILE`) is still visible to `get_steps()` at invalidation time. `go_to_step()` was also rewritten to call the same helper, fixing a related minor gap where it invalidated `completed_steps` but not `completed_step_tooltips`. | Tests: `test_device_switch_in_roomfit_clears_name_profile_completed` (test_smoke_regression_wizard.py, new), `test_invalidate_from_first_step_clears_whole_sequence` (test_wizard_controller.py, new, parametrized over all three `FlowType` values), `test_invalidate_from_pops_tooltips_too` (test_wizard_controller.py, new), `test_reselecting_same_device_preserves_filters` (test_smoke_regression_wizard.py, new -- guards the same-device no-op path added alongside this fix) |
| 249 | **Found while reviewing `#246` Stage 2's PR (PR #20):** the new device-switch confirmation gated on `_has_unsaved_changes()`, which compared only `current_filters` against `last_pushed_filters` -- but in L/R mode the real payload lives in `filters_l`/`filters_r` (which `WizardState.filters` deliberately prefers, see `#247`). Unpushed work living only in the L/R lists (`current_filters` empty) therefore switched devices *without* a prompt, silently destroying exactly the payload the confirmation was added to protect; `closeEvent`'s unsaved-changes prompt shared the same gap. Same field-subset-drift class as `#247`. | FIXED | YES | PR #20 review pass | `_has_unsaved_changes()` now reads `state.filters` (the computed accessor that prefers `filters_l + filters_r` in L/R mode) instead of `current_filters`, and the push-success snapshot in `_on_write_complete` stores `list(state.filters)` for the same reason -- so the dirty check and its snapshot compare the same operand in both channel modes, keeping the post-push "clean" state correct for L/R pushes too. | Test: `test_lr_only_filters_trigger_prompt` (test_smoke_regression_wizard.py, TestDeviceSwitchConfirmation, new); existing `#158` dirtiness tests (test_smoke_regression_operations.py) still cover the stereo path against the real method |
| 250 | **User-reported (live app, manual QA):** back on the Source step, "Continue" was sometimes disabled even with a source checked -- toggling any checkbox off and back on made it enabled again, and this didn't reproduce every time. Root cause was `OperationFeedbackManager` (`src/gui/operation_feedback.py`), not the Source step itself: `start_operation()` snapshots every registered action button's `isEnabled()` (`SourcePage.Continue` starts disabled before any sources exist), and `finish_operation()`/the 30s timeout unconditionally restore that snapshot when the operation ends. On device connect, `AsyncBridge.run_async()`'s wrapper coroutine emits the operation's own result signal (`capabilities_ready`) *before* its `finally` block emits `operation_finished` -- both queued to the main thread in that order -- so `_on_capabilities_ready` -> `SourcePage.set_sources()` legitimately enables Continue (a default source gets pre-checked) *before* `finish_operation()` runs and reverts it back to the stale pre-operation snapshot (disabled). Any operation whose own success handler enables a button it had force-disabled at start hits the same pattern; it "didn't happen always" because it's only visible when the button's true state differs from its snapshot -- e.g. a later reconnect where Continue was already enabled beforehand shows no symptom. | FIXED | YES | (branch `claude/connect-step-ux-issues-slcixl`) | First attempt used a simpler `if not btn.isEnabled(): restore snapshot` guard, which fixed the reported direction but was wrong for the mirror case (`/code-review ultra`, run twice, independently caught this): a button that legitimately becomes *disabled* mid-operation for an unrelated reason (e.g. re-probing a device whose source list no longer includes the previously active/default source, so `set_sources()` correctly disables Continue) would still get forced back to its stale, possibly-`True` snapshot, since the guard can't distinguish "still disabled because nothing touched it" from "disabled again for a new, legitimate reason" -- both look identical as a bare boolean. Also flagged: several other registered buttons (preset Load/Copy/Delete/Export/Rename, REW-pull Continue) are re-enabled by list-selection-changed handlers that don't check `is_active`, so under the guard-based fix a selection made mid-operation would permanently escape the restore-to-snapshot safety net (Req 13.1 double-submit protection). Replaced with an explicit opt-in: `OperationFeedbackManager.note_button_state_changed(btn)` updates that button's *snapshot itself* (not just a live-value check) when business logic legitimately changes its enabled state while an operation may be in flight; `_restore_button_states()` went back to always applying the snapshot. `_on_capabilities_ready` now calls `note_button_state_changed()` right after `set_sources()`, so Continue's snapshot always reflects its true state in either direction, while every other registered button that doesn't opt in keeps the original blind-restore behavior (no regression, and Req 13.1 protection is intact for buttons that were never part of this bug). | Tests (test_gui_operation_timeout.py): `test_finish_does_not_stomp_a_noted_button_re_enabled_mid_operation` (renamed from the original, now opts in like production code), `test_finish_still_reverts_an_un_noted_mid_operation_enable` (new -- guards the double-submit-protection finding), `test_note_button_state_changed_is_a_noop_when_no_operation_active` (new). **Follow-up (`/code-review ultra`, third pass):** `_on_capabilities_ready` referenced Continue via a hardcoded `self._source_page.action_buttons()[0]` index rather than a named accessor -- if `SourcePage.action_buttons()` ever returned more than one widget ahead of Continue, `note_button_state_changed()` would silently snapshot the wrong button, reintroducing this exact bug while looking fixed, with no test guarding the list-order/Continue coupling. Fixed by adding `SourcePage.continue_button()`, a stable named reference distinct from `action_buttons()`'s bulk-registration list, used at the one call site; guarded by `test_continue_button_accessor_matches_action_buttons_entry` (test_gui_pages.py, new). Investigated whether the same fragility is live elsewhere (per a request for a universal fix, not a one-off patch): audited every `setEnabled()` call site across all pages/views registered with `OperationFeedbackManager` for one driven by an async bridge-result handler mid-operation, the specific precondition for this bug class. `RewPullView.Continue` looked like a second live instance at first (`set_measurements()`, called from `_on_measurements_listed` inside the same `run_async` signal-ordering pattern, also calls an `_update_continue_enabled()`-style method) but isn't: unlike `SourcePage.set_sources()`, `set_measurements()` never auto-selects a list item, so Continue can only go disabled->enabled via a genuine synchronous user click on a list row, never as a direct result of population -- already asserted by the pre-existing `test_continue_disabled_until_valid_selection` (test_gui_views.py). `FiltersPage`'s and the preset views' `setEnabled()` calls are all synchronous user-input reactions (file dialogs, list-selection clicks) never touched by a bridge signal, so they're not exposed either. No further call sites needed opting in. |
| 251 | **Design/UX redesign, not a reported bug:** the Filters step's File Import / Pull from REW API toggle, the sidebar's standalone "Pull from REW" entry point, and both preset pages' ("Presets on Device", "My Saved Presets") "Load into Editor" actions were three independent ways to get filter data into the wizard, two of which (the sidebar shortcuts) bypassed the normal wizard step order and backfilled `completed_steps` via `QuickSetupDialog` — duplicated logic the project's own conventions flag as a smell, and a shortcut path with no equivalent anywhere else in the app. | FIXED | YES | `a39f8f6` | Consolidated into a single Filters-step "Import source" dropdown with four options — REW File, REW API, Device, Local Library — always reached via the normal wizard order (no shortcuts, no step-skipping). The Device option merges PEQ presets and RoomFit profiles into one list regardless of the wizard's current EQ_TYPE (a saved preset's origin doesn't have to match the flow pushing it); Device and Local Library selections carry their own channel mode, so neither needs a separate Stereo/L-R toggle. Removed as a consequence: the sidebar "Pull from REW" nav item and its standalone `RewPullView` instance; both preset pages' "Load into Editor" signal/button; `QuickSetupDialog` and its `_ensure_wizard_state_for_load()` gate (confirmed dead once the three shortcuts using it were gone); the old hidden `_roomfit_combo`/`refresh_roomfit_dropdown()` pipeline (superseded by the merged Device list, which needed the richer `PresetItem` data already used by "Presets on Device" rather than that pipeline's bare profile-name strings); `_sidebar_load_in_progress` and the `_jump_to_review()`/`_mark_prior_steps_completed()`/`_apply_source_summary()` helpers it required. `_on_profile_recalled` (Local Library's completion handler) now uses the same plain `advance()` every other filters producer already used, instead of the sidebar-only jump — this means a stale `PUSH` completion left over from lazy invalidation's non-destructive back-browsing (`#246`) is no longer cleared by a fresh Local Library recall, matching the pre-existing (unfixed) behavior of every other producer rather than a regression specific to this change; see `TestProfileRecallInvalidatesStalePush` (test_gui_integration_profile.py) for the documented current behavior. Sidebar `"home"` relabeled "Resume Setup" -> "Setup Wizard" in the same change (unrelated rename, bundled since it touches the same sidebar file). | Tests: `TestFiltersPage` (test_gui_pages.py, rewritten + new Device/Local Library panel coverage), `TestProfileRecallInvalidatesStalePush` (test_gui_integration_profile.py, rewritten), `test_issue24_filters_device_panel_load_connected`/`test_issue27_roomfit_profile_selected_triggers_pull`/`test_issue65_profile_load_sets_channel_mode_*` (test_smoke_regression_operations.py, rewritten against the new handlers), `TestIssue162ConsistentStepSummaries` (test_smoke_regression_wizard.py, folded down to the single remaining completion path); deleted (premise removed): `TestIssue87SidebarPresetLoadChecks`, `TestIssue95WizardStateSkipsDialog`, `TestIssue19EqTypeRoomfitMode` (test_smoke_regression_wizard.py), `TestQuickSetupDialog` (test_gui_dialogs.py), `TestRefreshRoomfitDropdown` (test_gui_integration_primary.py), and the sidebar-load-specific tests in test_smoke_fixed_issues.py |
| 252 | **Design follow-up, not a reported bug (#165c):** the Filters step's merged Device list used a plain em-dash text format instead of `PresetsDeviceView`'s established `Name  [ChannelMode]  [Type]  (active)` bold/accent convention, and there was no way to tell from either list which named preset/profile (if any) matched the device's *live* PEQ config when it didn't match a saved name. | FIXED | YES | `e7d3c2f, f5cdb69` | Extracted `build_preset_list_item()` (`list_item_style.py`) so both lists render identically by construction. Added a synthetic "Custom" row (WiiM Home's own term) to both lists whenever the active-PEQ-name read comes back empty — the hardware-confirmed signal that a raw band write orphaned the device's saved-name association (`docs/wiim_api_notes.md`, `docs/corrections.md` 2026-07-05). In the Filters step it's selectable and routes to the same live-PEQ pull as "Current configuration on device"; in "Presets on Device" it's initially non-selectable (Export/Save/Copy/Delete all keyed by saved name), then made selectable for Export/Save/Copy once `WiiMAdapter.read_preset_preview_or_live()` was added — a plain `read_peq()` bypassing the named-preset preview/restore dance entirely, since the config is already live. Delete stays disabled whenever the Custom row is part of the selection. Copy prompts for a real name first (`QInputDialog`), since "Custom" isn't one the device assigned. | Tests: `TestReadPresetPreviewOrLive` (test_wiim_adapter.py), `test_empty_active_name_shows_custom_row`/`test_custom_row_selectable_but_disables_delete` (test_gui_views.py), `test_custom_row_shown_when_active_peq_name_empty`/`test_custom_row_load_emits_device_pull_requested_not_item_selected` (test_gui_pages.py), `test_export_custom_item_reads_live_not_preview`/`test_save_custom_item_reads_live_not_preview`/`test_copy_custom_item_prompts_for_name_and_renames`/`test_copy_custom_item_cancelled_prompt_aborts`/`test_copy_named_presets_skip_name_prompt` (test_smoke_regression_operations.py) |
| 253 | **User-reported (live app review, not smoke testing):** "Presets on Device"'s Export as REW File and Save to My Presets both silently exported/saved only the first selected item when multiple presets were multi-selected (ctrl+click/shift+click/select-all) — the toolbar buttons enable for a multi-select and Save's own docstring claimed "for each selected item," but both handlers did `item = items[0]` and dropped the rest with no error or warning. Copy to Another Device and Delete were already correct (both already looped over every selected item). | FIXED | YES | `c38ed7c` | Replaced the single-item `export_preset()`/`save_preset()` dispatch methods with batch `export_presets()`/`save_presets()` (`PrimaryWorkflowManager`), each looping sequentially — not concurrently, since a named-preset read briefly loads it onto the device's live DSP and concurrent reads would race on that shared state — over every selected item, catching per-item failures and emitting a single `presets_export_complete`/`presets_save_complete(succeeded, failed)` summary, mirroring the existing `_do_delete_presets`/`_do_copy_presets_batch_multi` batch pattern exactly. A single preset with no filters now reports as "0 exported, 1 failed" instead of a specific `EmptyPresetFiltersError` message — a deliberate consistency trade-off matching how Delete already handles a single-item failure. `MainWindow`: a 1-item Export selection keeps the existing per-file save dialog (exact filename control); 2+ items instead pick one destination folder once, then each preset is auto-named inside it from its own device-prefixed name. Save always batches (no dialog either way, same auto-naming as before). | Tests: `TestPresetExportBatch`/`TestPresetSaveBatch` (test_gui_integration_primary.py, new — processes-all and partial-failure-continues cases), `test_export_multiple_presets_uses_folder_picker_and_processes_all`/`test_export_multiple_presets_cancelled_folder_picker_aborts`/`test_save_multiple_presets_processes_all`/`test_presets_export_complete_shows_success_and_partial_failure`/`test_presets_save_complete_shows_success_and_partial_failure` (test_smoke_regression_operations.py, new); existing `test_issue24_presets_device_export_connected`/`test_issue24_presets_device_save_connected` rewritten to mock the new batch methods instead of the now-internal-only `_do_preset_export`/`_do_preset_save` |
| 254 | **Found by `/code-review ultra --fix` (10 parallel finder agents) auditing the #251-253 diffs, not smoke testing:** independently confirmed by 3-4 angles each: (1) `FiltersPage`'s Device-panel cache (`_device_peq_items`/`_device_roomfit_items`/`_device_active_*_name`) is separate from `WizardState` and was never cleared on a device switch (`MainWindow._on_device_selected`) — browsing back to the Filters step's Device panel after switching devices could show, and let the user load, the previous device's presets under names that might not exist on the newly connected device. (2) `_on_peq_presets_unavailable`/`_on_roomfit_profiles_hidden` only forwarded to `PresetsDeviceView`, never to `FiltersPage`, unlike the success-path handlers (`_on_peq_presets_ready`/`_on_roomfit_profiles_ready`), which forward to both — a device that stopped reporting profile enumeration or RoomFit support left `FiltersPage`'s merged list showing stale entries. Also confirmed as duplication, not bugs: the Custom-row "insert when active_name==''" logic was hand-copied between `PresetsDeviceView._populate_peq_list` and `FiltersPage._populate_device_list`; and `_do_export_presets`/`_do_save_presets` hand-copied `_do_delete_presets`'s sequential accumulate/emit batch-loop pattern. One finder's claim that removing `_jump_to_review()` "reintroduces #239" was investigated and found to describe an already-deliberate, tested trade-off (see `TestProfileRecallInvalidatesStalePush`'s docstring, present since #251), not a new regression — not fixed. | FIXED | YES | `acd3f35` | Added `FiltersPage.clear_device_presets()` (clears both caches, refetches immediately if the Device panel is currently showing), called from `_on_device_selected` on every device switch; added `FiltersPage.set_peq_unavailable()`/`set_roomfit_hidden()` mirroring `PresetsDeviceView`'s own, wired into `_on_peq_presets_unavailable`/`_on_roomfit_profiles_hidden` alongside the existing `PresetsDeviceView` calls. Extracted `build_peq_rows()` (`presets_device_view.py`, next to `build_custom_peq_item`) so both list-builders share the one "when does Custom appear" rule instead of two copies. Extracted `PrimaryWorkflowManager._run_batch_preset_requests()` so `_do_export_presets`/`_do_save_presets` share one sequential accumulate/emit loop instead of two hand-copies of `_do_delete_presets`'s pattern (`_do_delete_presets` itself was left as-is — its extra empty-name skip branch and post-loop `refresh_presets()` step don't map cleanly onto the shared helper's shape, and forcing it in risked a real behavior change for low payoff). Lower-severity findings from the same review (a double Device-list rebuild per refresh since PEQ/RoomFit arrive as two signals, no caching of the Device/Local Library fetch across repeated source-combo switches) were reviewed and intentionally left as-is — real but low-impact, and fixing them wasn't worth the added surface area in this pass; caching in particular would need cache-invalidation wired into every place that mutates presets elsewhere (Delete/Copy/Save/Rename), trading "occasionally re-fetches" for "can go stale in more ways." One remaining nit — the identical channel-mode-coercion lines in `_on_device_item_selected`/`_on_local_profile_selected` — was small and risk-free enough to fix on follow-up request: extracted `MainWindow._apply_channel_mode_from_item()`. | Tests: `test_device_switch_clears_filters_page_device_cache` (test_smoke_regression_wizard.py), `test_peq_presets_unavailable_forwards_to_both_views`/`test_roomfit_profiles_hidden_forwards_to_both_views` (test_smoke_regression_operations.py), `test_set_peq_unavailable_clears_peq_items`/`test_set_roomfit_hidden_clears_roomfit_items`/`test_clear_device_presets_empties_merged_list`/`test_clear_device_presets_refetches_when_device_panel_active`/`test_clear_device_presets_does_not_refetch_when_other_panel_active` (test_gui_pages.py); `_apply_channel_mode_from_item` covered indirectly by the existing device/local-profile-selection tests in test_gui_integration_profile.py/test_smoke_regression_operations.py (behavior-preserving extraction, no new test needed). `test_clear_device_presets_refetches_when_device_panel_active`/`test_clear_device_presets_does_not_refetch_when_other_panel_active` were replaced by `#255`'s fix — see that row's Test Reference column for the current names |
| 255 | **Left as review comments on PR #24 itself (not this repo's own `/code-review`), not smoke testing:** two real bugs confirmed against the actual code: **BUG 1** — `FiltersPage.clear_device_presets()` (added by `#254`'s fix) emitted `device_presets_requested` synchronously from `MainWindow._on_device_selected`, before the new device's `WiiMAdapter` exists (`_wiim_adapter` is only replaced later, asynchronously, in `_on_capabilities_ready` once the capability probe resolves) — switching devices while the Filters step's Device panel was open refetched presets against the *old* device, and nothing re-fetched once the new adapter became ready, so the panel stuck on stale data. **BUG 2** — `_on_device_presets_requested` was missing the `_is_busy()` guard its sibling dropdown-switch handlers (`_on_device_pull_requested`, `_on_rew_api_pull_requested`) already have, so a rapid source-dropdown toggle mid-operation could dispatch a concurrent `list_presets()` call. Also confirmed as real but cosmetic: a fragile `sizeHint().expandedTo(sizeHint().__class__(...))` pattern in `build_preset_list_item()` (the reviewer's claim that `QSize` was "already imported" was itself wrong — it wasn't — but the underlying fix suggestion was still correct); an inline `PresetItem` import inside a loop body with no comment documenting that at most one Custom row can ever appear; two selection handlers with no comment explaining why they skip a wizard-state pre-check now that `QuickSetupDialog` is gone; `conftest.py`'s blocking-modal doc comment not mentioning `QInputDialog.getText`. Two suggestions were checked and found not to apply: an empty-preset-name guard for `_export_presets_to_folder`/`_on_preset_save_requested` (unreachable in practice — the synthetic Custom row's `PresetItem.name` is always the literal string `"Custom"`, never empty, and every other row is a real device-reported name) and further-extracting `_populate_local_list`'s `_PresetItemWidget` construction (already a deliberate, documented trade-off in the original redesign plan, not fresh duplication). One finding (RoomFit `channelMode` defaulting to `"Stereo"` when absent) was raised then retracted by the reviewer's own follow-up comments after checking `wiim_api.log` against real hardware sessions. Separately, user-reported: the Filters step's new "Import source" dropdown (and Settings' Theme dropdown, the only other `QComboBox` in the app) had no visible chevron/arrow, giving no visual cue that it's a dropdown — root cause was `QComboBox::down-arrow` in both `fluent_dark.qss`/`fluent_light.qss` setting only `width`/`height` with no `image`, so styling `::drop-down` opts Qt out of its native arrow but nothing else was ever painted in its place; a border-only CSS triangle (no image asset, matching the theme's otherwise asset-free convention) was tried first but confirmed (via an offscreen-rendered screenshot) to paint as a solid filled box in this Qt build rather than a triangle, so an actual SVG icon was used instead. | FIXED | YES | `1b432ad, 561ce49` | BUG 1: `clear_device_presets()` no longer emits `device_presets_requested` under any circumstance; `MainWindow._on_capabilities_ready()` re-triggers `list_presets()` itself, once the new adapter is actually live, if the Filters step's Device panel is the panel currently showing (read via new public `FiltersPage.current_source` property, replacing a would-be reach into the page's private `_current_source`). BUG 2: added the missing `_is_busy()` guard. Cosmetic items: `list_item_style.py` now imports `QSize` and calls `QSize(0, LIST_ITEM_HEIGHT)` directly; `PresetItem` promoted to a top-level import in `main_window.py` (both prior inline-import call sites removed) plus a comment on the "stop after first Custom row" assumption; doc comments added to `_on_device_item_selected`/`_on_local_profile_selected` and to `conftest.py`. Chevron: added `chevron_down_dark.svg`/`chevron_down_light.svg` (`src/gui/assets/icons/`); `ThemeManager._load_stylesheet()` substitutes a new `%ICONS_DIR%` placeholder token in the loaded QSS text for the icons directory's real absolute path (as a forward-slash POSIX path, since Qt's `url()` always wants those regardless of platform) rather than baking in a literal relative path, since Qt resolves a stylesheet's relative `url()`s against the process's working directory, not the `.qss` file's own location -- a literal relative path would have been packaging-launch-method-dependent. All three PyInstaller `.spec` files updated to bundle the two new SVGs alongside the existing app-icon files (that bundle list is a hand-picked filename tuple, not a directory glob, so a new asset file silently doesn't ship unless added there too). **Follow-up commit `561ce49`:** the per-theme SVG filenames and differing comment text in the chevron rule broke `test_qss_parity.py` (masks color values only, not filenames) on CI; fixed by having both theme files reference an identical `%CHEVRON_ICON%` token, resolved to the theme-specific filename in `ThemeManager._load_stylesheet()` (now theme-aware) instead of being baked into the QSS itself. | Tests: `test_capabilities_ready_refetches_device_presets_when_panel_active`/`test_capabilities_ready_does_not_refetch_when_other_panel_active` (test_smoke_regression_wizard.py, new), `test_device_presets_requested_blocked_when_busy` (test_gui_filter_handlers.py, new, `TestDevicePresetsRequestedBusyGuard`), `test_clear_device_presets_never_refetches`/`test_current_source_reflects_combo_selection` (test_gui_pages.py, replacing the two `#254` tests whose asserted behavior BUG 1's fix corrected). No automated test for the chevron itself (rendered appearance, not testable via pytest) -- verified manually via an offscreen-rendered `ThemeManager`-driven screenshot in both themes before and after the fix |
| 256 | **User-raised design question, not a reported bug:** the Filters step's Device panel had a dedicated "Current configuration on device" button sitting alongside the merged preset list's own synthetic "Custom" row (`#252`) -- on any device that supports PEQ profile enumeration, both did exactly the same thing (pull the live PEQ config), which is confusing UI redundancy the user correctly flagged as misleading. The button's only non-redundant job was devices that can't enumerate saved presets at all (`supports_profile_enumeration: false`): the merged list's PEQ side depended entirely on that capability, so such a device showed no "Custom" row and no named presets -- an empty PEQ section with the button as the sole way to reach the live config. Investigation also surfaced two real, pre-existing gaps in the same code path: `_peq_active_info_or_default()` degraded a genuine read *failure* to `("", "Stereo")` -- the same value used for "confirmed no active preset" -- so a transient read error already rendered an unearned "Custom" row even before this change (masked because `list_peq_profiles()` succeeding independently meant the list still showed real data either way); and `MainWindow._load_device_presets()` (the "Presets on Device" sidebar's fetch) had an early return on `not supports_profile_enumeration` that skipped calling `list_presets()` entirely, silently dropping the RoomFit fetch too, even though `PrimaryWorkflowManager.refresh_presets()` was already designed to fetch PEQ and RoomFit fully independently (`#174`) -- profile enumeration and RoomFit support are separate, independently-probed capabilities (`EQv2GetNewList` vs. `GetAcousticCapability`/RoomFit fallback probe), not two symptoms of one "cheap device" condition. | FIXED | YES | `59c7ea1` | Removed the button entirely. `PrimaryWorkflowManager._fetch_peq()` (inside `refresh_presets()`) now gates the live-config read (`read_peq()`, backing the synthetic "Custom" row) on `supports_peq`, and only gates the *named-preset* list (`list_peq_profiles()`) on `supports_profile_enumeration` -- so a device that can't enumerate presets still gets a "Custom"-only PEQ section instead of an empty one, in both the Filters step's Device panel and "Presets on Device." `_peq_active_info_or_default()` now degrades to `(None, "Stereo")` on a real read failure instead of `("", "Stereo")`, and `_fetch_peq()` emits `peq_presets_unavailable` (not a spurious `peq_presets_ready([], "", ...)`) when nothing was actually confirmed -- `None` (unknown) and `""` (confirmed no match) are no longer conflated. `peq_presets_ready`'s Qt signal signature changed from `Signal(list, str, str)` to `Signal(list, object, str)` to let `None` cross the signal boundary (`MainWindow._on_peq_presets_ready`'s `@Slot` decorator updated to match). Removed the early-return in `_load_device_presets()` so the RoomFit fetch is never skipped as a side effect of a PEQ-only capability gap. Both `FiltersPage`'s and `PresetsDeviceView`'s existing Custom-row rendering (`build_peq_rows`/`build_custom_peq_item`, from `#252`) needed no changes -- they already handled `active_name=None` (no row) vs. `""` (show "Custom") correctly; only the upstream signal producer needed to stop conflating the two. Export/Save/Copy on a Custom row synthesized this way work identically to the enumeration-capable case, since both key off `PresetItem.is_custom` regardless of how the row was derived. Updated help docs (`managing_presets.md`, `import_and_push.md`, `pull_and_export.md`) and `docs/qa_signoff.md` to drop the button and describe the unified list, including the no-enumeration "Custom is the only PEQ row" case; documented (not fixed, by design) that "Custom" only ever reflects the live config on the currently-selected `state.primary_source` -- other sources on the same device aren't visible in this list. | Tests: `test_no_peq_support_emits_unavailable_without_listing`/`test_no_enumeration_but_peq_supported_shows_custom_row`/`test_no_enumeration_and_read_fails_emits_unavailable`/`test_load_device_presets_fetches_roomfit_without_enumeration` (test_smoke_regression_operations.py, new, `TestPresets`); `test_165c_do_list_presets_degrades_gracefully_on_active_name_failure` (same file, updated: PEQ side now asserts `is None` instead of `== ""`); `test_action_buttons_registered_on_startup` (test_main_window_settings.py, updated: `_device_current_btn` assertion removed) |
| 257 | **User-raised follow-up questions after `#256`, not a reported bug:** two real gaps identified while confirming the merged Device list's "(active)"/"Custom" labeling is fully accurate. (1) The synthetic "Custom" row's presence signal (`active_name == ""`) is confirmed on hardware (`docs/corrections.md`, 2026-07-03) to fire for *two* distinct device states with no way to tell them apart from the list alone: a source with genuinely hand-adjusted filters that lost its saved-preset association (raw `EQSetLV2SourceBand` write, `docs/corrections.md` 2026-07-05), and a source that's simply never had a preset loaded -- confirmed to return the identical `Name:""` signature with an all-default flat template (stock ISO frequencies, zero gain), same as the already-documented "nonexistent source" signature. A fresh device, or an untouched source, shows "Custom (active)" exactly like one with real custom content. (2) Neither the named-preset "(active)" highlight nor the "Custom" row ever inspected `EQStat` (PEQ on/off) or RoomFit's global on/off state -- confirmed by code review, the only place device-side enabled/disabled state was surfaced anywhere in the GUI was the Diagnostics panel's raw source-slot table. A source/device with PEQ or RoomFit fully toggled off still showed its matching row as plain "(active)", indistinguishable from one actually being applied to audio. | FIXED (1: documented, not fixed -- no reliable way to distinguish flat-default from genuinely-custom content without guessing at undocumented band-value heuristics, which CLAUDE.md's domain rules warn against; 2: fixed in code) | YES | `53e1a71` | (1) Documented the ambiguity directly in the help docs (`managing_presets.md`, `import_and_push.md`, `pull_and_export.md`) rather than attempting to distinguish it -- users needing to know which case they're looking at are pointed at the Review step's actual filter values. (2) `PEQSettings.enabled`/`get_roomfit_status()`'s enabled bool -- both already read for other purposes, just previously discarded -- are now threaded through the full pipeline: `PrimaryWorkflowManager._peq_active_info_or_default()`/new `_roomfit_active_info_or_default()` return `(name, ..., enabled)` tuples; `peq_presets_ready`/`roomfit_profiles_ready` signals gained a trailing `bool` arg (`Signal(list, object, str, bool)`/`Signal(list, str, bool)`); `PresetsDeviceView`/`FiltersPage`'s `set_peq_presets()`/`set_roomfit_profiles()` gained an `active_enabled: bool = True` parameter; `build_peq_rows()` (presets_device_view.py) now returns `(item, is_active, is_eq_off)` 3-tuples instead of 2-tuples; `build_preset_list_item()` (list_item_style.py) gained an `is_eq_off: bool = False` parameter that changes the suffix from "(active)" to "(active, PEQ off)"/"(active, RoomFit off)" (read from `item.preset_type`) when true. `is_eq_off` can only ever be True on the one row that's already `is_active` -- every other row is unaffected since it isn't what's currently loaded anyway. All new parameters default to the pre-existing behavior (`True`/no qualifier), so no existing call site needed updating beyond the signal/slot signature changes themselves. `qa_signoff.md` updated with the new qualifier's expected behavior. | Tests: `test_active_peq_item_shows_eq_off_qualifier`/`test_custom_row_shows_eq_off_qualifier`/`test_active_roomfit_item_shows_eq_off_qualifier`/`test_eq_off_qualifier_defaults_to_omitted` (test_gui_views.py, new, `TestPresetsDeviceViewActiveHighlight`); `test_device_list_shows_eq_off_qualifier_for_both_types` (test_gui_pages.py, new); `test_peq_off_forwards_enabled_false_to_both_views`/`test_roomfit_off_forwards_enabled_false_to_both_views`/`test_peq_and_roomfit_enabled_default_true` (test_smoke_regression_operations.py, new, `TestPresets`) |


Updated 2026-08-08: added `#268` from a `/code-review ultra --fix` pass (10 finder angles) run
against this session's `#266`/`#267` diff. Two independent angles converged on the same
architectural gap: the FILTERS change-detection added for `#266`'s Review/Push-staleness fix
(`_on_filters_accepted`'s signature comparison) only ran on the narrow warnings-acknowledgment
path, never on `_on_peq_ready` (device pull / file import -- the common case) or
`_on_profile_recalled` (Local Library), both of which called `advance()` directly. Consolidated
into a shared `_confirm_filters_selection()` helper called from all three paths. A separate pair
of angles (line-by-line scan, language-pitfall specialist) independently found the same class of
bug in `MyPresetsView`: `_get_selected_profile()`/`_on_rename_clicked()` read `currentItem()`
instead of the actual selection (can disagree after a ctrl-click deselect under
`ExtendedSelection`), and the right-click context menu's Delete action only ever acted on the
item under the cursor, silently dropping the rest of a multi-selection Qt's right-click leaves
intact -- both fixed. The other ~25 findings across the 10 angles were reviewed and intentionally
not acted on: mostly either pre-existing patterns predating this session's diff (e.g. `set_dimmed`
dead code, the flat nine-attribute Device-panel state, the per-step `if step ==` chain in
`_on_step_changed`) flagged as architectural observations rather than bugs, or acceptable
safe-side tradeoffs (e.g. the filters-signature comparison using exact float equality instead of
`fp_compare`'s tolerance -- can only cause a spurious *extra* invalidation, never a missed one, so
not worth the added complexity). Verified both fixes empirically (reverted, confirmed the new
tests fail, restored, confirmed pass). Full verification: `ruff check src/` clean, `mypy src/`
clean, `test_gui_views.py` (86), `test_smoke_regression_operations.py` (targeted subset + full
file), `test_smoke_regression_wizard.py` (49), `test_wizard_integration.py` +
`test_gui_integration_flow.py` + `test_gui_filter_handlers.py` (46) all pass.

Updated 2026-08-07: added `#258`-`#266` from a QA pass covering `docs/qa_signoff.md`'s manual test
procedure plus direct user observations. `#266` was initially logged as investigated/could-not-
reproduce: new regression tests covering both the RoomFit-capable/PEQ-chosen-at-EQ_TYPE and
PEQ_ONLY device paths through the actual `_on_device_selected`/`_on_capabilities_ready` handler
chain, checking both `WizardController.completed_steps` and `StepIndicator`'s own rendered
`_completed` flags, did not reproduce the reported discrepancy for that exact click sequence.
Full verification for this pass: `ruff check src/` clean, `mypy src/` clean (163 source files) +
`mypy src/translator src/models` clean (17 files, strict), targeted suites (`test_gui_views.py`,
`test_gui_pages.py`, `test_gui_dialogs.py`, `test_gui_components.py`, `test_help_view.py`,
`test_device_name.py`, `test_gui_integration_primary.py`, `test_gui_integration_flow.py`,
`test_gui_adapter_injection.py`, `test_wizard_integration.py`) all pass, plus full runs of
`test_smoke_regression_operations.py` (156 passed) and `test_smoke_regression_wizard.py` (42
passed).

Follow-up (2026-08-07, same day): asked to look for *any* code path (not just the originally
reported click sequence) that could produce `#266`'s symptom, a broader architectural sweep of
`_on_capabilities_ready`/`_on_discovery_complete`/`ConnectPage.set_devices()` found a real,
reproducible bug unrelated to the manual device-switch flow originally investigated (fixed, see
`#266`'s row) -- but a second follow-up the same day, given a much more precise repro description
from the user (exact prior state, and the user's own correct hypothesis that an earlier flow-type
change was implicated), located the actual confirmed root cause: `_on_device_item_selected`
(Filters step's Device-panel preset selection) changes `flow_type` with no invalidation at all,
unlike every other flow-changing handler. Both fixes are now in place; `#266` is `FIXED`.

Third follow-up (2026-08-07, still same day): asked whether other RoomFit/PEQ-specific steps
(`NAME_PROFILE` named directly) could hit the same bug class, and for a consolidated fix rather
than another per-call-site patch. Confirmed `NAME_PROFILE` is vulnerable via the identical
mechanism, and found a third, still-open instance (`_on_capabilities_ready`'s `PEQ_ONLY` branch).
Consolidated the invalidation into `WizardController.set_flow_type()` itself so no future call
site can forget it. Also added `#267`, a separate user-raised UX request (hide the wizard
breadcrumb entirely -- not just dim it -- while viewing the three sidebar destinations that have
no entry point back into the wizard, reclaiming that row's height for the destination view).

| 258 | **User-reported (manual QA):** the Filters step's "Device" and "Local Library" `QListWidget`s had no visible selected-row highlight matching the rest of the app (REW API picker, Presets on Device, My Saved Presets) -- those three all set the `selectableList` QSS class (`fluent_light.qss`/`fluent_dark.qss`'s `QListWidget[class="selectableList"]::item:selected` rule), but the two Filters-page lists were built as plain `QListWidget()` with no class property, so Qt's default (near-invisible) selection styling applied instead. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | Added `setProperty("class", "selectableList")` to `FiltersPage._device_list`/`_local_list` (`filters_page.py`), matching `RewPullView`/`PresetsDeviceView`/`MyPresetsView`'s existing construction. | No new automated test -- styling-only (QSS class presence), covered by the same convention as the other three lists; no existing test asserts the class property on those either. Verified by reading the QSS rule and confirming the property is now set identically. |
| 259 | **User-reported (manual QA):** RoomFit's "Push" page in Dry Run mode always named a source in its summary (e.g. "20 bands validated for wifi, bluetooth, auxIn (L/R)"), even on the very first RoomFit run of a session where those sources were never chosen for RoomFit at all -- `state.selected_sources` was carrying values left over from an earlier PEQ-flow visit (or the `DEFAULT_SOURCE` fallback), and RoomFit applies globally, not per-source (CLAUDE.md domain rules), so naming a source here was actively misleading. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | `MainWindow._on_push_requested()`'s dry-run branch now checks `flow_type == FlowType.ROOMFIT` and builds a source-free summary ("Dry run complete: N bands validated (Stereo). No changes were written to device.") for that case; the PEQ branch is unchanged. | `test_roomfit_dry_run_summary_omits_stale_source` (test_smoke_regression_operations.py, new, `TestPushWriteOperations`) -- seeds `state.selected_sources` with stale PEQ-flow values, drives a RoomFit dry run, and asserts none of them appear in the summary. |
| 260 | **User-raised question, not a clear-cut bug:** "Presets on Device" and the Filters step's "Device" list scope their "Custom"/"(active)" state to `state.primary_source` (the Source-step selection), not the device's currently-playing input -- already documented in code as deliberate (PEQ is per-source; RoomFit is global, CLAUDE.md), but invisible in the UI itself, and "Presets on Device" is reachable directly from the sidebar before the Source step has ever been visited (`state.primary_source` silently defaults to `DEFAULT_SOURCE`/"wifi" in that case). | FIXED (clarified in UI, not changed in behavior -- the underlying per-source scoping is correct/intended per CLAUDE.md's domain rules) | YES | (branch `claude/qa-test-issues-mo9go6`) | `peq_presets_ready`'s signal/handler chain (`PrimaryWorkflowManager` -> `MainWindow._on_peq_presets_ready` -> both views' `set_peq_presets()`) now carries the source name the PEQ list/active state was actually fetched for. Both `PresetsDeviceView` (caption under the "PEQ Presets" header) and `FiltersPage` (caption above the merged Device list) show "Showing live PEQ status for source: {source}" whenever that source is known, making the per-source scope explicit instead of silent. RoomFit needs no caption (it's global). | Signal/plumbing change covered indirectly by the full `test_smoke_regression_operations.py`/`test_gui_views.py`/`test_gui_pages.py` suites (all passing with the new trailing signal arg); no dedicated caption-visibility test added (label text/visibility, same tier as other caption/label assertions already untested elsewhere in these views). |
| 261 | **User-reported (manual QA, contradicts `docs/qa_signoff.md` Test 12a's stated expectation):** setting `supports_profile_enumeration: false` on a device (WiiM Mini capability-file override) produced an *empty* preset list in "Presets on Device" and "No presets found on device" in the Filters step, instead of Test 12a's documented expectation that "Custom" is the only PEQ row shown. Root cause: `build_peq_rows()` only ever prepended the synthetic "Custom" row when `active_name == ""` (the hardware-confirmed "live config matches no saved preset" signal) -- but on an enumeration-unsupported device, `items` is *always* empty (there's no enumerated list to compare against at all), so a device whose live PEQ config already had a real, non-empty device-assigned Name (e.g. set via WiiM Home before this app was ever used) produced zero rows and no visible PEQ information whatsoever, silently violating Test 12a. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | `build_peq_rows()` (`presets_device_view.py`) gained an `enumeration_supported: bool = True` parameter. When `False`, the live config always gets a row (using the device's real reported Name if it has one, `"Custom"` if not) rather than only when `active_name == ""` -- there's no enumerated list to compare it against in the first place on such a device. `build_custom_peq_item()` gained an optional `name` override for this case (still routed `is_custom=True`, since without enumeration this app can't confirm the name corresponds to a real, separately-manageable saved preset). Threaded end to end: `PrimaryWorkflowManager.peq_presets_ready` signal gained a trailing `bool` (`DeviceCapabilities.supports_profile_enumeration`); `PresetsDeviceView`/`FiltersPage.set_peq_presets()` gained the same parameter, stored and passed to `build_peq_rows()`. | `test_no_enumeration_with_named_live_config_still_shows_a_row` (test_smoke_regression_operations.py, new, `TestPresets`) -- a device with `supports_profile_enumeration=False` and a real `Name="Movie Night"` live config now shows exactly one row (not zero) in both `PresetsDeviceView` and `FiltersPage`'s merged list. Existing `test_no_enumeration_but_peq_supported_shows_custom_row`/`test_no_enumeration_and_read_fails_emits_unavailable` (the `active_name == ""` and read-failure cases) still pass unchanged. |
| 262 | **User-reported (manual QA):** named PEQ presets/RoomFit profiles containing non-English-alphabet letters (e.g. Slavic/Cyrillic) were stripped by this app's client-side name validation, even though WiiM Home and the device itself accept them -- `src/utils/device_name.py`'s allowed-character set was ASCII-only (`[A-Za-z0-9_\- ]`), an unstated assumption layered on top of the 2026-07-12 corrections.md row, which only ever confirmed the two *extra* punctuation characters (dash, space), never that "letters" meant ASCII specifically. **Follow-up:** `DEVICE_NAME_RULE_TEXT`'s updated wording was never propagated to the two help docs that quote it verbatim (`managing_presets.md`, `using_roomfit.md` -- both still said plain "letters"), leaving `test_help_content_integrity.py::test_help_device_name_rule_matches_constant` failing since this fix's original commit; caught incidentally while running that file for an unrelated change. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | Widened `_ALLOWED_NAME_CHARS` to Python's Unicode-aware `\w` (matches any Unicode letter/digit plus underscore) combined with the confirmed dash/space: `[^\w\- ]`. `DEVICE_NAME_RULE_TEXT` updated to "letters (any language), numbers, spaces, - and _". Logged as a `docs/corrections.md` row per CLAUDE.md's domain rules (assumption revised) -- still not independently hardware-verified by this project's own testing; the 2026-07-12 row's "OPEN ISSUE" caveat carries forward. Follow-up: both help docs' naming-rule bullet updated to say "letters (any language)", matching `DEVICE_NAME_RULE_TEXT` exactly. | `test_allows_non_latin_alphabet_letters`/`test_false_for_non_latin_alphabet_letters` (test_device_name.py, new) using a Cyrillic name. Follow-up: `test_help_content_integrity.py::test_help_device_name_rule_matches_constant` (pre-existing test, not new) now passes for both files; full file (32 tests) and `test_help_view.py` (35) reverified passing. |
| 263 | **User-raised question ("why does this not exist?"):** "My Saved Presets" (`MyPresetsView`) had no multi-select, unlike "Presets on Device" (`PresetsDeviceView`), which uses `ExtendedSelection` with full batch Export/Save/Copy/Delete support. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | Changed `_list_widget`'s selection mode to `ExtendedSelection`. Delete now batches over every selected preset (partial-failure-tolerant: deletes what it can, reports "Deleted X, Y failed" on a mixed result, matching `PresetsDeviceView`'s own batch-delete convention) -- `delete_requested` changed from `Signal(str)` to `Signal(list)`, and `MainWindow._on_profile_delete_requested` updated to match. Rename/Duplicate/Copy to Another Device stay single-item-only by design (Rename edits one inline name field, Duplicate makes one copy, and `copy_to_device_requested` is `Signal(object)` carrying a single `Profile`) -- their toolbar buttons are now explicitly gated on `selected_count == 1`, while Delete's is gated on `>= 1`. | `test_selection_mode_is_extended`/`test_multi_select_enables_delete_but_not_single_item_actions`/`test_delete_clicked_with_multi_select_emits_all_selected_names` (test_gui_views.py, new, `TestMyPresetsViewMultiSelect`); existing `test_delete_requested_signal` (both the toolbar and context-menu variants) and `test_issue38_my_presets_view_has_toolbar` (test_smoke_regression_operations.py) updated for the new list-based signal payload. |
| 264 | **User-reported (manual QA):** pressing Enter right after opening the "User Guide" window closed it; the "Diagnostics" window didn't react the same way to Enter, which seemed inconsistent. Root cause: `MainWindow` hosts both `HelpView` and `DiagnosticsPanel` inside real `QDialog`s (`_help_dialog`/`_diagnostics_dialog`), where `QPushButton.autoDefault` defaults to `True` -- Qt then silently auto-picks the first-created `autoDefault` button in the dialog as the Enter-key target, with no explicit opt-in from this app's code anywhere. `HelpView`'s "X" close button is built first (before the search/TOC controls) and the panel opens with initial keyboard focus already on it, so Enter closed it; `DiagnosticsPanel` has no close button, so its own (also un-opted-in) default button just did something less visibly alarming. Confirmed empirically, not just reasoned: reverting the fix and re-running a targeted repro script showed `close_btn.autoDefault()`/`isDefault()` both `True` and `close_requested` firing on a bare Enter keypress; with the fix, both `False` and nothing fires. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | `make_action_button()` (`action_button.py`, used by every action button across the app, including both panels') now calls `setAutoDefault(False)` on every button it builds. The three dialogs that *do* want an explicit Enter-triggered default button (`crash_dialog.py`, `unsaved_changes_dialog.py`, `warning_confirm_dialog.py`) already call `setDefault(True)` on their chosen button afterward, which re-enables the needed behavior regardless of the new default (`isDefault()` bypasses `autoDefault()` in Qt's own key-handling) -- unaffected. | `test_enter_on_default_focus_does_not_close_dialog` (test_help_view.py, new, `TestHelpViewSearch`) -- wraps a real `HelpView` in an actual `QDialog` (mirroring `MainWindow`'s real hosting, unlike the pre-existing `test_enter_does_not_close_dialog`, which only ever tested Enter inside the search field and never caught this) and asserts a bare Enter on the default-focused close button does not emit `close_requested`. |
| 265 | **User-raised question, surfaced a real gap:** "navigating back from Push to Filters, selecting a different filter and clicking Continue, subsequent steps don't get reset" -- asked which steps reset on a revisited-step change and why. Answer: by design (`WizardController.go_to_step()`'s docstring, `#246` Stage 2/lazy invalidation), browsing back never clears anything; only an actual *change* on the revisited step invalidates steps after it, detected per-step by whichever handler owns that step (Source/EQ_TYPE/Device-switch already did this: `_on_source_changed`/`_on_eq_type_selected`/`_on_device_selected` each compare the new selection against the previous confirmed one before calling `invalidate_after()`). Filters was the one step missing this: `_on_filters_accepted()` unconditionally called `advance()` with no change check at all, so re-confirming Filters with a genuinely different preset/file left Review/Push's checkmarks (and a stale reviewed-filters state) in place. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | `_on_filters_accepted()` now builds a `(current_filters, filters_l, filters_r, channel_mode)` signature and compares it against `MainWindow._last_confirmed_filters_signature` (a new field, since -- unlike Source's own state field -- `state.current_filters` is already overwritten by the producer, i.e. file import/device pull/preset load, well before Continue is ever clicked, so there's no "previous confirmed" value left to read back off wizard state itself; it has to be tracked separately). A changed signature calls `invalidate_after(WizardStep.FILTERS)` before `advance()`; an unchanged one (browse back, re-confirm the same selection) invalidates nothing, mirroring Source's own change-vs-no-change split. | `test_filters_reselection_invalidates_downstream_same_selection_does_not` (test_smoke_regression_wizard.py, new, `TestIssue57BackNavClearsCompletedSteps`) -- covers both halves: re-confirming the same filters keeps Review's checkmark, loading a different filter set and re-confirming clears it. |
| 266 | **User-reported (manual QA):** "after finishing a dry run in PEQ mode, then navigating back to Connect and choosing another device, 'Source' step remained checked (with all others unchecked)." Investigation happened in three passes. **Pass 1** (originally suspected trigger): a manual device switch through `_on_device_selected`'s `invalidate_after(WizardStep.CONNECT)` -> `steps_invalidated` -> `StepIndicator.sync()` chain -- could not reproduce it; `SOURCE` was correctly cleared in every manual-switch scenario tried, for both RoomFit-capable and PEQ_ONLY devices. **Pass 2** (asked to broaden the search beyond that one click sequence): found and fixed a real, independent bug in the discovery/probe pipeline -- `ConnectPage`'s background device scan (started on Connect entry/refresh) is a one-shot async task nothing cancels once the user picks a device from progressive results and moves on; if it completes *after* the wizard has advanced past Connect, `discovery_complete` -> `ConnectPage.set_devices()`'s single-device auto-select (Req 2.4) unconditionally re-emits `device_selected` for the already-connected device, `_on_device_selected`'s no-op-past-busy-check branch still launches a fresh probe, and `_on_capabilities_ready` used to unconditionally `advance()` whatever step was currently active. Worth fixing regardless, but a timing-dependent bug didn't match the user's precise, deterministic repro. **Pass 3** (given a much more specific description -- "had all but Push checked, switched device, and only Source stayed checked" -- and the user's own hypothesis that an earlier EQ-type change might leave stale data): found the actual, confirmed root cause. `_on_device_item_selected` (the Filters step's merged Device-list selection -- loading a RoomFit profile while in PEQ flow, or a PEQ preset while in RoomFit flow) calls `set_flow_type()` directly with **no invalidation at all**, unlike every other flow-changing handler (`_on_eq_type_selected`, `_on_device_selected`). `WizardStep.SOURCE` exists in PEQ's step sequence but not RoomFit's; `WizardController.invalidate_from()` only pops entries for steps that are part of the *current* sequence, so a stale `SOURCE` entry left by this call site is silently skipped by every subsequent invalidation while RoomFit is active (it's simply not iterated), then resurrected the moment something -- e.g. a later device switch, which always resets flow_type to PEQ -- makes SOURCE part of the sequence again. This exactly reproduces "all cleared except Source": PEQ session completes CONNECT/EQ_TYPE/SOURCE, user loads a RoomFit preset from the Device panel (silently orphaning the SOURCE entry), continues the RoomFit flow up to Push, then switches devices -- RoomFit's own steps (EQ_TYPE/FILTERS/REVIEW/NAME_PROFILE/PUSH) get correctly invalidated by the device-switch's `invalidate_after(CONNECT)` call (they're part of RoomFit's sequence), but the orphaned SOURCE entry isn't part of that sequence either, so it survives untouched until the switch's `set_flow_type(PEQ)` makes it visible again. **Pass 4** (user asked whether other steps -- specifically `NAME_PROFILE`, RoomFit-only -- could hit the same bug, and wanted a consolidated fix rather than another per-call-site patch): confirmed yes -- `NAME_PROFILE` is `SOURCE`'s exact mirror image (exists only in RoomFit's sequence), vulnerable via the identical mechanism. Auditing every `set_flow_type()` call site found a **third**, still-open instance: `_on_capabilities_ready`'s `set_flow_type(FlowType.PEQ_ONLY)` branch also had no invalidation, reachable via `_on_device_selected`'s intentional no-op branch (a manual re-click of the *already-connected* device while genuinely browsing at Connect -- deliberately skips invalidation/reset, "capabilities can change server-side") followed by a re-probe reporting different RoomFit support than the prior, still-completed session. Rather than patch a third call site, moved the invalidation into `WizardController.set_flow_type()` itself -- the single place a flow_type transition happens -- so no future call site can forget it again. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | **Pass 3** (Filters Device-panel selection), two fixes in `main_window.py`: `_on_capabilities_ready()` only calls `set_flow_type()`/`advance()` when `WizardController.current_step == WizardStep.CONNECT` (rest of the capability refresh still runs unconditionally); `_on_discovery_complete()` discards results arriving after the wizard has left Connect, so the redundant probe is never launched. **Pass 4** (consolidation), in `wizard_controller.py`: `set_flow_type()` now derives the invalidation point generically -- the first step where `steps_for_flow(old)` and `steps_for_flow(new)` diverge, walking from CONNECT -- and calls `invalidate_from()` on the *old* sequence from that point, before assigning the new flow_type. This one change protects every current call site (`_on_eq_type_selected`, `_on_device_item_selected`, `_on_capabilities_ready`'s `PEQ_ONLY` branch) and any future one uniformly; the three call sites' own hand-rolled `invalidate_after(WizardStep.EQ_TYPE)` calls were removed as now-redundant (consolidate, don't duplicate). `_on_device_selected`'s own `invalidate_after(WizardStep.CONNECT)` is deliberately kept -- it's a *device-identity* invalidation (must fire even when flow_type ends up unchanged, e.g. switching between two RoomFit-capable devices), a different concern from the *flow-type-transition* invalidation `set_flow_type()` now owns. | Pass 3: `test_late_capabilities_probe_does_not_advance_past_connect`/`test_late_capabilities_probe_still_refreshes_device_data`/`test_late_discovery_complete_does_not_reselect_device`/`test_discovery_complete_still_populates_connect_page_on_connect` (`TestIssue9LateProbeAfterLeavingConnect`, test_smoke_regression_wizard.py); `test_loading_roomfit_preset_while_in_peq_clears_stale_source`/`test_stale_source_does_not_resurface_after_later_device_switch` (`TestIssue9DeviceItemFlowSwitchStaleSource`, same file) -- the second directly reproduces `completed_steps == {CONNECT: ..., SOURCE: ...}` when reverted. Pass 4: `TestWizardControllerFlowTypeInvalidation` (test_wizard_controller.py, new, 8 tests) covers PEQ->RoomFit (SOURCE), RoomFit->PEQ (NAME_PROFILE, the user's specific question), both flows->PEQ_ONLY (EQ_TYPE), no-resurrection-after-switch-back, common-prefix preservation, the same-flow_type no-op, and both `steps_invalidated` emission/non-emission cases; `test_reprobe_losing_roomfit_support_clears_name_profile` (`TestIssue9ReprobeSameDeviceDifferentCapabilities`, test_smoke_regression_wizard.py) end-to-end reproduces the third latent instance through the real handler chain. All new tests verified empirically (temporarily reverted the relevant fix, confirmed failure, restored, confirmed pass) rather than just reasoned about. Full suite verification: `ruff check src/` clean, `mypy src/` clean (163 files) + `mypy src/translator src/models` clean (strict), `test_wizard_controller.py` (58 tests total, 9 new), `test_wizard_controller_pbt.py` (5, property-based, re-run since core `set_flow_type()` logic changed), `test_smoke_regression_wizard.py` (49), `test_smoke_regression_operations.py` (156), plus `test_gui_adapter_injection.py`/`test_gui_flow_type.py`/`test_gui_integration_flow.py`/`test_gui_operation_timeout.py`/`test_gui_pages.py`/`test_wizard_integration.py` (140) all pass. The Pass-1 `TestIssue9SourceCheckmarkAfterDeviceSwitch` tests are kept as regression coverage for the originally-suspected (but not actual) trigger. |
| 267 | **User-raised UX concern, not a functional bug:** the wizard's step-indicator breadcrumb (Connect/EQ Type/Source/Filters/Review/Push) stayed visible and clickable while viewing "Presets on Device," "My Saved Presets," and "Settings" -- three sidebar destinations that replace the wizard page on screen and have no entry point back into the wizard of their own (the user returns only via the sidebar's "Setup Wizard" entry). A still-visible breadcrumb over an unrelated page reads as "you're still in the wizard" and permanently wastes a fixed-height row those views could use instead -- confirmed by screenshot: "My Saved Presets" and "Settings" both had visibly more usable vertical space once the row was removed, with Settings in particular gaining room before its content needed to scroll. The breadcrumb was already partially addressed for this case (a `dimmed` muted-style treatment, `_sync_navigation_chrome`'s `_SIDEBAR_DESTINATION_KEYS` branch) but only ever de-emphasized it, never hid it. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | `_sync_navigation_chrome()` (`main_window.py`) now calls `self._step_indicator.setVisible(False)` for the three sidebar-destination page keys and `setVisible(True)` when returning to any wizard step, replacing the now-fully-superseded `set_dimmed(True)`/`set_dimmed(False)` calls (dimming a widget that's about to be hidden has zero observable effect, so keeping both would just be confusing dead code) -- `StepIndicator.set_dimmed()` itself is untouched/still available, only its two call sites here were removed. **Update (2026-08):** `set_dimmed()` was subsequently removed entirely in the dead-code pass that also removed `get_peq_enabled()`/`FilterTable.clear()` -- no caller ever reappeared, and no hidden consolidation need was found on re-verification, so the "keep as cheaper fallback" rationale above no longer applies. No layout restructuring needed: the step indicator's row is `Fixed`-height inside the content `QVBoxLayout` while the `QStackedWidget` below it has `stretch=1`, so hiding the row lets Qt's layout system reclaim that space automatically. Verified visually via screenshot (offscreen-rendered `MainWindow`, all three destinations plus the return-to-wizard case) in addition to the automated assertions. | `test_step_indicator_hidden_for_every_sidebar_destination` (test_smoke_fixed_issues.py, new, parametrized over `presets_device`/`my_presets`/`settings`) -- verified empirically to fail without the fix and pass with it (also caught the pre-existing `test_step_indicator_click_from_sidebar_destination_resets_sidebar_highlight`, updated from asserting `_dimmed` to asserting `isVisible()`, matching the new mechanism); `test_profile_recalled_highlights_review_and_resets_sidebar` updated the same way. Full suite: `ruff check src/` clean, `mypy src/` clean (163 files), `test_smoke_fixed_issues.py` (26), `test_gui_components.py`/`test_smoke_regression_wizard.py`/`test_gui_integration_flow.py`/`test_wizard_integration.py` (142), `test_smoke_regression_operations.py` (156) all pass. **Follow-up:** user caught, from the screenshots, that "My Saved Presets" specifically did *not* look like it had reclaimed the space -- its title sat noticeably lower than "Presets on Device"/"Settings", and its toolbar sat well above the window's bottom edge with a large gap after it. Root cause: `MyPresetsView` (`my_presets_view.py`) relies on its preset list's own `stretch=1` to claim the page's spare vertical space, but the empty state (list hidden, "No saved presets yet." label shown instead -- the exact case in the screenshot, since no presets existed) has *no* widget with an explicit stretch factor. Debug inspection (`content_layout.itemAt(i).geometry()`) showed Qt splitting the newly-larger leftover space roughly evenly between `_empty_label` and `_toolbar` (both plain `Preferred`-policy widgets) instead of leaving the toolbar pinned at its natural size -- a *pre-existing* latent bug (unrelated to the `#267` step-indicator change itself) that simply had too little leftover space to be visually obvious before that change freed up more of it. `PresetsDeviceView` never had this problem because its empty-state widget is deliberately given `Expanding`/`Expanding` size policy for exactly this reason (existing code, with its own explanatory comment) -- `MyPresetsView`'s title/empty-label/toolbar had no equivalent policy pinning. Fixed by applying the identical, already-established pattern: `title` and `_toolbar` pinned to `(Preferred, Fixed)` so neither ever absorbs leftover space, `_empty_label` given `(Preferred, Expanding)` so it alone claims it (matching `PresetsDeviceView`'s `_empty_widget`). Verified with a debug geometry dump before/after, then visually via a regenerated screenshot. New test: `test_toolbar_stays_at_bottom_with_empty_list` (test_gui_views.py, new, `TestMyPresetsViewToolbarLayout`) -- the existing two toolbar-position tests in that class only ever populated the list (`set_presets([_make_profile(...)])`), so the empty-state case had no coverage at all; verified empirically to fail without the fix (toolbar height changed from 140px to 273px as the view grew) and pass with it. `test_gui_views.py` (82 tests) reverified passing after the fix. |
| 268 | **Found via `/code-review ultra --fix`, not manual QA:** two independent bug classes in this session's `#266`/`#267` diff. (1) The Review/Push staleness fix added for `#266` (`_on_filters_accepted`'s "did the filter selection change since Review last saw it" check, invalidating downstream steps on a real change) only ran on the narrow warnings-acknowledgment path -- `_on_peq_ready` (device pull/file import, the common case) and `_on_profile_recalled` (Local Library) both called `wizard_controller.advance()` directly, bypassing it entirely, so browsing back to Filters and picking a different device preset/local preset/imported file (the overwhelming majority of real usage) left a stale Review/Push checkmark in place -- the exact bug class `#266`'s own fix was written to close, just left open on its two most-used entry points. (2) `MyPresetsView`'s single-item toolbar actions (Rename/Duplicate/Copy) used `QListWidget.currentItem()` instead of `selectedItems()`, which can point at a different item than the actual selection after a ctrl-click deselect under `ExtendedSelection`; separately, its right-click context menu's Delete action only ever emitted the single item under the cursor, silently dropping the rest of an existing multi-selection (Qt's right-click doesn't clear a prior selection the way left-click does), contradicting the toolbar Delete button's own batch behavior on the same selection. | FIXED | YES | (branch `claude/qa-test-issues-mo9go6`) | (1) `main_window.py`: extracted `_confirm_filters_selection()` from `_on_filters_accepted`'s inline signature-comparison logic and call it from `_on_peq_ready` (before its `advance()`) and `_on_profile_recalled` (before its `advance()`) as well, so every path that leaves FILTERS runs the same staleness check. (2) `my_presets_view.py`: added `_get_selected_item()` (reads `selectedItems()`, returns `None` unless exactly one is selected) and routed `_get_selected_profile()`/`_on_rename_clicked()` through it instead of `currentItem()`; `_build_context_menu()` now batches Delete over the full selection when the right-clicked item is part of a multi-selection (falling back to just that item otherwise), and disables Copy/Rename/Duplicate in that case to match the toolbar's single-item-only restriction on those three. | `test_on_peq_ready_invalidates_stale_review_on_filter_change`, `test_on_profile_recalled_invalidates_stale_review_on_filter_change` (test_smoke_regression_operations.py, new, `TestPushWriteOperations`); `test_context_menu_delete_batches_full_selection`, `test_context_menu_delete_single_item_when_not_multi_selected`, `test_ctrl_click_deselect_leaves_current_item_stale` (test_gui_views.py, new, `TestMyPresetsViewMultiSelect`) -- all five verified empirically to fail without their respective fix and pass with it. Existing suites reverified: `test_gui_views.py` (86), `test_smoke_regression_operations.py`, `test_smoke_regression_wizard.py` (49), `test_wizard_integration.py`/`test_gui_integration_flow.py`/`test_gui_filter_handlers.py` (46). `ruff check src/` and `mypy src/` both clean. **Follow-up:** PR #25's CI caught a real fallout the local targeted-suite runs missed -- `test_gui_integration_profile.py::TestProfileRecallInvalidatesStalePush::test_recall_does_not_clear_stale_push_completion` was a pre-existing test whose own docstring explicitly documented "a stale PUSH completion... is NOT cleared" on profile recall as a known, accepted gap in the lazy-invalidation model (written during an earlier #246 redesign, anticipating this exact fix). Fix (1) above closes precisely that gap, so the test's asserted behavior became wrong the moment `_on_profile_recalled` started calling `_confirm_filters_selection()`. Updated the test (renamed to `test_recall_of_different_filters_clears_stale_push_completion`) to assert `WizardStep.PUSH not in completed_steps`, and rewrote the class docstring to record that the gap it once documented is now closed by `#268`. `ruff`/`mypy` clean on the file; `test_gui_integration_profile.py` (6 tests) passes. |
| 269 | **User-reported (manual QA, app version 0.8.0):** in "My Saved Presets," "Copy to Another Device" stayed greyed out whenever more than one preset was selected, while the same action in "Presets on Device" worked fine under multi-select -- asked why the two views differed. Root cause: `#268` had deliberately restricted My Saved Presets' Copy (along with Rename/Duplicate) to exactly one selected item, since `copy_to_device_requested` only ever carried a single `Profile` and `MainWindow._on_local_preset_copy_to_device_requested()` asked once, per click, which write-mode (PEQ or RoomFit) that one Profile represented -- a locally saved `Profile` has no `preset_type` field, unlike a `PresetsDeviceView` row, which already knows its own type from the device's own PEQ/RoomFit listings. That asymmetry was real (not an oversight), but nothing prevented extending it to a batch: ask the PresetTypeDialog once for the whole selection (same one-prompt UX Presets on Device already uses for its own batch actions), then copy every selected profile under that answer. | FIXED | YES | (branch `claude/app-0-8-0-test-issues-hn7265`) | `my_presets_view.py`: `copy_to_device_requested` now carries `list[Profile]` instead of a single `Profile`; `_on_selection_changed()` enables Copy for `selected_count >= 1` (was `== 1`); `_on_copy_clicked()`/the context menu's Copy action both emit the full batch (right-click reuses the same batch-aware `selected` logic Delete already had). `main_window.py`: `_on_local_preset_copy_to_device_requested()` takes `list[Profile]`, asks `PresetTypeDialog` once for the whole batch, builds one `PresetItem` per profile for the activation-warning HTML, and passes a list of raw `(name, channel_mode, filters, filters_l, filters_r)` tuples through. `secondary_workflows.py`: `copy_local_profile_to_devices`/`_do_copy_local_profile_to_devices` (single-profile) became `copy_local_profiles_to_devices`/`_do_copy_local_profiles_to_devices` (batch) -- builds a `PEQSettings` per profile (a build failure for one profile no longer aborts the rest of the batch, matching `_do_copy_presets_batch_multi`'s per-item error handling), then reuses the existing shared `_write_preset_copies_to_devices` primitive for the actual per-device writes (that primitive already supported a multi-item `reads` list; only the local-profile side was artificially limited to one). The now-redundant `copy_local_profile_complete` signal (profile_name, n_devices, succeeded, failed) was retired in favor of the existing `copy_batch_complete` (n_items, n_devices, succeeded, failed), since the local-profile path's data shape is now identical to the device-to-device batch path's -- removed `MainWindow._on_copy_local_profile_complete()` as dead code. | `test_multi_select_enables_delete_and_copy_but_not_single_item_actions`, `test_copy_clicked_with_multi_select_emits_all_selected_profiles`, `test_context_menu_copy_batches_full_selection` (test_gui_views.py, `TestMyPresetsViewMultiSelect`); `test_batch_of_multiple_profiles_writes_all_and_aggregates_counts` (test_gui_integration_secondary.py, `TestCopyLocalProfileToDevicesValidation`) -- a 3-profile batch with one build-failure in the middle, asserting the two valid profiles still reach the write primitive and the failure is aggregated, not dropped; `test_local_preset_copy_batches_multiple_selected_profiles` (test_smoke_regression_operations.py) -- asserts `MainWindow` dispatches every selected profile's raw fields, not just the first. |
| 270 | **User-reported (live app):** all action buttons on the Review page were disabled, with no operation visibly in progress. Follow-up: the user then reported the same thing app-wide -- every action button disabled everywhere, even Continue, and connecting to a fresh device didn't unblock them. Root cause: `OperationFeedbackManager` is a single global instance whose one flat action-button list spans every page (Connect/Source/Filters/Review/NameProfile/Push/presets/settings). `AsyncBridge` allows a result handler for one operation to synchronously dispatch a second, unrelated operation before the first's own `operation_finished` is processed (e.g. `_on_capabilities_ready` dispatching `list_presets()`) -- `MainWindow._on_bridge_operation_finished` already discards the outer operation's now-stale finish signal via its token check, so only the inner operation's `finish_operation()` ever runs. `start_operation()` unconditionally re-snapshotted every button's `isEnabled()` on *every* call, including that inner one -- capturing "every button already disabled by the outer operation" as everyone's supposed prior state. `finish_operation()` then permanently restored the whole app's buttons to disabled, since no later operation's snapshot could ever recover the true value. SourcePage's Continue button was the sole survivor, since `_on_capabilities_ready` already calls `note_button_state_changed()` for it (`#250`'s fix) -- the only button with a compensating call. | FIXED | YES | `1e78075` | `operation_feedback.py`: `start_operation()` now only snapshots `_prior_enabled` on the outermost (not-yet-active) call, guarded by a `was_active` check -- a nested/re-entrant call disables buttons (idempotent, already disabled) but leaves the snapshot untouched. | `TestNestedOperationDoesNotCorruptSnapshot` (test_gui_operation_timeout.py, new) -- `test_nested_start_operation_does_not_overwrite_snapshot` reproduces the exact nested-dispatch sequence and asserts buttons return to enabled; `test_nested_start_operation_preserves_an_already_disabled_button` confirms a button genuinely disabled beforehand still stays disabled. Both verified empirically to fail on the pre-fix code and pass after. |
| 271 | **Found via `/code-review ultra --fix`** run against `#270`'s own fix (not manual QA): two related state-desync bugs in the same nested-dispatch pattern `#270` fixed, both missed because that fix only guarded the button snapshot, leaving `start_operation()`'s other mutable state (cancel timer/button, long-op/hard-timeout timers) unconditionally reset on every call. (1) A nested call that flips `cancellable` from `True` to `False` (e.g. a cancellable discovery scan synchronously dispatching a non-cancellable device write) left the outer operation's `_cancel_timer` running and its Cancel button, if already shown, visible -- the timer would later fire and show Cancel for an operation that's actually non-cancellable; clicking it correctly no-ops in `request_cancel()`, but nothing ever hides the button, so it sat stuck on screen for the rest of the operation. (2) `_long_op_timer`/`_timeout_timer` restarted on every nested `start_operation()` call, so the 30s hard-timeout "absolute safety net" silently pushed further out on each nested dispatch instead of measuring from the outermost operation's true start, defeating the one guarantee that timer exists to provide. | FIXED | YES | `3c4da70` | `operation_feedback.py`: cancellable state still reflects the innermost (actually running) operation, since that's what `AsyncBridge.request_cancel()` would cancel, but flipping to non-cancellable now also stops the cancel timer and hides any already-shown Cancel button. `_long_op_timer`/`_timeout_timer` now only (re)start on the outermost call, guarded by the same `was_active` check `#270` added for the button snapshot. | Three new tests in `TestNestedOperationDoesNotCorruptSnapshot` (test_gui_operation_timeout.py): `test_nested_non_cancellable_stops_an_outer_cancellable_timer`, `test_nested_non_cancellable_hides_an_already_shown_cancel_button`, `test_nested_start_operation_does_not_extend_the_hard_timeout` -- all three verified empirically to fail on the pre-fix code (the hard-timeout test needed a tight timing window, not just a generous `waitUntil`, to actually catch the restart) and pass after. |
| 272 | **User-reported (live app):** three related list-styling/UX inconsistencies. (1) No consistent visual style across the app's ~9 `QListWidget` instances -- font size, selection-highlight spacing (gap between adjoining rows under multi-select), and alternating row shading each varied independently list-to-list (matches the "Highlighted entry styling in 'Filters' page 'Device' and 'Local Library' tables doesn't match the rest of the app" gap waived in `qa_signoff.md`'s 07.08.2026 sign-off). (2) "Presets on Device" (`PresetsDeviceView`'s PEQ/RoomFit lists) had no right-click context menu, unlike "My Saved Presets." (3) In "My Saved Presets" and the Filters step's Local Library panel, the Stereo/L-R channel-mode label rendered as a button-like colored badge (misaligned row-to-row) and the band count showed only the Left channel's count for L/R presets, mislabeled as a flat total. | FIXED | YES | `06d4408` | (1) Added `style_selectable_list()` (`list_item_style.py`), the one place every list's class/alternating-rows/spacing convention is applied; every `QListWidget` construction site now calls it instead of setting its own subset by hand. Added `size_list_item()` so every plain-text row (not just the pre-existing custom-widget ones) gets the shared `LIST_ITEM_HEIGHT`. Added `alternate-background-color` to the `selectableList` QSS rule in both themes; added `LIST_ITEM_SPACING` (2px) to `constants.py`. (2) `PresetsDeviceView` gained `_show_context_menu()`/`_build_context_menu()`, mirroring `MyPresetsView`'s existing menu (Export/Save/Copy/Delete, batched over a multi-selection, Delete disabled when the synthetic "Custom" row is part of the batch), wired to both the PEQ and RoomFit lists' `customContextMenuRequested`. (3) Removed `_PresetItemWidget` (the badge/multi-label custom row widget) and its now-dead `_channel_mode_display()` helper entirely; added `preset_row_text()`/`build_local_preset_list_item()` (`list_item_style.py`) rendering a plain-text row identical in convention to the device-side lists' `build_preset_list_item()`, e.g. "MyPreset  [Stereo: 7 bands]" or "MyPreset  [L: 5 bands / R: 11 bands]" (now genuinely per-channel, not Left-only). `MyPresetsView`'s inline-rename flow simplified to blank/restore the item's own text instead of removing/rebuilding a child widget. Updated `docs/prd.md`, `qa_signoff.md`'s Test 13 procedure, and `managing_presets.md` (in-app User Guide) to match. | Tests: `TestPresetsDeviceViewContextMenu` (6 new tests, test_gui_views.py) -- action set/order, Custom-row Delete-disabled, single-vs-batch right-click targeting (both PEQ and RoomFit lists), no-op on empty position; `test_issue39_lr_profile_renders_lr_badge_not_stereo`/`test_issue39_stereo_profile_renders_stereo_badge` (test_smoke_regression_operations.py, updated in place -- now assert the plain row text instead of the removed `_mode_badge`/`_band_label` widgets, and the L/R case now asserts both channels' counts, not just Left's); `test_measurement_list_scrollable_when_window_too_short` (test_gui_views.py, window-height constant bumped to account for the now-uniform `LIST_ITEM_HEIGHT` row height). Full targeted suites (test_gui_views.py, test_gui_pages.py, test_help_view.py, test_gui_picker_dialogs.py, test_gui_components.py, test_smoke_regression_wizard.py, test_help_content_integrity.py, test_smoke_regression_operations.py -- 167 tests) and `ruff`/`mypy` (whole `src/`, plus the `translator`/`models` strict gate) all pass. |
| 273 | **Found via `/code-review ultra --fix`** run against `#272`'s own diff (not manual QA; 8 finder agents across correctness/reuse/simplification/efficiency/CLAUDE.md-convention/cross-file-tracing/altitude/line-by-line angles): two real, empirically-confirmed bugs plus a duplication finding corroborated independently by 4 of the 8 agents. (1) `MyPresetsView._populate_list()` called `QListWidget.clear()` before `_cancel_rename()`, so a repopulate (search-field typing, or an external `set_presets()`/`refresh()` call) firing while a rename was in progress touched a `QListWidgetItem` `clear()` had already destroyed -- `RuntimeError: Internal C++ object (PySide6.QtWidgets.QListWidgetItem) already deleted`, empirically reproduced by temporarily reverting the fix. (2) `RewPullView._list_floor_height()`'s "show a couple of rows" floor didn't account for the inter-row spacing `style_selectable_list()` (from `#272`) now applies via `setSpacing()` -- `QListView.setSpacing(s)` adds `s` px on *both* sides of every row (confirmed empirically: consecutive rows' tops are `row_height + 2*spacing` apart, and the first row itself starts `spacing` px in), so the true content height for `rows` rows is `rows*row_height + (2*rows-1)*spacing`, not `rows*row_height`; the old formula under-counted the floor and clipped the second row by a few pixels. (3) `PresetsDeviceView`'s new `_show_context_menu()`/`_build_context_menu()` reimplemented `MyPresetsView`'s existing selection-batching rule ("if the right-clicked item is part of a multi-selection, batch over the whole selection; else act on just the clicked item") and itemAt/None-check/exec scaffold near-verbatim instead of sharing it -- the new code's own docstring admitted it "matches"/"same rationale as" the pre-existing implementation while duplicating it anyway. Also fixed as a smaller cleanup: `Profile.band_counts()` (`src/models/profile.py`) lost its only production callers when `#272`'s `preset_row_text()` replaced the old badge-based rendering that used to call it, leaving it dead weight exercised only by its own tests. | FIXED | YES | `26e70ea` | (1) `_populate_list()` now calls `_cancel_rename()` before `.clear()`, not after. (2) `_list_floor_height()`'s formula corrected to `rows*row_height + (2*rows-1)*spacing + 2*frameWidth`. (3) Added `resolve_batch_targets()`/`show_list_context_menu()` (`list_item_style.py`), the shared batching-rule/scaffold both `MyPresetsView._build_context_menu()`/`_show_context_menu()` and `PresetsDeviceView`'s equivalents now call instead of each carrying their own copy; also collapsed `PresetsDeviceView`'s duplicated per-list context-menu wiring block (`setContextMenuPolicy`/`customContextMenuRequested.connect`, copy-pasted between `_build_peq_section`/`_build_roomfit_section`) into one `_wire_context_menu()` helper. Removed `Profile.band_counts()` and its `gain_matches` import; extracted `_end_rename()` in `MyPresetsView` for the two rename-restore call sites (`_on_rename_finished`/`_cancel_rename`) the same review pass flagged as a smaller duplication. Findings NOT acted on (deliberately, after evaluation): QMenu/QAction objects built per right-click with no explicit `deleteLater()` (an efficiency-agent finding) -- same pre-existing Qt idiom `MyPresetsView`'s context menu already used before `#272`, cleaned up automatically when the parenting view is destroyed, not a real leak for a page that lives for the app's session; redundant per-click selection recomputation and no scan-based test enforcing every `QListWidget` calls `style_selectable_list()` -- both read as speculative/preventive "ultra"-mode findings rather than confirmed defects, out of scope for this pass. | Tests: `test_repopulate_during_active_rename_does_not_crash` (test_gui_views.py, `TestMyPresetsViewRename`, new) -- empirically verified to fail with the pre-fix ordering (`RuntimeError`) and pass after; `test_list_floor_height_shows_full_min_visible_rows_without_clipping` (test_gui_views.py, `TestRewPullView`, new) -- asserts the last of `_MIN_VISIBLE_ROWS` rows' `visualItemRect().bottom()` stays inside the viewport at exactly the computed floor height, empirically verified to fail with the pre-fix formula (row clipped by ~5px) and pass after. Full suites reverified after all four fixes: test_gui_views.py (95), test_gui_pages.py/test_help_view.py/test_gui_picker_dialogs.py/test_gui_components.py/test_gui_integration_primary.py/test_gui_filter_handlers.py/test_gui_device_transform.py/test_models.py (459), test_smoke_regression_operations.py/test_smoke_regression_wizard.py/test_help_content_integrity.py/test_qss_parity.py (249) -- all pass. `ruff check src/`/`mypy src/` (whole tree) and the `translator`/`models` strict-mode gate all clean. |
| 274 | **Found via `/code-review`** posted as a GitHub review on PR #32 (not manual QA; 8 finder agents, 6 findings survived independent re-verification against checked-out source). (1) `resolve_batch_targets()` (`list_item_style.py`) read `item.data(UserRole)` with no None-guard, unlike `PresetsDeviceView`'s pre-existing `_get_all_selected_items()`, which explicitly guards it -- a `None` payload would silently drop from the toolbar batch path but crash the new context-menu path (`AttributeError: 'NoneType' object has no attribute 'is_custom'`), the two paths diverging in failure mode despite being meant to share identical batch semantics. (2) `NameProfilePage`'s existing-profiles list kept a pre-existing `setMaximumHeight(150)`, tuned for Qt's old ~20-24px default row height; once `#272`'s `style_selectable_list()`/`size_list_item()` applied there too, the new ~48px rows made it fit only ~3 rows instead of ~6-7, with no visible affordance change to signal the new scroll behavior. (3) `preset_row_text()` always pluralized "bands" (e.g. "R: 1 bands"), and `#272`'s own regression test asserted this exact wrong string, locking the bug into the suite instead of catching it. (4) `#236`'s dead-code removal (`626b1d4`) was incomplete: a dangling `# --- Warnings section ---` comment, a stale module docstring line, and -- more substantively -- `filters_accepted`'s only emitter (the removed "Continue with adjustments" button) was gone, but the `Signal`, `MainWindow`'s `.connect()`, and `_on_filters_accepted()` were all still in place with zero remaining `.emit()` sites; `_confirm_filters_selection()`'s docstring still described `_on_filters_accepted` as one of three live UI paths requiring its invalidation check, overstating how robust the safety net actually was. (5) `fluent_dark.qss`/`fluent_light.qss` each hardcoded the same `alternate-background-color` literal twice -- once on `QTableWidget`/`QTableView`, once on `#272`'s new `QListWidget[class="selectableList"]` rule -- instead of sharing it, so a future theme tweak could silently make lists and tables drift apart. (6) `Profile.tags` and `schema_migrator`'s `tags: []` backfill were left in place after the dead-code sweep (`626b1d4`) removed `ProfileRepository`'s `add_tag`/`remove_tag`/`get_by_tag` methods, schema-persisted on every saved/migrated profile with zero remaining read/write call sites, inviting a future contributor to assume tagging is a live feature. | FIXED | YES | `85ded77` | (1) `resolve_batch_targets()` now skips items with a `None` UserRole payload in both the multi-select and single-item branches, matching the toolbar path's guard; docstring updated to say so explicitly. (2) Added `_band_count_text()` (`list_item_style.py`) for correct singular/plural band counts; `preset_row_text()` now calls it for both the L/R and Stereo branches. (3) `NameProfilePage`'s cap is now computed from `LIST_ITEM_HEIGHT`/`LIST_ITEM_SPACING` for a fixed `_visible_rows = 4` instead of a stale hardcoded pixel value. (4) Removed the dangling comment, the stale "Supports inline validation warnings..." docstring line, the dead `filters_accepted` Signal, and `main_window.py`'s connect() to it; kept `_on_filters_accepted()` itself since ~11 wizard-integration tests call it directly as their generic "advance past FILTERS" seam (removing it would require a much larger, out-of-scope test-seam refactor), and corrected both its own docstring and `_confirm_filters_selection()`'s to state plainly that it has no live UI trigger anymore. **Update (2026-08):** per explicit follow-up review request, `_on_filters_accepted()` was removed entirely rather than kept as a test seam (`9f08291`) -- the ~11 test call sites now use a new `conftest.py` helper, `advance_past_filters(window)`, which inlines the same two-line body without reintroducing a production-code seam. `main_window.py`'s comments/docstrings and two other test files' stale references to the removed method were updated to match; 537 tests re-verified, `ruff`/`mypy` clean. (5) Extracted the shared `alternate-background-color` declaration into one `QTableWidget, QTableView, QListWidget[class="selectableList"]` combined-selector rule per theme file (QSS has no variables, so shared selectors are the idiomatic way to express "one value everywhere"), removing the duplicate from each original block. (6) Removed `Profile.tags` and the `tags` backfill from `schema_migrator._migrate_v0_to_v1()`; kept the `schema_version` bump itself. | Test: `test_smoke_regression_operations.py::test_issue39_lr_profile_renders_lr_badge_not_stereo` (expected string corrected from "R: 1 bands" to "R: 1 band"); `test_schema_migrator.py` (`tags` assertions removed from 3 tests, `test_version_0_preserves_existing_tags` removed entirely -- it tested only the removed backfill); `test_profile_repository.py::test_load_applies_migration` (`tags` assertion removed). No new tests added -- (1)/(4)/(5)/(6) are guard/cleanup fixes with no independently observable behavior change beyond what the existing suite already covers (a `None`-payload list item and a live `filters_accepted` emission were both already unreachable in practice); (2) has no existing test asserting visible-row count for this list (noted as a gap, not addressed here -- out of scope for a review-response fix). Full re-verification: 537 tests across every touched/affected suite (test_gui_views, test_gui_pages, test_smoke_regression_operations, test_schema_migrator, test_profile_repository, test_wizard_integration, test_smoke_regression_wizard, test_gui_integration_flow, test_gui_integration_profile, test_models) pass; `ruff check src/` and `mypy src/` (164 files) both clean; `mypy src/translator src/models` strict mode also clean. |
| 275 | **User-reported (live app):** "My Saved Presets" and the Filters step's Local Library panel tagged every preset/channel with 12 bands (the WiiM Pro Plus's `max_filters`), regardless of how many filters the preset actually used. Root cause: `preset_row_text()`'s `#272` rewrite counted raw `len(profile.filters/_l/_r)`. A `Profile` saved from a device read (`PrimaryWorkflowManager._do_preset_save()`, the common case) always carries exactly `capabilities.max_filters` slots -- `read_peq()`/`parse_wiim_band_array()` parse the device's full fixed-size `EQBand` array 1:1, and any slot the user hasn't configured comes back as a `type="OFF"` placeholder (mode `-1`) rather than being omitted, so the saved profile's band list is always full-width. This is the same class of gap the pre-`#272` badge rendering avoided via the now-removed `Profile.band_counts()` (`#273`), which counted non-zero-gain bands as "active" -- `#272` replaced it with a flat length count and lost that distinction, and since virtually every saved preset is device-read-padded, the flat count was wrong for nearly all of them, not just an edge case. | FIXED | YES | `192b542` | Added `_active_band_count()` (`list_item_style.py`), counting bands where `filter.type != "OFF"` (the authoritative disabled marker set by both `read_peq()`'s device-array parsing and REW file import, per `docs/wiim_api_notes.md`) rather than `band_counts()`'s old non-zero-gain heuristic, which could misclassify a real 0 dB filter as inactive. `preset_row_text()` now calls it for both the L/R and Stereo branches instead of `len()`. | Test: `test_issue275_band_count_excludes_off_padding` (test_smoke_regression_operations.py, new) -- builds L/R and Stereo profiles mixing real and `OFF`-type filters (including an all-`OFF` channel) and asserts the rendered row text counts only the real ones, empirically verified to fail with the pre-fix `len()`-based count and pass after. |
| 276 | **User-reported (live app):** the "Select Target Devices" dialog (`DevicePickerDialog`, shown by "Copy to Another Device" from both "Presets on Device" and "My Saved Presets") mixed two independent selection signals: a checkbox per row (the one that actually drives `selected_devices()`) plus Qt's native blue row-selection highlight/border-left from `style_selectable_list()` (`#272`) -- clicking a row's text (rather than the small checkbox glyph) turned it blue but changed nothing, reading as a broken/unresponsive control. | FIXED | YES | `3886d18` | Added `style_checkable_list()` (`list_item_style.py`) -- same alternating-row shading/spacing as `style_selectable_list()`, but sets a distinct `checkableList` QSS class with no `:selected` tint/border-left, and puts the list in `QAbstractItemView.SelectionMode.NoSelection` so there's no native highlight for a leftover style rule to fall back to. `DevicePickerDialog` now builds its list as a small `_CheckableListWidget(QListWidget)` subclass overriding `mousePressEvent` to toggle the clicked row's checkbox directly (via `itemAt()`) instead of relying on Qt's glyph-only hit test, regardless of where in the row the click lands. A plain `itemClicked`-based toggle was considered and rejected: that signal fires *after* Qt's own checkbox-glyph click has already flipped the state, so unconditionally toggling there would immediately flip a direct glyph click back to its original value. **Update (2026-08):** `/code-review` on PR #34 found 3 correctness issues in the first cut (empirically verified, not just theoretical, before fixing): (1) `mousePressEvent` had no `event.button()` guard, so right/middle-click also toggled the checkbox with no visual cue; (2) the early `return` after toggling skipped `super().mousePressEvent()` entirely, so `currentItem()` stayed `None` after a click and arrow-key navigation didn't continue from the clicked row; (3) reported as "`NoSelection` blocks Space-to-toggle" but empirically that specific claim was false (`QTest.keyClick(..., Qt.Key.Key_Space)` toggles identically under `NoSelection` and `SingleSelection`) -- the real cause was (2): with no current item, Space had nothing to act on. Fixed by guarding on `Qt.MouseButton.LeftButton` and calling `self.setCurrentItem(item)` after a toggle instead of an early return -- confirmed by testing that falling through to a plain `super().mousePressEvent()` call (the review's literal inline suggestion) reintroduces the original double-toggle bug on glyph clicks, so `setCurrentItem()` restores bookkeeping without re-running glyph hit-testing. Also deduplicated `checkableList`'s QSS block into `selectableList`'s via combined selectors (both theme files) and made `style_checkable_list()` delegate to `style_selectable_list()` instead of restating two of its four lines. | Tests (`test_gui_picker_dialogs.py`, new): `test_device_picker_list_has_no_selection_highlight` -- asserts the list carries the `checkableList` class and `NoSelection` mode, not `selectableList`; `test_device_picker_click_anywhere_in_row_toggles_checkbox` -- clicks the row's far-right edge (away from the checkbox glyph) via `qtbot.mouseClick` on the viewport and asserts `checkState()` toggles Unchecked -> Checked -> Unchecked, empirically verified to fail (no state change) against the pre-fix plain `QListWidget`; `test_device_picker_right_click_does_not_toggle_checkbox` -- asserts right/middle-click leave `checkState()` unchanged; `test_device_picker_click_sets_current_item_for_keyboard_nav` -- asserts `currentItem()` is the clicked item after a non-glyph click, empirically verified to fail (`currentItem()` was `None`) against the pre-fix version. |
| 277 | **User-reported (live app), two related gaps in the same "Select Target Devices" dialog found during `#276`'s manual follow-up:** (1) keyboard navigation (arrow keys move the current row, Space toggles its checkbox -- both already worked, per `#276`'s fix) gave no visible indication of which row was current -- `checkableList`'s QSS defined `::item:hover` for the mouse but no `::item:focus` rule for the keyboard-navigated current item, so Qt fell back to its unstyled default dotted focus rectangle instead of matching the mouse-hover background, making keyboard navigation look broken even though it functioned. (2) Devices were listed in raw `discovered_devices` order -- whatever order mDNS/subnet-scan discovery happened to return them in (first-to-respond-first), not alphabetically -- inconsistent with `ConnectPage`'s device list, which already sorts by `_device_sort_key()` (casefolded name). | FIXED | YES | `917dcc6` | (1) Added `QListWidget[class="checkableList"]::item:focus` (both theme files) with the same `background-color` as the existing `::item:hover` rule, plus `outline: none` to suppress Qt's default focus rectangle. `selectableList` needed no equivalent -- there, arrow-key movement also moves the actual selection, so its existing `::item:selected` rule already covers keyboard nav. (2) `DevicePickerDialog.__init__()` now sorts `self._devices` with `key=lambda d: d.name.casefold()`, mirroring `ConnectPage._device_sort_key()`'s convention so device order doesn't flip between the two screens a user sees in the same session. | Tests: `test_checkable_list_keyboard_focus_matches_mouse_hover` (`test_qss_parity.py`, new) -- extracts `background-color` from both `checkableList` rules in each theme file and asserts they match; `test_device_picker_lists_devices_alphabetically` (`test_gui_picker_dialogs.py`, new) -- passes devices in a deliberately non-alphabetical, mixed-case order and asserts the rendered row order is casefolded-alphabetical. **Update (2026-08):** `/code-review` on PR #35 found the sort key had no fallback for a blank device name, contradicting the PR's own claim of matching `ConnectPage`'s order -- `DeviceInfo.name` can genuinely be `""` (`subnet_scanner.py`/`zeroconf_discover.py` both default to blank rather than raising), so a blank-named device sorted first here (casefolds to `""`) but under "U" on `ConnectPage` (`_device_display_name()`'s `"Unknown Device"` fallback), the exact cross-screen order-flip the row above claims to prevent -- it also left the row label itself blank (`" (192.168.1.13)"`, no identifying text). Fixed by extracting the fallback into a new shared `device_display_name()` (`src/utils/device_identity.py`), used by both `ConnectPage._device_display_name()` (now delegates instead of reimplementing) and `DevicePickerDialog` (for both the sort key and the row label), so the two screens can't drift onto different fallback text again. Also fixed, from the same review: `test_qss_parity.py`'s new `_rule_property` helper used an unanchored `.index()` substring search (could silently match a future selector that's a literal prefix of the intended one) and duplicated `test_smoke_fixed_issues.py`'s near-identical local `_onboarding_title_color` helper -- both replaced by one shared, selector-anchored `extract_rule_property()` (`scripts/check_qss_parity.py`). Fix commit: `360913c`. | Tests: `test_device_picker_sorts_blank_named_device_as_unknown_device` (`test_gui_picker_dialogs.py`, new) -- a blank-named device among named ones, asserts it sorts under "U" and displays as "Unknown Device (ip)", empirically verified to fail (sorted first, blank label) against the pre-fix version. |

Updated 2026-08-14: added `#270`-`#271`, both a process-gap correction, not new investigation --
the underlying fixes had already landed in `1e78075`/`3c4da70` before the user asked why they
weren't logged here, per CLAUDE.md's "every hardware-detected GUI bug goes in this file, fix and
regression test update the same row in the same commit" rule. `#270` was reported live during the
session ("all action buttons on Review disabled", then "everywhere, even Continue, even a fresh
device connect doesn't unblock them") and traced to `OperationFeedbackManager`'s single global
button-snapshot getting corrupted by a nested/re-entrant `start_operation()` call. `#271` was found
immediately after by `/code-review ultra --fix` run against `#270`'s own fix, catching two sibling
desync bugs (`_cancel_timer`/button, `_long_op_timer`/`_timeout_timer`) in the same nested-dispatch
pattern that the narrower `#270` fix hadn't guarded.

Updated 2026-08-06: added `#252` (a `#251` design follow-up, not a reported bug) and `#253` (a
real, user-caught bug). `#252`: unified the Filters step's merged Device list and the sidebar's
"Presets on Device" list to render identically (shared `build_preset_list_item()`), and added a
synthetic "Custom" row to both -- surfacing the device's live/unnamed PEQ config (WiiM Home's own
term for it) as a selectable list entry, including working Export/Save/Copy via a new
`WiiMAdapter.read_preset_preview_or_live()` that skips the named-preset preview/restore dance
since the config is already live. `#253`: while reviewing that work, the user asked whether
multi-select Export/Save were "really fully e2e implemented" -- they weren't: both silently
processed only `items[0]` despite the toolbar enabling for a multi-select, unlike Copy/Delete
which already batched correctly. Fixed by giving Export/Save the same batch-loop treatment.

Updated 2026-08-05: added `#251` -- a design/UX redesign (not a reported bug) consolidating the
three independent ways filter data got into the wizard (Filters-step File/REW-API toggle, sidebar
"Pull from REW", both preset pages' "Load into Editor") into one Filters-step source dropdown,
removing `QuickSetupDialog` and the sidebar-shortcut jump-to-Review path as a consequence. See the
row for the full list of removed code and rewritten/deleted tests.

Updated 2026-07-12 (sixth pass): added `#235`-`#237`, found by a full `/code-review ultra --fix` pass (10 finder angles + 1-vote verification + a gap sweep) against the `#222`-`#234` diff, then fixed. `#235` (rename/duplicate bypassing device-name sanitization) was the highest-confidence finding, independently surfaced by two finder angles (wrapper/proxy correctness and reuse) since `#222` had already fixed the identical class of bug on a sibling code path (`NameProfilePage`) in the same diff; a third instance of the same gap (`_save_filters_to_presets()`'s auto-generated default name) surfaced later, not from the automated review, while cross-checking the in-app help docs against current behavior for accuracy -- added to the same row rather than as a new one, since it's the same fix applied at a third call site. `#237` (`FilterTable.clear()` not resetting `maximumHeight`) led to a live-debugging round with the user after a manual follow-up edit (`setMaximumHeight(-1)`) reintroduced a related bug -- Qt clamps negative `setMaximumHeight()` arguments to `0`, not "unbounded", confirmed empirically rather than assumed; the row documents both the original gap and this second-order pitfall. A fourth, user-reported issue surfaced in the same session (not from the automated review -- found by the user re-testing the live app after `#234`'s fix landed): `#234`'s own fix was incomplete, reintroducing its exact symptom through a different mechanism; see the correction on that row above. Also fixed but not logged as standalone rows: two literal `—`-as-plain-text artifacts left over in `filter_table.py` comments by `#220`'s incomplete em-dash cleanup (see the fifth-pass correction below) -- these were caught only by directly searching for the actual Unicode em-dash character across the touched files, which turned up zero hits, then searching for the *escape-sequence name typed as literal text* instead, which found both. A repo-wide scan (not scoped to this diff, done only to sanity-check the pattern) found roughly 375 genuine em-dash characters already in comments/docstrings across the pre-existing codebase, none introduced by this session -- CLAUDE.md's "ASCII hyphen only" rule is evidently not actually enforced by `ruff` for this pattern (`ruff check` passes clean on files containing them; RUF001-3 target Unicode homoglyphs/confusables, not em-dashes specifically) and appears to be the project's long-standing, unenforced prose style rather than a rule anyone has been fixing to. Out of scope for this pass -- flagging here rather than mass-editing ~375 unrelated lines. Three findings from the same review pass were investigated and deliberately left unfixed as out-of-scope (touch code untouched by this diff): `MainWindow._do_undo_multi_source()` re-restoring already-succeeded sources if the user clicks Undo again after a partial multi-source failure (`_push_page.mark_undo_complete` is only called when `failed == 0`, so the button stays enabled with no per-source completion tracking); `MainWindow._do_undo_roomfit()` re-reading the same backup JSON file `RoomFitSafeWrite.undo()` already parsed internally, an ordering dependency that only stays safe because `undo()` doesn't delete its own backup file (undocumented invariant); and a `rich_text` parameter on `WarningConfirmDialog`/`_confirm_action()` with no auto-detection or enforcement, unlike the `QMessageBox.question()` it replaced -- a footgun for the next caller who builds an HTML message and forgets the flag, currently harmless since the one call site that needs it has it. Full verification: targeted suite (`test_gui_components.py`, `test_gui_pages.py`, filters-page tests) passes, `ruff check`/`mypy` clean on all touched files.

Updated 2026-07-12 (fifth pass): added `#220`-`#221`, found by a full `/code-review ultra --fix` pass (10 finder angles + 1-vote verification + a gap sweep) against this session's entire uncommitted working-tree diff. Also fixed but not logged as standalone rows (code-quality cleanups, not independently user-facing GUI bugs): an ASCII-hyphen violation (em-dash) in a new `filter_table.py` comment (**correction, 2026-07-12:** this was not actually fixed -- the em-dash character had been replaced with the literal six-character escape-name text (backslash, u, 2, 0, 1, 4) typed out as plain comment text, rather than an ASCII `--`; since these are `#` comments, not string literals, Python never interprets that text as an escape there, so it just displayed as garbled backslash-u text, not a real fix. Two occurrences ("Gain [...] cell shows..." and "Q factor [...] same cell/tooltip swap...") were found still present and genuinely fixed to `--` in the pass logged below), and a defensive `logger.warning()` added to `PushPage.set_stage()` for an unrecognized stage string (currently unreachable -- every real caller passes a hardcoded literal -- but nothing at the type level guarantees that). Three findings were investigated and deliberately left unfixed: `WarningConfirmDialog` showing its `title` both as the window title and as the in-body warning heading (confirmed real and inconsistent with `DevicePickerDialog`/`PushConfirmation`'s convention of a distinct window title, but this same diff's own new `TestWarningConfirmDialog.test_displays_title_and_message` asserts the title appears in `windowTitle()` by design, so "fixing" it would contradict tested intent -- flagged for a follow-up design decision instead); `MainWindow._do_undo_roomfit()` re-reading and re-parsing the same backup JSON file that `RoomFitSafeWrite.undo()` already parsed internally to derive `was_new_profile` a second time (confirmed redundant I/O, but the clean fix -- adding `was_new_profile` to `WriteResult` -- touches `src/adapters/safe_write.py`, a file untouched by this diff, so it's out of scope for this pass); and `ConnectPage._device_sort_key()`/`_sorted_insert_index()` technically being non-Qt logic that CLAUDE.md's "would this need Qt to test?" rule would place in `utils/` (verified true but low-value -- two tiny single-caller private helpers with no existing `utils/` equivalent to reuse and no current test gap).

Updated 2026-07-12 (fourth pass, retroactive): added `#210`-`#219`. These were found and fixed in the same working-tree session as `#203`-`#209` (and, in `#211`/`#219`'s case, in the session immediately before it) but were never logged at the time, discovered only by auditing `git diff HEAD` (with `--ignore-cr-at-eol`, since most of the working tree's apparent diff was CRLF/LF noise from unrelated files, not real changes) against the current row list. Exact fix commits are unknown since none of this work is committed yet -- all show `(pending commit)`. Row numbering here is best-effort chronological reconstruction from task history, not independently timestamped. This gap is a process reminder: CLAUDE.md requires logging a GUI bug and updating its status in the *same commit* as the fix, which is easy to skip when a session runs through many small fixes back-to-back without committing in between -- log as you go, not retroactively.

Updated 2026-07-12 (third pass): added `#203`-`#209` from further manual use following the `#202` fix. `#206` (stepper-label eliding) is worth flagging as a methodology note: the initial research estimate (~950px minimum width needed) was wrong -- built from a static font-metrics calculation that assumed Qt would compress the step indicator down through normal layout minimum-size constraints, which it doesn't (`ElidingLabel` doesn't override `minimumSizeHint()`, so a plain layout never shrinks it far enough to elide). The real trigger only reproduced once the empirical test replicated `MainWindow`'s actual mechanism: `setMinimumSize()` on the top-level window overrides the layout's own larger natural minimum, letting children be compressed below their `minimumSizeHint()` in a way an isolated widget/layout never would be. `#208` (onboarding card clipping) is a similar case: the first fix attempt (`QSizePolicy.Minimum` for the card's height) was verified as *wrong* by the same empirical-measurement discipline before being replaced -- `Minimum` still carries Qt's `GrowFlag` and silently reintroduced the prior session's over-growth bug, caught by re-running the same offscreen measurement script across multiple parent sizes rather than trusting the size-policy reasoning alone. Both reinforce the existing lesson already logged for `#202`: don't trust code-only reasoning about Qt layout behavior -- render the widget and measure it. Full verification: targeted suite across all touched files (test_gui_pages.py, test_gui_dialogs.py, test_gui_components.py, test_gui_views.py, test_gui_filter_handlers.py) passes, `ruff check src/` clean, `mypy src/` clean (152 source files).

Updated 2026-07-12 (second pass): added `#202` -- user caught, immediately after the ultra-review pass below shipped, that the Review step's FilterTable was rendering as an unusable sliver rather than the intended 600px-wide table. Root cause was in the *same diff* the ultra review had just covered (the `FILTER_TABLE_MAX_WIDTH` wiring from earlier in this session) but the review's finder angles missed it, since none of them actually rendered the page -- a reminder that a code-only review can't substitute for looking at the thing. Fixed by replacing an `addWidget(..., alignment=...)` call with the stretch-sandwich idiom `page_layout.py` already documents for exactly this failure mode (smoke `#179`). Verified this time by actually instantiating `ReviewPage` under `QT_QPA_PLATFORM=offscreen` and reading back `_filter_table.width()` (600, matching the cap) before writing the regression test, not just by reasoning about the code.

Updated 2026-07-12: added `#200`-`#201`, both found via `/code-review ultra --fix` run against this session's uncommitted dialog-consolidation/Push-page/content-width diff (10 finder angles + a gap sweep, each candidate independently verified). `#200` (preview-warning skipped on one of two `_ensure_wizard_state_for_load` early-return paths) was flagged independently by 4 of the 10 finder angles, the highest-confidence finding of the pass. Also fixed in the same pass but not logged as standalone smoke-test rows (not user-facing GUI bugs, just code-quality cleanups on new code from the same diff): two ASCII-hyphen violations (em-dashes) in newly-added docstrings/comments, a duplicated "optional warning box" embedding block between `DevicePickerDialog`/`QuickSetupDialog` consolidated into a shared `add_optional_warning_box()` helper, a dead `_confirm_copy_activation()` method removed after its only production caller was rewired away (tests redirected to the still-live `_copy_activation_warning_html()`), and a stale `FilterTable` docstring (`~400px`) that no longer matched the wired-up `FILTER_TABLE_MAX_WIDTH=600` constant. Full re-verification after fixes: `ruff check src/` clean, `mypy src/` clean (151 files), targeted suite (dialogs/pages/components/wizard+operations regressions/push export) 375 passed.

Updated 2026-07-02: full regression-suite hardening pass. Audited every issue's cited regression test against the actual bug description and fix code (not just checking the test existed/passed). Found 35 issues where the test would not have caught the original bug reappearing, split across two failure modes: (a) **tests that never existed** despite the doc claiming coverage -- `#26`, `#40`, `#45`, `#47`, `#51`, `#58`, `#64`, `#69`, `#121`, `#123` (10 issues, all in test_smoke_regression_operations.py) -- all now have real tests written against the current production code, with row corrections above for `#58`/`#69` where the originally-intended function no longer exists; (b) **tests that existed but only checked shallow signals** (mock was called, signal exists, sizeHint() unchanged, constant has the right value) instead of the actual behavior the bug was about -- `#2`, `#7`, `#8`, `#15`, `#20`, `#29`, `#30`, `#31`, `#39`, `#46`, `#49`, `#53`, `#62`, `#63`, `#70`, `#72`, `#74`, `#85`, `#88`, `#91`/`#117`, `#96`, `#97`, `#99`, `#100`, `#109`, `#120`, `#156`, `#157` (25 issues across test_smoke_regression_wizard.py, test_smoke_regression_operations.py, test_smoke_high_level.py, test_rew_parser.py, test_smoke_fixed_issues.py, test_gui_components.py) -- all strengthened to exercise the real code path and assert the specific outcome the bug affected. One of these (`#109`) turned out not to be a test-only problem: the underlying layout-jitter bug was genuinely still present in `push_page.py` (bare `setVisible()`, no space reservation) despite the row saying FIXED -- see `#109`'s corrected row above for the real source fix applied. A second real production bug was found and fixed in passing (not from the original 35, surfaced while writing `#62`'s new undo test): `BackupManager.create_backup()` crashed with an unhandled `TypeError` (not the intended `BackupError`) when backing up L/R-mode settings with `bands_l`/`bands_r` as `None` rather than an empty list -- fixed in `src/repository/backup_manager.py` by normalizing with `or []` before the length check, with a dedicated regression test (`test_lr_mode_none_bands_l_raises_backup_error_not_typeerror` in test_backup_manager.py). Full verification: `pytest` across all touched test files passes (110+ in the operations/backup-manager files alone, 73 in high-level/rew_parser, 30 in fixed-issues, 46+34 in gui_components/gui_pages, 22 in wizard), `ruff check src/` clean, `mypy src/` clean (141 source files, 0 errors).

Updated 2026-06-29: re-verified every `Fix Commit` reference for `#129`-`#160` against actual commit diff content (not commit-message wording). Found and corrected two classes of error: (1) `#134`-`#144` all cited `b1557fc`, but the described functions/files (`RewPullView`, `page_layout.py`, `StepIndicator.set_dimmed()`, `SidebarNav.set_active_key()`, `_warm_up_stacked_pages()`, `_ensure_wizard_state_for_load()` move, the mypy annotation fix, etc.) were actually introduced one commit earlier in `30822db` ("Phase B.1/B.2"); `b1557fc` only contains later, correctly-attributed follow-up work (`#145`-`#148`, which were left unchanged) plus incidental call-site updates to functions `30822db` had already defined. Corrected all 11 rows to `30822db`. (2) `#149`-`#151` still showed `(pending commit)` though the work had since landed: filled in `fadff48` for `#149` (`RoomFitSafeWrite`, `verify_bands`/`compare_band_lists`, `delete_roomfit_profile` all confirmed there) and `4373b75` for `#150` and `#151` (`supported_filter_types` plumbing and `PushedFiltersDialog` respectively, both confirmed there). Rows `#152`-`#160` were already correct on disk (the committed `HEAD` copy still says `(pending commit)` for some of these but the on-disk working copy had already been hand-corrected in an uncommitted edit prior to this audit) and needed no changes.

Updated 2026-06-28 (third pass): added `#148` — clicking a step-indicator pill while viewing a sidebar destination reproduced the same sidebar/step-pill double-highlight bug as `#138`/`#142`/`#144`/`#147`, on yet another path. Per explicit request, this was fixed architecturally instead of with one more individual patch: replaced every hand-maintained `sidebar_nav.set_active_key()`/`step_indicator.set_dimmed()`/`set_current()` call site with a single `_sync_navigation_chrome()` handler wired to `QStackedWidget.currentChanged`, which every navigation path already funnels through.

Updated 2026-06-28 (second pass): added issues `#145`-`#147` from further manual testing — Presets on Device empty-state layout/icon consistency (`#145`), a generalization of `#141`'s window-resync fix beyond the one-time startup warm-up (`#146`), and a correction + fix for `#144`, which had not actually covered the "My Saved Presets" Load entry point (`#147`). `#144`'s row text was corrected in place to flag the inaccuracy rather than silently editing history. None of this session's changes are committed yet.

Updated 2026-06-28: added issues `#134`-`#144` from this session's "Pull from REW" embedded-screen conversion, visual-consistency pass (alignment, titles, FiltersPage layout), window-scaling/first-navigation fix, step-indicator dimming work, and a follow-up fix (`#144`) for a missing step-indicator highlight on Review when arriving via any sidebar-load path. None of this session's changes are committed yet (`Fix Commit` shows `(pending)` for all of them, to be filled in once committed). `#119` was annotated to cross-reference `#141`'s related-but-distinct root cause rather than being marked fixed, since the two have different (and not both confirmed-identical) repro triggers.

Updated 2026-06-27: added issues `#129`-`#132` from manual smoke testing, each with a direct regression test except `#130` (dead-code removal, no behavior to test).

Updated 2026-06-26 to improve traceability between issue rows and automated tests.

- Exact test-reference names were aligned for issues `#2`, `#9`, `#10`, `#11`, `#12`, `#13`, `#34`, `#38`, `#48`, `#50`, `#62`, `#69`, `#74`, `#79`, `#85`, and `#92`.
- Issues `#103`, `#104`, and `#105` were normalized from `Test = YES` to `Test = N/A` because the tracker already described them as code-cleanup or non-isolated fixes without a meaningful standalone automation target.
- Issue `#109` was temporarily normalized to `Test = NO` during the audit; it is now covered by a direct component test in `test_gui_components.py`.
- Additional behavior-strengthening pass completed for issues `#23`, `#25`, `#27`, `#38`, `#48`, `#50`, and `#60`, replacing shallow structural checks with direct behavioral assertions.

### Recommended Follow-up Tests

- **Superseded by the 2026-07-02 hardening pass above.** The prior note here ("no new tests required", "existing lower-level coverage is already adequate" for `#88`/`#96`/`#97`/`#99`/`#120`) was based on the tests existing and passing, not on verifying they'd actually catch the described bug reappearing -- the 2026-07-02 audit found several of exactly those issues (`#88`, `#96`, `#97`, `#99`, `#120`) had tests that re-implemented the fix logic inline instead of calling real production code, so they were rewritten rather than left as "optional." No further action needed from this note; see the 2026-07-02 entry for what changed.
