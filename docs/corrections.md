# Assumption Corrections Ledger

*Whenever an assumption from the specifications fails during implementation, log it here. This file acts as the project memory.*

| Date | Assumption | Failure / Observation | Root Cause | Implemented Solution |
| --- | --- | --- | --- | --- |
| 2026-06-12 | All WiiM devices have `project` field starting with `WiiM_`. | WiiM Mini reports `project: "Muzo_Mini"` — a legacy/OEM name. | WiiM Mini uses older firmware branding from before the WiiM rebrand. | Added `Muzo_Mini` to `WIIM_PROJECT_PREFIXES` in `capability_prober.py`. |
| 2026-06-12 | mDNS TXT records include DeviceName, project, and Release fields. | TXT records on tested devices are empty for these fields. | WiiM mDNS services don't populate TXT record metadata. | Added `_enrich_device()` in `discovery_module.py` to call `getStatusEx` after mDNS resolves IPs. |
| 2026-06-12 | Source names in `EQGetLV2SourceBandEx` are case-insensitive. | Source names are CASE-SENSITIVE. `hdmi` (lowercase) returns the L/R channel-mode slot; `HDMI` (uppercase) returns the Stereo slot. Same source, different case = different PEQ data. | The WiiM API uses case to distinguish channel-mode storage slots for the same physical input. | Document in API notes. Users must use the exact case the WiiM app uses (typically uppercase for physical inputs: HDMI, Ethernet; lowercase for logical: wifi, bluetooth, line-in, optical). |
| 2026-06-12 | WiiM devices have 10 PEQ bands (letters a-j). | WiiM Amp Ultra (firmware 20260409) has 12 bands (letters a-l). | Newer firmware expanded the band count from 10 to 12. | Updated `_BAND_LETTERS` to `"abcdefghijkl"`. The `max_filters` capability should be probed dynamically rather than hardcoded. |
| 2026-06-12 | WiiM PEQ mode values are: -1=OFF, 0=LS, 1=PEAK, 2=HS. | Additional modes exist: 3=LP (Low-Pass), 5=HP (High-Pass). | Newer firmware added LP/HP filter types to the PEQ engine. | Added modes 3 and 5 to `_MODE_MAP`. Added "LP" and "HP" to `FilterType` literal. |
| 2026-06-12 | Source enumeration possible via API. | WiiM API returns valid PEQ data for ANY source name (even non-existent physical inputs). No API endpoint exists to enumerate actual physical inputs. `getStatusEx` has no `InputList` field on tested devices. | The PEQ engine has internal slots for all possible sources regardless of hardware. | OPEN ISSUE: Need alternative approach to source discovery. Current workaround: probe canonical names and show all. User must know their device's physical inputs. |
| 2026-06-14 | RoomFit uses dedicated commands: `getRoomFitStatus`, `getRoomFitBands`, `setRoomFitBands`. | These commands do not exist. RoomFit uses the standard LV2 PEQ command family (`EQGetLV2SourceBandEx`, `EQSetLV2SourceBand`, `EQv2GetNewList`) with `"EQLevel": 2` added to the JSON payload. Profile CRUD also uses standard commands (`EQSourceSave`, `EQv2Delete`, `EQv2SourceLoad`) with `EQLevel: 2`. | RoomFit is not a separate subsystem — it's a second EQ level within the existing LV2 plugin architecture. The original assumptions were based on speculative community docs. | Code needs updating: `capability_prober._probe_roomfit()` and `wiim_adapter.read_roomfit()`/`write_roomfit()` must use the real commands. Tests need fixture updates. Documented in `wiim_api_notes.md`. |
| 2026-06-14 | RoomFit `EQGetLV2SourceBandEx` + `EQLevel: 2` returns the DSP-active profile's bands (same as PEQ reads). | It returns the API "working buffer" — which is the last profile loaded via `EQv2SourceLoad`, NOT the DSP-active state. Without a prior load, returns a flat default template regardless of what the app shows as active. Buffer is device-global, persistent across connections and reboots (stored in flash). Not cleared by profile deletion, profile switching, toggling RoomFit, or device reboot. Only overwritten when a profile is actively edited/calibrated via the WiiM app or loaded via API. | RoomFit has three layers: profile storage, API working buffer, and DSP-active state. PEQ (EQLevel 1) conflates buffer and DSP; RoomFit (EQLevel 2) separates them. | Read workflow must first load the target profile via `EQv2SourceLoad` before reading bands. Buffer is harmless to WiiM app (app ignores it for display/playback). Documented in `wiim_api_notes.md`. |
| 2026-06-14 | RoomFit direct writes (`EQSetLV2SourceBand` + `EQLevel: 2`) apply changes to the DSP immediately (like PEQ writes). | Writes only modify the API working buffer. Must be followed by `EQSourceSave` to persist. Saving to the ACTIVE profile name deactivates RoomFit; saving to a NEW profile name does NOT deactivate. Buffer retains data after save (not cleared). Delete of non-active profiles also does not deactivate. | RoomFit is profile-based. The DSP activation mechanism is internal to the WiiM app. Overwriting the active profile triggers a reload/deactivation cycle; creating a new profile is non-disruptive. | Write workflow: load → modify buffer → save. If overwriting active profile: warn user about deactivation. If saving as new name: no disruption. Level 4 confirmed with these semantics. |
| 2026-06-14 | WiiM Mini (no RoomFit hardware) will reject RoomFit commands or return "unknown command". | WiiM Mini silently accepts ALL RoomFit commands (`EQv2GetNewList`+EQLevel:2, `EQGetLV2SourceBandEx`+EQLevel:2, `EQSourceSave`+EQLevel:2, `EQv2Delete`+EQLevel:2) with HTTP 200 and valid-looking responses. The probe reaches `roomfit_level = 4` even though the device has no RoomFit capability. Band reads return data with `EQStat: Off` but the response structure is valid. Profile save/delete return "OK". | The WiiM Mini's API never rejects unknown EQLevel values — it just processes the command against an inactive/empty subsystem. There is no API-level distinction between "device has RoomFit but it's off" and "device doesn't have RoomFit at all." | OPEN ISSUE: Currently the Mini reports `roomfit_level = 4`. This is harmless in practice (RoomFit reads return empty/off data, writes persist to an unused profile store), but is semantically incorrect. Possible fix: after `EQSourceSave`, verify the profile appears in `EQv2GetNewList` (round-trip check). Low priority since Mini RoomFit operations are no-ops. The round-trip-verify suggestion is now implemented for real RoomFit hardware -- see the 2026-06-29 row below; `RoomFitSafeWrite` reads back the saved profile and verifies bands after every write, which doubles as the round-trip check this row speculated about. |
| 2026-06-29 | `wiim_adapter.delete_peq_profile()` could be reused as-is for deleting a RoomFit profile, since both are `EQv2Delete` calls against the same plugin. | `delete_peq_profile()` omits `EQLevel` entirely. Per `docs/wiim_api_notes.md`'s RoomFit Profile CRUD table, deleting an RC (RoomFit) profile requires `"EQLevel": 2` in the payload -- without it the command targets the PEQ-profile namespace, not RoomFit's. No `delete_roomfit_profile()` existed at all prior to this fix, so RoomFit's rollback-by-delete path (a brand-new profile that fails write verification) had no way to remove the bad profile. | PEQ and RoomFit profile storage share the same `EQv2Delete` command but live in separate namespaces distinguished only by `EQLevel`, mirroring every other RoomFit command (`EQSourceSave`, `EQGetLV2SourceBandEx`, etc. all need the same parameter). This was never implemented because nothing in the app called profile-delete on the RoomFit path until write-verification (this row) needed it. | Added `delete_roomfit_profile(profile_name)` to `wiim_adapter.py`, sending `EQv2Delete` with `EQLevel: 2`. Used by the new `RoomFitSafeWrite` class (`safe_write.py`) as the rollback action when a brand-new RoomFit profile fails post-write verification. |
| 2026-06-14 | EQ writes to slave devices must be redirected to the master node. | PEQ and RoomFit filters are per-device, not per-group. Each device in a multiroom group has independent EQ. Writing to a slave's IP works correctly and modifies only that slave's EQ. There is no need for redirection. | The assumption came from pywiim/community docs about the legacy graphic EQ system, which IS group-wide. The LV2 PEQ engine is per-device. | Removed slave write guard from WiiMAdapter and SafeWrite. Removed rule 6 from steering. Requirement 17 rewritten to display-only. Task 41 cancelled. |
| 2026-06-15 | pip-audit will find no vulnerabilities. | pip-audit found 53 vulnerabilities in 15 system-level packages (certifi, cryptography, pip, setuptools, urllib3, etc.). None are in project direct dependencies (httpx, pydantic, PySide6, zeroconf). | System packages on Ubuntu 24.04 are behind latest PyPI versions. These are OS-managed and not bundled with the application. | No code changes needed. PyInstaller bundles only project dependencies which are clean. Documented in `docs/qa_signoff.md`. System admin should run `apt upgrade` to patch OS-level packages. |
| 2026-06-15 | RoomFit DSP-active state can be toggled via the HTTP API (e.g. `EQSourceOff`/`EQChangeSourceFX` with `EQLevel: 2`, or a dedicated `setRoomCorrection` command). | No API command toggles the RoomFit DSP-active state. Tested: `EQSourceOff`+EQLevel:2 (OK, no effect), `EQChangeSourceFX`+EQLevel:2 (OK, no effect), `EQSourceOff` without pluginURI (Failed), `setRoomCorrection:0` (OK, readable via `getRoomCorrection`, no effect on RoomFit), `MCURoomCorrection:0` (unknown command), `EQSetRoomFit:Off` (Failed), `EQSetLV2Stat`+EQLevel:2+EQStat:Off (Failed). RoomFit remained active in the WiiM app throughout all tests. | The RoomFit DSP activation/deactivation is handled exclusively by the WiiM app's internal logic. The HTTP API provides no mechanism to select or deselect a RoomFit profile for DSP processing. | OPEN ISSUE: GUI RoomFit toggle disabled with tooltip "RoomFit toggle not yet supported — use the WiiM Home app". `# TODO: RoomFit toggle` added to `wiim_adapter.py`. Revisit if future firmware or community research reveals a mechanism. |
| 2026-06-20 | CLI commands need full capability probing before every operation. | CLI commands like `peq-toggle` and `load-preset` run a full 8-call capability probe (~4-5s overhead) even though they don't use any capability flags. The adapter methods (`enable_peq`, `disable_peq`, `load_peq_profile`) make no capability checks internally. | The CLI was designed as a proof-of-concept using the same code path as the GUI. Each CLI invocation is a fresh process with no state, so the probe runs every time. The GUI caches capabilities after a single probe at device connection, so it's unaffected. | KNOWN LIMITATION: CLI-only latency issue (~4s overhead on simple operations). Not worth adding probe-level complexity since CLI is not user-facing. If CLI becomes a focus, introduce tiered probing (skip unneeded probe steps per command) or cache capabilities per-device in a local file. |
| 2026-06-27 | `max_filters` (counted from distinct band-letter prefixes in `EQGetLV2BandEx`) represents bands available in the device's *current* channel mode -- per-channel when probed in L/R mode, total when probed in Stereo mode -- not two independent per-channel limits. | OPEN ISSUE: not yet verified against real hardware in both channel modes. The new device-capability-file default-10-band cap (`src/models/device_capability_file.py::merge_into`) and `# ASSUMPTION:` comment in `capability_prober.py::_probe_peq` rely on this holding. | Unconfirmed -- the probe has always run with whatever channel mode the device happened to be in at connect time; no test has compared band counts probed in Stereo vs L/R on the same device. | `# TODO:` added at `capability_prober.py::_probe_peq`. Run `EQGetLV2BandEx` (Stereo) and `EQGetLV2SourceBandEx` (L/R) against the same device and compare band-letter counts to confirm or correct this row. See 2026-07-03 row below -- confirmed. |
| 2026-07-03 | (Confirms row 2026-06-27 above) `max_filters` represents bands available in the device's *current* channel mode -- per-channel when probed in L/R mode, total when probed in Stereo mode. | **Confirmed against real hardware.** On a 12-band-capable device, `EQBandL`/`EQBandR` each independently report 12 bands (letters a-l) when probed in L/R mode -- 24 addressable bands total, 12 per channel -- while `EQBand` reports 12 bands total when probed in Stereo mode. Not two independent 12-band-per-channel limits reported as one number; the letter count is genuinely per-channel in L/R mode. | N/A -- confirms the original hypothesis rather than correcting it. | The `# TODO:` at `capability_prober.py::_probe_peq` and the device-capability-file default-10-band cap (`src/models/device_capability_file.py::merge_into`) can now treat this as settled: `max_filters` counted from either probe already reflects the correct per-channel-in-L/R semantics, no code change needed. Documented in `docs/wiim_api_notes.md` Capability Nuances. |
| 2026-06-27 | A device whose API reports 12 PEQ bands (e.g. WiiM Amp Ultra, firmware 20260409+) exposes all 12 for use, so capping to 10 by default would under-use the hardware. | Confirmed by device owner: the official WiiM Home App itself only exposes/uses 10 of the 12 API-reported bands in its own PEQ UI -- the 11th/12th are API-reachable but not surfaced anywhere in WiiM's own app. | WiiM Home App's PEQ UI was built around a 10-band limit before the LV2 plugin's band count was later expanded in firmware; the extra bands were never wired into the app UI. | The bundled `device_capabilities.json` does not raise any model's `max_bands` ceiling above the generic default -- this app's 10-band default cap (Requirement 7) intentionally matches WiiM's own app rather than exposing the extra API bands. Documented in `docs/wiim_api_notes.md` Capability Nuances. |
| 2026-06-28 | REW "Notch" filters can be approximated on WiiM as a narrow PEAK filter, so `rew_parser._TYPE_MAP` mapped `Notch`/`Notch Q` -> `PEAK`. | REW notches imply >60 dB of attenuation at the target frequency; WiiM's PEQ gain range is capped at -12 dB, so a Notch can never be faithfully reproduced. Also found the two REW import paths disagreed: the text-file path (`_is_unsupported_filter`) already treated Notch as unsupported and skipped it, while the live REW-API path (`_classify_filter_type`) silently went through `_TYPE_MAP` and converted it to PEAK with *no* warning at all -- a real inconsistency, not just a missing feature. | The API-path classifier checks `_SKIP_TYPES` before falling through to `_TYPE_MAP`; Notch had only ever been added to the text-file path's dedicated regex checks, never to the shared skip set. | Removed `Notch`/`Notch Q` from `_TYPE_MAP`; added both to `_SKIP_TYPES` (with the text-path's prefix-matching loop explicitly excluding them so the more specific "Notch Q" label isn't shadowed). Both import paths now skip Notch consistently and surface it as a `SkippedBand` placeholder in the Review table (Phase B3) instead of silently converting or dropping it. |
| 2026-06-29 | Probing `_wiim._tcp.local.` then falling back to `_linkplay._tcp.local.` sequentially is a cheap, safe way to support both newer and older WiiM/LinkPlay devices, since at most one extra probe round-trip is added. | A device that only advertises `_linkplay._tcp.local.` (no `_wiim._tcp.local.` service at all) never gets a real chance to be found: `_probe_service` waited the *full* configured timeout on the non-existent `_wiim._tcp.local.` type before even starting the `_linkplay._tcp.local.` probe, and `DiscoveryModule`'s grace-period orchestration gives up on the combined mDNS task in less time than that -- so the app always fell through to the slower subnet scan, even though the device was instantly discoverable via mDNS by other tools (e.g. `avahi-browse`). | "Probe one type, fall back to the next" assumed both probes were cheap regardless of whether the first type's service exists on the network; in practice an absent service type costs a full timeout, not a fast no-op. | Rewrote `ZeroconfDiscover._probe_service` (now `_probe_services`) to browse `_wiim._tcp.local.` and `_linkplay._tcp.local.` concurrently via a single `AsyncServiceBrowser` call across one shared timeout window, so neither type can starve the other's chance to respond. |
| 2026-06-29 | Fixing `ZeroconfDiscover`'s sequential-double-timeout bug (row above) would resolve a device owner's report of mDNS discovery always falling back to the subnet scan. | The code fix was verified correct in isolation (confirmed against the device owner's real 4-device LAN from a WSL2 environment: mDNS found all 4 devices in 1.8-3.3s). But the device owner still saw the *packaged Windows `.exe`* take ~12s and fully exhaust the 254-host subnet scan with zero mDNS hits. `Get-NetFirewallRule` showed the app's installed firewall rule allows inbound only on the "Private" network profile and explicitly blocks it on "Public"; `Get-NetConnectionProfile` showed the active Wi-Fi was classified "Public" by Windows. Reclassifying it to "Private" dropped discovery to 2s. | Two independent root causes were compounding the same symptom: (1) the code bug fixed in the row above, confirmed via WSL2 (whose network stack/avahi-daemon isn't subject to this Windows-exe-specific firewall rule, so it never exhibited symptom 2); and (2) a Windows network-profile/firewall configuration issue that blocks inbound mDNS replies to the packaged app regardless of how correct its probe timing is. Fixing (1) alone could not have surfaced (2), since WSL2 testing structurally cannot reproduce a native-Windows-Firewall-rule symptom. | No code can fix (2) — it's a Windows OS/network setting, and the existing firewall rule's Public-network block is a reasonable default the app shouldn't silently override. Documented the network-profile requirement in `src/gui/assets/help/troubleshooting.md`, `README.md`, and `docs/architecture.md`; tightened `ConnectPage`'s empty-state hint text; added a `DiscoveryModule` log warning (`discovery_module.py`, Phase 3) that fires specifically when mDNS contributes zero devices but the subnet scan succeeds, naming the Public/Private network profile as the likely cause for anyone who shares a support bundle. |
| 2026-06-28 | (a) A missing `q` field on a live REW-API `FilterSetting` can safely default to `1.0` for any filter type. (b) REW's bare `LS`/`HS` shelf tokens (and `LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`) carry a Q value comparable to the dedicated `LS Q`/`HS Q` tokens, so `_TYPE_MAP` mapped all of them to `LS`/`HS` and substituted a flat `_BUTTERWORTH_Q` (0.707) for the ones with no explicit Q in REW's text export. | Real REW live-API output (`GET /measurements/:id/filters`) confirmed `q` is entirely absent -- not just unset -- for every type with no Q field in REW's own format: `LP`/`HP`/`LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1`. (a) was wrong for `LP`/`HP`: REW documents these as a *fixed* Q of 0.7071, so defaulting to 1.0 silently corrupted every live-API-sourced LP/HP band (a REW-text import of the same filter got the correct 0.707/0.7071, so the two import paths disagreed on Q for these types, same class of bug as the Notch row above). (b) was a more fundamental error, not just a missing default: REW documents `LS`/`HS` (bare), `LS 6dB`/`HS 6dB`, and `LS 12dB`/`HS 12dB` as using a shelf-*slope* parameter `S` (0.9 / 0.5 / 1.0 respectively) -- a different physical quantity from Q in the underlying biquad math, not an interchangeable number. Plugging a Butterworth *Q* value into a field REW defines as *S* is not a reasonable approximation, it's mixing up two different filter parameterizations. `LP1`/`HP1` (1st-order, no Q at all) were also being silently passed through on the live-API path via `_TYPE_MAP`, even though the text-file path already treated them as unsupported. | `_classify_filter_type`'s missing-`q` fallback (`setting.get("q", 1.0)`) was a single generic default applied regardless of filter type, never updated when LP/HP were added (row above, 2026-06-12). `LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1` were in `_TYPE_MAP` (supported on the live-API path) while the text-file path's `_is_unsupported_filter` had already special-cased most of them as unsupported -- the two paths were never reconciled. | `parse_filter_settings_with_rows` now defaults a missing `q` to `_BUTTERWORTH_Q` (renamed-precision 0.7071, matching REW's documented value) only for `canonical_type in ("LP", "HP")`; all other types keep the inert `1.0` fallback (never expected to fire, since REW always sends `q` for those). `LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1` moved from `_TYPE_MAP` to `_SKIP_TYPES`, so both import paths now skip them consistently and surface a `SkippedBand` placeholder instead of silently substituting the wrong parameter. `# TODO:` -- if a hardware-validated REW-S -> WiiM-Q conversion (or confirmation that WiiM's shelf implementation accepts `S` directly) is ever found, revisit `_SKIP_TYPES` to re-enable these six shelf variants; until then they're conservatively unsupported rather than guessed. |
| 2026-06-29 | `SafeWrite`/`RoomFitSafeWrite`'s write-verification could compare the `PEQSettings` passed into `execute()` directly against the device's read-back, since both represent "the same bands". | A device-owner repro of a real L/R push (gain 15 dB, gain -21 dB, and Q 29.255 on three different bands -- all legitimately out of WiiM's supported range) failed verification and rolled back every time, even though the device wrote and stored the data correctly. `generate_wiim_band_array()` clamps out-of-range gain (±12 dB) and Q (0.01-24.0) to the WiiM-supported range *at write time*, without mutating the caller's `CanonicalFilter` list -- so the device's read-back reflects the clamped value while `SafeWrite` was comparing against the original, pre-clamp value the user imported. Any band needing clamping was therefore guaranteed to "mismatch", regardless of band count. (A `_describe_first_mismatch()` diagnostic added the same day, to help debug this, also had its own minor bug: it returned an early "band count mismatch" message whenever intended/read-back lengths differed, even when the real `compare_band_lists()` truncates and would have passed -- this produced a misleading diagnostic that initially looked like the root cause but wasn't.) | `validate_filters_for_device()`'s Review-step clamping is intentionally display-only ("Does NOT modify gain/Q values — only flags them. Actual clamping is done by the WiiM generator at write time.", per its own docstring) -- the actual clamp only happens once, inside `generate_wiim_band_array()`, at the point of building the wire payload. Nothing downstream of that point (verification) accounted for it. | Added `clamp_filters_for_verification()` (`src/translator/wiim_generator.py`), mirroring `generate_wiim_band_array()`'s truncate+clamp logic without re-logging (the real write already logs each clamp). `safe_write.py` now builds its verification baseline through this helper (`_clamped_for_verification()`) at all four verify call sites in `SafeWrite` and `RoomFitSafeWrite`, comparing against what was *actually* written rather than what was originally imported. |
| 2026-06-29 | Adding `RoomFitSafeWrite`/clamp-aware verification to the main Push flow (the 2026-06-29 row above, and #11/#149) was assumed to cover "RoomFit writes" as a category, since it's the only RoomFit write path exercised by the wizard. | "Copy to Another Device" (`MainWindow._do_copy_preset_to_device()`) wrote RoomFit profiles via a bare `target_adapter.write_roomfit()` call with no `RoomFitSafeWrite` at all -- no backup, no verify, no rollback -- and its PEQ branch, despite already using `SafeWrite`, never checked the returned `WriteResult.success` before declaring victory. Separately, the CLI's `set-roomfit-filters` command called `adapter.write_roomfit()` directly, unlike its PEQ sibling command (which already used `SafeWrite`). Each of these write paths was added independently over time and never cross-checked against the others for the same safety guarantee. | "Every device write follows the 5-step safety protocol... No exceptions" (CLAUDE.md) was being satisfied per-feature as each feature was built, not audited across all write call sites as a category. Every place that calls `adapter.write_peq()`/`write_roomfit()` directly (not through `SafeWrite`/`RoomFitSafeWrite`) is a gap in this guarantee, and there's no test or lint rule that catches a new one being added. | Wrapped both bypassing call sites in `RoomFitSafeWrite`/checked their `WriteResult`, per the smoke_test_issues.md #154 row. No structural enforcement was added (e.g. a lint rule banning direct `adapter.write_peq`/`write_roomfit` calls outside `safe_write.py`) -- flagging this as a residual risk for the next person who adds a new write path. |
| 2026-06-29 | The grep-based audit behind the row above ("Copy to Another Device" + CLI `set-roomfit-filters`) was assumed to have covered every direct `write_roomfit()` call site once those two were fixed. | A third bypass existed in the same file: `MainWindow._do_undo_roomfit()` (restoring a RoomFit profile from a backup file via the sidebar's Undo action) also called `self._wiim_adapter.write_roomfit()` directly, with no `RoomFitSafeWrite`, no verify, no rollback. It was missed in the first audit despite being in the same file as one of the two fixes, because the audit was driven by a one-off grep rather than a repeatable check. It also discarded the backup's channel mode and per-channel (L/R) band lists (`_channel_mode`, `_filters_l`, `_filters_r` -- all underscore-prefixed and unused), so an L/R RoomFit backup would have silently restored as stereo-only bands even before this fix. | A manual one-off grep audit finds what you remember to search for, not everything matching the pattern -- exactly the gap the previous row's "residual risk" note anticipated. | Routed `_do_undo_roomfit()` through `RoomFitSafeWrite.execute()` using the channel mode and `filters_l`/`filters_r` already returned by `parse_backup_filters()` (previously discarded), reporting an error banner instead of "restored from backup" on verification failure. Closed the residual-risk gap for real this time: added `TestNoDirectWriteBypass::test_no_file_outside_safe_write_calls_adapter_write_directly` (`src/tests/test_safe_write.py`), which scans every `.py` file under `src/` for a direct `.write_peq(`/`.write_roomfit(` call outside `safe_write.py`/`wiim_adapter.py`/`src/tests/` and fails if one is found -- a fourth bypass will now fail CI instead of requiring another manual audit. |
| 2026-06-28 | A "None" (empty REW PEQ slot) and an "All pass" filter could be represented slightly differently depending on which import path read them, since each path's None/All-pass handling had evolved independently. | (a) Text-file path: a `None` slot always becomes an explicit `CanonicalFilter(type="OFF", ...)` in both `filters` and `rows` (`_NONE_RE`, checked before any skip logic). Live-API path: a `None` slot was completely omitted from both lists (`parse_filter_settings_with_rows` only special-cased it to *suppress* the `SkippedBand` it would otherwise add, but never substituted an `OFF` entry) -- so the same measurement, read via the two paths, produced different output lengths and different WiiM band-letter alignment for everything after the empty slot. (b) Text-file path normalized the skip label to `"All-pass"` (hyphenated) regardless of input spelling; live-API path reported the raw token, which real REW output always spells `"All pass"` (space) -- so the same skipped band showed a different label in the Review table depending on import path. | (a) was a leftover from when issue #97 was fixed (`cbc8ae7`): the original bug was a hard "Unknown filter type" error, and the chosen fix was simply "skip silently" -- nobody at the time compared this against the text path's pre-existing `OFF`-placeholder behavior, so the two paths quietly diverged. (b) `_is_unsupported_filter`'s `All[\s-]?pass` regex was written to tolerate a hyphen on input (defensively, no evidence REW ever emits one) but its hardcoded return value used the hyphenated form rather than the space form that's both REW's actual spelling and `_SKIP_TYPES`'s key. | `parse_filter_settings_with_rows` now checks `type_token == "None"` before calling `_classify_filter_type` and appends an explicit `CanonicalFilter(type="OFF", frequency_hz=1000.0, gain_db=0.0, q=1.0)` to both `filters` and `rows`, regardless of `enabled` -- byte-for-byte matching `_NONE_RE`'s text-path behavior. `_is_unsupported_filter`'s All-pass branch now returns `"All pass"` (space) instead of `"All-pass"`; the regex still accepts a hyphenated input but normalizes the output, so both paths report the identical label. |
| 2026-07-02 | (Corrects row 2026-06-15 above) No API command toggles the RoomFit DSP-active state; the HTTP API can read/write/save profiles but cannot activate or deactivate them on the DSP. | **This was wrong.** The same two commands tested on 2026-06-15 (`EQChangeSourceFX` to enable, `EQSourceOff` to disable) at `EQLevel: 2` do work, but require `"source_name": ""` (empty string) rather than a populated source name. The status read uses `EQGetLV2SourceBandEx` with the same empty-`source_name` payload and inspects the returned `EQStat` field. Retested against real hardware (2026-07-02) with the corrected empty `source_name`: `EQChangeSourceFX`/`EQSourceOff` reliably toggle RoomFit, confirmed by re-reading `EQGetLV2SourceBandEx` and observing `EQStat` flip between `"On"`/`"Off"`. Separately confirmed that `setRoomCorrection` (tried and dismissed as a toggle on 2026-06-15) is unrelated: it only ever carries `{"RC_Version": "...", "Time": "yyyy:MM:dd HH:mm"}`, written after a calibration run -- it records calibration metadata, not DSP state. | The RoomFit master on/off switch is a *global* toggle (applies regardless of which input is active), unlike every other RoomFit/PEQ command which is scoped to a specific `source_name`. The 2026-06-15 test followed the convention used by every other command in this family (a populated `source_name`) and got back `{"status":"OK"}` with no observable effect -- the device silently applied the command to the wrong (per-source) scope rather than rejecting it, which is exactly the kind of false negative `# ASSUMPTION:`-driven hardware probing can produce when a required field's *correct* value is "present but empty" rather than "absent" or "populated." | Documented the confirmed commands, curl reproduction, and root cause in `docs/wiim_api_notes.md` ("RoomFit DSP Toggle -- CONFIRMED" section, and the RoomFit Three-Layer Architecture table). Adapter code (`src/adapters/wiim_adapter.py`) does not implement `enable_roomfit()`/`disable_roomfit()`/status-read using these commands -- a GUI toggle is intentionally out of scope (product decision, 2026-07-02), so the existing "not yet supported" tooltip and `# TODO: RoomFit toggle` marker are now stale references to the old "not possible" conclusion rather than an open task. |
| 2026-07-02 | `EQv2SourceLoad` payload is untested and assumed to vary by profile type: PEQ loads omit `EQLevel` and include `source_name`; RoomFit loads add `EQLevel: 2` (`docs/wiim_api_notes.md` RoomFit Profile CRUD table, "Load PEQ profile"/"Load RC profile" rows, both originally speculative). | RoomFit profile loads via `EQv2SourceLoad` always send `{"EQLevel":2,"pluginURI":"http://moddevices.com/plugins/caps/EqNp","Name":"<profile>"}` -- no `source_name` field, and `EQLevel:2` is fixed rather than varying by context. Confirmed working against real hardware (2026-07-02) via curl. | The original table entries were extrapolated from other commands' `source_name`/`EQLevel` conventions rather than observed; the real command is global like the DSP toggle above, not per-source. Whether PEQ (non-RoomFit) profile loads use this same command with a different payload, or a different command entirely, remains unconfirmed. | Corrected the "Load RC profile" row in `docs/wiim_api_notes.md` to the confirmed payload. Left "Load PEQ profile" flagged as an open question (untested, possibly the wrong command entirely) rather than guessing further. |
| 2026-07-03 | (Corrects row 2026-06-12 above) "Source enumeration possible via API" was tested and found false -- no endpoint enumerates a device's input sources; `getStatusEx` has no populated `InputList` field, so source discovery requires guessing from a hardcoded per-model table. | **This was incomplete.** Two commands do enumerate a device's inputs: `getAudioInputCapbility` (device's supported/physical inputs) and `getAudioInputEnable` (same set plus a live "enabled/shown in app" flag per input, `enable: 0`/`1`). Both return entries keyed by a `mode` field that is exactly the `source_name` value used by every PEQ/RoomFit command. Confirmed against real hardware (2026-07-03): WiiM Amp Ultra returns `wifi`, `line-in`, `bluetooth`, `optical`, `HDMI`, `udisk`; WiiM Sound and WiiM Sound Lite both return `wifi`, `auxIn`, `bluetooth` -- exactly matching the existing hardcoded table. WiiM Mini returns `"unknown command"` for both -- it genuinely has no enumeration endpoint, so the 2026-06-12 finding still holds for that one device. | The 2026-06-12 test only checked `getStatusEx`'s `InputList` field (unpopulated on tested devices) and didn't try this separate command pair, which lives outside the `getStatusEx`/PEQ command families entirely. | Documented both commands, per-device confirmed results, and curl reproduction in `docs/wiim_api_notes.md` ("Source Enumeration" section under "PEQ Source Names"). WiiM Mini still requires the hardcoded per-model table and `device_capabilities.json` override -- runtime enumeration should be attempted first and fall back to the table on `"unknown command"`. Adapter/discovery code does not yet call these commands; wiring source enumeration into discovery is an open follow-up, not done as part of this documentation pass. |
| 2026-07-03 | The "PEQ Preset Commands" table in `docs/wiim_api_notes.md` (`EQGetLV2List`, `EQGetLV2NewList`, `EQSaveLV2Preset`, `EQSaveLV2SourcePreset`, `EQLoadLV2Preset`, `EQLoadLV2SourcePreset`, `EQDeleteLV2Preset`, `EQRenameLV2Preset`) reflects commands actually usable for PEQ preset CRUD; carried no "unconfirmed" caveat despite never having been hardware-tested by this project. | None of these command names could be corroborated during the same investigation that confirmed the RoomFit DSP toggle and `EQv2SourceLoad` payload (rows above) -- every reachable path for profile list/save/load/delete used the `EQv2GetNewList`/`EQv2SourceLoad`/`EQSourceSave`/`EQv2Delete` family instead, for both PEQ and RoomFit, differentiated only by `EQLevel` (1 vs 2) -- the same pattern already confirmed for RoomFit's Save/Delete/List. Separately, confirmed that PEQ's existing `EQChangeSourceFX`/`EQSourceOff` enable/disable commands are sent with an explicit `"EQLevel": 1` field (previously undocumented, neither shown nor denied in the existing entry) -- the identical command pair to the RoomFit DSP toggle, differing only in `EQLevel` (1 vs 2) and a real vs. empty `source_name`. | The "PEQ Preset Commands" table traces back to the reference material listed at the bottom of `docs/wiim_api_notes.md` (community/third-party API docs), not to this project's own hardware confirmation -- it was written without a caveat distinguishing it from the confirmed sections around it, so it read as equally authoritative. | Added an "unverified" callout directly above the PEQ Preset Commands table in `docs/wiim_api_notes.md`, pointing at the `EQv2*` family as the preferred path where the two overlap. Added the `EQLevel: 1` field to the PEQ enable/disable JSON payloads. Confirmed working via curl against real hardware (2026-07-03): `EQChangeSourceFX`/`EQSourceOff` with `EQLevel:1` + a real `source_name` reliably toggles `EQStat` for that one source, round-tripped On->Off->On with bands unchanged. The "Load PEQ profile" row was initially corrected to an `EQLevel: 1` payload inferred by symmetry with the confirmed RoomFit row -- that inference turned out to be wrong; see the row below. |
| 2026-07-03 | (Corrects the "Load PEQ profile" row above) `EQv2SourceLoad` with `{"EQLevel":1,"pluginURI":"...","Name":"<profile>"}` (no `source_name`, inferred by symmetry with the confirmed RoomFit `EQv2SourceLoad` payload) loads the named PEQ profile onto whichever source the caller cares about. | **This was wrong, and worth flagging as a near-miss.** Sent that exact payload with `Name:"flat"` intending to load it onto `wifi`. The response reported `"source_name":"HDMI"` -- a source nobody requested -- and a follow-up read of `wifi` confirmed it was completely untouched (still the prior profile, unchanged bands). Repeating with `Name:"<original profile>"` to "restore" also reported `"source_name":"HDMI"`, meaning both the test load and the restore silently landed on the same unintended source rather than on `wifi` as assumed. No band or `EQStat` corruption was observed once `HDMI` was independently re-read (its data matched what `wifi`/`line-in` already showed, consistent with the same profile having been active on multiple sources before the test) -- but this was a lucky outcome, not a guaranteed one; the same sequence performed against a fresh/unknown source could have silently swapped a live source's PEQ preset without any visible error. | Working theory: `EQv2SourceLoad` at `EQLevel: 1` operates on the device's currently-live/selected input, not a source named in the payload -- mirroring the already-documented legacy "current source, implicit" pattern (`EQChangeFX`, bare `EQOff`) rather than the explicit-`source_name` pattern used by `EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`. This never surfaced as a problem for the RoomFit row it was copied from because RoomFit's load/activation is deliberately global -- whichever source is implicitly targeted internally doesn't matter for a global read. PEQ has no such fallback: it's explicitly per-source, so an implicit "current source" target is a functional gap, not just a documentation nuance. | Marked the "Load PEQ profile" row and the RoomFit Profile CRUD table in `docs/wiim_api_notes.md` with an explicit warning not to rely on this payload. Added a caveat to the "Previewing a Saved Profile's Bands" section distinguishing RoomFit (safe, confirmed) from PEQ (unsafe, unresolved). Follow-up test (2026-07-03) resolved it: adding an explicit `"source_name":"wifi"` field to the same payload fixed the problem completely -- response and a follow-up read both correctly showed `wifi` changed to the requested profile and back. `docs/wiim_api_notes.md` updated accordingly; PEQ loads now documented as requiring `source_name`, same as every other per-source PEQ/RoomFit command except the two deliberately-global RoomFit calls. |
| 2026-07-03 | (Resolves the open question above) Omitting `source_name` from a PEQ `EQv2SourceLoad` call might be a genuine stateless "preview a saved preset's bands by name" mechanism, since the response returns full band data without touching the source being tracked. | **Ruled out -- it's a real write, not a preview, and it targets whichever input is currently live/active on the device.** Disambiguating test: with `HDMI` set as the device's active input, a no-`source_name` load of `"flat"` reported `"source_name":"HDMI"` in its response, and a direct follow-up read of `HDMI` (no second load in between) confirmed `HDMI` itself now held `Name:"flat"` with all-zero gains -- a real, persisted change. Repeating the test after switching the device's active input to `wifi` reproduced the identical pattern: the no-`source_name` load's response reported `"source_name":"wifi"` this time, and a direct read confirmed `wifi` had changed to `"flat"`. The target tracked the live input switch exactly, ruling out both a fixed default (e.g. always `HDMI`) and a stateless no-op. | `EQv2SourceLoad` at `EQLevel: 1`, when `source_name` is omitted, applies to the device's currently-active input rather than failing or defaulting to nothing -- there is no PEQ-level equivalent to RoomFit's deliberate global/empty-`source_name` scope; PEQ silently falls back to "whatever's currently playing." | Documented in `docs/wiim_api_notes.md` (RoomFit Profile CRUD caveat and "Previewing a Saved Profile's Bands" section) as **ruled out and unsafe** rather than merely unconfirmed: never call `EQv2SourceLoad` without `source_name` for PEQ, since doing so silently overwrites whichever source the user is actually listening to, with no error. The explicit-`source_name` load-then-restore pattern remains the only confirmed-safe way to inspect a saved PEQ preset's bands. Both `HDMI` and `wifi` were left holding `"flat"` on the test device after this investigation and needed restoring back to `M16` via explicit-`source_name` loads. |
| 2026-07-03 | (Corrects/strengthens the "PEQ Preset Commands" row above) `EQGetLV2List`/`EQGetLV2NewList` returning `{"status":"Failed"}` was ambiguous -- could mean the command is recognized but the call shape was wrong, not necessarily that the command is dead. | **Confirmed dead, not just unconfirmed.** Retested with `EQLevel` added to `EQGetLV2NewList`'s payload, and with both commands pointed at the older `Eq4p` plugin URI instead of the modern `EqNp` one (bare and JSON forms) -- all four additional attempts returned `{"status":"Failed"}`, for five failing call shapes total across this and the prior row. Separately, `EQRenameLV2Preset` (the one command in this table with no known `EQv2*` equivalent) was tested directly against a disposable test profile and also returned `{"status":"Failed"}`, confirmed by a follow-up list read showing the name genuinely unchanged. | Unknown why these commands fail specifically -- `{"status":"Failed"}` doesn't distinguish "wrong parameters" from "plugin/subsystem removed" from any other structural rejection. What's now settled is *that* they fail consistently across every reasonable parameter variation, not *why*. | Strengthened the "unverified" callout in `docs/wiim_api_notes.md`'s "PEQ Preset Commands" section to "confirmed non-functional on current firmware," directing readers to the `EQv2*` family instead. Flagged a genuine capability gap: **no known API command renames a saved PEQ/RoomFit profile** -- neither this legacy family nor the confirmed `EQv2*` family exposes rename. Documented the only known workaround (save-as-new-name via `EQSourceSave`, then `EQv2Delete` the old name), which loses the original `UpdateAt` metadata. |
| 2026-07-03 | (Resolves the open question raised in the 2026-07-03 "Source Enumeration" row above) Whether `source_name:"udisk"` (listed by `getAudioInputCapbility` as a distinct hardware input on Amp Ultra) returns genuinely distinct PEQ data from `"wifi"`, or is handled as one of the API's "any string returns default-looking data" fallback cases, was untested. | **Resolved: `udisk` is not a real PEQ source.** `EQGetLV2SourceBandEx` with `source_name:"udisk"` returned `Name:""` and an all-default L/R-mode template (stock ISO frequencies, zero gain on every band) -- the same signature already documented for nonexistent source names, and visibly different in shape from `wifi`'s populated `Name:"M16"` Stereo-mode response. Confirmed further by attempting a write: `EQv2SourceLoad` targeting `source_name:"udisk"` returned `{"status":"Failed"}` outright, unlike genuine sources (`wifi`, `HDMI`, `line-in`), which accept loads normally. Separately noticed: `udisk` appears in `getAudioInputCapbility`'s output but never in `getAudioInputEnable`'s (confirmed across all four device-owner-tested devices -- not `enable:0`, simply absent from the list). | `getAudioInputCapbility` reports a broader hardware-capability concept (the device can physically play from an attached USB drive) that is a different concept from the LV2 plugin's addressable per-source PEQ storage slots. `getAudioInputEnable` tracks the narrower set of inputs the app itself treats as switchable, and the PEQ engine's behavior agrees with that narrower list, not the broader capability list -- `udisk`'s absence from `getAudioInputEnable` was the signal pointing at this before the PEQ-layer test even confirmed it directly. Root cause confirmed (device owner, 2026-07-03): USB playback on Amp Ultra is implemented as a media server routed through the same network-playback pipeline as `wifi` -- it isn't a distinct physical input line, so it never appears as a switchable input, even with a USB drive connected and actively playing (tested directly). | Documented in `docs/wiim_api_notes.md` ("Source Enumeration" section) with the practical consequence spelled out: build a PEQ source picker from `getAudioInputEnable`'s output, not `getAudioInputCapbility`'s -- the former already excludes `udisk` (and presumably other capability-only, non-addressable entries) without needing a hardcoded denylist, which is more robust than filtering `udisk` out by name. Confirmed safe by direct testing with active USB playback. Confirms the original "PEQ Source Names" table's claim that `wifi` already covers USB disk. |
