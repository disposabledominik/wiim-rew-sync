# Assumption Corrections Ledger

*Whenever an assumption from the specifications fails during implementation, log it here. This file acts as the project memory.*

> **Looking for the current `source_name`/`EQLevel` rules, not the investigation history?** See the "`source_name` & `EQLevel` Cheat Sheet" near the top of `docs/wiim_api_notes.md` — it's the condensed, current answer. This file is append-only history of how each rule was discovered (rows 2026-06-15, 2026-07-02, 2026-07-03 below cover the RoomFit-toggle and PEQ-load `source_name` mistakes in detail).

| Date | Assumption | Failure / Observation | Root Cause | Implemented Solution |
| --- | --- | --- | --- | --- |
| 2026-06-12 | All WiiM devices have `project` field starting with `WiiM_`. | WiiM Mini reports `project: "Muzo_Mini"` — a legacy/OEM name. | WiiM Mini uses older firmware branding from before the WiiM rebrand. | Added `Muzo_Mini` to `WIIM_PROJECT_PREFIXES` in `capability_prober.py`. |
| 2026-06-12 | mDNS TXT records include DeviceName, project, and Release fields. | TXT records on tested devices are empty for these fields. | WiiM mDNS services don't populate TXT record metadata. | Added `_enrich_device()` in `discovery_module.py` to call `getStatusEx` after mDNS resolves IPs. |
| 2026-06-12 | Source names in `EQGetLV2SourceBandEx` are case-insensitive. | Source names are CASE-SENSITIVE. `hdmi` (lowercase) returns the L/R channel-mode slot; `HDMI` (uppercase) returns the Stereo slot. Same source, different case = different PEQ data. | The WiiM API uses case to distinguish channel-mode storage slots for the same physical input. | Document in API notes. Users must use the exact case the WiiM app uses (typically uppercase for physical inputs: HDMI, Ethernet; lowercase for logical: wifi, bluetooth, line-in, optical). |
| 2026-06-12 | WiiM devices have 10 PEQ bands (letters a-j). | WiiM Amp Ultra (firmware 20260409) has 12 bands (letters a-l). | Newer firmware expanded the band count from 10 to 12. | Updated `_BAND_LETTERS` to `"abcdefghijkl"`. The `max_filters` capability should be probed dynamically rather than hardcoded. |
| 2026-06-12 | WiiM PEQ mode values are: -1=OFF, 0=LS, 1=PEAK, 2=HS. | Additional modes exist: 3=LP (Low-Pass), 5=HP (High-Pass). | Newer firmware added LP/HP filter types to the PEQ engine. | Added modes 3 and 5 to `_MODE_MAP`. Added "LP" and "HP" to `FilterType` literal. |
| 2026-06-12 | Source enumeration possible via API. | WiiM API returns valid PEQ data for ANY source name (even non-existent physical inputs). No API endpoint exists to enumerate actual physical inputs. `getStatusEx` has no `InputList` field on tested devices. | The PEQ engine has internal slots for all possible sources regardless of hardware. | OPEN ISSUE: Need alternative approach to source discovery. Current workaround: probe canonical names and show all. User must know their device's physical inputs. |
| 2026-06-14 | RoomFit uses dedicated commands: `getRoomFitStatus`, `getRoomFitBands`, `setRoomFitBands`. | These commands do not exist. RoomFit uses the standard LV2 PEQ command family (`EQGetLV2SourceBandEx`, `EQSetLV2SourceBand`, `EQv2GetNewList`) with `"EQLevel": 2` added to the JSON payload. Profile CRUD also uses standard commands (`EQSourceSave`, `EQv2Delete`, `EQv2SourceLoad`) with `EQLevel: 2`. | RoomFit is not a separate subsystem — it's a second EQ level within the existing LV2 plugin architecture. The original assumptions were based on speculative community docs. | Code needs updating: `capability_prober._probe_roomfit()` and `wiim_adapter.read_roomfit()`/`write_roomfit()` must use the real commands. Tests need fixture updates. Documented in `wiim_api_notes.md`. |
| 2026-06-14 | RoomFit `EQGetLV2SourceBandEx` + `EQLevel: 2` returns the DSP-active profile's bands (same as PEQ reads). | It returns the API "working buffer" — which is the last profile loaded via `EQv2SourceLoad`, NOT the DSP-active state. Without a prior load, returns a flat default template regardless of what the app shows as active. Buffer is device-global, persistent across connections and reboots (stored in flash). Not cleared by profile deletion, profile switching, toggling RoomFit, or device reboot. Only overwritten when a profile is actively edited/calibrated via the WiiM app or loaded via API. | RoomFit has three layers: profile storage, API working buffer, and DSP-active state. PEQ (EQLevel 1) conflates buffer and DSP; RoomFit (EQLevel 2) separates them. | Read workflow must first load the target profile via `EQv2SourceLoad` before reading bands. Buffer is harmless to WiiM app (app ignores it for display/playback). Documented in `wiim_api_notes.md`. |
| 2026-06-14 | RoomFit direct writes (`EQSetLV2SourceBand` + `EQLevel: 2`) apply changes to the DSP immediately (like PEQ writes). | Writes only modify the API working buffer. Must be followed by `EQSourceSave` to persist. Saving to the ACTIVE profile name deactivates RoomFit; saving to a NEW profile name does NOT deactivate. Buffer retains data after save (not cleared). Delete of non-active profiles also does not deactivate. | RoomFit is profile-based. The DSP activation mechanism is internal to the WiiM app. Overwriting the active profile triggers a reload/deactivation cycle; creating a new profile is non-disruptive. | Write workflow: load → modify buffer → save. If overwriting active profile: warn user about deactivation. If saving as new name: no disruption. Level 4 confirmed with these semantics. |
| 2026-06-14 | WiiM Mini (no RoomFit hardware) will reject RoomFit commands or return "unknown command". | WiiM Mini silently accepts ALL RoomFit commands (`EQv2GetNewList`+EQLevel:2, `EQGetLV2SourceBandEx`+EQLevel:2, `EQSourceSave`+EQLevel:2, `EQv2Delete`+EQLevel:2) with HTTP 200 and valid-looking responses. The probe reaches `roomfit_level = 4` even though the device has no RoomFit capability. Band reads return data with `EQStat: Off` but the response structure is valid. Profile save/delete return "OK". | The WiiM Mini's API never rejects unknown EQLevel values — it just processes the command against an inactive/empty subsystem. There is no API-level distinction between "device has RoomFit but it's off" and "device doesn't have RoomFit at all." | OPEN ISSUE: Currently the Mini reports `roomfit_level = 4`. This is harmless in practice (RoomFit reads return empty/off data, writes persist to an unused profile store), but is semantically incorrect. Possible fix: after `EQSourceSave`, verify the profile appears in `EQv2GetNewList` (round-trip check). Low priority since Mini RoomFit operations are no-ops. The round-trip-verify suggestion is now implemented for real RoomFit hardware -- see the 2026-06-29 row below; `RoomFitSafeWrite` reads back the saved profile and verifies bands after every write, which doubles as the round-trip check this row speculated about. |
| 2026-06-29 | `wiim_adapter.delete_peq_profile()` could be reused as-is for deleting a RoomFit profile, since both are `EQv2Delete` calls against the same plugin. | `delete_peq_profile()` omits `EQLevel` entirely. Per `docs/wiim_api_notes.md`'s RoomFit Profile CRUD table, deleting an RC (RoomFit) profile requires `"EQLevel": 2` in the payload -- without it the command targets the PEQ-profile namespace, not RoomFit's. No `delete_roomfit_profile()` existed at all prior to this fix, so RoomFit's rollback-by-delete path (a brand-new profile that fails write verification) had no way to remove the bad profile. | PEQ and RoomFit profile storage share the same `EQv2Delete` command but live in separate namespaces distinguished only by `EQLevel`, mirroring every other RoomFit command (`EQSourceSave`, `EQGetLV2SourceBandEx`, etc. all need the same parameter). This was never implemented because nothing in the app called profile-delete on the RoomFit path until write-verification (this row) needed it. | Added `delete_roomfit_profile(profile_name)` to `wiim_adapter.py`, sending `EQv2Delete` with `EQLevel: 2`. Used by the new `RoomFitSafeWrite` class (`safe_write.py`) as the rollback action when a brand-new RoomFit profile fails post-write verification. |
| 2026-06-14 | EQ writes to slave devices must be redirected to the master node. | PEQ and RoomFit filters are per-device, not per-group. Each device in a multiroom group has independent EQ. Writing to a slave's IP works correctly and modifies only that slave's EQ. There is no need for redirection. | The assumption came from pywiim/community docs about the legacy graphic EQ system, which IS group-wide. The LV2 PEQ engine is per-device. | Removed slave write guard from WiiMAdapter and SafeWrite. Removed rule 6 from steering. Requirement 17 rewritten to display-only. Task 41 cancelled. |
| 2026-06-15 | pip-audit will find no vulnerabilities. | pip-audit found 53 vulnerabilities in 15 system-level packages (certifi, cryptography, pip, setuptools, urllib3, etc.). None are in project direct dependencies (httpx, pydantic, PySide6, zeroconf). | System packages on Ubuntu 24.04 are behind latest PyPI versions. These are OS-managed and not bundled with the application. | No code changes needed. PyInstaller bundles only project dependencies which are clean. Documented in `docs/qa_signoff.md`. System admin should run `apt upgrade` to patch OS-level packages. |
| 2026-06-15 | RoomFit DSP-active state can be toggled via the HTTP API (e.g. `EQSourceOff`/`EQChangeSourceFX` with `EQLevel: 2`, or a dedicated `setRoomCorrection` command). | No API command toggles the RoomFit DSP-active state. Tested: `EQSourceOff`+EQLevel:2 (OK, no effect), `EQChangeSourceFX`+EQLevel:2 (OK, no effect), `EQSourceOff` without pluginURI (Failed), `setRoomCorrection:0` (OK, readable via `getRoomCorrection`, no effect on RoomFit), `MCURoomCorrection:0` (unknown command), `EQSetRoomFit:Off` (Failed), `EQSetLV2Stat`+EQLevel:2+EQStat:Off (Failed). RoomFit remained active in the WiiM app throughout all tests. | The RoomFit DSP activation/deactivation is handled exclusively by the WiiM app's internal logic. The HTTP API provides no mechanism to select or deselect a RoomFit profile for DSP processing. | OPEN ISSUE: GUI RoomFit toggle disabled with tooltip "RoomFit toggle not yet supported — use the WiiM Home app". `# TODO: RoomFit toggle` added to `wiim_adapter.py`. Revisit if future firmware or community research reveals a mechanism. |
| 2026-06-20 | CLI commands need full capability probing before every operation. | CLI commands like `peq-toggle` and `load-preset` run a full 8-call capability probe (~4-5s overhead) even though they don't use any capability flags. The adapter methods (`enable_peq`, `disable_peq`, `load_peq_profile`) make no capability checks internally. | The CLI was designed as a proof-of-concept using the same code path as the GUI. Each CLI invocation is a fresh process with no state, so the probe runs every time. The GUI caches capabilities after a single probe at device connection, so it's unaffected. | KNOWN LIMITATION: CLI-only latency issue (~4s overhead on simple operations). Not worth adding probe-level complexity since CLI is not user-facing. If CLI becomes a focus, introduce tiered probing (skip unneeded probe steps per command) or cache capabilities per-device in a local file. |
| 2026-06-27 | `max_filters` (counted from distinct band-letter prefixes in `EQGetLV2BandEx`) represents bands available in the device's *current* channel mode -- per-channel when probed in L/R mode, total when probed in Stereo mode -- not two independent per-channel limits. | OPEN ISSUE: not yet verified against real hardware in both channel modes. The new device-capability-file default-10-band cap (`src/models/device_capability_file.py::merge_into`) and `# ASSUMPTION:` comment in `capability_prober.py::_probe_peq` rely on this holding. | Unconfirmed -- the probe has always run with whatever channel mode the device happened to be in at connect time; no test has compared band counts probed in Stereo vs L/R on the same device. | `# TODO:` added at `capability_prober.py::_probe_peq`. Run `EQGetLV2BandEx` (Stereo) and `EQGetLV2SourceBandEx` (L/R) against the same device and compare band-letter counts to confirm or correct this row. See 2026-07-03 row below -- confirmed. |
| 2026-07-03 | (Confirms row 2026-06-27 above) `max_filters` represents bands available in the device's *current* channel mode -- per-channel when probed in L/R mode, total when probed in Stereo mode. | **Confirmed against real hardware.** On a 12-band-capable device, `EQBandL`/`EQBandR` each independently report 12 bands (letters a-l) when probed in L/R mode -- 24 addressable bands total, 12 per channel -- while `EQBand` reports 12 bands total when probed in Stereo mode. Not two independent 12-band-per-channel limits reported as one number; the letter count is genuinely per-channel in L/R mode. | N/A -- confirms the original hypothesis rather than correcting it. | The `# TODO:` at `capability_prober.py::_probe_peq` and the device-capability-file default-10-band cap (`src/models/device_capability_file.py::merge_into`) can now treat this as settled: `max_filters` counted from either probe already reflects the correct per-channel-in-L/R semantics, no code change needed. Documented in `docs/wiim_api_notes.md` Capability Nuances. |
| 2026-06-27 | A device whose API reports 12 PEQ bands (e.g. WiiM Amp Ultra, firmware 20260409+) exposes all 12 for use, so capping to 10 by default would under-use the hardware. | Confirmed by device owner: the official WiiM Home App itself only exposes/uses 10 of the 12 API-reported bands in its own PEQ UI -- the 11th/12th are API-reachable but not surfaced anywhere in WiiM's own app. | WiiM Home App's PEQ UI was built around a 10-band limit before the LV2 plugin's band count was later expanded in firmware; the extra bands were never wired into the app UI. | The bundled `device_capabilities.json` does not raise any model's `max_bands` ceiling above the generic default -- this app's 10-band default cap (Requirement 7) intentionally matches WiiM's own app rather than exposing the extra API bands. Documented in `docs/wiim_api_notes.md` Capability Nuances. |
| 2026-06-28 | REW "Notch" filters can be approximated on WiiM as a narrow PEAK filter, so `rew_parser._TYPE_MAP` mapped `Notch`/`Notch Q` -> `PEAK`. | REW notches imply >60 dB of attenuation at the target frequency; WiiM's PEQ gain range is capped at -12 dB, so a Notch can never be faithfully reproduced. Also found the two REW import paths disagreed: the text-file path (`_is_unsupported_filter`) already treated Notch as unsupported and skipped it, while the live REW-API path (`_classify_filter_type`) silently went through `_TYPE_MAP` and converted it to PEAK with *no* warning at all -- a real inconsistency, not just a missing feature. | The API-path classifier checks `_SKIP_TYPES` before falling through to `_TYPE_MAP`; Notch had only ever been added to the text-file path's dedicated regex checks, never to the shared skip set. | Removed `Notch`/`Notch Q` from `_TYPE_MAP`; added both to `_SKIP_TYPES` (with the text-path's prefix-matching loop explicitly excluding them so the more specific "Notch Q" label isn't shadowed). Both import paths now skip Notch consistently and surface it as a `SkippedBand` placeholder in the Review table (Phase B3) instead of silently converting or dropping it. |
| 2026-06-29 | Probing `_wiim._tcp.local.` then falling back to `_linkplay._tcp.local.` sequentially is a cheap, safe way to support both newer and older WiiM/LinkPlay devices, since at most one extra probe round-trip is added. | A device that only advertises `_linkplay._tcp.local.` (no `_wiim._tcp.local.` service at all) never gets a real chance to be found: `_probe_service` waited the *full* configured timeout on the non-existent `_wiim._tcp.local.` type before even starting the `_linkplay._tcp.local.` probe, and `DiscoveryModule`'s grace-period orchestration gives up on the combined mDNS task in less time than that -- so the app always fell through to the slower subnet scan, even though the device was instantly discoverable via mDNS by other tools (e.g. `avahi-browse`). | "Probe one type, fall back to the next" assumed both probes were cheap regardless of whether the first type's service exists on the network; in practice an absent service type costs a full timeout, not a fast no-op. | Rewrote `ZeroconfDiscover._probe_service` (now `_probe_services`) to browse `_wiim._tcp.local.` and `_linkplay._tcp.local.` concurrently via a single `AsyncServiceBrowser` call across one shared timeout window, so neither type can starve the other's chance to respond. |
| 2026-06-29 | Fixing `ZeroconfDiscover`'s sequential-double-timeout bug (row above) would resolve a device owner's report of mDNS discovery always falling back to the subnet scan. | The code fix was verified correct in isolation (confirmed against the device owner's real 4-device LAN from a WSL2 environment: mDNS found all 4 devices in 1.8-3.3s). But the device owner still saw the *packaged Windows `.exe`* take ~12s and fully exhaust the 254-host subnet scan with zero mDNS hits. `Get-NetFirewallRule` showed the app's installed firewall rule allows inbound only on the "Private" network profile and explicitly blocks it on "Public"; `Get-NetConnectionProfile` showed the active Wi-Fi was classified "Public" by Windows. Reclassifying it to "Private" dropped discovery to 2s. | Two independent root causes were compounding the same symptom: (1) the code bug fixed in the row above, confirmed via WSL2 (whose network stack/avahi-daemon isn't subject to this Windows-exe-specific firewall rule, so it never exhibited symptom 2); and (2) a Windows network-profile/firewall configuration issue that blocks inbound mDNS replies to the packaged app regardless of how correct its probe timing is. Fixing (1) alone could not have surfaced (2), since WSL2 testing structurally cannot reproduce a native-Windows-Firewall-rule symptom. | No code can fix (2) — it's a Windows OS/network setting, and the existing firewall rule's Public-network block is a reasonable default the app shouldn't silently override. Documented the network-profile requirement in `src/gui/assets/help/troubleshooting.md`, `README.md`, and `docs/architecture.md`; tightened `ConnectPage`'s empty-state hint text; added a `DiscoveryModule` log warning (`discovery_module.py`, Phase 3) that fires specifically when mDNS contributes zero devices but the subnet scan succeeds, naming the Public/Private network profile as the likely cause for anyone who shares a support bundle. |
| 2026-06-28 | (a) A missing `q` field on a live REW-API `FilterSetting` can safely default to `1.0` for any filter type. (b) REW's bare `LS`/`HS` shelf tokens (and `LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`) carry a Q value comparable to the dedicated `LS Q`/`HS Q` tokens, so `_TYPE_MAP` mapped all of them to `LS`/`HS` and substituted a flat `_BUTTERWORTH_Q` (0.707) for the ones with no explicit Q in REW's text export. | Real REW live-API output (`GET /measurements/:id/filters`) confirmed `q` is entirely absent -- not just unset -- for every type with no Q field in REW's own format: `LP`/`HP`/`LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1`. (a) was wrong for `LP`/`HP`: REW documents these as a *fixed* Q of 0.7071, so defaulting to 1.0 silently corrupted every live-API-sourced LP/HP band (a REW-text import of the same filter got the correct 0.707/0.7071, so the two import paths disagreed on Q for these types, same class of bug as the Notch row above). (b) was a more fundamental error, not just a missing default: REW documents `LS`/`HS` (bare), `LS 6dB`/`HS 6dB`, and `LS 12dB`/`HS 12dB` as using a shelf-*slope* parameter `S` (0.9 / 0.5 / 1.0 respectively) -- a different physical quantity from Q in the underlying biquad math, not an interchangeable number. Plugging a Butterworth *Q* value into a field REW defines as *S* is not a reasonable approximation, it's mixing up two different filter parameterizations. `LP1`/`HP1` (1st-order, no Q at all) were also being silently passed through on the live-API path via `_TYPE_MAP`, even though the text-file path already treated them as unsupported. | `_classify_filter_type`'s missing-`q` fallback (`setting.get("q", 1.0)`) was a single generic default applied regardless of filter type, never updated when LP/HP were added (row above, 2026-06-12). `LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1` were in `_TYPE_MAP` (supported on the live-API path) while the text-file path's `_is_unsupported_filter` had already special-cased most of them as unsupported -- the two paths were never reconciled. | `parse_filter_settings_with_rows` now defaults a missing `q` to `_BUTTERWORTH_Q` (renamed-precision 0.7071, matching REW's documented value) only for `canonical_type in ("LP", "HP")`; all other types keep the inert `1.0` fallback (never expected to fire, since REW always sends `q` for those). `LS`/`HS`/`LS 6dB`/`HS 6dB`/`LS 12dB`/`HS 12dB`/`LP1`/`HP1` moved from `_TYPE_MAP` to `_SKIP_TYPES`, so both import paths now skip them consistently and surface a `SkippedBand` placeholder instead of silently substituting the wrong parameter. `# TODO:` -- if a hardware-validated REW-S -> WiiM-Q conversion (or confirmation that WiiM's shelf implementation accepts `S` directly) is ever found, revisit `_SKIP_TYPES` to re-enable these six shelf variants; until then they're conservatively unsupported rather than guessed. |
| 2026-06-29 | `SafeWrite`/`RoomFitSafeWrite`'s write-verification could compare the `PEQSettings` passed into `execute()` directly against the device's read-back, since both represent "the same bands". | A device-owner repro of a real L/R push (gain 15 dB, gain -21 dB, and Q 29.255 on three different bands -- all legitimately out of WiiM's supported range) failed verification and rolled back every time, even though the device wrote and stored the data correctly. `generate_wiim_band_array()` clamps out-of-range gain (±12 dB) and Q (0.01-24.0) to the WiiM-supported range *at write time*, without mutating the caller's `CanonicalFilter` list -- so the device's read-back reflects the clamped value while `SafeWrite` was comparing against the original, pre-clamp value the user imported. Any band needing clamping was therefore guaranteed to "mismatch", regardless of band count. (A `_describe_first_mismatch()` diagnostic added the same day, to help debug this, also had its own minor bug: it returned an early "band count mismatch" message whenever intended/read-back lengths differed, even when the real `compare_band_lists()` truncates and would have passed -- this produced a misleading diagnostic that initially looked like the root cause but wasn't.) | `validate_filters_for_device()`'s Review-step clamping is intentionally display-only ("Does NOT modify gain/Q values — only flags them. Actual clamping is done by the WiiM generator at write time.", per its own docstring) -- the actual clamp only happens once, inside `generate_wiim_band_array()`, at the point of building the wire payload. Nothing downstream of that point (verification) accounted for it. | Added `clamp_filters_for_verification()` (`src/translator/wiim_generator.py`), mirroring `generate_wiim_band_array()`'s truncate+clamp logic without re-logging (the real write already logs each clamp). `safe_write.py` now builds its verification baseline through this helper (`_clamped_for_verification()`) at all four verify call sites in `SafeWrite` and `RoomFitSafeWrite`, comparing against what was *actually* written rather than what was originally imported. |
| 2026-06-29 | Adding `RoomFitSafeWrite`/clamp-aware verification to the main Push flow (the 2026-06-29 row above, and #11/#149) was assumed to cover "RoomFit writes" as a category, since it's the only RoomFit write path exercised by the wizard. | "Copy to Another Device" (`MainWindow._do_copy_preset_to_device()`) wrote RoomFit profiles via a bare `target_adapter.write_roomfit()` call with no `RoomFitSafeWrite` at all -- no backup, no verify, no rollback -- and its PEQ branch, despite already using `SafeWrite`, never checked the returned `WriteResult.success` before declaring victory. Separately, the CLI's `set-roomfit-filters` command called `adapter.write_roomfit()` directly, unlike its PEQ sibling command (which already used `SafeWrite`). Each of these write paths was added independently over time and never cross-checked against the others for the same safety guarantee. | "Every device write follows the 5-step safety protocol... No exceptions" (CLAUDE.md) was being satisfied per-feature as each feature was built, not audited across all write call sites as a category. Every place that calls `adapter.write_peq()`/`write_roomfit()` directly (not through `SafeWrite`/`RoomFitSafeWrite`) is a gap in this guarantee, and there's no test or lint rule that catches a new one being added. | Wrapped both bypassing call sites in `RoomFitSafeWrite`/checked their `WriteResult`, per the smoke_test_issues.md #154 row. No structural enforcement was added (e.g. a lint rule banning direct `adapter.write_peq`/`write_roomfit` calls outside `safe_write.py`) -- flagging this as a residual risk for the next person who adds a new write path. |
| 2026-06-29 | The grep-based audit behind the row above ("Copy to Another Device" + CLI `set-roomfit-filters`) was assumed to have covered every direct `write_roomfit()` call site once those two were fixed. | A third bypass existed in the same file: `MainWindow._do_undo_roomfit()` (restoring a RoomFit profile from a backup file via the sidebar's Undo action) also called `self._wiim_adapter.write_roomfit()` directly, with no `RoomFitSafeWrite`, no verify, no rollback. It was missed in the first audit despite being in the same file as one of the two fixes, because the audit was driven by a one-off grep rather than a repeatable check. It also discarded the backup's channel mode and per-channel (L/R) band lists (`_channel_mode`, `_filters_l`, `_filters_r` -- all underscore-prefixed and unused), so an L/R RoomFit backup would have silently restored as stereo-only bands even before this fix. | A manual one-off grep audit finds what you remember to search for, not everything matching the pattern -- exactly the gap the previous row's "residual risk" note anticipated. | Routed `_do_undo_roomfit()` through `RoomFitSafeWrite.execute()` using the channel mode and `filters_l`/`filters_r` already returned by `parse_backup_filters()` (previously discarded), reporting an error banner instead of "restored from backup" on verification failure. Closed the residual-risk gap for real this time: added `TestNoDirectWriteBypass::test_no_file_outside_safe_write_calls_adapter_write_directly` (`src/tests/test_safe_write.py`), which scans every `.py` file under `src/` for a direct `.write_peq(`/`.write_roomfit(` call outside `safe_write.py`/`wiim_adapter.py`/`src/tests/` and fails if one is found -- a fourth bypass will now fail CI instead of requiring another manual audit. |
| 2026-06-28 | A "None" (empty REW PEQ slot) and an "All pass" filter could be represented slightly differently depending on which import path read them, since each path's None/All-pass handling had evolved independently. | (a) Text-file path: a `None` slot always becomes an explicit `CanonicalFilter(type="OFF", ...)` in both `filters` and `rows` (`_NONE_RE`, checked before any skip logic). Live-API path: a `None` slot was completely omitted from both lists (`parse_filter_settings_with_rows` only special-cased it to *suppress* the `SkippedBand` it would otherwise add, but never substituted an `OFF` entry) -- so the same measurement, read via the two paths, produced different output lengths and different WiiM band-letter alignment for everything after the empty slot. (b) Text-file path normalized the skip label to `"All-pass"` (hyphenated) regardless of input spelling; live-API path reported the raw token, which real REW output always spells `"All pass"` (space) -- so the same skipped band showed a different label in the Review table depending on import path. | (a) was a leftover from when issue #97 was fixed (`cbc8ae7`): the original bug was a hard "Unknown filter type" error, and the chosen fix was simply "skip silently" -- nobody at the time compared this against the text path's pre-existing `OFF`-placeholder behavior, so the two paths quietly diverged. (b) `_is_unsupported_filter`'s `All[\s-]?pass` regex was written to tolerate a hyphen on input (defensively, no evidence REW ever emits one) but its hardcoded return value used the hyphenated form rather than the space form that's both REW's actual spelling and `_SKIP_TYPES`'s key. | `parse_filter_settings_with_rows` now checks `type_token == "None"` before calling `_classify_filter_type` and appends an explicit `CanonicalFilter(type="OFF", frequency_hz=1000.0, gain_db=0.0, q=1.0)` to both `filters` and `rows`, regardless of `enabled` -- byte-for-byte matching `_NONE_RE`'s text-path behavior. `_is_unsupported_filter`'s All-pass branch now returns `"All pass"` (space) instead of `"All-pass"`; the regex still accepts a hyphenated input but normalizes the output, so both paths report the identical label. |
| 2026-07-02 | (Corrects row 2026-06-15 above) No API command toggles the RoomFit DSP-active state; the HTTP API can read/write/save profiles but cannot activate or deactivate them on the DSP. | **This was wrong.** The same two commands tested on 2026-06-15 (`EQChangeSourceFX` to enable, `EQSourceOff` to disable) at `EQLevel: 2` do work, but require `"source_name": ""` (empty string) rather than a populated source name. The status read uses `EQGetLV2SourceBandEx` with the same empty-`source_name` payload and inspects the returned `EQStat` field. Retested against real hardware (2026-07-02) with the corrected empty `source_name`: `EQChangeSourceFX`/`EQSourceOff` reliably toggle RoomFit, confirmed by re-reading `EQGetLV2SourceBandEx` and observing `EQStat` flip between `"On"`/`"Off"`. Separately confirmed that `setRoomCorrection` (tried and dismissed as a toggle on 2026-06-15) is unrelated: it only ever carries `{"RC_Version": "...", "Time": "yyyy:MM:dd HH:mm"}`, written after a calibration run -- it records calibration metadata, not DSP state. | The RoomFit master on/off switch is a *global* toggle (applies regardless of which input is active), unlike every other RoomFit/PEQ command which is scoped to a specific `source_name`. The 2026-06-15 test followed the convention used by every other command in this family (a populated `source_name`) and got back `{"status":"OK"}` with no observable effect -- the device silently applied the command to the wrong (per-source) scope rather than rejecting it, which is exactly the kind of false negative `# ASSUMPTION:`-driven hardware probing can produce when a required field's *correct* value is "present but empty" rather than "absent" or "populated." | Documented the confirmed commands, curl reproduction, and root cause in `docs/wiim_api_notes.md` ("RoomFit DSP Toggle -- CONFIRMED" section, and the RoomFit Three-Layer Architecture table). Adapter code (`src/adapters/wiim_adapter.py`) does not implement `enable_roomfit()`/`disable_roomfit()`/status-read using these commands -- a GUI toggle is intentionally out of scope (product decision, 2026-07-02), so the existing "not yet supported" tooltip and `# TODO: RoomFit toggle` marker are now stale references to the old "not possible" conclusion rather than an open task. |
| 2026-07-02 | `EQv2SourceLoad` payload is untested and assumed to vary by profile type: PEQ loads omit `EQLevel` and include `source_name`; RoomFit loads add `EQLevel: 2` (`docs/wiim_api_notes.md` RoomFit Profile CRUD table, "Load PEQ profile"/"Load RC profile" rows, both originally speculative). | RoomFit profile loads via `EQv2SourceLoad` always send `{"EQLevel":2,"pluginURI":"http://moddevices.com/plugins/caps/EqNp","Name":"<profile>"}` -- no `source_name` field, and `EQLevel:2` is fixed rather than varying by context. Confirmed working against real hardware (2026-07-02) via curl. | The original table entries were extrapolated from other commands' `source_name`/`EQLevel` conventions rather than observed; the real command is global like the DSP toggle above, not per-source. Whether PEQ (non-RoomFit) profile loads use this same command with a different payload, or a different command entirely, remains unconfirmed. | Corrected the "Load RC profile" row in `docs/wiim_api_notes.md` to the confirmed payload. Left "Load PEQ profile" flagged as an open question (untested, possibly the wrong command entirely) rather than guessing further. |
| 2026-07-03 | (Corrects row 2026-06-12 above) "Source enumeration possible via API" was tested and found false -- no endpoint enumerates a device's input sources; `getStatusEx` has no populated `InputList` field, so source discovery requires guessing from a hardcoded per-model table. | **This was incomplete.** Two commands do enumerate a device's inputs: `getAudioInputCapbility` (device's supported/physical inputs) and `getAudioInputEnable` (same set plus a live "enabled/shown in app" flag per input, `enable: 0`/`1`). Both return entries keyed by a `mode` field that is exactly the `source_name` value used by every PEQ/RoomFit command. Confirmed against real hardware (2026-07-03): WiiM Amp Ultra returns `wifi`, `line-in`, `bluetooth`, `optical`, `HDMI`, `udisk`; WiiM Sound and WiiM Sound Lite both return `wifi`, `auxIn`, `bluetooth` -- exactly matching the existing hardcoded table. WiiM Mini returns `"unknown command"` for both -- it genuinely has no enumeration endpoint, so the 2026-06-12 finding still holds for that one device. | The 2026-06-12 test only checked `getStatusEx`'s `InputList` field (unpopulated on tested devices) and didn't try this separate command pair, which lives outside the `getStatusEx`/PEQ command families entirely. | Documented both commands, per-device confirmed results, and curl reproduction in `docs/wiim_api_notes.md` ("Source Enumeration" section under "PEQ Source Names"). WiiM Mini still requires the hardcoded per-model table and `device_capabilities.json` override -- runtime enumeration should be attempted first and fall back to the table on `"unknown command"`. Adapter/discovery code does not yet call these commands; wiring source enumeration into discovery is an open follow-up, not done as part of this documentation pass. |
| 2026-07-03 | The "PEQ Preset Commands" table in `docs/wiim_api_notes.md` (`EQGetLV2List`, `EQGetLV2NewList`, `EQSaveLV2Preset`, `EQSaveLV2SourcePreset`, `EQLoadLV2Preset`, `EQLoadLV2SourcePreset`, `EQDeleteLV2Preset`, `EQRenameLV2Preset`) reflects commands actually usable for PEQ preset CRUD; carried no "unconfirmed" caveat despite never having been hardware-tested by this project. | None of these command names could be corroborated during the same investigation that confirmed the RoomFit DSP toggle and `EQv2SourceLoad` payload (rows above) -- every reachable path for profile list/save/load/delete used the `EQv2GetNewList`/`EQv2SourceLoad`/`EQSourceSave`/`EQv2Delete` family instead, for both PEQ and RoomFit, differentiated only by `EQLevel` (1 vs 2) -- the same pattern already confirmed for RoomFit's Save/Delete/List. Separately, confirmed that PEQ's existing `EQChangeSourceFX`/`EQSourceOff` enable/disable commands are sent with an explicit `"EQLevel": 1` field (previously undocumented, neither shown nor denied in the existing entry) -- the identical command pair to the RoomFit DSP toggle, differing only in `EQLevel` (1 vs 2) and a real vs. empty `source_name`. | The "PEQ Preset Commands" table traces back to the reference material listed at the bottom of `docs/wiim_api_notes.md` (community/third-party API docs), not to this project's own hardware confirmation -- it was written without a caveat distinguishing it from the confirmed sections around it, so it read as equally authoritative. | Added an "unverified" callout directly above the PEQ Preset Commands table in `docs/wiim_api_notes.md`, pointing at the `EQv2*` family as the preferred path where the two overlap. Added the `EQLevel: 1` field to the PEQ enable/disable JSON payloads. Confirmed working via curl against real hardware (2026-07-03): `EQChangeSourceFX`/`EQSourceOff` with `EQLevel:1` + a real `source_name` reliably toggles `EQStat` for that one source, round-tripped On->Off->On with bands unchanged. The "Load PEQ profile" row was initially corrected to an `EQLevel: 1` payload inferred by symmetry with the confirmed RoomFit row -- that inference turned out to be wrong; see the row below. |
| 2026-07-03 | (Corrects the "Load PEQ profile" row above) `EQv2SourceLoad` with `{"EQLevel":1,"pluginURI":"...","Name":"<profile>"}` (no `source_name`, inferred by symmetry with the confirmed RoomFit `EQv2SourceLoad` payload) loads the named PEQ profile onto whichever source the caller cares about. | **This was wrong, and worth flagging as a near-miss.** Sent that exact payload with `Name:"flat"` intending to load it onto `wifi`. The response reported `"source_name":"HDMI"` -- a source nobody requested -- and a follow-up read of `wifi` confirmed it was completely untouched (still the prior profile, unchanged bands). Repeating with `Name:"<original profile>"` to "restore" also reported `"source_name":"HDMI"`, meaning both the test load and the restore silently landed on the same unintended source rather than on `wifi` as assumed. No band or `EQStat` corruption was observed once `HDMI` was independently re-read (its data matched what `wifi`/`line-in` already showed, consistent with the same profile having been active on multiple sources before the test) -- but this was a lucky outcome, not a guaranteed one; the same sequence performed against a fresh/unknown source could have silently swapped a live source's PEQ preset without any visible error. | Working theory: `EQv2SourceLoad` at `EQLevel: 1` operates on the device's currently-live/selected input, not a source named in the payload -- mirroring the already-documented legacy "current source, implicit" pattern (`EQChangeFX`, bare `EQOff`) rather than the explicit-`source_name` pattern used by `EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`. This never surfaced as a problem for the RoomFit row it was copied from because RoomFit's load/activation is deliberately global -- whichever source is implicitly targeted internally doesn't matter for a global read. PEQ has no such fallback: it's explicitly per-source, so an implicit "current source" target is a functional gap, not just a documentation nuance. | Marked the "Load PEQ profile" row and the RoomFit Profile CRUD table in `docs/wiim_api_notes.md` with an explicit warning not to rely on this payload. Added a caveat to the "Previewing a Saved Profile's Bands" section distinguishing RoomFit (safe, confirmed) from PEQ (unsafe, unresolved). Follow-up test (2026-07-03) resolved it: adding an explicit `"source_name":"wifi"` field to the same payload fixed the problem completely -- response and a follow-up read both correctly showed `wifi` changed to the requested profile and back. `docs/wiim_api_notes.md` updated accordingly; PEQ loads now documented as requiring `source_name`, same as every other per-source PEQ/RoomFit command except the two deliberately-global RoomFit calls. |
| 2026-07-03 | (Resolves the open question above) Omitting `source_name` from a PEQ `EQv2SourceLoad` call might be a genuine stateless "preview a saved preset's bands by name" mechanism, since the response returns full band data without touching the source being tracked. | **Ruled out -- it's a real write, not a preview, and it targets whichever input is currently live/active on the device.** Disambiguating test: with `HDMI` set as the device's active input, a no-`source_name` load of `"flat"` reported `"source_name":"HDMI"` in its response, and a direct follow-up read of `HDMI` (no second load in between) confirmed `HDMI` itself now held `Name:"flat"` with all-zero gains -- a real, persisted change. Repeating the test after switching the device's active input to `wifi` reproduced the identical pattern: the no-`source_name` load's response reported `"source_name":"wifi"` this time, and a direct read confirmed `wifi` had changed to `"flat"`. The target tracked the live input switch exactly, ruling out both a fixed default (e.g. always `HDMI`) and a stateless no-op. | `EQv2SourceLoad` at `EQLevel: 1`, when `source_name` is omitted, applies to the device's currently-active input rather than failing or defaulting to nothing -- there is no PEQ-level equivalent to RoomFit's deliberate global/empty-`source_name` scope; PEQ silently falls back to "whatever's currently playing." | Documented in `docs/wiim_api_notes.md` (RoomFit Profile CRUD caveat and "Previewing a Saved Profile's Bands" section) as **ruled out and unsafe** rather than merely unconfirmed: never call `EQv2SourceLoad` without `source_name` for PEQ, since doing so silently overwrites whichever source the user is actually listening to, with no error. The explicit-`source_name` load-then-restore pattern remains the only confirmed-safe way to inspect a saved PEQ preset's bands. Both `HDMI` and `wifi` were left holding `"flat"` on the test device after this investigation and needed restoring back to `M16` via explicit-`source_name` loads. |
| 2026-07-03 | (Corrects/strengthens the "PEQ Preset Commands" row above) `EQGetLV2List`/`EQGetLV2NewList` returning `{"status":"Failed"}` was ambiguous -- could mean the command is recognized but the call shape was wrong, not necessarily that the command is dead. | **Confirmed dead, not just unconfirmed.** Retested with `EQLevel` added to `EQGetLV2NewList`'s payload, and with both commands pointed at the older `Eq4p` plugin URI instead of the modern `EqNp` one (bare and JSON forms) -- all four additional attempts returned `{"status":"Failed"}`, for five failing call shapes total across this and the prior row. Separately, `EQRenameLV2Preset` (the one command in this table with no known `EQv2*` equivalent) was tested directly against a disposable test profile and also returned `{"status":"Failed"}`, confirmed by a follow-up list read showing the name genuinely unchanged. | Unknown why these commands fail specifically -- `{"status":"Failed"}` doesn't distinguish "wrong parameters" from "plugin/subsystem removed" from any other structural rejection. What's now settled is *that* they fail consistently across every reasonable parameter variation, not *why*. | Strengthened the "unverified" callout in `docs/wiim_api_notes.md`'s "PEQ Preset Commands" section to "confirmed non-functional on current firmware," directing readers to the `EQv2*` family instead. Flagged a genuine capability gap: **no known API command renames a saved PEQ/RoomFit profile** -- neither this legacy family nor the confirmed `EQv2*` family exposes rename. Documented the only known workaround (save-as-new-name via `EQSourceSave`, then `EQv2Delete` the old name), which loses the original `UpdateAt` metadata. |
| 2026-07-03 | (Resolves the open question raised in the 2026-07-03 "Source Enumeration" row above) Whether `source_name:"udisk"` (listed by `getAudioInputCapbility` as a distinct hardware input on Amp Ultra) returns genuinely distinct PEQ data from `"wifi"`, or is handled as one of the API's "any string returns default-looking data" fallback cases, was untested. | **Resolved: `udisk` is not a real PEQ source.** `EQGetLV2SourceBandEx` with `source_name:"udisk"` returned `Name:""` and an all-default L/R-mode template (stock ISO frequencies, zero gain on every band) -- the same signature already documented for nonexistent source names, and visibly different in shape from `wifi`'s populated `Name:"M16"` Stereo-mode response. Confirmed further by attempting a write: `EQv2SourceLoad` targeting `source_name:"udisk"` returned `{"status":"Failed"}` outright, unlike genuine sources (`wifi`, `HDMI`, `line-in`), which accept loads normally. Separately noticed: `udisk` appears in `getAudioInputCapbility`'s output but never in `getAudioInputEnable`'s (confirmed across all four device-owner-tested devices -- not `enable:0`, simply absent from the list). | `getAudioInputCapbility` reports a broader hardware-capability concept (the device can physically play from an attached USB drive) that is a different concept from the LV2 plugin's addressable per-source PEQ storage slots. `getAudioInputEnable` tracks the narrower set of inputs the app itself treats as switchable, and the PEQ engine's behavior agrees with that narrower list, not the broader capability list -- `udisk`'s absence from `getAudioInputEnable` was the signal pointing at this before the PEQ-layer test even confirmed it directly. Root cause confirmed (device owner, 2026-07-03): USB playback on Amp Ultra is implemented as a media server routed through the same network-playback pipeline as `wifi` -- it isn't a distinct physical input line, so it never appears as a switchable input, even with a USB drive connected and actively playing (tested directly). | Documented in `docs/wiim_api_notes.md` ("Source Enumeration" section) with the practical consequence spelled out: build a PEQ source picker from `getAudioInputEnable`'s output, not `getAudioInputCapbility`'s -- the former already excludes `udisk` (and presumably other capability-only, non-addressable entries) without needing a hardcoded denylist, which is more robust than filtering `udisk` out by name. Confirmed safe by direct testing with active USB playback. Confirms the original "PEQ Source Names" table's claim that `wifi` already covers USB disk. |
| 2026-07-04 | Smoke issue #163: the `wifi` source's PEQ bands changed unexpectedly right after a RoomFit profile push, hypothesized as `EQLevel` cross-contamination -- a RoomFit write (`EQLevel: 2`) somehow bleeding into the PEQ (`EQLevel: 1`) storage for the same source. | **Disproved.** App-log analysis of the session showing the symptom found a "Load into Editor" action against a **PEQ** preset on `wifi`, timestamped ~70 seconds before the RoomFit push that was originally blamed. The RoomFit push's own write/read/save calls all correctly carried `EQLevel: 2` and never touched `wifi`'s `EQLevel: 1` storage -- the PEQ change was real, but it was `wifi`'s own preset load activating live (the exact mechanism #166 in this same pass adds a confirmation dialog for), not a RoomFit/PEQ cross-write. | No cross-contamination exists between `EQLevel` values; they are fully independent storage namespaces, consistent with everything else confirmed about the LV2 plugin's level-keyed layout. The apparent correlation was coincidental proximity in time between two unrelated user actions in the same session, not causation. | Reassigned smoke #163 to #166 in `docs/smoke_test_issues.md` (Status: `REASSIGNED`) rather than carrying it as a separate open RoomFit/PEQ isolation bug. No code change needed for #163 itself -- #166's confirmation dialog for PEQ preset preview/load actions is the actual fix for what users were experiencing (an unexpected live PEQ change from an action that didn't look like it should have one). |
| 2026-07-04 | (Corrects the RoomFit Band Read/Write section's example payloads, in place since 2026-06-14) `EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`/`EQSourceSave` at `EQLevel:2` treat `source_name` the same way PEQ does -- a real value (e.g. `"wifi"`) selects one specific per-source RoomFit storage slot and is required. | **Wrong -- RoomFit is a single global buffer, not per-source at all.** Direct comparison test against `EQGetLV2SourceBandEx`+`EQLevel:2`: `source_name:""`, `source_name:"HDMI"`, and an arbitrary nonexistent string (`"zzz_bogus_source"`) all returned byte-identical data (`Name:"Lođa zatvorena REW"`, identical L/R bands; the empty-string input was echoed back as `source_name:"default"`). But `source_name:"wifi"` returned a completely different dataset: a distinct filter curve (different modes/freqs/gains, including HP/LP shapes) with an empty `Name`, and this data does not appear anywhere in `EQv2GetNewList`'s profile list. A follow-up write test confirmed these are genuinely separate storage, not a display artifact: a marker value (`l_gain:-9.99`) written via `EQSetLV2SourceBand` with `source_name:"wifi"` never appeared when reading back via `source_name:""` or `source_name:"HDMI"`. Separately, `EQSourceSave` succeeded (`{"status":"OK"}`, profile appeared in `EQv2GetNewList`) with all three tested forms of `source_name` -- a real value (`"wifi"`), empty string (`""`), and the key omitted entirely -- contradicting the "required real value" assumption for save as well. | RoomFit is addressed as a single global buffer/profile-selection mechanism via `source_name:""` (the "default" slot) -- the same one `EQv2SourceLoad` (no `source_name` at all) reads and writes, and the one the WiiM app and profile-list system treat as "the" RoomFit state. This matches the already-established product understanding that RoomFit is global, never per-source (CLAUDE.md). The `"wifi"` dataset is most likely an orphaned leftover from this project's own 2026-06-14 testing, performed under the same wrong per-source assumption this row corrects -- the LV2 plugin's generic architecture happily stores data under any `source_name` string given, including at `EQLevel:2`, but only the `""`/"default" slot is what actually matters to the product. | Corrected the "RoomFit Band Read/Write" section's example payloads in `docs/wiim_api_notes.md` from `"source_name":"wifi"` to `"source_name":""`, and the RoomFit Profile CRUD table's "Save current as RC profile" row to omit `source_name` entirely, matching its `EQv2SourceLoad`/`EQv2Delete`/`EQv2GetNewList` siblings (all of which already omit it) rather than presenting `""`/real-value/omitted as three equally-valid options -- listing multiple "confirmed working" forms for one field was flagged as its own misimplementation risk, since it invited picking the familiar-but-wrong real-value form. Split the combined PEQ+RoomFit `source_name` cheat sheet into two separate tables (per readability feedback) and added an explicit warning: never use a real source name for any RoomFit command. Left unresolved and flagged as low-priority: whether `EQSourceSave`'s saved *content* actually differs between the omitted/empty-string/real-value forms -- all three returned success but weren't independently compared before the disposable test profiles were deleted. |
| 2026-07-04 | (Follow-up to the RoomFit `source_name`/`EQSourceSave` row immediately above) Since RoomFit's `EQSourceSave` was just confirmed to ignore `source_name` entirely (any form works, always saves the same global buffer), PEQ's `EQSourceSave` might behave the same way -- i.e. `source_name` might not actually matter for which source's data gets captured, given PEQ presets are already documented as globally loadable objects once saved (CLAUDE.md: "PEQ presets are also global"). | **Wrong -- PEQ's `EQSourceSave` genuinely requires a real `source_name` to capture the intended source.** First test attempt was inconclusive: compared `wifi` and `line-in`, but both already held identical data (`Name:"M16"`, identical bands) before any test ran, so all three save variants producing identical results proved nothing. Retested after deliberately diverging the two sources: wrote a marker (`i_gain:-7.77`, an inert Off-band parameter with zero audible effect) to `wifi` only via `EQSetLV2SourceBand`, confirmed `line-in` unaffected (`i_gain:0.000` unchanged). Device's live/active input throughout was `HDMI` (neither test source). Saved three disposable profiles -- `source_name:"wifi"` (real), `source_name:""` (empty), `source_name` omitted -- then loaded each onto `line-in` and read back: the real-value save showed `i_gain:-7.770` (correctly captured `wifi`'s marker); both the empty-string and omitted saves showed `i_gain:0.000` (did not capture `wifi`'s marker). | Unlike RoomFit (confirmed a single global buffer, row immediately above), PEQ's per-source buffers are genuinely distinct, and `EQSourceSave` needs `source_name` to know *which* source's current settings to snapshot into the new profile. This doesn't contradict "PEQ presets are globally loadable" (CLAUDE.md) -- that describes what a preset becomes *after* saving (portable, source-independent); the save command itself still needs to identify what to capture. Exactly what the empty/omitted forms captured instead (the live `HDMI` input, or some other fallback) wasn't pinned down -- `HDMI`'s exact live `i_gain` wasn't recorded, so both remaining candidates were indistinguishable in this test -- but this doesn't affect the practical conclusion either way. | Corrected the PEQ `source_name` cheat-sheet row for `EQSourceSave` in `docs/wiim_api_notes.md` from "not independently stress-tested, treat as inherited convention" back to a confirmed hard requirement, and added the same note to the RoomFit Profile CRUD table's "Save current as PEQ profile" row. Flagged explicitly as the opposite rule from RoomFit's `EQSourceSave` (which should omit `source_name`) -- same command name, opposite correct usage depending on `EQLevel`, the single easiest cross-domain mistake to make in this file. |
| 2026-07-04 | Smoke issue #164: adding `"rc_output": "AUDIO_OUTPUT_SPEAKER_MODE"` to `write_roomfit()`'s `EQSetLV2SourceBand` write payload (matching a field observed in the WiiM app's own write traffic) would make profiles saved via this app show `Type: "RC"` instead of `"Custom"` in the device's profile list. | **Contradicted by this repo's own already-confirmed documentation.** `docs/wiim_api_notes.md`'s RoomFit "Quirk" note (added in the immediately-preceding commit, from direct hardware testing) states plainly: "Profiles saved via `EQSourceSave` with `EQLevel: 2` get `Type: \"Custom\"` instead of `\"RC\"`... The `\"RC\"` type is reserved for profiles created by the device's own RoomFit calibration process." Adding `rc_output` to the write payload does not change which command actually sets `Type` (`EQSourceSave`, not `EQSetLV2SourceBand`), so it cannot produce the claimed effect. | The `rc_output` claim was carried over from an earlier planning draft written before the later hardware-testing pass (same session, different investigation) confirmed `Type` is permanently `"Custom"` for app-saved profiles regardless of payload fields. The two findings were never reconciled before landing in the same diff. | Kept the `rc_output` field in the write payload (harmless -- it matches real app traffic and is documented as a valid, if inert for this purpose, profile metadata field) but corrected the `docs/smoke_test_issues.md` #164 row to drop the false "Type: RC" claim. No functional code change; this row exists so the false claim doesn't get treated as confirmed behavior later. |
| 2026-07-04 | `EQSetLV2ChannelMode` (documented in `docs/wiim_api_notes.md`'s PEQ "Write" section, inherited unverified from community reference material) reliably switches a source's channel mode, and its use in `WiiMAdapter._set_channel_mode()` (called before every `write_peq()`, `wiim_adapter.py:469-485`) is a working, necessary step. | Production `wiim_api.log` shows three independent real-world calls to this command -- 2026-06-29 (device 192.168.0.28), 2026-07-01 (192.168.0.29), 2026-07-04 (192.168.0.222) -- targeting `source_name:"bluetooth"` (one `channelMode:"L/R"`, two `channelMode:"Stereo"`). All three returned `{"status":"Failed"}`. No successful call to this command has ever appeared in logs, for any source. `_set_channel_mode()` does not inspect the response at all -- `await self._client.command(command)` discards the return value -- and `WiiMResponseError` is only raised on a non-200 HTTP status (`wiim_http.py`), which this isn't (HTTP 200, `Failed` body) -- so this failure has never surfaced anywhere except the raw DEBUG log line. | Unconfirmed. Two explanations remain indistinguishable from available evidence: (a) the command doesn't work at all on current firmware (matching the already-confirmed-dead "PEQ Preset Commands" family), or (b) `bluetooth` specifically doesn't support channel-mode switching. Separately: `EQSetLV2SourceBand` already carries its own `channelMode` field as part of the write that follows this call (`_write_peq_batch_lr`/`_write_peq_sequential_lr`), so it's plausible the pre-switch call is simply redundant and its failure has no functional consequence -- but this has only been inferred from the absence of a related smoke-test report, not confirmed by checking whether a `bluetooth` L/R push actually lands in the correct mode end-to-end. | Corrected `docs/wiim_api_notes.md`'s PEQ "Write" section: `EQSetLV2ChannelMode` is now documented as unconfirmed/observed-failing rather than stated as working, citing the three logged failures. Flagged the deeper open question -- whether `_set_channel_mode()`'s unchecked failure has any real effect on write correctness -- as requiring either (1) a live test confirming a `bluetooth` L/R push produces correct L/R band data end-to-end despite this failure, or (2) code changes to check the response and either tolerate a documented-safe failure explicitly or surface it. Not fixed in code as part of this documentation pass -- see follow-up task. |
| 2026-07-04 | (Resolves both open questions in the row above) Whether `EQSetLV2ChannelMode`'s failure was specific to `bluetooth`, and whether `_set_channel_mode()`'s unchecked failure has any real effect on write correctness, was unconfirmed. | **Resolved on both counts.** (1) Universality: retested `EQSetLV2ChannelMode` + `channelMode:"L/R"` against `wifi` and `HDMI` in addition to `bluetooth` -- all three returned `{"status":"Failed"}`. Not bluetooth-specific; dead across the board. (2) Correctness impact: immediately after reproducing the failing pre-switch call against `bluetooth`, a real `EQSetLV2SourceBand` write with `channelMode:"L/R"` embedded (matching the app's actual write shape) succeeded (`{"status":"OK"}`), and a follow-up read confirmed `channelMode` had genuinely switched to `"L/R"` with two per-channel test markers (`l_gain:-3.33`/`-6.66`) landing correctly in `EQBandL`/`EQBandR` respectively. The pre-switch call's failure had no effect on the write's correctness. | `EQSetLV2ChannelMode` is dead on current firmware, same category as the already-confirmed-dead "PEQ Preset Commands" family -- not a per-source limitation. `EQSetLV2SourceBand`'s own inline `channelMode` field is sufficient by itself; the separate command was never necessary for this app's actual (full-band) write shape. | Corrected `docs/wiim_api_notes.md`'s PEQ "Write" section: `EQSetLV2ChannelMode` is now documented as confirmed non-functional (not merely "observed failing"), and explicitly noted as harmless given the confirmed-working inline-`channelMode` alternative. `src/adapters/wiim_adapter.py`'s `_set_channel_mode()` still issues this dead call before every `write_peq()` -- functionally inert but worth removing as cleanup; not fixed in code as part of this documentation pass. |
| 2026-07-04 | (Unplanned byproduct of the row above) A source with no explicit L/R-mode data yet, or switching an existing source's mode via a partial write, would return/produce a blank or default-template band set, same as the already-documented "API accepts any source name and returns a default template for nonexistent ones" behavior. | The confirmed-working L/R write against `bluetooth` (row above) specified only one parameter (`l_gain`) per channel, yet the read-back showed five other bands (`a`-`e`) fully populated with a distinct, real-looking filter curve completely different from `bluetooth`'s Stereo-mode data (`Name:"M16"`) -- data this test never wrote and had no reason to expect. The device revealed pre-existing L/R-mode band data for `bluetooth` left over from some earlier, unrelated configuration; the mode switch and the partial write neither reset it nor replaced it with defaults. | PEQ band storage is keyed by `(source_name, channelMode)`, not `source_name` alone -- a source configured in both Stereo and L/R at different times retains two independent stored band sets, and switching mode via `EQSetLV2SourceBand`'s inline `channelMode` field surfaces whichever set matches the new mode, untouched except for whatever the write explicitly specifies. | Documented in `docs/wiim_api_notes.md` ("Write" section): any write that changes `channelMode` must include the full intended band set in the same call, since a partial write surfaces old, possibly unrelated per-mode data rather than a blank template. **Flagged as an open safety concern, not yet investigated:** `WiiMAdapter._write_peq_sequential`/`_write_peq_sequential_lr` (`wiim_adapter.py`) send one band per `EQSetLV2SourceBand` call with 100ms gaps, each call carrying the target `channelMode` -- meaning a mode-switching sequential write could leave the device processing audio with a mix of newly-written and stale, unrelated per-mode bands for the full write duration, and an interruption mid-write could leave the device in a state matching neither the old nor the new intended configuration. Needs a live test against a device/path that actually exercises the sequential (non-batch) write branch to determine real severity -- not resolved here; see follow-up task. |
| 2026-07-04 | (Resolves the open safety concern flagged in the row above) Whether `WiiMAdapter._write_peq_sequential`/`_write_peq_sequential_lr`'s one-band-per-call path genuinely exposes a mixed/stale-data window during a channel-mode-switching write, and whether `SafeWrite`'s rollback is immune to the same risk, was unconfirmed pending a live test against a device exercising the non-batch path. | **Confirmed as a real risk by code analysis plus the row-above evidence -- no new live device session was available to independently re-probe mid-sequence, so this is inference from an equivalent already-observed case, not a fresh reproduction.** The row-above test (a single-band `EQSetLV2SourceBand` call against `bluetooth`, carrying `channelMode`, that surfaced stale bands `a`-`e`) is structurally identical to iteration 0 of `_write_peq_sequential_lr`'s loop -- same command, same one-band payload shape, same inline `channelMode`. Since the device's `(source_name, channelMode)`-keyed storage has no time-based behavior (state after any call is purely "whatever was last written to that band, that slot"), the same result generalizes to every later iteration: for the full ~1s+ duration of a 10-12 band sequential run (100ms sleep x N calls, plus real per-call HTTP round-trip time on top), the device processes live audio through a mix of newly-written and stale/unrelated bands, not merely a cosmetic display artifact. Separately, `SafeWrite._rollback()` (`safe_write.py`) restores state by calling the *same* `WiiMAdapter.write_peq()` the forward write used, which re-checks `capabilities.supports_batch_write` and takes the identical one-band-per-call path on non-batch devices -- rollback is not a special atomic path. Whether that makes rollback actually dangerous depends on the scenario: if the failed write changed `channelMode`, the mode being rolled back to was never touched by the failed write (only the new mode's storage was), so rollback's sequential calls are simply re-confirming values already sitting there -- low practical risk. But if the failed write did NOT change `channelMode` (new bands overwriting the same mode's storage), the forward write already partially clobbered that storage before failing, so rollback must sequentially restore it band-by-band from a partially-corrupted starting point -- and an interruption during *that* sequential rollback (crash, network drop) can leave the device in a third state matching neither the pre-write nor the attempted-write configuration, which is exactly the failure mode `SafeWrite`'s five-step protocol exists to rule out. | `_write_peq_sequential`/`_write_peq_sequential_lr` were built to accommodate devices that reject a full-payload `EQSetLV2Band`/`EQSetLV2SourceBand` write, without accounting for the fact that a partial write's safety depends on whether the target mode's storage already held correct data before the operation started -- `write_peq()` has no way to know this since it never inspects the device's pre-write mode. `SafeWrite._rollback()` assumed "call `write_peq()` again with the backed-up (complete) settings" was inherently safe because the *content* is complete; it didn't account for the *delivery* still being non-atomic on the same non-batch devices where the forward write is non-atomic. Also worth noting: `capabilities.supports_batch_write` defaults to `False` and is set `False` on *any* probe exception (`capability_prober.py::_probe_batch_write`), not just for confirmed-incapable models -- so this sequential path is reachable via transient probe failures on otherwise-capable devices, not only a fixed legacy-hardware subset. | **Documentation only in this pass -- no behavioral code change**, per CLAUDE.md's guidance to document a confirmed-but-unvalidated-fix risk, add `# TODO:` markers, and keep the conservative existing behavior rather than ship an unverified change to a safety-critical write path without hardware access to confirm it actually helps. Added `# TODO:` markers at `WiiMAdapter._write_peq_sequential`/`_write_peq_sequential_lr` (`wiim_adapter.py`) and `SafeWrite._rollback` (`safe_write.py`) referencing this row. Proposed fix, not implemented (needs hardware validation + product sign-off): (a) for any write that changes `channelMode`, attempt one full-payload batch write first regardless of the cached `supports_batch_write` flag -- a cached `False` doesn't distinguish "device categorically rejects batch payloads" from a transient probe failure -- and only fall back to the one-band-per-call loop if that attempt is rejected; (b) where the sequential fallback is genuinely unavoidable, bracket the mode-switching write with `EQSourceOff`/`EQChangeSourceFX` (already confirmed working, see PEQ Enable/disable above) so the source is muted for the exposure window instead of playing live audio through the stale/new band mix -- this addresses the audible-glitch risk but not the interrupted-mid-write data-integrity risk; (c) implement both inside `write_peq()` itself so `SafeWrite._rollback()` inherits the fix automatically instead of needing a parallel code path. See `docs/wiim_api_notes.md` PEQ "Write" section for the user-facing version of this note. |
| 2026-07-04 | (Narrows the scope of the row above) Fixing the sequential-write mode-switch risk would require always sending the full intended band set (real filters + explicit OFF for unused bands), rather than a sparse/partial payload, on every write. | **Already true -- not a gap.** `generate_wiim_band_array()` (`src/translator/wiim_generator.py`) already pads every write, batch or sequential, to the device's full `max_bands` length with explicit OFF entries for anything unused ("If fewer than max_bands, remaining bands are padded as OFF"); `_write_peq_sequential`/`_write_peq_sequential_lr` are fed this same fully-padded array. So the content is never actually incomplete -- the risk documented in the row above is purely about delivery granularity: on a `supports_batch_write:false` device, the device only accepts one band per `EQSetLV2SourceBand` call (the reason the sequential fallback exists at all), so a band's already-decided final value (even "OFF") has no effect on the device until *that specific band's own call* has gone out. No amount of front-loading content changes this, since these devices structurally cap each call at one band. | The two concerns -- "is the intended content complete" and "is the multi-call delivery atomic" -- are independent, and only the first was ever in question; the second is an inherent constraint of non-batch hardware, not a bug this app introduced. | No code change (none was needed for the content side). Re-scoped the row above's proposed fix as **low-priority tech debt**, not a correctness gap: the trigger requires *both* (a) a device that failed or lacks the batch-write probe and (b) a write that changes `channelMode` on that same call -- a narrow intersection, and `SafeWrite`'s existing read-back verification already catches a wrong end state (rollback's own non-atomicity aside, per the row above). Downgraded urgency in the `# TODO:` markers at `WiiMAdapter._write_peq_sequential`/`_write_peq_sequential_lr` (`wiim_adapter.py`) and `SafeWrite._rollback` (`safe_write.py`) accordingly -- documented corner case, not queued for implementation. |
| 2026-07-04 | Whether `supports_batch_write` actually reads `True` or `False` on any of this project's owned/tested devices had never been checked -- `capability_prober.py`'s probe logic was implemented and reasoned through (rows above), but no one had looked at the GUI Diagnostics panel's capability dump against real hardware to see which value each device actually reports. | **Confirmed via the GUI Diagnostics panel** (device owner, 2026-07-04): of the 4 owned devices, only **WiiM Mini** reports `supports_batch_write: false`. WiiM Amp Ultra, WiiM Sound, and WiiM Sound Lite all report `true`. | Unconfirmed why WiiM Mini specifically rejects the full-payload `EQSetLV2Band` probe write while the other three accept it -- plausibly related to it being the only device in this lineup with no RoomFit hardware and the oldest/lowest-powered platform (legacy `Muzo_Mini` branding, 2026-06-12 row), but this is speculation, not confirmed by this test. | Added a "Batch write" column to `docs/wiim_api_notes.md`'s Device Capability Matrix with these four confirmed values. This narrows the practical scope of the two rows above (sequential-write mode-switch tech debt): on this project's currently-owned hardware, only WiiM Mini can ever reach `_write_peq_sequential`/`_write_peq_sequential_lr`, and WiiM Mini already has no RoomFit, so `RoomFitSafeWrite`'s equivalent write path is unaffected on that model. WiiM Ultra (non-Amp) and WiiM Amp Pro/Pro/Pro Plus/Amp remain unconfirmed -- not owned/tested. |
| 2026-07-04 | (Closes the "worth removing as cleanup" item left open by the two 2026-07-04 `EQSetLV2ChannelMode` rows above) `WiiMAdapter._set_channel_mode()` still issued the confirmed-dead `EQSetLV2ChannelMode` command before every `write_peq()`. | N/A -- not a new hardware finding, just the deferred code cleanup being done now that both correctness questions were resolved. | N/A. | Removed `_set_channel_mode()` and its call site from `write_peq()` (`src/adapters/wiim_adapter.py`); `channelMode` is now set only inline on the `EQSetLV2SourceBand` write, as already documented. Updated the stale comment in `safe_write.py` referencing the removed method. In `test_wiim_adapter.py`: adjusted call-count assertions in `TestWritePeqBatch`/`TestWritePeqLRBatch`/`TestWritePeqSequential`/`TestWritePeqLRSequential`/`TestReadPeqPresetPreview` (one fewer command per `write_peq()` call), and replaced `TestSetChannelMode` (which asserted the dead command *was* sent) with `TestNoStandaloneChannelModeCommand`, a regression guard asserting it never is. `ruff check src/` and `mypy src/` both clean; full `test_wiim_adapter.py` + `test_safe_write.py` suite (109 tests) passes. |
| 2026-07-04 | RoomFit's per-buffer band commands (`EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`) omit `source_name` entirely, the same as RoomFit's profile-CRUD commands (`EQv2SourceLoad`/`EQv2Delete`/`EQv2GetNewList`/`EQSourceSave`). | **Wrong -- band read/write needs `source_name` present as `""`, not omitted.** Omitting the key entirely (as `capability_prober.py`'s `_probe_roomfit()` level-2 probe and `wiim_adapter.py`'s `read_roomfit()`/`write_roomfit()` did) fails against real hardware: the probe never advances `roomfit_level` past 1, `merge_into()`'s ladder (2026-07-04, RoomFit ladder row) then correctly caps `roomfit_level` below 2 on every device, and `_on_capabilities_ready()` sees `roomfit_level < 2` for every device and skips the EQ_TYPE step entirely -- reported by a user as "all devices default to PEQ, EQ Type step never shown." | An earlier pass generalized the profile-CRUD finding (`source_name` genuinely omitted for `EQv2SourceLoad` etc., confirmed 2026-07-02) into a blanket "RoomFit never sends `source_name`" rule and applied it to the band read/write commands too. Those need the *empty-string* form instead -- a third, distinct case from "omitted" that the generalization collapsed away. `docs/wiim_api_notes.md`'s "source_name & EQLevel Reference" section now states the three forms explicitly to prevent this from re-collapsing. | Added `source_name=""` to `capability_prober.py`'s level-2 probe and to both `write_roomfit()` write branches (L/R and Stereo) in `wiim_adapter.py`. `read_roomfit()`'s buffer read now calls the existing `self._read_fx_status("", eq_level=2)` helper directly (already correct, already used by `get_roomfit_status()`) instead of a second, separately-maintained `encode_wiim_command` call -- removes the duplication that let the two drift apart in the first place. Added a guard to `encode_wiim_command()` (`wiim_commands.py`) that raises if `eq_level=2` is combined with a real (non-empty) `source_name`, so this exact mistake can't recur silently at any RoomFit call site, present or future. Updated `test_read_roomfit_success`/`test_write_roomfit_success` (`test_wiim_adapter.py`) and the RoomFit probe tests (`test_capability_prober.py`) to assert the empty-string form is present, not just that a real source name is absent (the gap that let this regression through undetected). |
| 2026-07-04 | (Follow-up to the row above, found during code review) The `encode_wiim_command()` guard as first shipped only rejected a *real* `source_name` for `eq_level=2` -- it did not reject an *omitted* `source_name` for the band/toggle commands, which is the actual mistake that caused the regression in the row above. A hardcoded allowlist of the 4 band/toggle command names was also "allow by default": any future RoomFit command not added to it would silently fall through to the profile-CRUD (omission-allowed) behavior. | N/A -- not a new hardware finding; caught by a code-review pass re-reading the shipped guard against its own stated purpose, not by a device test. | The guard's initial fix only closed the "real value" direction of the mistake because that was the form the fix's own regression test happened to exercise first; the "omitted when it should be empty" direction -- the one that actually caused the regression -- wasn't separately guarded until reviewed. | Split the guard into two exhaustive, doc-driven sets in `wiim_commands.py`: `_ROOMFIT_REQUIRES_EMPTY_SOURCE_NAME` (band/toggle commands) and `_ROOMFIT_REQUIRES_OMITTED_SOURCE_NAME` (profile-CRUD commands). `encode_wiim_command()` now raises for: a real `source_name` at `eq_level=2` (any command); an omitted `source_name` for a command in the first set; and an omitted `source_name` for any `eq_level=2` command in *neither* set -- so a new, unclassified RoomFit command fails loudly instead of silently defaulting to "omission is fine". Also tightened `capability_prober.py`'s `_probe_profile_enumeration()` to require the real success shape's `"custom"`/`"preset"` keys rather than just rejecting a `"status"` key (a malformed dict missing both would otherwise still read as success, reproducing #170's false-positive class with a different malformed shape). Added `test_roomfit_unclassified_command_rejects_omitted_source_name` (`test_wiim_commands.py`) and `test_malformed_dict_response_not_treated_as_enumeration_support` (`test_capability_prober.py`). `ruff check src/` and `mypy src/` clean; full `test_wiim_commands.py`/`test_wiim_adapter.py`/`test_capability_prober.py` (117 tests) and `test_gui_pages.py`/`test_smoke_regression_wizard.py`/`test_gui_views.py` (118 tests) pass. |
| 2026-07-05 | `CapabilityProber._probe_roomfit()`'s level-4 write-capability test (save the buffer to a throwaway name `__wiim_rew_sync_probe__`, then delete it) is a non-destructive, read-only-equivalent check that doesn't alter which RoomFit profile is active. | **Wrong -- this probe silently deactivates the user's real active RoomFit profile on every device connection.** User-reported symptom: neither "Presets on Device" nor the "Name Profile" step highlighted the active RoomFit profile, even though a profile was genuinely selected in the WiiM app moments before connecting. Confirmed via `wiim_api.log`: the very first `EQGetLV2SourceBandEx` status read after connecting already showed `"EQStat":"On","Name":""` -- `EQStat` (RoomFit correction is on) didn't match `Name` (no profile identified), and 3 real saved profiles existed. User independently confirmed via live testing: reaching the "Name Profile" step (which triggers a `get_roomfit_status()` read, downstream of the connection-time probe) coincides with the WiiM Home app itself showing RoomFit as deselected. | The RoomFit buffer "adopts" whatever name it's saved under (already documented, RoomFit Write workflow section) -- this was understood for *intentional* saves, but not accounted for in the probe's own throwaway save. Saving to `__wiim_rew_sync_probe__` renames the buffer's identity to that name; the probe's own cleanup then deletes that exact profile, leaving the buffer pointing at a name that no longer exists. Subsequent reads report `Name:""` (orphaned) even though `EQStat` is unaffected -- and the WiiM app's own "currently active profile" UI apparently keys off this same Name association, so it shows RoomFit as deselected even though the DSP toggle itself never changed. | Capture the buffer's pre-probe `Name` from the already-in-hand level-2 probe response (`band_resp`, no extra API call needed) before the level-4 save/delete sequence runs. After cleanup (`EQv2Delete` of the throwaway profile), if a pre-probe name existed, restore it via `EQv2SourceLoad`. Skipped entirely if the buffer had no name to begin with (fresh device, no saved profiles yet). Documented the general hazard in `docs/wiim_api_notes.md`'s RoomFit Write workflow section: any code that saves to a throwaway/temporary name and deletes it must capture and restore the pre-save name, not just this one call site. Added `test_roomfit_level_4_probe_restores_active_profile` and `test_roomfit_level_4_probe_skips_restore_when_no_prior_active_name` (`test_capability_prober.py`); existing `test_roomfit_level_4_full_support` unaffected. `ruff check src/` and `mypy src/` clean; full `test_capability_prober.py` (25 tests) passes. |
| 2026-07-05 | (Same-day follow-up, PEQ side) `CapabilityProber._probe_batch_write()`'s read-then-write-back-unchanged test (`EQGetLV2BandEx` read, `EQSetLV2Band` write of the identical bands) is content-preserving and therefore has no observable effect on the device. | **Wrong in the same way as the RoomFit row above -- a raw write drops the live source's `Name` association even with byte-identical band values.** User noticed the active PEQ preset shown in the WiiM Home app reverting to unnamed/"Custom" after using this app, with the same filters still in place, and asked whether it was related to the RoomFit finding above. Confirmed directly via a curl reproduction against real hardware (device owner, 2026-07-05): read `EQGetLV2SourceBandEx` for source `HDMI` -- `Name:"M16"`; read the bare `EQGetLV2BandEx` (what the probe uses) -- also `Name:"M16"`, `source_name:"HDMI"` (confirming `HDMI` was the live source); write those exact same `EQBand` values back via bare `EQSetLV2Band` (`{"status":"OK"}`); re-read `EQGetLV2SourceBandEx` for `HDMI` -- `Name:""`. `EQStat` and every band value were unchanged throughout; only `Name` was lost. | Same underlying device behavior as the RoomFit row above, different command family: writing bands -- even unmodified ones -- to a source's PEQ buffer via `EQSetLV2Band`/`EQSetLV2SourceBand` clears that source's "this equals saved preset X" association, independent of whether the written values actually differ from what's already there. `_probe_batch_write()` runs unconditionally on every PEQ-capable device connection (whenever `supports_peq`), so this fired on effectively every session. Compounding this, `_probe_batch_write()` uses the bare/live-source commands (`EQGetLV2BandEx`/`EQSetLV2Band`) rather than the per-source ones production code (`write_peq()`) actually uses, because the prober runs before the wizard's source is chosen and has no `source_name` of its own yet -- so the probe's write lands on whatever the device happens to be playing from at connection time, not necessarily the source the user is about to configure in this app. | Capture the pre-write `Name` *and* the read response's own echoed `source_name` (the live source, discoverable this way even though the prober doesn't have one configured yet) before the write-back. After the write attempt (success or failure -- a client-side timeout doesn't guarantee the device didn't still apply it), restore the name via `EQv2SourceLoad` scoped to that same `source_name` -- PEQ's `EQv2SourceLoad` requires an explicit `source_name` or it silently targets whatever's live instead (`docs/corrections.md`, 2026-07-03), unlike RoomFit's deliberately global/omitted form. Skipped entirely if the buffer had no name to begin with. Documented the general hazard (applies to any read-modify-write-back-unchanged pattern, not just this probe) in `docs/wiim_api_notes.md`'s PEQ Write section. Added `test_batch_write_probe_restores_active_preset` and `test_batch_write_probe_skips_restore_when_no_prior_active_name` (`test_capability_prober.py`); existing `test_wiim_pro_detected_correctly` and other batch-write-asserting tests unaffected (their fixtures carry no `Name`/`source_name` fields, so the restore is a no-op for them). `ruff check src/` and `mypy src/` clean; full `test_capability_prober.py` (27 tests) passes. |
| 2026-07-05 | Since `RoomFitSafeWrite.execute()` writes bands to the buffer and saves them under `profile_name`, and the buffer "adopts" whatever name it's saved under (already-documented RoomFit behavior), it was assumed the *write/save step itself* was the only thing that could change which profile shows as active -- and per the pre-verification-protocol 2026-06-14 finding, only overwriting the already-active profile's name should do that. | **Incomplete -- the mandatory post-write verification read is an equally real cause, and fires on every push regardless of which profile is targeted.** User reported (live hardware testing): pushing a brand-new RoomFit profile, or overwriting a different existing (non-active) one, made *that* profile the device's active/selected one -- contradicting both the Name Profile step's own UI text ("Saving does not make this profile active on your device") and the 2026-06-14 finding's claim that saving to a new/different name is non-disruptive. Separately, Undo after overwriting an existing profile restored the old bands correctly but left the wrong profile active. Root cause, found by code inspection (not yet independently curl-confirmed against hardware, unlike the #175/#177 rows above): `RoomFitSafeWrite.execute()`'s mandatory verification step calls `read_roomfit()`, which -- per the RoomFit architecture already documented (buffer requires a prior `EQv2SourceLoad` to select which profile a read returns) -- always issues `EQv2SourceLoad(profile_name)` before reading bands back. `EQv2SourceLoad` is a confirmed real, persisted write (2026-07-02/2026-07-03 rows above), not a stateless peek, so *every* `execute()` call makes `profile_name` the buffer's `Name` as an unavoidable side effect of verifying the write succeeded -- independent of whether `profile_name` was new, a different existing profile, or already active. | The 2026-06-14 finding predates `RoomFitSafeWrite`'s read-back-and-verify protocol (added 2026-06-29, per the "Safety before convenience" design principle) -- it characterized a bare write+save sequence tested in isolation, before verification's own `EQv2SourceLoad` was in the picture. Once verification was added, its read-back call became the dominant, previously-unaccounted-for source of the activation side effect, since it runs unconditionally on every push (new profile, non-active overwrite, or active overwrite alike) where the 2026-06-14 write/save-only test only exercised the overwrite-active case. | Added a capture-and-restore step to `RoomFitSafeWrite.execute()` (`src/adapters/safe_write.py`): reads the active profile via `get_roomfit_status()` before doing anything else, and restores it via the now-public `WiiMAdapter.load_roomfit_profile()` (previously private `_load_roomfit_profile()`, renamed to match `load_peq_profile()`'s naming and because it now needs to be called from outside the adapter) in a `finally` block after the write/verify/rollback sequence completes -- skipped when the captured name equals `profile_name` (redundant self-restore) or is empty (nothing was active). Because this runs on *every* `execute()` call, Undo's own restore-write (which also goes through `execute()`) naturally re-derives and preserves whatever the original, now-fixed push correctly left active, without needing to persist any extra state in the backup file. Flagged as not yet independently hardware-confirmed via curl the way #175/#177 were -- the mechanism follows directly from already-hardware-confirmed `EQv2SourceLoad` semantics, but the specific claim that overwriting the already-active profile keeps `EQStat` on throughout (superseding the 2026-06-14 finding) should be re-verified on real hardware. Documented in `docs/wiim_api_notes.md`'s RoomFit Write workflow section. Added `TestRoomFitActiveProfileRestore` (6 cases) and `TestRoomFitPushThenUndoPreservesActiveProfile` (a push-then-undo integration test using a stateful fake device, since this scenario spans two separate `execute()` calls that a stateless mock can't model) to `test_safe_write.py`. `ruff check src/` and `mypy src/` clean; full `test_safe_write.py` (39 tests) and `test_wiim_adapter.py` pass. |
| 2026-07-05 | The #178 fix (capture-and-restore the pre-write active RoomFit profile in `RoomFitSafeWrite.execute()`) was scoped as complete once its own unit tests passed, since `read_roomfit_preset_preview()` (`wiim_adapter.py`) already implemented the identical capture/restore shape independently for a different caller. | A `/code-review ultra` pass on the same diff found three related gaps the #178 fix alone didn't close: (1) the CLI's `get-roomfit-filters` command called `adapter.read_roomfit()` directly, with the exact same activation side effect and no restore at all -- a live, shipping bug, not a hypothetical; (2) three RoomFit push/undo tests in `test_smoke_regression_operations.py` used a bare, unspec'd `MagicMock()` adapter, so the new `get_roomfit_status()` call inside `execute()` raised `TypeError` on `await`, which was silently caught by `execute()`'s own best-effort exception handling -- the tests passed, but exercised none of the #178 restore logic; (3) `RoomFitSafeWrite.execute()` and `read_roomfit_preset_preview()` each independently reimplemented the same "skip if empty/same name, else load and log-on-failure" logic, which is exactly the kind of duplication this project's `docs/corrections.md` 2026-06-29/2026-06-30-era rows warn drifts apart over time if not consolidated. | The #178 fix was verified in isolation (its own new tests, its own call site) rather than audited against every other caller of the same underlying primitive (`read_roomfit()`) or against the codebase's own existing implementation of the identical pattern -- the same "audit one call site, miss the others" gap already documented for `write_roomfit()` bypasses (rows above, 2026-06-29/2026-06-30). | Extracted `WiiMAdapter.restore_roomfit_active_profile(original_name, current_name, *, context)` as the single shared implementation of the skip/restore decision; `read_roomfit_preset_preview()` and `RoomFitSafeWrite.execute()` both now call it instead of hand-rolling the logic. Fixed the CLI to use `read_roomfit_preset_preview()` instead of `read_roomfit()` directly. Fixed the three affected tests to properly mock `get_roomfit_status()`/`restore_roomfit_active_profile()` as `AsyncMock`s so the #178 logic is genuinely exercised rather than silently skipped. `capability_prober.py`'s two probes (RoomFit level-4, PEQ batch-write) were deliberately left as separate implementations and documented as such -- they run before a `WiiMAdapter` instance exists (probing determines what capabilities to construct one with), so they can't call the new shared adapter method and talk to the raw HTTP client directly instead; this is an architectural constraint, not an oversight. Added `TestRestoreRoomfitActiveProfile` (`test_wiim_adapter.py`, 4 cases) as the new method's own direct unit coverage, and `test_get_roomfit_filters_uses_preview_not_raw_read` (`test_cli.py`) as a regression guard for the CLI bypass. `ruff check src/` and `mypy src/` clean; full combined suite (`test_safe_write.py`, `test_wiim_adapter.py`, `test_smoke_regression_operations.py`, `test_cli.py`, `test_app_settings.py`, `test_capability_prober.py`, 332 tests) passes. |
| 2026-07-05 | (Corrects the 2026-06-14 row on RoomFit save-to-active-name behavior) Saving to the currently-active RoomFit profile name deactivates RoomFit, requiring the user to re-select it in-app. This was carried forward as an open `# TODO:` in `RoomFitSafeWrite.execute()` pending hardware re-verification, since the #178 fix's same-name-skips-restore logic depends on it being false. | **Confirmed false via curl reproduction on real hardware.** Read RoomFit status (`Name:"REW"`, `EQStat:"On"`), wrote the buffer's own unchanged bands back via `EQSetLV2SourceBand`, saved via `EQSourceSave` under the same already-active name `"REW"`, then re-read status: `Name:"REW"`, `EQStat:"On"` -- both fields identical before and after the overwrite. | The 2026-06-14 finding was most likely conflating this with a different mechanism that produces a similar-looking symptom: `RoomFitSafeWrite`'s own mandatory post-write verification read requires `EQv2SourceLoad`-ing the profile being verified (documented at length in the #178 rows above) -- if that verification/restore machinery wasn't in place yet when the original 2026-06-14 test was run (it predates `RoomFitSafeWrite` itself, added 2026-06-29), a bare write+save could have looked like it "deactivated" the profile if the test's own follow-up read happened to load something else in between, without the original investigation isolating that as a separate step. | Removed the `# TODO:` from `RoomFitSafeWrite.execute()`'s docstring in `safe_write.py` and updated `docs/wiim_api_notes.md`'s RoomFit Write workflow section to mark the original 2026-06-14 claim as superseded. No code change needed -- `RoomFitSafeWrite.execute()`'s existing behavior (skip the restore step when the written profile equals the one that was already active) was already correct under the now-confirmed real behavior; this was purely a documentation/confidence update. |
| 2026-07-05 | The #177 fix (`CapabilityProber._probe_roomfit()`'s level-4 write-capability test capturing and restoring the pre-probe active profile `Name` after its save-then-delete cleanup) was assumed to have closed the probe's side-effect gap entirely, since the hardware test behind #177 confirmed `EQStat` stayed unchanged across that sequence. | **Incomplete -- that hardware test only exercised the case where `EQStat` was already `"On"` going in.** Live user report: a device with RoomFit originally *disabled* came back with RoomFit *enabled* after nothing more than connecting to it (a routine capability probe, no user-initiated write). Root cause not yet isolated to a single command via curl (unlike #175/#177/#178) -- `# ASSUMPTION:` in `capability_prober.py` is that `EQSourceSave` (or another step in the save-then-delete sequence) flips `EQStat` from `"Off"` to `"On"` as a side effect of persisting a profile, which the original #177 test never had a chance to observe since it started from `"On"`. | Each RoomFit probe side-effect (buffer `Name`, now `EQStat`) has so far been discovered and fixed one at a time, only after a user hits it on hardware the specific starting state wasn't tested against -- the same "audit one call site/one state, miss the others" pattern already called out in the 2026-07-05 #178-follow-up row above, now recurring one level down (one *field*, not one call site). | `_probe_roomfit()`'s level-4 test now captures `EQStat` from the same level-2 read it already used for `Name`, and restores it unconditionally after cleanup via the documented DSP on/off toggle commands (`EQChangeSourceFX`/`EQSourceOff`, RoomFit "DSP on/off toggle" section, `docs/wiim_api_notes.md`) -- `EQChangeSourceFX` if it was `"On"`, `EQSourceOff` if `"Off"`, regardless of which direction (if any) the write test actually moves it. This makes the fix correct independent of eventually confirming the precise mechanism. `# TODO:` a curl reproduction analogous to #178's, isolating which exact command in the level-4 sequence changes `EQStat`, would let this be marked confirmed rather than defensive. Broader audit of the rest of the probe sequence for the same defensive-restore blind spot (this session, in response to the user's "could there be other such cases" question): `_probe_batch_write()`'s PEQ write-capability test already restores `Name` (#175) and was checked against the same risk for `EQStat` -- the 2026-07-05 `EQSetLV2Band`-drops-`Name` finding above explicitly confirms that write leaves `EQStat` unchanged, so no equivalent gap exists there. Every other probe step (`getStatusEx`, `getAudioInputEnable`, `EQGetLV2BandEx`, `EQv2GetNewList`, `EQGetLV2SourceBandEx` status reads, `GetMultiroomInfo`) is a pure read with no write-capable counterpart to audit. Added/updated tests in `TestRoomFitLevelDetection` (`test_capability_prober.py`): `test_roomfit_level_4_full_support` and `test_roomfit_level_4_probe_restores_active_profile` now assert `EQChangeSourceFX` is issued once (pre-probe `EQStat:"On"`); `test_roomfit_level_4_probe_skips_restore_when_no_prior_active_name` (the exact shape of the reported regression -- `EQStat:"Off"`, no active profile) now asserts `EQSourceOff` is issued once instead. `ruff check src/` and `mypy src/` clean; full `test_capability_prober.py` (27 tests) passes. |
| 2026-07-05 | The #190 fix above (restore `EQStat` to its pre-probe value, run unconditionally in the `finally` block) was treated as sufficient once the device ended up in the right final state, with the restore placed after the existing delete/name-restore cleanup purely for code locality next to where `Name` is restored. | **The ordering itself was a live-audio safety gap, not just a style choice.** User clarified a product behavior not previously documented: a RoomFit profile stays "selected" in the WiiM Home app even while `EQStat` is `"Off"` -- disabling RoomFit doesn't clear the working buffer's `Name`, it only stops applying it. So if a device had RoomFit disabled but a profile still selected underneath, and the #190 bug's `EQSourceSave` side effect flips `EQStat` to `"On"`, that selected profile's real, un-vetted-by-this-app filter values are immediately and audibly applied to live playback for as long as `EQStat` stays `"On"` -- a genuine hearing/speaker-damage risk at loud volume if those values are extreme, not a cosmetic mismatch. The original #190 fix was correct about the *final* state but left the restore behind two extra network round-trips (`EQv2Delete`, then `EQv2SourceLoad`) it didn't need to wait for, needlessly widening the window during which an unknown filter could be live. | The original #190 analysis focused on "does the device end up unchanged," carried over from the `Name`-restore fix's own framing (#177), without separately asking "how long is the window where it's *wrong*, and does anything happen to the user during that window." `EQStat` and `Name` aren't symmetric in this respect: a wrong `Name` at rest is invisible until someone reads it, but a wrong `EQStat` is immediately audible the instant it's wrong. | Reordered `_probe_roomfit()`'s level-4 `finally` block: the `EQStat` restore (`EQChangeSourceFX`/`EQSourceOff`) now fires first, immediately after the `EQSourceSave` call, before the `EQv2Delete` cleanup and `EQv2SourceLoad` name-restore that follow it -- cutting the maximum exposure window from three round-trips to one. Documented the "selected profile survives disable" behavior in `docs/wiim_api_notes.md`'s "DSP on/off toggle" section (previously undocumented) and reframed the #190 write-up there as a live-audio safety issue rather than a bookkeeping one. Added explicit call-ordering assertions (`save_index < fx_index < delete_index` and the `EQSourceOff` equivalent) to `test_roomfit_level_4_probe_restores_active_profile` and `test_roomfit_level_4_probe_skips_restore_when_no_prior_active_name` so a future refactor that moves the restore back to the end of cleanup fails a test instead of silently reintroducing the wider window. `ruff check src/` and `mypy src/` clean; full `test_capability_prober.py` (27 tests) passes. |
| 2026-07-05 | The reordered #190 fix above (restore `EQStat` immediately after `EQSourceSave`, minimizing the exposure window to one round-trip) was treated as a sufficient safety fix, on the assumption that shrinking the window to as small as possible was the correct mitigation strategy for an audio-affecting side effect. | **Wrong mental model of what "exposure" even means here.** User corrected a more fundamental misunderstanding: the working buffer this probe reads and writes (`EQGetLV2SourceBandEx`/`EQSetLV2SourceBand`) is *not* what's applied to the audio pipeline, even while `EQStat` is `"On"` -- what's applied is the persisted, *selected* named profile. A profile stays selected while `EQStat` is `"Off"` (previous row). So on a device with RoomFit disabled and a named profile like "DangerDanger" still selected, if `EQSourceSave` flips `EQStat` to `"On"` at all, "DangerDanger"'s real, on-disk filter values become audible immediately -- not "whatever's briefly in the buffer" (which the previous fix's mitigation implicitly assumed mattered), and not something a same-request-cycle restore can undo, because the audio was already live during that window regardless of how short it was. Shrinking a window to one round-trip instead of three is still a real number of milliseconds of an extreme, un-vetted filter reaching a speaker at whatever volume the user has playing -- not a fix, just a smaller version of the same exposure. | Two fixes in a row (the original #190 restore, then the reordering) both treated this as a "make the device end up in the right final state, as quickly as possible" problem, by analogy with the `Name`-restore fix (#177) it was modeled on. But `Name` and `EQStat` differ in a way neither fix accounted for: a wrong `Name` has no effect until something reads it, so "restore it before anyone notices" is a complete fix. A wrong `EQStat`, if it enables a real selected profile, has its effect the instant it happens, with no read step in between -- there is no "before anyone notices" for sound reaching a speaker. Any fix built on "restore quickly" rather than "never let the wrong state occur" was going to be incomplete for this specific field. | Replaced the restore-after strategy with a skip-before guard: `_probe_roomfit()` now checks, right after the level-2 read, whether `EQStat` is `"Off"` *and* a real profile is selected (`Name` non-empty). If so, it skips the level-4 write test entirely -- no `EQSourceSave`, no `EQv2Delete`, no toggle commands at all -- and `roomfit_level` stays at 3 (write unconfirmed) for that connection, matching CLAUDE.md's "safety before convenience" principle over "capability-driven" completeness for this one specific test. The test still runs normally in the two cases proven safe: `EQStat` already `"On"` (whatever's selected is already audible; hardware-confirmed the sequence only relabels-and-restores the same bytes, `docs/corrections.md` 2026-07-05 above) or `EQStat` `"Off"` with nothing selected (a fresh device has no real profile to expose). The #190/#190-reorder `EQStat`-restore-immediately-after-save logic is kept as defense-in-depth for those two remaining cases, in case the "safe states" analysis is itself incomplete once the exact `EQSourceSave` mechanism is eventually hardware-confirmed (`# TODO:` unchanged). Rewrote the live-audio-safety paragraphs in `docs/wiim_api_notes.md`'s RoomFit "Write workflow" section to describe the skip-guard rather than the superseded restore-after framing. Added `test_roomfit_level_4_probe_skipped_when_disabled_with_selected_profile` (`test_capability_prober.py`) as the regression test for the actual reported scenario -- `EQStat:"Off"`, `Name:"DangerDanger"` -- asserting `EQSourceSave`/`EQv2Delete`/`EQv2SourceLoad`/`EQChangeSourceFX`/`EQSourceOff` are never called and `roomfit_level` stays 3. Noted but not yet addressed: `roomfit_level == 3` can now mean either "device genuinely lacks RoomFit write support" or "write skipped this session for safety" -- the CLI's existing `"roomfit_level >= 4"` error-message rewrite (`src/cli/main.py`) says "Dedicated RoomFit filters not available on this device," which is inaccurate for the second case; distinguishing them would need a new capability field and isn't done here. `ruff check src/` and `mypy src/` clean; full `test_capability_prober.py` (28 tests) passes. |
| 2026-07-05 | `RoomFitSafeWrite.execute()` treated a push's activation of the written profile as an unwanted side effect to suppress -- it restored whichever profile was active *before* the push once done (#178), and discarded the pre-write `EQStat` read entirely. | Not a hardware-observation row like the ones above -- a product decision. Restoring the old selection fights user intent (pushing a RoomFit config is meant to apply it) and left a gap symmetric to #190/#191-row-above: if `EQSourceSave` flips `EQStat` on as an undocumented side effect during a *normal* push (not just probing), the restored-but-unrelated previously-active profile becomes audible with RoomFit now silently on. | The `Name`-restore fix (#178) and the #190 row above both treated "the device ends up in its original state" as the goal for every write path, without separately asking whether that was actually the *desired* behavior for a real, user-initiated push (as opposed to incidental probing, where restoring the original state is unambiguously correct). | Redesigned (smoke #191): a **successful** push now deliberately activates the profile and turns RoomFit on via confirmed commands (`WiiMAdapter.enable_roomfit()`/`disable_roomfit()`/`set_roomfit_enabled()`, new), not an unconfirmed side effect; a **failed** push restores original selection/enable-state; **Undo** (new `RoomFitSafeWrite.undo()`) restores the *original push's* full pre-state from three new optional `BackupRecord` fields (`pre_write_active_profile`, `pre_write_roomfit_enabled`, `was_new_profile`) and is now always offered (previously hidden for new-profile pushes), never deleting a new-profile's profile on undo (product decision -- non-destructive). `write_roomfit()`'s gate loosened `>= 4` → `>= 2`, letting a real push confirm write capability for a device whose probe skipped that test for #190 safety. "Copy to Another/Multiple Device(s)" shares the same `execute()` path, so it inherits this automatically -- only its confirmation dialog needed a new RoomFit-specific warning. Found and fixed in review, before landing: `BackupManager.create_backup()`'s stereo-mode `filters = settings.bands if settings.bands else None` coalesced an intentionally-empty new-profile metadata backup to `None`, which `Profile`'s validator then rejected (`bands is not None` check now used instead). Also consolidated three pre-existing duplications this change already touched: a shared `_require_roomfit_level()` gate (was hand-rolled 4x in `wiim_adapter.py`), a shared `set_roomfit_enabled()` setter, and a shared `_restore_selection_and_enable_state()` helper used by both `execute()`'s failure paths and `undo()` (previously two independently-written near-duplicate blocks). |
| 2026-07-06 | `read_roomfit_preset_preview()`'s docstring claimed reading a saved profile "has no live-audio consequence to consent to (RoomFit's buffer is decoupled from its DSP on/off state)" -- so the method restored the previously-selected profile `Name` after previewing, but discarded the `EQStat` value it had captured (`_enabled, original_name = await self.get_roomfit_status()`), never restoring it. | **User-reported (live hardware):** "Presets on Device", Load into Editor, Export as REW File, and Save to My Presets -- all read-only actions, and all funneling through this one method -- re-enabled RoomFit if it had been off before the action, exactly the live-audio-safety pattern already fixed for the *write* path in #190/#191. | Same root-cause family as #190/#191, one level removed: `EQv2SourceLoad` (issued by `load_roomfit_profile()`, which `read_roomfit()` calls before every band read) apparently also flips `EQStat` on as an undocumented side effect, not just `EQSourceSave` as previously confirmed. The #191 redesign fixed this for every *write* path (`RoomFitSafeWrite`) but the preview *read* path predates that fix and was never revisited against the updated understanding. | `read_roomfit_preset_preview()` now captures and restores both `Name` and `EQStat` via a new shared `WiiMAdapter.restore_roomfit_selection_and_enable_state()` method -- moved up from `RoomFitSafeWrite`'s own private `_restore_selection_and_enable_state()` (safe_write.py), which is now a thin delegator, so the write path and the preview-read path share one implementation instead of two that can independently miss a field the way this one did. Corrected the docstring's disproven claim. Defensively applied the same fix to PEQ's `read_peq_preset_preview()` (`set_peq_enabled()`, new) since `load_peq_profile()` wraps the same `EQv2SourceLoad` command family -- marked `# ASSUMPTION:` since this has not been independently hardware-confirmed for PEQ's per-source `EQStat`, only reasoned by analogy; restoring it is cheap and safe regardless of whether the analogy holds. Separately fixed two tests in `test_gui_push_export.py` (`TestPushProgressUpdates`) that asserted `_do_push()`'s pre-#189 hardcoded `progress_update("Backing up...")`/`("Verifying...")` emissions, which #189 replaced with the `on_stage`/`stage_changed` callback mechanism -- stale, not a regression; deleted rather than rewritten since `test_issue189_*` (`test_smoke_regression_operations.py`) already covers the replacement behavior. |
| 2026-07-06 | `SafeWrite` (PEQ's push/copy/undo protocol) had no enable-state awareness at all: `execute()` never called `enable_peq()`/`disable_peq()`, and the GUI had zero visibility into whether PEQ was on or off for a source -- the exact gap RoomFit had before #191, now confirmed to matter (#192: `EQv2SourceLoad` unconditionally enables both RoomFit's global buffer and PEQ's per-source form). | Not a hardware-observation row -- a product decision, mirroring #191's own framing: a successful PEQ push should deliberately enable PEQ rather than leave it to an unconfirmed side effect, and Undo should restore the true original enable-state rather than whatever the push's own success path leaves behind. | Same root-cause family as #191: "the device ends up in its original state" had never been separately evaluated as the *desired* outcome for a real, user-initiated push versus incidental probing -- PEQ's `SafeWrite` predated that distinction entirely (RoomFit's `RoomFitSafeWrite` got it in #191; PEQ's plain `SafeWrite` was never revisited). | Mirrored #191 for PEQ: a **successful** push now turns PEQ on via `WiiMAdapter.enable_peq()` (best-effort, deliberate); a **failed** push restores the source's original enable-state via `set_peq_enabled()`; **Undo** (new `SafeWrite.undo()`) restores bands via `execute()` then corrects the enable-state to the true original (captured in a new optional `BackupRecord.pre_write_peq_enabled` field), since `execute()`'s own success path always enables and can't be trusted alone for undo. `SecondaryWorkflowManager._do_undo()` now delegates to `SafeWrite.undo()` instead of re-parsing the backup and calling `execute()` directly, which also fixed a latent bug where a failed undo was reported to the user as success (the method never inspected `execute()`'s return value). `src/cli/main.py`'s direct `SafeWrite.execute()` call (a `set-peq-filters`-style command) and `_do_copy_preset_to_device()`'s PEQ branch both inherit the new behavior automatically, same as #191's Copy-to-Device inheritance -- no code change needed at either site. Copy-to-Device's confirmation dialog (`_confirm_roomfit_copy_activation()`) generalized to `_confirm_copy_activation()`, covering both PEQ and RoomFit in one dialog rather than adding a second near-identical PEQ-only method (`PresetsDeviceView` enforces mutual exclusion between its two preset lists today, so a mixed selection can't currently occur, but the shared method avoids the "second near-identical dialog" drift pattern regardless and is already correct if that constraint is ever relaxed). Also extracted a small `_html_bullet_list()` helper (`main_window.py`) used by both confirmation dialogs, replacing a two-line escape/replace snippet that had been duplicated at each call site. **Found and fixed in code review, before landing:** the new `SafeWrite.undo()` initially rejected any backup with `not filters` ("Backup contains no filters"), mirroring `RoomFitSafeWrite.undo()`'s own guard -- but a PEQ source with zero bands is a legitimate state (flat EQ), and `parse_backup_filters()` can't distinguish that from a missing/corrupt backup (both parse to `[]`). Removed the guard for PEQ's `undo()` so a push that happened while a source had no filters can still be undone. **Also consolidated in code review:** `execute()`'s success path, `_rollback()`, `undo()` (`safe_write.py`), and `read_peq_preset_preview()` (`wiim_adapter.py`) each had their own near-identical `try: await self._adapter.<enable_peq/set_peq_enabled>(...) except Exception: logger.warning(...)` block -- the same duplication risk #192 itself was fixing for RoomFit. Extracted a shared `WiiMAdapter.set_peq_enabled_best_effort(source_name, enabled, *, context)`, mirroring `restore_roomfit_selection_and_enable_state()`'s role for RoomFit; all four call sites now delegate to it instead of wrapping `set_peq_enabled()`/`enable_peq()` individually. |
| 2026-07-06 | The #192 fix's `# ASSUMPTION:` markers treated it as unconfirmed which command was responsible for RoomFit's `EQStat` auto-enable side effect; separately, #190's fix comments attributed its own (structurally similar) auto-enable finding to `EQSourceSave`, also unconfirmed. | **User wrote and ran an isolated curl-based verification script** (read `EQStat`/`Name` -> disable if needed -> issue exactly one command -> re-read -> restore) against real hardware, testing `EQv2SourceLoad` and `EQSourceSave` independently. Result: with `EQStat:"Off"` and a real profile ("REW") selected, re-issuing `EQv2SourceLoad("REW")` alone flipped `EQStat` to `"On"` -- reproduced with no other command involved. Re-issuing `EQSourceSave` alone (saving the buffer under a throwaway name) did **not** flip `EQStat` -- it stayed `"Off"`. | `EQv2SourceLoad` (RoomFit's profile-load command) unconditionally turns `EQStat` on as an undocumented side effect of loading a profile -- now confirmed, not assumed. This also revises the likely root cause of #190: that fix's level-4 write-capability test sequence is `EQSourceSave` (throwaway save) -> `EQv2Delete` (cleanup) -> `EQv2SourceLoad` (restore original name, added by #177) -- given `EQSourceSave` alone is now shown not to cause the flip, the final `EQv2SourceLoad` restore step was almost certainly the actual mechanism behind #190's originally-reported auto-enable, not `EQSourceSave` as the fix's own code comments assumed at the time. | Updated `docs/wiim_api_notes.md` rules 1 and 4 to state the `EQv2SourceLoad` finding as confirmed rather than an open assumption, and to note the revised understanding of #190's likely mechanism. No functional code change needed for either #190 or #192: #190's skip-guard (never attempt the level-4 test at all when `EQStat` is off and a profile is selected) is correct regardless of which exact command in the sequence causes the flip, and #192's unconditional restore-after-preview fix is likewise correct regardless of confirming the mechanism -- this was a documentation-accuracy update, not a behavior change. The `# ASSUMPTION:`/"unconfirmed" language in `capability_prober.py`'s code comments (citing `EQSourceSave`) is now understood to be probably-wrong about the specific command, though its safety conclusion is unaffected; left as-is rather than rewritten, since correcting historical comments case-by-case risks losing the original reasoning trail -- this ledger entry is the correction of record. PEQ's parallel `# ASSUMPTION:` in `wiim_adapter.py` (`read_peq_preset_preview()`/`load_peq_profile()`) remains open -- this test only exercised RoomFit's global `EQv2SourceLoad`, not PEQ's per-source form. |
| 2026-07-10 | The WiiM API surface this project uses (the confirmed `EQLevel:1`/`EQLevel:2` LV2 `EqNp` commands in `docs/wiim_api_notes.md`) was treated as a complete picture of what WiiM devices support, since it matched every documented and hardware-tested behavior encountered so far. | Further API research surfaced a second, separate command family never referenced anywhere in this project: `RoomCorrGet`, `RoomCorrSet`/`RoomCorrSetLR`, `RoomCorrSetMode`, `setRoomCorrection`. A hardware validation script (`scripts/validate_new_wiim_commands.sh`, iteratively built and hardened across this investigation) confirmed all five are real and recognized by the device across three models (WiiM Amp Ultra, WiiM Mini, WiiM Sound) -- none return "unknown command." | This appears to be the calibration-*result*-push path used internally by the device's own on-device RoomFit measurement flow, running in parallel to -- not instead of -- the `EQLevel:2` LV2 commands this app already uses for RoomFit read/write. `RoomCorrSet`/`RoomCorrSetLR`'s write behavior was inconsistent across devices (a real, detectable band-value change on one device with `EQStat` already `Off`; accepted with zero effect on another even after explicitly forcing `EQStat` off first to rule that out as the variable) -- the working theory is that these commands are gated on an internal calibration-session state opened by a real on-device measurement run, consistent with `RoomCorrSetMode:Measure` (the likely session-open call; its exact-case `"Measure"`/`"Playback"` values were confirmed against hardware, not guessed) itself returning `{"status":"Failed"}` on every device tested cold. Confirming the true session-open sequence would require capturing HTTP traffic during an actual on-device calibration run -- not attempted; explicitly out of scope since this family duplicates functionality this app already has via the confirmed `EQLevel:2` commands. | Documented the full family in a new "Calibration-result push commands" section of `docs/wiim_api_notes.md` (RoomFit API), marked out of scope/do-not-use, with the session-gating theory and per-command hardware results. No code change -- this app has no reason to open a calibration session or call this family. `scripts/validate_new_wiim_commands.sh` kept in the repo as a reusable hardware-validation tool for any future API research of this kind. |
| 2026-07-10 | `docs/wiim_api_notes.md` stated "no known command renames a saved profile" for both PEQ and RoomFit profiles (`EQRenameLV2Preset` confirmed dead, and nothing in the `EQv2*` family was believed to do it either). | **Wrong.** The same research above found `EQv2Rename:{"pluginURI":"...","Name":"<old>","newName":"<new>"}` (first identified via RoomFit's own calibration-profile rename flow) to be a real, working command. Hardware validation confirmed it works as a clean round-trip (rename forward, verify list, rename back, verify restored) not just for that one special case but for ordinary user-saved PEQ presets and RoomFit profiles alike, on two device models (WiiM Amp Ultra, WiiM Mini). | The original "no rename" conclusion was based on `EQRenameLV2Preset` (the legacy `LV2`-preset-family command, confirmed dead in `docs/corrections.md` 2026-07-03) failing, without separately testing the unrelated `EQv2*`-family command of the same apparent purpose -- an assumption of completeness that further research disproved. | Updated `docs/wiim_api_notes.md`: added `EQv2Rename` to both PEQ and RoomFit "Profile CRUD" tables, and replaced the "no known command renames a saved profile" capability-gap note with a correction pointing at the confirmed-working command. No code change in this pass -- this app doesn't currently expose a rename UI action, so this is a documentation update only; a future rename feature can now be built on a confirmed command instead of the save-as-new+delete-old workaround the old note prescribed. |
| 2026-07-10 | `docs/wiim_api_notes.md`'s "Dead commands" section listed `EQSetLV2ChannelMode` as failing on every tested source, with the note that `EQSetLV2SourceBand`'s inline `channelMode` field was "the only reliable way to switch it" -- implying no standalone channel-mode-switch command exists at all. | Further research found a different, non-`LV2`-named command, `EQSetChannelMode:{"EQLevel":<1|2>,"source_name":"...","pluginURI":"...","channelMode":"Stereo"\|"L/R"}`, to be a real, working standalone channel-mode-switch command. Hardware validation confirmed a clean round-trip (flip mode, verify via read, flip back, verify restored) on two device models. | `EQSetLV2ChannelMode`'s failure was correctly diagnosed as dead, but the phrase "the only reliable way to switch it" overstated the conclusion -- it was only true of the commands this project had tested at the time, not of the full command surface, which further research subsequently expanded. | Updated `docs/wiim_api_notes.md`'s PEQ "Write" and "Dead commands" sections to document `EQSetChannelMode` as the real, confirmed fix for `EQSetLV2ChannelMode`'s failure, while keeping the inline-`channelMode`-on-write approach as this app's own method (it already covers the need in one call, so no code change). |
| 2026-07-10 | `docs/wiim_api_notes.md`'s RoomFit capability-detection heuristic required both an empty `custom` profile list *and* `EQStat:"Off"` on a band read to signal "no RoomFit hardware" (WiiM Mini, 2026-06-14). Separately, this project's own confirmed invariant that `EQLevel:1` and `EQLevel:2` are fully independent storage namespaces (2026-07-04) had only ever been tested on devices *with* real RoomFit profiles. | Hardware validation of the `RoomCorr*` family (above) on a WiiM Mini (empty RoomFit profile list) produced two new observations: (1) a fresh, untouched `EQLevel:2` band read reported `EQStat:"On"`, contradicting the documented "Off" signal; (2) after a test sequence that included `RoomCorrSetMode`/`setRoomCorrection`/`RoomCorrSet` calls at `EQLevel:2`, the real `EQLevel:1` `"wifi"` source's `channelMode`/`Name` had changed (`"L/R"`/`"Closed LR"` -> `"Stereo"`/`"Auto"`), despite nothing in that sequence targeting `EQLevel:1` directly. A follow-up isolation test, run to determine whether this app's own `EQChangeSourceFX`/`EQSourceOff` calls (used by `capability_prober.py`'s RoomFit probing) were responsible, confirmed they were **not**: each was issued alone against the same device/source and produced no change to `channelMode`/`Name` in either case. | The `EQLevel:1`/`EQLevel:2` isolation invariant does not hold on RoomFit-less hardware for the *reverse-engineered* `RoomCorr*`/`RoomCorrSetMode`/`setRoomCorrection` commands specifically -- these appear to fall back to operating on `EQLevel:1`'s live source when no real `EQLevel:2` implementation exists to target, rather than being rejected or no-op'ing safely. This does not extend to this app's own confirmed command set (`EQChangeSourceFX`, `EQSourceOff`, `EQGetLV2SourceBandEx`, `EQSetLV2SourceBand`, `EQv2SourceLoad`, `EQSourceSave`, `EQv2Delete` at `EQLevel:2`), which the isolation test specifically exonerated. | Updated `docs/wiim_api_notes.md`: revised the capability-detection note to rely on the empty-`custom`-list condition alone (dropped the `EQStat:"Off"` requirement); added a safety note to the new "Calibration-result push commands" section scoping the isolation-boundary risk specifically to that reverse-engineered family, not to this app's own commands. No code change -- `capability_prober.py`'s existing `EQLevel:2` calls are confirmed safe on RoomFit-less devices by this same investigation, and this app never calls the `RoomCorr*` family. |
| 2026-07-10 | `RoomCorrGetMode` was documented as untested, presumed (by naming symmetry with `RoomCorrSetMode`) to return a `{"Mode":"..."}` read of the current calibration-session state. | User ran it against all 4 owned devices. WiiM Sound and WiiM Sound Lite returned the expected `{"Mode":"Playback"}`. WiiM Amp Ultra and WiiM Mini instead returned the full profile-dump shape identical to `RoomCorrGet` (`EQLevel`/`source_name`/`Name`/`EQBand`/`EQStat`, no `Mode` field) -- i.e. on those two devices `RoomCorrGetMode` isn't a distinct command at all, it just falls through to whatever handles `RoomCorrGet`. | Not fully root-caused. The immediate cause is that `RoomCorrGetMode` is only genuinely implemented (as its own handler) on some device firmwares (Sound, Sound Lite) and aliased/unimplemented on others (Amp Ultra, Mini). This correlates with the earlier finding (`docs/corrections.md`, 2026-07-10 row above re: `RoomCorrSet`/`RoomCorrSetLR`) that `RoomCorrSet` actually mutated the buffer on WiiM Sound but had no effect on WiiM Amp Ultra -- suggesting the whole `RoomCorr*` family's inconsistent behavior may be per-firmware implementation completeness rather than (or in addition to) a calibration-session gate that's equally absent on every device. Not confirmed: `RoomCorrSet` was never explicitly re-tested against WiiM Sound Lite or WiiM Mini to check whether the correlation holds all the way through. | Updated `docs/wiim_api_notes.md`'s `RoomCorrGetMode` row with the confirmed per-device split, and added a note to the family's "Do not use" paragraph connecting it to the earlier `RoomCorrSet` inconsistency as a working theory, explicitly flagged as unconfirmed. No code change -- this app doesn't use any command in this family regardless of the explanation. |
| 2026-07-10 | (Confirms/refines the row above) The correlation between `RoomCorrGetMode`'s response shape and `RoomCorrSet`'s mutation behavior was a suggestive but unconfirmed theory -- only 2 data points existed (Sound: real shape + mutated; Amp Ultra: aliased shape + no effect), and the theory's key gap (WiiM Sound Lite's `RoomCorrSet` behavior) had never been tested. | `scripts/validate_new_wiim_commands.sh` was extended to classify `RoomCorrGetMode`'s shape automatically (Section 1) and to surface the existing `RoomCorrSet` mutation test as a structured result, logged per device to a CSV plus a new `--summarize` mode that aggregates them into a verdict. Run with `--writes --yes` against all 4 owned devices: WiiM Sound (`REAL`/`MUTATED`), WiiM Sound Lite (`REAL`/`MUTATED`), WiiM Amp Ultra (`ALIASED`/`NOT_MUTATED`), WiiM Mini (`ALIASED`/`SKIPPED_NO_ROOMFIT` -- excluded, no real RoomFit storage to safely target). All 3 testable devices matched the predicted correlation exactly; `--summarize` printed "THEORY CONFIRMED across all 3 testable device(s)." | N/A -- confirms the theory rather than correcting it. `RoomCorrGetMode` is only genuinely implemented (as a distinct handler, separate from `RoomCorrGet`) on some device firmwares (Sound, Sound Lite) and this same subset is exactly where `RoomCorrSet` performs a real write; on firmwares where it's aliased to `RoomCorrGet` (Amp Ultra, Mini), the whole family behaves as an inert/no-op read-only shim for writes. This is a per-firmware implementation-completeness split, not a calibration-session gate that's equally absent everywhere (the leading theory before this test). | Updated `docs/wiim_api_notes.md`'s "Calibration-result push commands" section to state this as confirmed rather than a working theory. No code change -- this app doesn't use any command in this family regardless of the explanation. `scripts/validate_new_wiim_commands.sh`'s new `--results-file`/`--summarize` mechanism is kept in the repo for any future cross-device correlation test of this kind. |
| 2026-07-10 | (Refines the row above) "Per-firmware implementation completeness" was the best available explanation for the `RoomCorrGetMode`/`RoomCorrSet` split, but was a description of the symptom (which devices behave which way), not a mechanism (why). | User ran `GetAcousticCapability` (bare, no payload -- a general subsystem-version report, not itself part of the `RoomCorr*` family) against all 4 devices. It returned an `RC.Version` field of `"1.1"` on WiiM Sound and WiiM Sound Lite (exactly the two devices where `RoomCorrGetMode` returns a real `Mode` response and `RoomCorrSet` mutates), `"1.0"` on WiiM Amp Ultra (exactly where both are aliased/inert), and `{"status":"Failed"}` (the command itself unsupported) on WiiM Mini (which has no RoomFit hardware at all). | `RC.Version` is very likely the actual capability-gate field this whole family checks internally before deciding whether to implement `RoomCorrGetMode`/`RoomCorrSet` for real vs. fall back to an aliased/inert shim -- a specific, checkable mechanism, not just an observed correlation across opaque "firmwares." Caveat: only two `RC.Version` values (`1.0`, `1.1`) have been observed across the 4 owned devices, so "requires `RC.Version >= 1.1`" is the working assumption implied by ordering, not a confirmed threshold -- no device with a value in between, above, or a non-numeric-comparable format has been tested. | Documented `GetAcousticCapability` and the `RC.Version` correlation in `docs/wiim_api_notes.md`'s "Calibration-result push commands" section, replacing the vaguer "per-firmware" framing with the specific field while keeping the `>= 1.1` framing explicitly flagged as an assumption. No code change -- this app doesn't use any command in this family, and `capability_prober.py` has no use for `RC.Version` since it never calls `RoomCorrSet`/`RoomCorrGetMode`. |
| 2026-07-10 | A per-source PEQ storage slot, once written under a garbage/stray `source_name` (e.g. from a comma-joined multi-source string, or an ad-hoc test value), could plausibly be deleted or reset back to "as if never written" via some undocumented command, the same way `EQv2Delete` removes a named saved profile. | `scripts/cleanup_stray_peq_sources.sh` (diagnose + `--clean`) was run against all 4 owned devices. Both plausible mechanisms were tried per stray row: `EQv2Delete` using the row's own `Name`, and overwriting the row's bands with an all-`OFF` template. Both attempts returned `NOT REMOVABLE` on every device -- the row's content resets, but it never disappears from `EQGetSourceModes`. Further API research (broad search of the WiiM Home app's own client code for any source-scoped delete/clear/reset/factory command, and for what -- if anything -- calls `EQGetSourceModes`) found nothing: no such command exists anywhere in the app, and `EQGetSourceModes` plus its response model turned out to be entirely unused/dead code in the app version examined -- the app itself never issues this request, so there's no companion "manage/delete these rows" feature to find. | Per-source PEQ storage appears to be an append-only, permanent key-value store keyed by `source_name` at the firmware level, with no exposed (or even internally-used, by WiiM's own app) mechanism to remove an entry once created. This is a capability gap in the device's API surface itself, not a gap in this project's command coverage. | Documented this as a confirmed permanent limitation in `docs/wiim_api_notes.md`'s Source Discovery "Key rules" (new bullet). No code change to this app -- it never writes a garbage/multi-value `source_name` itself once the separate wizard-state bug (tracked outside this ledger) is fixed, so this is a data-hygiene wart on already-affected devices, not an ongoing risk. `scripts/cleanup_stray_peq_sources.sh` is kept in the repo, unmodified, in case a future firmware update adds real support for either mechanism -- re-running it would detect that automatically. |
| 2026-07-10 | Five previously-undocumented command-name strings (`EQMoveMode`, `EQMoveRCModes`, `EQGetModeParam`, `EQGetRCSubCal`, `EQSetRCSubCal`) surfaced during the search for a source-slot delete mechanism (row above). A hardware-testing script (`scripts/validate_new_wiim_commands.sh`, Section 2.1c) was initially written to guess their payload formats and effects safely on real devices, since "Move" in two of the names suggested possible calibration-state-transition risk similar to `RoomCorrSetMode`. | Before running the hardware script, further API research traced each command to its actual feature area instead of guessing blind. `EQMoveRCModes` is invoked from a UI element literally tagged for the Room Correction/calibration feature, with an inferred payload of `{"EQLevel":<int>,"newLevel":<int>,"pluginURI":"..."}` -- it migrates EQ data between two `EQLevel` namespaces during calibration finalization. `EQGetModeParam`'s payload is `{"EQLevel":2,"pluginURI":"...","Name":"<profile>"}`, read from the same Room Correction profile-management area. `EQGetRCSubCal`/`EQSetRCSubCal` turned out to belong to an entirely different feature -- the subwoofer settings screen -- with `EQSetRCSubCal`'s payload being `{"delayMs":<float, 3dp>,"levelDb":<float, 3dp>,"Version":"1.0","Time":<unix ms>}`, i.e. subwoofer output delay/level trim, nothing to do with PEQ/RoomFit filters. `EQMoveMode` (the fifth) has a defined command-name string but no feature anywhere was found to actually issue it -- purpose and payload remain genuinely unknown. | All four resolved commands sit outside this app's scope (calibration/RC-profile-management, or an unrelated subwoofer-trim feature) -- confirming this via API research made the planned hardware test unnecessary, and testing an unrelated subsystem's undocumented write command for no benefit would have been needless risk. | Removed the untested/never-hardware-confirmed Section 2.1c (`section2_mode_and_subcal`) and its Section-1 existence probes from `scripts/validate_new_wiim_commands.sh` -- speculative guess-testing no longer serves a purpose now that each command's real scope is known. Documented the four resolved commands' inferred payloads and scope, and the fifth's unknown status, in `docs/wiim_api_notes.md`'s new "Other RC/subwoofer-related commands" section. No code change to this app -- none of these five are used by it regardless of the explanation. |
| 2026-07-10 | `CapabilityProber._probe_roomfit()` level 1 treated any valid JSON dict from `EQv2GetNewList`+`EQLevel:2` as proof of RoomFit support, assuming RoomFit-less devices would answer "unknown command". | Real RoomFit-less hardware (WiiM Mini, 2026-07-10 capability dump) returns a valid-looking `{"custom": [], "preset": []}` -- never "unknown command" -- so the probe sailed through levels 2-4, including the real `EQSourceSave` write test, against a device with nothing to write to. Only the capability-file override for `WiiM_Mini` painted over the wrong probe result; any uncatalogued Mini-class device got `roomfit_level=4`. The Mini unit test could not catch this because its mock returned exactly the "unknown command" that real hardware never sends. | The empty-`custom` list is the sole no-RoomFit signal (`docs/wiim_api_notes.md`, Capability detection) -- there is no API-level rejection to detect; absence of profiles is the only observable difference. | `_probe_roomfit()` now returns (RoomFit unsupported) when `custom` is empty, before any write is issued. Mini test mock corrected to the real hardware response shape, plus an assertion that no RoomFit write command is ever sent during a Mini probe (`test_capability_prober.py`). |
| 2026-07-10 | `CapabilityProber._probe_peq()` treated any dict response to `EQGetLV2BandEx` as proof of PEQ support, and defaulted `max_filters` to 10 when no band letters were parseable. | Real generic-LinkPlay hardware (AudioCast, `docs/device_capability_examples/`) answers every `EQGetLV2*` command with a stock tone-control dict `{"Bass": 0, "EQEnable": 0, "Treble": 0}` regardless of plugin URI -- a dict, so the device probed as `supports_peq=True, max_filters=10`, a full false positive on hardware with no PEQ engine at all. | Generic LinkPlay firmware does not reject unrecognized `EQGetLV2*` command names -- it falls through to a legacy tone-control handler, so "returned a dict" carries no information. The 10-band fallback also masked a second gap: a device probed while in L/R mode returns `EQBandL`/`EQBandR` and no `EQBand`, so the letter count came up empty and the fallback silently supplied 10. | `_probe_peq()` now requires an `EQBand`/`EQBandL`/`EQBandR` key (same `has_bands` shape check `_probe_roomfit()` already used), counts band letters from `EQBand` or `EQBandL`, and treats an empty letter count as no-PEQ (`max_filters=0`) instead of inventing 10. New regression tests: generic-LinkPlay response shape, L/R-mode band counting, empty-`EQBand` no-PEQ (`test_capability_prober.py`). |
| 2026-07-10 | WiiM Mini's on-device auto-calibration RoomFit profile (the one with `Type: "RC"`, confirmed real above) would appear in the `EQLevel:2` profile list, the same namespace this app reads for RoomFit, since it's a RoomFit-branded profile. | Mini has no `EQLevel:2` storage at all (empty `custom` list, confirmed via the `_probe_roomfit()` fix above) -- yet Mini's own `RoomCorrGet` reports its calibration profile under `EQLevel:1`, the PEQ namespace, not `EQLevel:2`. The calibration profile is real and readable, just filed under the "wrong" level relative to what its `Type` field implies. | `EQLevel:1`/`EQLevel:2` is a storage-location distinction made by the device firmware, not a strict PEQ-vs-RoomFit content classifier -- a device can file a RoomFit-purposed profile (`Type:"RC"`) under the PEQ level if it has no independent `EQLevel:2` implementation to put it in, consistent with Mini being confirmed RoomFit-hardware-less. | Documented in `docs/wiim_api_notes.md`'s Capability detection note: "Mini has no RoomFit" is precisely true only of the `EQLevel:2` namespace -- its calibration profile lives in `EQLevel:1` instead. No code change -- this app's `list_roomfit_profiles()`/`read_roomfit()` correctly scope to `EQLevel:2` per source_name & EQLevel Reference and have no reason to also scan `EQLevel:1` for RoomFit-typed content. |
| 2026-07-10 | RoomFit's `rc_output` field (`"AUDIO_OUTPUT_SPEAKER_MODE"`, seen in real app write traffic and profile dumps) was assumed to be an inert metadata tag on every currently-owned device, since all 4 owned devices are single-output (Speaker mode only) and the field's value never varied across any hardware test run this session. | **Open question, not resolved.** Nothing in this session's hardware testing exercised a multi-output-capable device (e.g. one with `getSoundCardModeSupportList` reporting more than one mode), so whether `rc_output` actually selects between per-output RoomFit buffers on such hardware -- as opposed to being a fixed constant -- remains untested. The assumption that it's inert holds only for the Speaker-only fleet this project has access to. | The owned device fleet has no multi-output hardware, so this is a coverage gap in available test hardware, not a contradicted assumption. | `# ASSUMPTION:` comment already in place at `write_roomfit()`'s `rc_output` field (unchanged); this row records the gap explicitly so a future contributor with multi-output hardware knows to revisit it rather than assuming the existing single-value handling is confirmed-complete. No code change. |
| 2026-07-12 | This app had no client-side validation of RoomFit profile / PEQ preset names before pushing to a WiiM device, on the assumption any string a `QLineEdit` (max 32 chars) accepts is also accepted by the device's naming API. | **User-reported, not independently hardware-verified by this session.** The user reports the WiiM Home Android app states its own naming rule as "only letters, numbers and underscore are allowed", but found via their own hardware testing that dash (`-`) and space (` `) are also accepted by the device. Neither `docs/wiim_api_notes.md` nor any prior row here documents a naming character restriction for `EQSourceSave`'s `Name` field. | Unknown -- no prior investigation of the device's actual accepted name-character set exists in this project. The WiiM Home app enforcing a client-side rule doesn't by itself prove what the device API rejects versus what that app merely chooses to disallow. | `# ASSUMPTION:` marked in the new `src/utils/device_name.py` (`sanitize_device_name`/`has_invalid_device_name_chars`): encodes the WiiM Home app's stated rule plus the two user-confirmed extra characters (letters, numbers, `_`, `-`, space) as the allowed set. Wired into `NameProfilePage` to warn and strip disallowed characters before `name_confirmed` is emitted. **OPEN ISSUE:** this set is not independently hardware-verified by this project -- if a push is ever rejected for a name that passes this check (or accepted for one that doesn't), correct this row and the implementation. |
| 2026-07-18 | `capability_prober.py`'s `_apply_roomfit_model_fallback()` assumed that any *unlisted* device whose model string merely contains "mini" (case-insensitive) shares WiiM Mini's confirmed RoomFit-misreporting quirk (smoke #36), and force-disabled `supports_roomfit*` for it as a defensive default. | **Revised, not contradicted by new hardware evidence -- reassessed against what already exists.** No unlisted "mini"-named device has ever been hardware-tested to confirm or refute this quirk; the assumption was pure generalization from the one confirmed device (WiiM Mini/Muzo_Mini). Meanwhile, two other, model-name-free mechanisms already fully cover the real WiiM Mini: (1) `device_capabilities.json`'s exact "WiiM_Mini"/"Muzo_Mini" entry, and (2) `_probe_roomfit()`'s generic empty-profile-list detection plus `_probe_acoustic_capability()` correctly reading WiiM Mini's real `{"status": "Failed"}` response (both confirmed working, 2026-07-10 rows above). The substring guess added no coverage the other two mechanisms didn't already provide for the one confirmed case, while adding real risk for the unconfirmed one: a future device merely named similarly to "Mini" that genuinely supports RoomFit would have it silently disabled with no way to tell, other than someone noticing and adding an explicit `device_capabilities.json` entry to override it. | The fallback was written defensively, before the generic protocol-level RoomFit detection (`_probe_roomfit()`'s empty-`custom`-list check, `_probe_acoustic_capability()`'s `{"status": "Failed"}` handling) existed in its current, confirmed-correct form -- at the time, guessing from the model name may have been the only available signal for unlisted devices. It became redundant once those checks were hardened, but was never removed. | Removed `_apply_roomfit_model_fallback()` and its call site in `capability_prober.py` entirely; `_apply_capability_file()` now just returns `merge_into()`'s result directly. The `device_capabilities.json` entry remains the sole model-name-specific override, and the two generic protocol-level checks remain the sole non-name-based detection -- consistent with CLAUDE.md's "Capability-driven -- detect what each device supports rather than assuming" principle. If a real "mini"-named device is ever confirmed (by hardware test) to share the RoomFit-misreporting quirk, add it to `device_capabilities.json` by exact name/alias rather than reintroducing a substring guess. Tests updated: `test_capability_prober.py`'s `TestRoomfitModelFallback` renamed to `TestRoomfitCapabilityFileOverride`; the substring-fallback-specific tests were replaced with `test_unlisted_mini_named_model_trusts_genuine_probe_result`, a regression test confirming an unlisted "mini"-named model now keeps its genuinely-probed RoomFit value. |
| 2026-07-20 | (Supersedes the 2026-07-10 row above) `rc_output`'s fixed value, `"AUDIO_OUTPUT_SPEAKER_MODE"`, was logged as an *untested* assumption on multi-output hardware -- unconfirmed either way, since the owned fleet is Speaker-only. | **Externally corroborated, not project-hardware-confirmed, that the fixed value is actually wrong, not just untested.** Captured API traffic from a different open-source project (not this project's own hardware) shows a WiiM Ultra's `getSoundCardModeSupportList` response never lists `AUDIO_OUTPUT_SPEAKER_MODE` as an available mode at all (its options are AUX/SPDIF/COAX/PHONE_JACK) -- so the hard-coded value was being sent to at least one shipping model for which it isn't even a valid enum member. The same external traffic also shows a bare, no-payload query, `getActiveSoundCardOutputMode`, returning the device's currently active output directly (e.g. `{"mode": "AUDIO_OUTPUT_AUX_MODE", ...}`), confirmed in shape across 3 sample devices (WiiM Ultra -> AUX, Amp Ultra -> Speaker, Amp -> AUX). Per this project's evidentiary bar, this is recorded as externally-corroborated evidence, not a confirmed finding -- neither command has been exercised against this project's own owned hardware. | The original `rc_output` value was copied from this project's own single-output (Speaker-only) traffic captures and never validated against non-Speaker hardware, because no such hardware was available to test against. | `write_roomfit()` now calls a new private helper, `WiiMAdapter._query_active_output_mode()` (`src/adapters/wiim_adapter.py`), which queries `getActiveSoundCardOutputMode` live immediately before every RoomFit write and uses its `mode` field as `rc_output`. On any failure (network error, unsupported command on older firmware, malformed/missing `mode`), `rc_output` is omitted from the payload entirely -- never falling back to the old hard-coded value, since omitting is strictly safer than guessing wrong. This is a live per-write query, not a new cached `DeviceCapabilities` field, since the active output can change between connect-time probing and a later push. Tests added in `test_wiim_adapter.py` (`TestQueryActiveOutputMode`, plus new cases in `TestWriteRoomfit`). **OPEN ITEM:** revisit if a future hardware test contradicts the externally-sourced shape -- e.g. a different field name, or the command doesn't exist on some firmware. |
| 2026-07-27 | `docs/qa_signoff.md`'s Test 2 carries a NOTE stating the WiiM Home app doesn't allow two devices to share a friendly name (so the device-card IP-disambiguation path can't be exercised that way) -- a doc-only claim, predating this session, that had never been independently verified or logged to this ledger. | **Confirmed true by the device owner's own testing in the WiiM Home app (not this project's HTTP API):** the app does not allow renaming a device to a name already in use by another device on the account/network -- the rename is rejected client-side before it would ever reach either device's API. | The claim existed only as an unattributed note in `qa_signoff.md`; nothing had cross-checked it against this ledger or recorded how it was established. | No code change and no `qa_signoff.md` edit in this pass -- its existing Test 2 NOTE already states this correctly. Logged here to attach a hardware-testing citation and promote the claim from an unverified doc note to a corroborated ledger entry. `MainWindow`'s device cards continue to show IP alongside name regardless (still useful for distinguishing same-model devices that share a manufacturer default name before the user has renamed either). |
| 2026-07-27 | (Re-confirms the 2026-06-14 row above) Whether multiroom-grouped devices need a distinct PEQ/RoomFit test path beyond standalone devices had not been explicitly cross-checked against the existing 2026-06-14 finding that the API has no group-write mechanism -- `docs/qa_signoff.md`'s traceability matrix already lists no multiroom-specific scenario, but that finding's applicability to multiroom groups was never stated outright. | Re-confirmed by the device owner: multiroom-grouped devices behave identically to standalone devices from this app's PEQ/RoomFit perspective -- there is no group-level distinction to test, consistent with the 2026-06-14 row's finding that the API has no group-write mechanism and no slave-redirection logic exists in `WiiMAdapter`/`SafeWrite` to test in the first place. | The 2026-06-14 finding implied multiroom equivalence but never stated it explicitly, and nothing had cross-referenced the two. | No code change, no `qa_signoff.md` edit in this pass. The 2026-06-14 row above remains the authoritative record of the underlying finding; this row adds direct device-owner confirmation that it covers multiroom groups too. |
| 2026-08-02 | A prior code-review pass (commit `9b6f85e`) narrowed `_set_fx_state()`'s rejection check from "any dict with a truthy `error` key" to routing through `_is_write_rejection()`'s two confirmed shapes only (string containing "unknown", or `{"status": "Failed"}`), on the stated basis that "the device never actually sends" an `error`-keyed dict for `EQChangeSourceFX`/`EQSourceOff`. That claim was never hardware-tested or logged here, so it wasn't independently verifiable, and a follow-up `/code-review ultra` pass flagged it as unconfirmed. | **Not a hardware contradiction -- a policy reversal on evidentiary/safety grounds, since no hardware test of the dropped shape exists either way.** `_is_write_rejection()` gates `SafeWrite`'s rollback decision for `enable_peq`/`disable_peq`, the PEQ batch-write fallback, and RoomFit band writes. The two failure directions are not symmetric: a false negative (a real rejection read as acceptance) leaves the device half-written with no rollback triggered -- a silent, safety-relevant failure the app's own design principle #1 ("never write to a device without backup and verification") exists to prevent. A false positive (an unrecognized dict wrongly read as a rejection) only costs an extra rollback/retry, which `SafeWrite` already handles as a normal path. Given that asymmetry, catching a theoretical shape that's never been observed is cheaper than missing a real one that also hasn't been observed yet -- narrowing pre-emptively, before any hardware evidence ruled the dropped shape out, traded a cheap visible failure mode for an expensive silent one for no confirmed gain. | The narrowing commit treated "never observed sending X" as equivalent to "confirmed to never send X" and acted on it without either a `# ASSUMPTION:` comment or a `docs/corrections.md` row, which is the exact case CLAUDE.md's domain rules require one of for an unconfirmed hardware-behavior claim. | Restored the generic `isinstance(resp, dict) and bool(resp.get("error"))` check inside `_is_write_rejection()` itself (not just `_set_fx_state()`), alongside -- not instead of -- the two confirmed shapes, so all three call sites (`_set_fx_state`, the PEQ batch-write fallback, and the RoomFit band-write check) get the same margin. Documented directly in `_is_write_rejection()`'s docstring as a deliberate, permanent safety margin, with an explicit instruction not to narrow it back without hardware evidence recorded here. **Do not re-narrow this check on the grounds that the `error`-key shape "has never been observed" -- that was already true when it was added, and is not sufficient justification given the rollback-safety asymmetry above. Only remove it if hardware testing confirms the shape cannot occur, and record that test in a new row here.** Regression tests added: `test_enable_peq_raises_on_error_key`, `test_disable_peq_raises_on_error_key` in `test_wiim_adapter.py`. |
